import type { FrameworkApiLearningLevel, FrameworkApiReference } from '../types'

const RUN = 'https://turborepo.dev/docs/reference/run'
const CONFIG = 'https://turborepo.dev/docs/reference/configuration'
const TASKS = 'https://turborepo.dev/docs/crafting-your-repository/configuring-tasks'
const CACHE = 'https://turborepo.dev/docs/crafting-your-repository/caching'
const ENV = 'https://turborepo.dev/docs/crafting-your-repository/using-environment-variables'
const PRUNE = 'https://turborepo.dev/docs/reference/prune'

type Definition = {
  name: string; group: string; signature: string; beginner: string; when: string; example: string; returns: string; principle: string; pitfall: string; officialUrl: string
  level?: FrameworkApiLearningLevel
  params?: Array<[string, string, boolean, string, string?]>
  related: string[]
}

const p = (name: string, type: string, required: boolean, description: string, defaultValue?: string) => [name, type, required, description, defaultValue] as [string, string, boolean, string, string?]

const definitions: Definition[] = [
  { name: 'turbo run', group: '命令与任务选择', signature: 'turbo run [tasks] [options] [-- taskArgs]', beginner: '从 turbo.json 读取任务定义，把选中的 workspace 脚本展开为任务图，再按依赖、并发和缓存规则执行。', when: '需要统一运行一个或多个包的 build、test、lint、typecheck 或 dev 时。', example: 'turbo run build test --filter=@funhub/frontend-admin...', returns: '终端显示每个任务的 cache hit/miss、日志和退出状态；任一未容忍失败的任务失败时命令非零退出。', principle: 'Turbo 调度的是 package.json 中同名脚本；turbo.json 只描述图、输入输出和运行策略，不包含真正业务命令。', pitfall: '直接把参数追加到命令会传给所有同名任务；必须用 -- 分隔，并确认每个底层脚本都接受这些参数。', officialUrl: RUN, level: 'core', params: [p('tasks', 'string[]', true, 'turbo.json 中已声明、并在目标包 package.json 存在的任务名。'), p('options', 'RunOptions', false, 'filter、affected、cache、concurrency 等只控制 Turbo 调度的选项。'), p('taskArgs', 'string[]', false, '写在 -- 后，原样转交所有被选中的底层脚本。')], related: ['--filter', 'dependsOn', '--dry=json'] },
  { name: '--filter', group: '命令与任务选择', signature: '--filter=<package|directory|git-range|selector>', beginner: '用包名、目录、Git 范围和依赖关系语法缩小任务范围；多次写 --filter 时结果是并集。', when: '只开发一个应用、连同它的依赖构建，或只检查某个目录和变更范围时。', example: 'turbo run dev --filter=@funhub/frontend-admin\nturbo run build --filter=@funhub/frontend-admin...', returns: '只为匹配选择器的包建立任务；尾部 ... 包含依赖，前置 ... 包含依赖者。', principle: 'filter 先选择包，再由任务图决定任务顺序；它不是 shell 通配符，也不会自动修正缺失的 workspace 依赖声明。', pitfall: '截图里的三个 --filter 是并集，不代表依赖顺序；若包依赖未写进 package.json，Turbo 无法从名字猜出图。', officialUrl: RUN, level: 'core', params: [p('package', 'string', false, '精确包名或 glob，例如 @funhub/frontend-admin。'), p('...', 'dependency selector', false, '包名后包含依赖，包名前包含依赖者；两侧同时使用可取完整关系范围。'), p('[base...head]', 'git range', false, '用 Git 变更选择包；浅克隆缺少基线时可能退化为全部受影响。')], related: ['turbo run', '--affected', '--dry=json'] },
  { name: '--affected', group: '命令与任务选择', signature: '--affected', beginner: '根据当前分支相对基线的 Git 变更，只选择受影响的包及必要任务，适合增量 CI。', when: 'Pull Request 只想构建、测试真正可能受改动影响的工作区时。', example: 'turbo run build lint test --affected', returns: '默认近似 --filter=...[main...HEAD]；若 Git 历史不足，安全退化为把全部包视为已变更。', principle: '受影响分析依赖 Git 范围和包图；开启 futureFlags.affectedUsingTaskInputs 后可进一步按任务 inputs 判断。', pitfall: 'CI 使用浅克隆却没拉取基线，会让 affected 总是全量；先检查 checkout depth 与 TURBO_SCM_BASE/HEAD。', officialUrl: RUN, params: [p('TURBO_SCM_BASE / HEAD', 'environment variable', false, '覆盖默认 main 与 HEAD 比较边界。')], related: ['--filter', 'inputs', '--dry=json'] },
  { name: '--dry=json', group: '命令与任务选择', signature: '--dry=json | --dry=text', beginner: '只计算并打印将执行的任务、哈希、输入、输出和依赖，不真正运行脚本，是改任务图后的第一道检查。', when: '新增 filter、dependsOn、with、inputs 或 CI 增量规则后先验证计划时。', example: 'turbo run dev --filter=@funhub/frontend-admin --dry=json', returns: '输出包含 taskId、hash、inputs、outputs、dependencies、dependents、environmentVariables 和 command。', principle: 'dry run 使用与真实运行相同的图构建和选择逻辑，因此比凭终端日志猜任务范围可靠。', pitfall: 'dry run 能证明选择和配置，不能证明底层脚本真的能运行或缓存产物完整，仍需一次真实执行。', officialUrl: RUN, params: [p('format', 'json | text', false, 'JSON 适合机器断言，text 适合人工快速阅读。', 'text')], related: ['--graph', '--summarize', 'turbo run'] },
  { name: '--graph', group: '命令与任务选择', signature: '--graph[=file.svg|file.html|file.mermaid|file.dot]', beginner: '把最终任务图输出为终端 DOT 或可视化文件，用来检查谁依赖谁、为什么某任务被拉入。', when: '任务顺序意外、出现环、filter 范围过大或准备向团队解释构建图时。', example: 'turbo run build --filter=@funhub/frontend-admin... --graph=graph.mermaid', returns: '生成 SVG、HTML、Mermaid 或 DOT 图；不执行实际任务。', principle: '图展示的是包图与 task dependsOn 合成后的执行关系，可用于发现遗漏依赖和过度串行。', pitfall: 'PNG/JPG/PDF/JSON 图格式已标记为未来 v3 移除；新脚本应改用 SVG、HTML、Mermaid 或 DOT。', officialUrl: RUN, params: [p('file', 'path with supported extension', false, '通过扩展名决定输出格式；不传时把 DOT 打到 stdout。')], related: ['--dry=json', 'dependsOn', '--summarize'] },
  { name: '--summarize', group: '命令与任务选择', signature: '--summarize', beginner: '在 .turbo/runs 中保存本次运行的任务、哈希、缓存和耗时元数据，便于比较 CI 或排查缓存。', when: '需要长期分析命中率、任务耗时或两次运行为什么行为不同。', example: 'turbo run build --summarize', returns: '任务完成后写入一份 JSON 运行摘要；摘要可由后续脚本归档和统计。', principle: 'summary 是调度元数据，不是业务 trace；它帮助解释任务层表现，但不会代替应用运行时观测。', pitfall: '摘要可能包含路径、命令和环境变量名称，归档前检查敏感元数据并设置合适保留期限。', officialUrl: RUN, params: [p('output directory', '.turbo/runs', false, '默认摘要目录；通常加入构建产物而不是源码提交。')], related: ['--dry=json', '--graph', 'outputLogs'] },
  { name: '--concurrency', group: '命令与任务选择', signature: '--concurrency=<integer|percentage>', beginner: '限制同时运行的任务数量，可写固定整数或 CPU 百分比；它保留任务图的依赖约束。', when: 'CI 内存不足、本机资源竞争，或需要在正确依赖顺序下提高并发时。', example: 'turbo run test --concurrency=100%', returns: '调度器最多同时运行指定数量的就绪任务，依赖未完成的任务仍会等待。', principle: '并发只作用于图中当前可运行节点；与丢弃图的 --parallel 完全不同。', pitfall: '盲目设 100% 可能耗尽内存、数据库连接或测试端口；先测资源瓶颈再决定。', officialUrl: RUN, params: [p('value', 'number | percentage', true, '固定槽位或可用 CPU 百分比；仓库配置默认值是 10。')], related: ['turbo run', 'concurrency', 'persistent'] },
  { name: '--continue', group: '命令与任务选择', signature: '--continue[=never|dependencies-successful|always]', beginner: '控制某个任务失败后，其他任务是否继续；默认 never 会尽快停止尚未开始的工作。', when: 'CI 希望一次收集多个独立包的 lint/test 失败，但又不应运行依赖失败产物的任务。', example: 'turbo run lint test --continue=dependencies-successful', returns: '按策略继续独立任务，最终仍以失败退出码结束并汇总失败。', principle: 'dependencies-successful 只允许依赖链仍成功的任务继续，比 always 更能防止级联假失败。', pitfall: '对部署、迁移等有副作用任务使用 always 可能在前置失败后继续破坏状态。', officialUrl: RUN, params: [p('strategy', 'never | dependencies-successful | always', false, '决定失败后的调度策略。', 'never')], related: ['turbo run', 'dependsOn', 'interactive'] },
  { name: '--only', group: '命令与任务选择', signature: '--only', beginner: '只运行指定任务本身，忽略 turbo.json 为它声明的依赖任务。', when: '确认前置产物已经可靠存在，并在调试时刻意跳过任务图依赖。', example: 'turbo run test --filter=web --only', returns: '执行选中的 test，不先执行其 dependsOn 指向的 build 等任务。', principle: 'only 是显式绕过依赖图的诊断开关，不应成为修复图配置的常规手段。', pitfall: '缓存或本地残留产物可能让它偶尔成功，干净 CI 却失败；使用前必须说明外部前置条件。', officialUrl: RUN, params: [p('enabled', 'boolean flag', false, '出现该 flag 即跳过依赖任务。')], related: ['dependsOn', '--dry=json', 'turbo run'] },
  { name: '--cache', group: '命令与任务选择', signature: '--cache=<local:rw|remote:rw|...>', beginner: '按本次运行选择读取和写入本地、远程缓存，也能把某个缓存源设为只读或只写。', when: '调试缓存、让不可信 PR 只读缓存，或临时禁用远程缓存时。', example: 'turbo run build --cache=local:rw,remote:r', returns: '只从允许的缓存源恢复或写入任务结果；默认 local:rw,remote:rw。', principle: '缓存策略与任务 cache:false 是两层开关：前者控制来源读写，后者决定具体任务是否可缓存。', pitfall: '给外部 PR 开放 remote:w 可能污染共享缓存；令牌权限与分支信任必须分层。', officialUrl: RUN, params: [p('sources', 'cache source list', true, 'local/remote 后跟 r、w、rw；遗漏某源即禁用该源。')], related: ['cache', 'remoteCache.signature', 'outputs'] },

  { name: 'tasks', group: '任务图配置', signature: 'tasks: Record<string, TaskConfiguration>', beginner: 'turbo.json 的核心映射：键是 package.json script 名或 package#task，值描述依赖、缓存和运行行为。', when: '要让同名脚本进入 Turbo 调度，或为某一个包覆盖通用任务规则时。', example: '"tasks": { "build": { "dependsOn": ["^build"], "outputs": ["dist/**"] } }', returns: '形成可被 turbo run 调用的任务定义；没有对应 package script 的包不会凭空获得实现。', principle: '根任务定义给所有包共享，package#task 做精确覆盖，包级 turbo.json 可通过 extends 继承根配置。', pitfall: '只在 package.json 写脚本却没在 tasks 注册，turbo run 可能提示任务未定义；反过来只写 tasks 也不会生成脚本。', officialUrl: CONFIG, params: [p('task name', 'string', true, '对应 workspace package.json scripts 的键。')], related: ['dependsOn', 'outputs', 'inputs'] },
  { name: 'dependsOn', group: '任务图配置', signature: 'dependsOn?: string[]', beginner: '声明当前任务必须等待哪些任务；^task 指依赖包的任务，task 指同包任务，package#task 指精确跨包任务。', when: '共享包必须先 build、测试前必须生成代码，或跨包任务有真实先后关系时。', example: '"build": { "dependsOn": ["^build"] }', returns: '在任务图中增加有向边，调度器只在所有前置成功后运行当前任务。', principle: '^build 沿 package.json 内部依赖上游展开；依赖必须来自真实包图，不能靠目录相邻推断。', pitfall: '把所有任务都串成链会失去并发；把 dev 依赖设为 persistent 任务也不允许，应改用 with。', officialUrl: TASKS, level: 'core', params: [p('^task', 'string', false, '当前包的依赖包先运行该任务。'), p('task', 'string', false, '当前包内先运行另一任务。'), p('package#task', 'string', false, '精确依赖另一个 workspace 包的任务。')], related: ['tasks', 'with', '--graph'] },
  { name: 'outputs', group: '任务图配置', signature: 'outputs?: string[]', beginner: '声明任务会生成哪些文件，缓存命中时 Turbo 才能把这些文件和日志一起恢复。', when: 'build、codegen、coverage 等任务会写入 dist、.next 或报告目录时。', example: '"outputs": [".next/**", "!.next/cache/**"]', returns: '任务成功后匹配文件进入缓存；命中时恢复到原路径。空数组意味着只缓存日志。', principle: 'outputs 是相对每个包目录的 glob，可用排除模式移除不值得缓存的临时文件。', pitfall: '漏写产物会出现“FULL TURBO 但文件不存在”；写得过宽又会缓存临时文件、秘密或巨量目录。', officialUrl: CACHE, level: 'core', params: [p('include globs', 'string[]', true, '需要在缓存命中时恢复的稳定产物。'), p('exclude globs', 'string[]', false, '以 ! 开头排除缓存、临时文件或不稳定内容。'), p('path base', 'package directory', true, '所有 glob 默认相对任务所在 workspace 包。')], related: ['inputs', 'cache', '--dry=json'] },
  { name: 'inputs', group: '任务图配置', signature: 'inputs?: string[]', beginner: '控制哪些文件变化会让任务缓存失效；一旦配置就要用 $TURBO_DEFAULT$ 保留默认 Git 跟踪文件集合。', when: '文档变化不应重跑测试，或任务只依赖某些源码、配置和环境文件时。', example: '"inputs": ["$TURBO_DEFAULT$", "!README.md", "$TURBO_ROOT$/tsconfig.json"]', returns: '匹配文件内容进入任务哈希；未匹配的文件变化不会使该任务 miss。', principle: '$TURBO_DEFAULT$ 保留安全默认输入，$TURBO_ROOT$ 从仓库根引用共享配置。', pitfall: '只写 src/** 会意外排除 package.json、锁文件相关信息或配置，造成错误命中；先从默认集合做减法。', officialUrl: CONFIG, level: 'core', params: [p('$TURBO_DEFAULT$', 'microsyntax', false, '保留默认文件输入后再追加/排除。'), p('$TURBO_ROOT$', 'microsyntax', false, '引用仓库根文件而不是包目录。'), p('glob', 'string', false, '纳入或用 ! 排除具体文件模式。')], related: ['outputs', 'globalDependencies', '--affected'] },
  { name: 'cache', group: '任务图配置', signature: 'cache?: boolean = true', beginner: '决定某种任务结果是否允许进入缓存；build/test 通常开启，dev/watch 等常驻进程必须关闭。', when: '任务结果确定且 outputs 可复现时开启；交互、常驻或依赖外部易变状态时关闭。', example: '"dev": { "cache": false, "persistent": true }', returns: 'false 时每次都真实执行且不读写任务缓存；true 时按哈希尝试命中。', principle: '可缓存任务必须满足相同输入产生相同输出，不能依赖未声明时间、网络或随机状态。', pitfall: '为了提速缓存不确定任务，会把偶发成功或外部状态冻结为共享错误结果。', officialUrl: CONFIG, params: [p('enabled', 'boolean', false, '是否允许该任务读写缓存。', 'true')], related: ['outputs', 'persistent', '--cache'] },
  { name: 'persistent', group: '任务图配置', signature: 'persistent?: boolean = false', beginner: '标记 dev server 或 watcher 这类不会自然退出的长进程，让 Turbo 正确管理终端 UI 和生命周期。', when: 'package script 启动 Vite、Next.js、tsdown --watch、i18n watcher 等服务时。', example: '"dev": { "persistent": true, "cache": false }', returns: '任务持续运行直到用户中断；其他任务不能用 dependsOn 等待它“完成”。', principle: '持久任务没有完成点，因此不能作为普通前置；需要一起启动的关系由 with 表达。', pitfall: '只设 persistent:true 但忘记 cache:false，会给开发进程建立不合理的缓存语义。', officialUrl: CONFIG, params: [p('enabled', 'boolean', false, '把任务声明为常驻进程。', 'false')], related: ['with', 'interruptible', 'cache'] },
  { name: 'interruptible', group: '任务图配置', signature: 'interruptible?: boolean = false', beginner: '允许 turbo watch 在文件变化时中断并重新启动一个持久任务，适合服务端需要整进程重启的开发模式。', when: 'watch 触发后旧服务必须结束再加载新构建，而不是依赖应用自身 HMR 时。', example: '"dev": { "persistent": true, "interruptible": true, "cache": false }', returns: '相关输入变化时旧任务收到中断，随后由 watch 启动新进程。', principle: 'interruptible 解决“重启长进程”，与 persistent 配合；它不是任务超时或生产进程守护。', pitfall: '数据库迁移、写文件等无法安全中断的任务不要开启，否则可能留下半完成状态。', officialUrl: CONFIG, params: [p('enabled', 'boolean', false, '是否允许 watch 重启该持久任务。', 'false')], related: ['persistent', 'turbo watch', 'interactive'] },
  { name: 'interactive', group: '任务图配置', signature: 'interactive?: boolean = false', beginner: '允许任务接收终端输入，例如开发 CLI 的确认或快捷键；普通 CI 任务通常保持关闭。', when: '本地长期运行的工具确实需要 stdin，且 Turbo UI 要把输入转发给它时。', example: '"dev": { "persistent": true, "interactive": true, "cache": false }', returns: '任务可获得交互式终端输入；非交互环境仍可能没有 TTY。', principle: '交互输入会降低自动化和可重放性，因此应限定在本地开发任务。', pitfall: 'CI 中等待输入会永久挂起；部署、测试必须提供非交互 flag 或默认值。', officialUrl: CONFIG, params: [p('enabled', 'boolean', false, '是否把任务视为可交互。', 'false')], related: ['persistent', 'ui', '--continue'] },
  { name: 'with', group: '任务图配置', signature: 'with?: string[]', beginner: '启动某个任务时同时启动指定任务，专门适合多个必须一起运行的开发服务或 watcher。', when: '启动 frontend-admin 时总要同时启动 platform 与 comment-sdk watcher，且它们都是持久任务。', example: '"dev": { "with": ["@funhub/platform#dev", "@funhub-ugc/comment-sdk#dev"] }', returns: '选择主 dev 任务时三个长进程一起启动并由 Turbo 统一管理。', principle: 'with 表达陪跑关系，不等待对方完成；这正是 persistent 任务不能用于 dependsOn 时的官方建模方式。', pitfall: '把所有仓库服务塞进根 dev with 会让任何开发都全量启动；应放在真正需要它们的包级 turbo.json。', officialUrl: TASKS, level: 'core', params: [p('package#task', 'string', true, '需要与当前任务共同运行的精确任务标识。'), p('extends', '["//"]', true, '包级 turbo.json 继承根规则，避免复制通用配置。'), p('host task', 'package task', true, '只有选择主任务时才拉起 with 列表，保持入口明确。')], related: ['persistent', 'dependsOn', '--filter'] },
  { name: 'env', group: '环境与缓存正确性', signature: 'env?: string[]', beginner: '列出会改变某个任务产物或行为的环境变量；变量名和值都会进入任务哈希，并暴露给子进程。', when: '构建依赖 API origin、feature flag、NODE_ENV 或公开前端变量时。', example: '"build": { "env": ["NODE_ENV", "NEXT_PUBLIC_*"] }', returns: '变量变化会触发 cache miss；strict 模式下声明变量也可被任务读取。', principle: 'env 同时承担“影响哈希”和“允许运行时访问”，适合真正决定结果的变量。', pitfall: '把秘密值写进配置文件是错误的；这里只写变量名。漏声明则可能复用另一环境的旧产物。', officialUrl: ENV, level: 'core', params: [p('exact names', 'string[]', false, '明确列出影响当前任务结果的变量。'), p('wildcards', 'string[]', false, '例如 NEXT_PUBLIC_*，统一覆盖框架公开变量。'), p('values', 'process environment', true, '实际值来自运行环境并参与哈希，不应硬编码在 turbo.json。')], related: ['passThroughEnv', 'globalEnv', 'envMode'] },
  { name: 'passThroughEnv', group: '环境与缓存正确性', signature: 'passThroughEnv?: string[]', beginner: '允许某任务读取指定环境变量，但这些值不进入哈希，适合不改变任务结果的凭证或运行元数据。', when: '任务需要访问令牌上传产物，但令牌本身不应导致每次缓存失效时。', example: '"deploy": { "passThroughEnv": ["VERCEL_TOKEN"] }', returns: 'strict 模式下子进程可读取这些变量；值变化不会自动 cache miss。', principle: '是否可见和是否影响结果是两件事；pass-through 只解决可见性。', pitfall: '若变量其实影响输出却放这里，会错误命中；部署等副作用任务通常还应 cache:false。', officialUrl: ENV, params: [p('names', 'string[]', true, '允许传给具体任务但不参与哈希的变量名。')], related: ['env', 'globalPassThroughEnv', 'cache'] },
  { name: 'outputLogs', group: '任务图配置', signature: 'outputLogs?: "full"|"hash-only"|"new-only"|"errors-only"|"none"', beginner: '控制任务执行和缓存命中时显示多少日志，减少大型 CI 噪声而不改变任务本身。', when: '希望缓存命中只显示哈希、CI 只显示错误，或调试时保留完整输出。', example: '"build": { "outputLogs": "new-only" }', returns: '按策略输出日志；缓存仍会保存任务日志供恢复和排查。', principle: '日志策略只影响展示，不影响哈希、退出码和产物缓存。', pitfall: '长期 errors-only 会隐藏性能退化和 warning；调试失败时先切回 full。', officialUrl: CONFIG, params: [p('mode', 'OutputLogsMode', false, 'full 最详细，none 最安静。', 'full')], related: ['--summarize', 'ui', 'outputs'] },

  { name: 'globalDependencies', group: '仓库级配置', signature: 'globalDependencies?: string[]', beginner: '声明会影响所有任务哈希的仓库级文件，例如根配置或共享环境文件。', when: '根 tsconfig、全局构建配置或 .env 文件变化必须让所有相关任务失效。', example: '"globalDependencies": [".env", "tsconfig.json"]', returns: '任一匹配文件内容变化都会让仓库中所有可缓存任务产生新哈希。', principle: '它是全局失效锤子，应只放真正影响所有任务的文件；更局部的依赖写进任务 inputs。', pitfall: '写 **/* 会让任何文档或临时文件变化都全量 miss，彻底抵消增量缓存。', officialUrl: CONFIG, params: [p('globs', 'string[]', true, '相对仓库根、影响全部任务的文件模式。')], related: ['inputs', 'globalEnv', '--affected'] },
  { name: 'globalEnv', group: '环境与缓存正确性', signature: 'globalEnv?: string[]', beginner: '声明影响仓库所有任务结果的环境变量，并让它们进入全局哈希和子进程环境。', when: 'NODE_ENV 或统一版本号确实会改变几乎所有包的构建结果时。', example: '"globalEnv": ["NODE_ENV"]', returns: '任一变量变化会让所有任务缓存失效；strict 模式下任务可读取它们。', principle: '全局环境变量变化放大到整张任务图，局部变量优先使用 task env。', pitfall: '把每个应用自己的 URL 都设成 globalEnv 会制造无谓全仓 miss。', officialUrl: CONFIG, params: [p('names', 'string[]', true, '影响所有任务哈希的变量名或 wildcard。')], related: ['env', 'globalPassThroughEnv', 'globalDependencies'] },
  { name: 'globalPassThroughEnv', group: '环境与缓存正确性', signature: 'globalPassThroughEnv?: string[]', beginner: '让全部任务都能读取某些变量，但不把值加入哈希，通常只适合通用 CI 元数据或凭证。', when: '所有包都需要访问同一 CI 标识或只用于上传的 token，且它不改变产物。', example: '"globalPassThroughEnv": ["CI", "GITHUB_ACTIONS"]', returns: 'strict 模式下所有任务子进程获得变量，值变化不触发全仓 cache miss。', principle: '它扩大变量暴露面，应比 globalEnv 更谨慎并遵循最小权限。', pitfall: '不要把无需给所有包的秘密放全局；优先使用具体任务 passThroughEnv。', officialUrl: CONFIG, params: [p('names', 'string[]', true, '传给所有任务但不参与哈希的变量名。')], related: ['passThroughEnv', 'globalEnv', 'envMode'] },
  { name: 'envMode', group: '环境与缓存正确性', signature: 'envMode?: "strict" | "loose" = "strict"', beginner: 'strict 只把声明过的环境变量交给任务；loose 会把当前进程全部环境传下去。', when: '生产和 CI 使用 strict 建立可解释边界；迁移旧仓库时可短暂用 loose 排查漏声明。', example: '"envMode": "strict"', returns: '决定子进程可见环境集合；不会自动读取 .env 文件。', principle: '严格模式让环境依赖显式化，减少本机偶然变量造成的不可复现结果。', pitfall: '切 loose 让构建成功只是掩盖配置缺口；补齐 env/passThroughEnv 后应恢复 strict。', officialUrl: CONFIG, params: [p('mode', 'strict | loose', false, '环境变量过滤模式。', 'strict')], related: ['env', 'passThroughEnv', 'globalEnv'] },
  { name: 'concurrency', group: '仓库级配置', signature: 'concurrency?: number | string = 10', beginner: '为仓库设置默认并发任务上限，可写整数或百分比，命令行 --concurrency 可临时覆盖。', when: '团队希望所有本地和 CI 运行有统一资源上限。', example: '"concurrency": "75%"', returns: '限制同时运行的就绪任务数量，但保持依赖图顺序。', principle: '最优并发取决于 CPU、内存、I/O 和外部服务，不是越高越快。', pitfall: '固定 20 在小 CI runner 上可能 OOM；百分比也不限制数据库连接等非 CPU 资源。', officialUrl: CONFIG, params: [p('value', 'number | percentage string', false, '默认任务并发槽位。', '10')], related: ['--concurrency', 'persistent', 'ui'] },
  { name: 'ui', group: '仓库级配置', signature: 'ui?: "stream" | "tui" = "stream"', beginner: '选择传统流式日志或交互式终端界面；只影响本地查看和交互，不改变任务图。', when: '本地多 dev 进程想切换查看日志可用 tui，CI 通常使用 stream。', example: '"ui": "stream"', returns: '以选定界面展示任务状态和日志。', principle: 'UI 是观察调度的表层；机器分析应使用 dry JSON 或 summarize。', pitfall: '依赖 TUI 快捷键的工作流在无 TTY CI 中不可用，脚本不能以界面文本作为稳定 API。', officialUrl: CONFIG, params: [p('mode', 'stream | tui', false, '日志与交互界面模式。', 'stream')], related: ['interactive', 'outputLogs', '--summarize'] },
  { name: 'cacheDir', group: '仓库级配置', signature: 'cacheDir?: string = ".turbo/cache"', beginner: '改变本地文件缓存目录，便于磁盘挂载或 CI 缓存，但多数仓库保持默认即可。', when: '构建环境需要把缓存放到专用持久卷或受控路径。', example: '"cacheDir": ".cache/turbo"', returns: '本地缓存对象读写到指定仓库相对目录。', principle: '当前版本可自动在 worktree 间共享默认缓存；显式改 cacheDir 会改变这项行为。', pitfall: '把缓存目录提交 Git 或混入 Docker build context 会膨胀仓库与镜像。', officialUrl: CONFIG, params: [p('path', 'string', false, '仓库相对本地缓存路径。', '.turbo/cache')], related: ['--cache', 'remoteCache.signature', 'outputs'] },
  { name: 'remoteCache.signature', group: '仓库级配置', signature: 'remoteCache: { signature: true }', beginner: '让远程缓存产物带 HMAC-SHA256 签名，客户端恢复前验证内容未被替换。', when: '团队共享远程缓存并需要提高供应链完整性保护时。', example: '"remoteCache": { "signature": true }', returns: '写入产物附签名，读取时使用 TURBO_REMOTE_CACHE_SIGNATURE_KEY 验证。', principle: '签名保证完整性与来源密钥持有者，不负责加密缓存内容。', pitfall: '签名密钥泄漏后攻击者仍能伪造；轮换密钥会让旧缓存无法验证，需要规划失效。', officialUrl: CONFIG, params: [p('TURBO_REMOTE_CACHE_SIGNATURE_KEY', 'secret environment variable', true, '用于 HMAC 签名和验证，只保存在受信 Secret 管理中。')], related: ['--cache', 'cacheDir', 'globalPassThroughEnv'] },

  { name: 'turbo watch', group: '开发与交付工作流', signature: 'turbo watch [tasks] [options]', beginner: '持续观察任务输入和依赖变化，自动重新运行受影响任务；可配合 interruptible 重启持久进程。', when: '代码生成、库构建和服务端进程需要跨包联动重跑，而单个工具自身 watch 不足时。', example: 'turbo watch dev --filter=@funhub/frontend-admin...', returns: '初次运行任务后持续驻留，变化时只重跑受影响任务，直到收到中断。', principle: 'watch 复用任务图和输入定义，inputs 不准确会直接导致漏重跑或过度重跑。', pitfall: '不要同时叠加多层 watcher 写同一产物，否则会形成更新风暴和竞态。', officialUrl: 'https://turborepo.dev/docs/reference/watch', params: [p('tasks', 'string[]', true, '需要被持续监视和重跑的任务。')], related: ['interruptible', 'inputs', 'persistent'] },
  { name: 'turbo prune --docker', group: '开发与交付工作流', signature: 'turbo prune <package> --docker [--out-dir <dir>]', beginner: '为一个目标应用生成只包含它及其内部依赖的精简 monorepo，并把依赖清单与完整源码分层输出给 Docker。', when: 'monorepo 中只部署一个 Next.js/Node 应用，希望无关包变化不使依赖安装层失效。', example: 'turbo prune @funhub/frontend-admin --docker', returns: '默认生成 out/json、out/full 和裁剪后的 lockfile；Docker 可先复制 json 安装，再复制 full 构建。', principle: 'prune 保留目标应用所需的部分 monorepo；与 pnpm deploy 的独立 node_modules 交付目标不同。', pitfall: '裁剪只依据声明的 workspace 依赖；源码中偷偷跨包相对导入但未声明依赖会在镜像构建时暴露。', officialUrl: PRUNE, level: 'core', params: [p('package', 'workspace package name', true, '最终要构建/部署的目标包。'), p('--docker', 'boolean flag', true, '把 manifests 与 full source 分层输出。'), p('--out-dir', 'path', false, '自定义裁剪目录。', 'out')], related: ['outputs', '--filter', '--dry=json'] },
  { name: 'turbo docs', group: '开发与交付工作流', signature: 'turbo docs [query]', beginner: '从本地 Turbo CLI 打开或查询与当前安装版本匹配的官方文档，避免照搬不兼容版本示例。', when: '升级后确认某个配置字段、命令 flag 或迁移语义时。', example: 'turbo docs "with persistent tasks"', returns: '打开或返回与本地版本对应的官方文档结果；该能力要求 Turbo 2.7.5 或更高。', principle: '版本匹配文档能减少搜索结果中旧 turbo.build 域名和过期 flag 带来的混淆。', pitfall: '它是查文档入口，不会检查当前仓库配置正确性；仍需 dry run、构建与测试证据。', officialUrl: 'https://turborepo.dev/docs/reference/docs', params: [p('query', 'string', false, '希望检索的命令、字段或概念。')], related: ['--dry=json', 'turbo run', 'with'] },
]

export const turborepoFrameworkApis: FrameworkApiReference[] = definitions.map((item) => ({
  slug: `turborepo-${item.group.replace(/[^a-zA-Z0-9\u4e00-\u9fa5]+/g, '-').toLowerCase()}-${item.name.replace(/[^a-zA-Z0-9]+/g, '-').replace(/^-|-$/g, '').toLowerCase()}`,
  technologySlug: 'turborepo', name: item.name, group: item.group,
  kind: item.name.startsWith('turbo') || item.name.startsWith('--') ? 'command' : 'configuration',
  maturity: 'stable', signature: item.signature, beginner: item.beginner, whenToUse: item.when,
  example: item.example, exampleLanguage: item.name.startsWith('turbo') || item.name.startsWith('--') ? 'bash' : 'jsonc',
  returns: item.returns, interview: item.principle, pitfall: item.pitfall, officialUrl: item.officialUrl,
  learningLevel: item.level ?? 'advanced', runtime: 'server',
  parameters: (item.params ?? [p('value', 'configuration value', true, '按照签名提供当前任务所需值，并先用 dry run 检查最终解析结果。')]).map(([name, type, required, description, defaultValue]) => ({ name, type, required, description: `${description} 该值必须与当前仓库任务合同保持一致。`, defaultValue })),
  expectedOutput: `${item.returns} 验收时同时检查命令退出码、--dry=json 的任务范围以及需要恢复的产物，不只看一行成功日志。`,
  errorCases: [{ condition: item.pitfall.split('；')[0], handling: `${item.pitfall} 先运行 --dry=json 与 --graph 定位任务图，再清理本地缓存做一次冷启动验证。` }],
  relatedApis: item.related,
}))
