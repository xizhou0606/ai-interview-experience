import type { FrameworkApiReference } from '../types'
import { betterAuthLearningMeta } from './learning-meta'

type BetterAuthApiInput = {
  id: string
  name: string
  group: string
  signature: string
  purpose: string
  when: string
  call: string
  returns: string
  principle: string
  risk: string
  url: string
  kind?: FrameworkApiReference['kind']
  maturity?: FrameworkApiReference['maturity']
}

const betterAuthApi = ({
  id,
  name,
  group,
  signature,
  purpose,
  when,
  call,
  returns,
  principle,
  risk,
  url,
  kind = 'function',
  maturity = 'stable',
}: BetterAuthApiInput): FrameworkApiReference => {
  const learningMeta = betterAuthLearningMeta[name as keyof typeof betterAuthLearningMeta]
  if (!learningMeta) throw new Error(`Missing structured Better Auth learning metadata for ${name}`)
  return {
    slug: `better-auth-${id}`,
    technologySlug: 'better-auth',
    name,
    group,
    kind,
    maturity,
    signature,
    beginner: `${purpose} 可以把它理解成认证系统暴露给应用代码的一块明确能力，而不是让页面直接读写用户、账号或会话表。`,
    whenToUse: `${when} 调用前先判断它运行在浏览器还是可信服务端，并准备该端点要求的 cookie、headers、body 或 query。`,
    example: call,
    exampleLanguage: 'ts',
    returns: `${returns} 客户端通常得到 { data, error }，服务端 auth.api 失败默认抛 APIError；具体形状由当前 auth 配置和插件做类型推断。`,
    interview: `${principle} 面试时还要说明：认证回答“你是谁”，授权回答“你能做什么”；会话 token 是凭证而不是用户资料，敏感操作应校验权限、CSRF/Origin、会话新鲜度并保留审计记录。`,
    pitfall: `${risk} 不要信任客户端传来的 userId、role、organizationId 或重定向地址；服务端必须重新依据当前会话、成员关系和允许列表完成校验。`,
    officialUrl: url,
    ...learningMeta,
  }
}

const DOCS = {
  installation: 'https://www.better-auth.com/docs/installation',
  options: 'https://www.better-auth.com/docs/reference/options',
  basic: 'https://www.better-auth.com/docs/basic-usage',
  api: 'https://www.better-auth.com/docs/concepts/api',
  client: 'https://www.better-auth.com/docs/concepts/client',
  session: 'https://www.better-auth.com/docs/concepts/session-management',
  email: 'https://www.better-auth.com/docs/authentication/email-password',
  users: 'https://www.better-auth.com/docs/concepts/users-accounts',
  admin: 'https://www.better-auth.com/docs/plugins/admin',
  organization: 'https://www.better-auth.com/docs/plugins/organization',
  passkey: 'https://www.better-auth.com/docs/plugins/passkey',
} as const

const foundationApis = [
  betterAuthApi({ id: 'better-auth', name: 'betterAuth', group: '实例与调用约定', kind: 'function', signature: 'betterAuth(options: BetterAuthOptions): Auth', purpose: '创建服务端认证实例，统一装配数据库、密钥、基础 URL、邮箱密码、社交登录、会话策略和插件。', when: '应用启动时创建一次并导出共享实例', call: `import { betterAuth } from 'better-auth'\n\nexport const auth = betterAuth({\n  database,\n  emailAndPassword: { enabled: true },\n  trustedOrigins: ['https://app.example.com'],\n})`, returns: '带 handler、api、插件推断类型和配置上下文的 Auth 实例。', principle: '它是认证边界的组合根：配置决定开放哪些端点、怎样落库以及 cookie 和 token 的安全属性。', risk: '不要在每个请求中重复创建实例，也不要把低熵 secret、错误 baseURL 或通配 trustedOrigins 带入生产。', url: DOCS.installation }),
  betterAuthApi({ id: 'auth-handler', name: 'auth.handler', group: '实例与调用约定', kind: 'transport', signature: 'auth.handler(request: Request): Promise<Response>', purpose: '把标准 Web Request 交给 Better Auth 路由器，使 /api/auth/* 下的 GET、POST 和插件端点真正可以被浏览器访问。', when: '在 Next.js、Hono、Workers、React Router 等框架的 catch-all 认证路由挂载实例', call: `app.on(['GET', 'POST'], '/api/auth/*', c =>\n  auth.handler(c.req.raw),\n)`, returns: '标准 Response，可能包含 JSON、OAuth 重定向以及 Set-Cookie 等认证响应头。', principle: 'handler 是 HTTP 信任边界；所有公开认证流最终都经过端点校验、限流、hook 和 cookie 写入。', risk: '不要在 handler 前放会提前消费 request body 的中间件，也不能漏转发 OAuth callback 或 Set-Cookie。', url: DOCS.installation }),
  betterAuthApi({ id: 'auth-api', name: 'auth.api', group: '实例与调用约定', kind: 'object', signature: 'auth.api.<endpoint>({ body?, query?, headers?, asResponse?, returnHeaders? })', purpose: '把当前实例中所有核心与插件 HTTP 端点映射成可在服务端直接调用、且能推断参数与返回类型的函数对象。', when: 'Server Action、Route Handler、后台任务或测试需要复用同一认证规则', call: `const session = await auth.api.getSession({\n  headers: request.headers,\n  query: { disableCookieCache: true },\n})`, returns: '默认直接返回端点业务数据；asResponse 返回 Response，returnHeaders 同时返回响应头。', principle: '它复用的仍是端点语义而非数据库捷径，因此插件、hook、校验和错误契约保持一致。', risk: '服务端直调不会自动拥有用户身份；需要会话的端点仍必须传入原始 headers，设置 cookie 时还要转发响应头。', url: DOCS.api }),
  betterAuthApi({ id: 'create-auth-client', name: 'createAuthClient', group: '实例与调用约定', signature: 'createAuthClient(options?: ClientOptions): AuthClient', purpose: '创建浏览器或前端框架使用的类型安全客户端，配置认证服务地址、fetch 行为、会话刷新策略和客户端插件。', when: '前端应用初始化时创建并复用单例客户端', call: `import { createAuthClient } from 'better-auth/react'\n\nexport const authClient = createAuthClient({\n  baseURL: 'https://api.example.com',\n  sessionOptions: { refetchOnWindowFocus: true },\n})`, returns: '包含 signIn、signUp、session、账号方法、hooks 和插件扩展的 AuthClient。', principle: '客户端只是受控的 HTTP 代理和响应式状态层，最终安全判断必须发生在认证服务器。', risk: '跨域部署要正确配置 baseURL、credentials、CORS 和 trustedOrigins；客户端插件必须和服务端插件配套。', url: DOCS.client }),
  betterAuthApi({ id: 'client-fetch', name: 'authClient.$fetch', group: '实例与调用约定', signature: 'authClient.$fetch<T>(path: string, options?: BetterFetchOption): Promise<BetterFetchResponse<T>>', purpose: '暴露客户端底层 better-fetch 实例，用统一 baseURL、凭证、插件和错误解析调用没有专用方法的自定义认证端点。', when: '调用自建 Better Auth 插件端点或需要较底层的请求控制', call: `const { data, error } = await authClient.$fetch('/profile/preferences', {\n  method: 'POST',\n  body: { locale: 'zh-CN' },\n})`, returns: '包含类型化 data 或标准化 error 的 better-fetch 响应。', principle: '统一 fetch 管线保证 cookie、错误码、回调和客户端插件行为与内置方法一致。', risk: '不要用 $fetch 绕过已有类型安全方法；路径、method、body 仍需与服务端端点契约完全一致。', url: DOCS.client }),
  betterAuthApi({ id: 'error-codes', name: 'authClient.$ERROR_CODES', group: '实例与调用约定', kind: 'object', signature: 'authClient.$ERROR_CODES: Record<string, string>', purpose: '提供当前客户端可识别的认证错误码集合，便于用稳定 code 做国际化、表单提示和分支处理。', when: '实现登录注册错误翻译或穷举错误处理', call: `type AuthCode = keyof typeof authClient.$ERROR_CODES\nconst messages: Partial<Record<AuthCode, string>> = {\n  USER_ALREADY_EXISTS: '该邮箱已注册',\n}`, returns: '只读错误码映射；真正请求失败时仍从 result.error.code 读取本次错误。', principle: '机器可读 code 应驱动程序分支，message 主要面向人且可能变化或被本地化。', risk: '不要把后端详细错误直接展示给用户，以免泄露账号是否存在、内部配置或数据库信息。', url: DOCS.client }),
  betterAuthApi({ id: 'api-error', name: 'APIError', group: '实例与调用约定', kind: 'class', signature: 'new APIError(status, body?, headers?)', purpose: '表示 Better Auth 服务端 API 调用失败，携带 HTTP 状态、稳定错误信息和可选响应头。', when: '在服务端捕获 auth.api 错误，或自定义插件端点主动返回认证错误', call: `import { APIError } from 'better-auth/api'\n\nthrow new APIError('FORBIDDEN', {\n  message: '当前成员没有此权限',\n})`, returns: '一个可被 Better Auth 端点层转换为规范错误响应的 Error 实例。', principle: '结构化错误把传输状态、业务错误码和安全的用户提示分开，便于一致监控和客户端处理。', risk: '不要在 message、body 或日志中放密码、token、密钥与账号枚举细节。', url: DOCS.api }),
  betterAuthApi({ id: 'is-api-error', name: 'isAPIError', group: '实例与调用约定', signature: 'isAPIError(error: unknown): error is APIError', purpose: '用类型守卫判断 unknown 异常是否来自 Better Auth API，以便安全读取 status、message 等字段。', when: 'try/catch 包裹服务端 auth.api 调用并需要分类处理错误', call: `try {\n  await auth.api.signInEmail({ body })\n} catch (error) {\n  if (isAPIError(error)) return { code: error.status }\n  throw error\n}`, returns: '布尔值，并在 true 分支把 TypeScript 类型收窄为 APIError。', principle: '错误边界应区分预期认证失败与未知系统故障，前者可转换，后者应继续抛出并告警。', risk: '不要 catch 后一律返回成功或 401；数据库故障、程序错误与权限不足需要不同状态和监控。', url: DOCS.api }),
]

const coreAuthApis = [
  betterAuthApi({ id: 'sign-up-email', name: 'signUp.email / auth.api.signUpEmail', group: '核心认证与账号', signature: 'signUp.email({ name, email, password, image?, callbackURL? })', purpose: '创建邮箱密码用户，并依据配置决定是否自动登录、发送验证邮件以及写入新会话。', when: '开放账号注册页面或受信服务端代办注册', call: `const { data, error } = await authClient.signUp.email({\n  name: 'Ada', email, password, callbackURL: '/dashboard',\n})`, returns: '新用户、会话或需要验证/跳转的信息。', principle: '注册是建立身份而不是只插入 user；密码哈希、邮箱唯一性、验证策略和反滥用必须一起设计。', risk: '不要记录明文密码，也要避免通过不同错误或耗时泄露邮箱是否已存在。', url: DOCS.email }),
  betterAuthApi({ id: 'sign-in-email', name: 'signIn.email / auth.api.signInEmail', group: '核心认证与账号', signature: 'signIn.email({ email, password, rememberMe?, callbackURL? })', purpose: '校验邮箱密码凭据，成功后创建会话并在浏览器写入安全 cookie。', when: '用户使用本地邮箱密码登录', call: `const { data, error } = await authClient.signIn.email({\n  email, password, rememberMe: false,\n})`, returns: '登录用户、会话和可选跳转信息。', principle: '服务端只比较安全哈希，成功后发放随机会话 token；rememberMe 影响持久性，不等于绕过过期策略。', risk: '登录端点需要限流、防撞库和统一失败提示；不能在服务端直调后丢失 Set-Cookie。', url: DOCS.email }),
  betterAuthApi({ id: 'sign-in-social', name: 'signIn.social / auth.api.signInSocial', group: '核心认证与账号', signature: 'signIn.social({ provider, callbackURL?, idToken?, disableRedirect? })', purpose: '启动 OAuth/OIDC 社交登录，或校验受支持 provider 签发的 idToken 后建立本地账号和会话。', when: '接入 Google、GitHub、Apple 等官方 provider', call: `await authClient.signIn.social({\n  provider: 'github',\n  callbackURL: '/dashboard',\n  errorCallbackURL: '/login-error',\n})`, returns: '通常返回 provider 授权 URL/重定向响应，token 模式可直接返回用户与会话。', principle: 'OAuth state 防 CSRF，PKCE 绑定授权请求，回调后仍要校验 issuer、audience、nonce 和邮箱可信度。', risk: '不要强制链接未验证邮箱，也不要接受任意 callbackURL、provider 或来源不明的 idToken。', url: DOCS.basic }),
  betterAuthApi({ id: 'sign-out', name: 'signOut / auth.api.signOut', group: '核心认证与账号', signature: 'signOut(options?: { fetchOptions? }): Promise<Result>', purpose: '撤销当前会话并清除当前设备的认证 cookie，使后续请求不再被识别为已登录用户。', when: '用户主动退出或检测到本设备会话需要终止', call: `await authClient.signOut({\n  fetchOptions: { onSuccess: () => location.assign('/login') },\n})`, returns: '退出成功状态，并通过响应头使浏览器删除会话 cookie。', principle: '退出登录至少包含服务端会话失效和客户端凭证清除；仅清本地状态无法撤销被盗 token。', risk: '不要在请求真正成功前只清 UI；跨域时还要确保 Set-Cookie 能到达浏览器。', url: DOCS.basic }),
  betterAuthApi({ id: 'send-verification-email', name: 'sendVerificationEmail / auth.api.sendVerificationEmail', group: '核心认证与账号', signature: 'sendVerificationEmail({ email, callbackURL? })', purpose: '为指定邮箱生成一次性验证 token，并调用已配置的发送函数投递验证链接。', when: '注册后、登录被要求验证时或用户主动重新发送验证邮件', call: `await authClient.sendVerificationEmail({\n  email, callbackURL: '/email-verified',\n})`, returns: '通用成功状态；邮件由服务端回调异步投递。', principle: '验证链接证明用户控制该邮箱；token 应一次性、短期、绑定用途，并在使用后失效。', risk: '响应与耗时不要暴露邮箱是否存在；链接不可泄露到日志、分析参数或第三方 Referer。', url: DOCS.email }),
  betterAuthApi({ id: 'verify-email', name: 'verifyEmail / auth.api.verifyEmail', group: '核心认证与账号', signature: 'verifyEmail({ query: { token, callbackURL? } })', purpose: '消费邮件中的验证 token，把用户邮箱标记为已验证，并按配置决定是否自动建立会话。', when: '用户点击验证邮件链接后的回调页面或服务端路由', call: `const result = await auth.api.verifyEmail({\n  query: { token, callbackURL: '/dashboard' },\n})`, returns: '验证后的用户/状态以及可能的重定向响应。', principle: '一次性 token 的正确消费需要原子校验过期时间、用途和使用状态，防止重放。', risk: '不要把“能打开链接”当长期授权；验证后敏感操作仍需当前会话和权限检查。', url: DOCS.email }),
  betterAuthApi({ id: 'request-password-reset', name: 'requestPasswordReset / auth.api.requestPasswordReset', group: '核心认证与账号', signature: 'requestPasswordReset({ email, redirectTo? })', purpose: '生成密码重置 token 并触发重置邮件，启动忘记密码流程但不会直接修改密码。', when: '登录页提供“忘记密码”入口', call: `await authClient.requestPasswordReset({\n  email, redirectTo: '/reset-password',\n})`, returns: '不暴露账号存在性的通用确认结果。', principle: '重置链接本质是短期高权限凭证，应限制用途、有效期、频率并在成功后失效。', risk: '发送邮件建议异步避免计时枚举；redirectTo 必须是可信地址，日志中不得记录 token。', url: DOCS.email }),
  betterAuthApi({ id: 'reset-password', name: 'resetPassword / auth.api.resetPassword', group: '核心认证与账号', signature: 'resetPassword({ newPassword, token })', purpose: '校验一次性重置 token 后为对应账号写入新密码哈希。', when: '用户从重置邮件进入可信页面并提交符合策略的新密码', call: `const { data, error } = await authClient.resetPassword({\n  token, newPassword,\n})`, returns: '密码更新成功状态或 token 无效、过期的标准错误。', principle: '密码重置是凭证轮换，通常还应撤销旧会话并通知账号所有者。', risk: '不能只在前端校验密码强度；重置 token 必须防重放，成功后应考虑撤销所有现有会话。', url: DOCS.email }),
  betterAuthApi({ id: 'update-user', name: 'updateUser / auth.api.updateUser', group: '核心认证与账号', signature: 'updateUser({ name?, image?, ...additionalFields })', purpose: '让当前登录用户更新允许自助修改的资料字段，而不是任意覆盖完整 user 记录。', when: '个人资料页修改昵称、头像或已声明的 additionalFields', call: `await authClient.updateUser({\n  name: 'Ada Lovelace', image: avatarURL,\n})`, returns: '更新后的用户数据或成功状态。', principle: 'mass assignment 必须由服务端字段白名单约束，身份字段与授权字段不能和普通资料同等开放。', risk: '不要允许客户端修改 id、role、emailVerified 等受保护字段；图片 URL 与文本也要做内容安全处理。', url: DOCS.users }),
  betterAuthApi({ id: 'change-email', name: 'changeEmail / auth.api.changeEmail', group: '核心认证与账号', signature: 'changeEmail({ newEmail, callbackURL? })', purpose: '启动邮箱变更流程，并根据配置要求当前邮箱或新邮箱验证后才完成替换。', when: '已登录用户需要更换登录邮箱', call: `await authClient.changeEmail({\n  newEmail, callbackURL: '/settings/security',\n})`, returns: '已更新用户或等待验证的流程状态。', principle: '邮箱常是登录标识和恢复通道，变更必须防止攻击者借已劫持会话永久接管账号。', risk: '高风险系统应要求新鲜会话、重新认证与双向通知；不要默认跳过新邮箱验证。', url: DOCS.users }),
  betterAuthApi({ id: 'change-password', name: 'changePassword / auth.api.changePassword', group: '核心认证与账号', signature: 'changePassword({ currentPassword, newPassword, revokeOtherSessions? })', purpose: '让已有密码的当前用户验证旧密码后更换为新密码。', when: '账号安全设置中的主动改密', call: `await authClient.changePassword({\n  currentPassword, newPassword, revokeOtherSessions: true,\n})`, returns: '密码变更成功状态，并可同时撤销其他设备会话。', principle: '再次验证旧凭据能降低仅凭被盗会话改密的风险，撤销其他会话则缩短攻击者存活时间。', risk: 'OAuth-only 用户可能没有本地密码；不要把当前密码或新密码写入日志、错误跟踪和分析事件。', url: DOCS.users }),
  betterAuthApi({ id: 'set-password', name: 'auth.api.setPassword', group: '核心认证与账号', signature: 'auth.api.setPassword({ body: { newPassword }, headers })', purpose: '在可信服务端为当前会话用户设置本地密码，常用于原先只通过社交账号登录的用户。', when: '经过额外身份确认后给 OAuth-only 账号补充邮箱密码登录能力', call: `await auth.api.setPassword({\n  body: { newPassword },\n  headers: request.headers,\n})`, returns: '密码账号建立或更新后的成功状态。', principle: '增加一种登录凭据会扩大账号攻击面，因此必须把它当敏感安全设置而非普通资料编辑。', risk: '官方建议普通用户优先走忘记密码流程；不能仅凭长期旧会话无二次验证地设置密码。', url: DOCS.users }),
  betterAuthApi({ id: 'verify-password', name: 'auth.api.verifyPassword', group: '核心认证与账号', signature: 'auth.api.verifyPassword({ body: { password }, headers })', purpose: '在服务端验证当前用户输入的现有密码，不修改账号，用于敏感动作前重新确认身份。', when: '删除账号、导出密钥或修改安全设置前实施 step-up authentication', call: `await auth.api.verifyPassword({\n  body: { password }, headers: request.headers,\n})`, returns: '密码是否有效的结果；无密码账号需要选择其他重新认证方式。', principle: '重新认证把“持有会话”提升为“近期证明掌握主凭据”，可抵御无人值守设备上的会话滥用。', risk: '需要限流并立即丢弃明文密码；一次验证的授权窗口应短且绑定后续具体操作。', url: DOCS.users }),
  betterAuthApi({ id: 'delete-user', name: 'deleteUser / auth.api.deleteUser', group: '核心认证与账号', signature: 'deleteUser({ password? | token?, callbackURL? })', purpose: '按配置验证当前用户后删除账号，并触发相关生命周期回调清理认证数据。', when: '账号设置提供自助注销和隐私删除请求', call: `await authClient.deleteUser({\n  password, callbackURL: '/goodbye',\n})`, returns: '删除确认或要求邮件 token、密码、会话新鲜度的错误。', principle: '账号删除同时涉及身份撤销、数据保留合规、级联关系和不可逆性，应由事务与异步清理共同完成。', risk: '不要仅凭普通旧会话直接删除；必须防 CSRF，并明确业务数据、审计日志和备份的保留策略。', url: DOCS.users }),
  betterAuthApi({ id: 'list-accounts', name: 'listAccounts / auth.api.listUserAccounts', group: '核心认证与账号', signature: 'listAccounts(): Promise<Account[]>', purpose: '列出当前用户已关联的邮箱密码或 OAuth provider 账号，供安全设置页展示登录方式。', when: '展示和管理账号绑定、判断是否保留至少一种可登录凭据', call: `const { data: accounts } = await authClient.listAccounts()`, returns: '去除敏感 token 后的账号连接列表。', principle: 'user 是业务身份，account 是某个 provider 的凭据连接；一个 user 可以关联多个 account。', risk: '响应中不应暴露 accessToken、refreshToken 或 provider 密钥，也要避免在无会话时查询他人账号。', url: DOCS.users }),
  betterAuthApi({ id: 'link-social', name: 'linkSocial / auth.api.linkSocialAccount', group: '核心认证与账号', signature: 'linkSocial({ provider, callbackURL?, scopes? })', purpose: '在已登录用户确认下启动 OAuth 流程，把新的社交 provider 账号连接到当前 user。', when: '安全设置中增加另一种登录方式或授权额外 provider scope', call: `await authClient.linkSocial({\n  provider: 'github', callbackURL: '/settings/accounts',\n})`, returns: 'provider 授权跳转信息，回调成功后创建关联 account。', principle: '账号链接要证明同时控制当前会话与新 provider；邮箱相同本身不是充分证明。', risk: '谨慎配置 trustedProviders 和 allowDifferentEmails；错误的强制链接会造成账号接管。', url: DOCS.users }),
  betterAuthApi({ id: 'unlink-account', name: 'unlinkAccount / auth.api.unlinkAccount', group: '核心认证与账号', signature: 'unlinkAccount({ providerId, accountId? })', purpose: '从当前 user 移除一个已关联 provider 账号，使该凭据之后不能再登录此用户。', when: '用户撤销旧社交账号或清理不再使用的登录方式', call: `await authClient.unlinkAccount({\n  providerId: 'github', accountId,\n})`, returns: '解除关联后的账号信息或成功状态。', principle: '解绑是凭据撤销；系统必须确保用户仍保留至少一种可用且安全的恢复/登录方式。', risk: '不要允许解除最后一个可登录账号而把用户锁死；还要处理 provider token 的撤销与删除。', url: DOCS.users }),
  betterAuthApi({ id: 'refresh-token', name: 'refreshToken / auth.api.refreshToken', group: '核心认证与账号', signature: 'refreshToken({ providerId, accountId?, userId? })', purpose: '使用已保存的 OAuth refresh token 向 provider 换取新的 access token，并更新过期信息。', when: '服务端代表用户调用第三方 API 且现有 access token 已过期', call: `const token = await auth.api.refreshToken({\n  body: { providerId: 'google', accountId },\n  headers: request.headers,\n})`, returns: '新的 accessToken、可能轮换的 refreshToken 和过期时间。', principle: 'access token 短期使用，refresh token 长期且更敏感；轮换和存储策略决定泄露后的影响范围。', risk: '只能在可信服务端使用和保存 refresh token；provider 可能不返回新 refresh token，不能误删旧值。', url: DOCS.users }),
  betterAuthApi({ id: 'get-access-token', name: 'getAccessToken / auth.api.getAccessToken', group: '核心认证与账号', signature: 'getAccessToken({ providerId, accountId? })', purpose: '为当前用户指定 provider 取得可用 access token，并在需要时按配置刷新它。', when: '后端需要调用 GitHub、Google 等第三方资源 API', call: `const { data } = await authClient.getAccessToken({\n  providerId: 'google', accountId,\n})`, returns: '可用 accessToken 及相关过期信息。', principle: '第三方 token 代表用户授予的 scopes，应用授权必须同时考虑 token scope 与本地业务权限。', risk: '优先在服务端消费 token；返回浏览器会增加 XSS 窃取风险，日志与监控必须脱敏。', url: DOCS.users }),
]

const sessionApis = [
  betterAuthApi({ id: 'use-session', name: 'useSession', group: '会话管理', kind: 'hook', signature: 'authClient.useSession(): { data, isPending, error, refetch }', purpose: '在 React、Vue、Svelte 或 Solid 中响应式读取当前用户与会话，并在登录、退出或重新验证后自动更新界面。', when: '组件需要根据登录态渲染头像、导航或受限入口', call: `const { data: session, isPending, error, refetch } =\n  authClient.useSession()`, returns: '框架适配的响应式 session 状态、加载状态、错误和重新获取函数。', principle: '客户端 session 适合驱动体验，不是服务端授权证据；真正受保护的数据路由必须重新验证请求 cookie。', risk: '避免在 isPending 时误判为未登录造成闪烁，也不能只隐藏按钮而不保护后端。', url: DOCS.session }),
  betterAuthApi({ id: 'get-session', name: 'getSession / auth.api.getSession', group: '会话管理', signature: 'getSession({ query?: { disableCookieCache? }, headers? })', purpose: '一次性读取当前请求对应的有效 session 和 user；服务端版本从传入 headers 提取会话凭证。', when: '路由保护、SSR 预取、Server Action 或非响应式客户端逻辑需要当前身份', call: `const session = await auth.api.getSession({\n  headers: request.headers,\n  query: { disableCookieCache: true },\n})`, returns: '有效时返回 { session, user }，未登录或失效时返回 null。', principle: '传统会话以随机 cookie token 定位服务端状态；cookie cache 用签名数据换取更少数据库读取，但会增加撤销延迟。', risk: '敏感操作可禁用 cookie cache 强制查库；不能把解析 cookie 字符串等同于验证会话。', url: DOCS.session }),
  betterAuthApi({ id: 'list-sessions', name: 'listSessions / auth.api.listSessions', group: '会话管理', signature: 'listSessions(): Promise<Session[]>', purpose: '列出当前用户所有仍有效的设备会话，通常包含创建时间、IP 和 User-Agent 等辅助信息。', when: '“已登录设备”安全页面或异常会话检测', call: `const { data: sessions } = await authClient.listSessions()`, returns: '当前用户的有效会话列表，不应向客户端暴露可复用的原始 token。', principle: '每台设备独立会话使精确撤销成为可能，IP 与 User-Agent 只能作风险信号而不是强身份。', risk: '列表必须限定当前用户，显示信息要兼顾隐私；不要把 token 放进 DOM、URL 或前端日志。', url: DOCS.session }),
  betterAuthApi({ id: 'revoke-session', name: 'revokeSession / auth.api.revokeSession', group: '会话管理', signature: 'revokeSession({ token })', purpose: '按会话标识撤销当前用户的一条指定会话，常用于远程登出某台设备。', when: '用户在设备列表发现不认识的登录或主动退出单个旧设备', call: `await authClient.revokeSession({ token: selectedSession.token })`, returns: '目标会话撤销后的成功状态。', principle: '服务端删除/失效会话记录才能使被盗凭证停止工作；短 cookie cache 可能使生效存在窗口。', risk: '端点必须确认目标会话属于当前用户；UI 不应长期保存或展示完整 token。', url: DOCS.session }),
  betterAuthApi({ id: 'revoke-other-sessions', name: 'revokeOtherSessions / auth.api.revokeOtherSessions', group: '会话管理', signature: 'revokeOtherSessions(): Promise<Result>', purpose: '撤销当前用户除本次登录以外的全部会话，同时保留正在操作的设备。', when: '改密、检测到风险或用户选择“退出其他设备”', call: `await authClient.revokeOtherSessions()`, returns: '批量撤销其他会话的成功状态。', principle: '保留当前会话兼顾可用性，但若当前设备本身可能被劫持，应改用全量撤销并重新认证。', risk: 'cookie cache 会影响即时性；高风险事件还应轮换密码、token 并通知用户。', url: DOCS.session }),
  betterAuthApi({ id: 'revoke-sessions', name: 'revokeSessions / auth.api.revokeSessions', group: '会话管理', signature: 'revokeSessions(): Promise<Result>', purpose: '撤销当前用户包括本设备在内的全部会话，迫使所有终端重新登录。', when: '账号疑似被盗、重大安全设置变更或用户选择“退出所有设备”', call: `await authClient.revokeSessions()\nlocation.assign('/login')`, returns: '全量会话撤销状态；当前浏览器也将失去有效身份。', principle: '全局会话撤销是事件响应的重要止损手段，但 OAuth provider 自身 token 可能还需单独撤销。', risk: '操作应要求明确确认和新鲜会话；完成后前端要清理缓存并处理 401。', url: DOCS.session }),
  betterAuthApi({ id: 'update-session', name: 'updateSession / auth.api.updateSession', group: '会话管理', signature: 'updateSession(additionalFields: Record<string, unknown>)', purpose: '更新 schema 中显式声明为可写的会话附加字段，例如界面偏好；核心 token、userId、过期时间不可由此改写。', when: '需要把少量请求期上下文保存在当前会话附加字段', call: `await authClient.updateSession({ theme: 'dark', locale: 'zh-CN' })`, returns: '更新后的 session 或成功状态。', principle: '会话附加字段随每次认证传播，应保持最小、非敏感并受 schema 约束，不能替代业务数据库。', risk: '不要把 role、权限或租户归属作为客户端可改字段，也要控制 cookie cache 的大小。', url: DOCS.session }),
]

const adminOps = [
  ['create-user', 'admin.createUser / auth.api.createUser', 'admin.createUser({ email, password, name, role? })', `await authClient.admin.createUser({ email, password, name: 'Ada', role: 'user' })`, '由管理员创建用户，可选择初始角色。', '后台录入内部账号或客服受控开户', '返回新建 user。', '这是特权开户，不应复用公开注册端点的自助语义。', '必须校验管理员权限、密码交付流程并审计操作者。'],
  ['list-users', 'admin.listUsers / auth.api.listUsers', 'admin.listUsers({ query?, searchField?, searchValue?, limit?, offset? })', `const users = await authClient.admin.listUsers({ query: { limit: 20, offset: 0 } })`, '分页、搜索和过滤用户目录。', '管理后台用户列表与风控调查', '返回 users、total、limit 和 offset。', '管理查询需要服务端分页与允许的搜索字段，避免全表和敏感数据泄露。', '不要把任意排序字段或大 limit 直接下推数据库。'],
  ['get-user', 'admin.getUser / auth.api.getUser', 'admin.getUser({ id })', `const user = await authClient.admin.getUser({ id: userId })`, '按用户 id 读取管理视角的单个用户。', '客服或审计页面查看账号状态', '返回目标 user。', '对象存在性与可查看权限是两个独立判断。', '响应要最小化敏感字段，并记录谁查看了谁。'],
  ['update-user', 'admin.updateUser / auth.api.adminUpdateUser', 'admin.updateUser({ userId, data })', `await authClient.admin.updateUser({ userId, data: { name: 'Ada' } })`, '由管理员更新目标用户允许的字段。', '客服更正资料或后台维护属性', '返回更新后的 user。', '管理写入仍应字段白名单化，不能因管理员端就允许 mass assignment。', '谨防修改 emailVerified、role 等安全字段绕过专用流程。'],
  ['set-role', 'admin.setRole / auth.api.setRole', 'admin.setRole({ userId, role })', `await authClient.admin.setRole({ userId, role: 'admin' })`, '给用户设置一个或多个全局管理角色。', '实施后台 RBAC 角色变更', '返回更新后的 user。', '角色变更是授权策略变更，必须遵守最小权限、职责分离和可追溯性。', '禁止普通管理员给自己提权，并处理最后一个超级管理员保护。'],
  ['set-user-password', 'admin.setUserPassword / auth.api.setUserPassword', 'admin.setUserPassword({ userId, newPassword })', `await authClient.admin.setUserPassword({ userId, newPassword })`, '管理员直接重设目标用户密码。', '经过客服身份核验后的账号恢复', '返回密码更新成功状态。', '这是高风险凭据轮换，通常应迫使用户下次登录改密并撤销旧会话。', '绝不通过工单明文传输密码，操作要二次确认和完整审计。'],
  ['ban-user', 'admin.banUser / auth.api.banUser', 'admin.banUser({ userId, banReason?, banExpiresIn? })', `await authClient.admin.banUser({ userId, banReason: 'abuse', banExpiresIn: 86400 })`, '禁止目标用户继续登录，可配置原因和期限。', '滥用处置、合规冻结或安全事件响应', '返回带 ban 状态的 user。', '封禁是认证入口策略，和删除账号、撤销单一会话的语义不同。', '同步撤销现有会话并避免把内部风控原因原样暴露。'],
  ['unban-user', 'admin.unbanUser / auth.api.unbanUser', 'admin.unbanUser({ userId })', `await authClient.admin.unbanUser({ userId })`, '解除用户封禁状态。', '申诉通过或临时封禁到期后的恢复', '返回恢复后的 user。', '解封应基于审批和证据，不能只是 UI 切换。', '确认原风险已消除，记录审批人与原因。'],
  ['list-user-sessions', 'admin.listUserSessions / auth.api.listUserSessions', 'admin.listUserSessions({ userId })', `const sessions = await authClient.admin.listUserSessions({ userId })`, '管理员查看指定用户的有效会话。', '安全调查和远程设备处置', '返回目标用户会话列表。', '会话元数据属于敏感安全信息，访问本身也应审计。', '不要向管理前端暴露完整可复用 token。'],
  ['revoke-user-session', 'admin.revokeUserSession / auth.api.revokeUserSession', 'admin.revokeUserSession({ sessionToken })', `await authClient.admin.revokeUserSession({ sessionToken })`, '管理员撤销指定会话。', '只隔离一台可疑设备', '返回撤销成功状态。', '精确撤销降低对用户其他设备的影响。', '先确认 token 所属用户和事件上下文，避免越权任意撤销。'],
  ['revoke-user-sessions', 'admin.revokeUserSessions / auth.api.revokeUserSessions', 'admin.revokeUserSessions({ userId })', `await authClient.admin.revokeUserSessions({ userId })`, '管理员撤销目标用户全部会话。', '账号接管响应或强制重新认证', '返回批量撤销状态。', '全量撤销是快速止损但不替代密码和第三方 token 轮换。', '操作需要高权限、确认和审计，避免误伤。'],
  ['impersonate-user', 'admin.impersonateUser / auth.api.impersonateUser', 'admin.impersonateUser({ userId })', `await authClient.admin.impersonateUser({ userId })`, '创建管理员模拟目标用户的受限会话。', '客服复现权限或产品问题', '返回模拟会话并切换当前身份。', '模拟登录应同时保留真实操作者和被模拟主体，审计不能只记后者。', '严格限制权限、时长和环境，禁止访问支付或密钥等高风险动作。'],
  ['stop-impersonating', 'admin.stopImpersonating / auth.api.stopImpersonating', 'admin.stopImpersonating()', `await authClient.admin.stopImpersonating()`, '结束模拟会话并恢复管理员原身份。', '客服排障完成后退出模拟', '返回恢复后的管理员会话。', '显式退出能避免后续操作继续落在被模拟用户身份上。', 'UI 要持续醒目标识模拟状态，并确保异常流程也能安全退出。'],
  ['remove-user', 'admin.removeUser / auth.api.removeUser', 'admin.removeUser({ userId })', `await authClient.admin.removeUser({ userId })`, '管理员删除目标用户及认证关联数据。', '依法删除、测试账号清理或确认后的封号处置', '返回删除成功状态。', '管理删除比封禁更不可逆，需明确级联与数据保留策略。', '高权限审批、幂等处理、审计和备份策略缺一不可。'],
  ['has-permission', 'admin.hasPermission / auth.api.userHasPermission', 'admin.hasPermission({ permissions } | { userId, permissions })', `const allowed = await authClient.admin.hasPermission({ permissions: { user: ['delete'] } })`, '检查当前管理员或指定用户是否拥有全局管理权限。', '在执行管理动作前做服务端授权判断', '返回 permission check 结果。', '权限检查应围绕资源与动作，而不是在各处硬编码角色字符串。', '客户端检查只用于界面，服务端动作必须再次检查。'],
] as const

const adminApis = [
  betterAuthApi({ id: 'admin-plugin', name: 'admin', group: 'Admin 插件', signature: 'admin(options?: AdminOptions): BetterAuthPlugin', purpose: '在服务端注册用户管理、封禁、会话撤销、角色、模拟登录等管理端点和 schema 字段。', when: '应用需要受控的内部管理后台', call: `import { admin } from 'better-auth/plugins'\n\nexport const auth = betterAuth({ plugins: [admin()] })`, returns: '可加入 betterAuth plugins 的 Admin 服务端插件。', principle: '插件把特权端点纳入同一认证管线，但谁能调用仍由 adminRoles、adminUserIds 和权限配置决定。', risk: '不要仅靠前端隐藏入口；默认角色、模拟时长和高危权限必须显式配置。', url: DOCS.admin }),
  betterAuthApi({ id: 'admin-client-plugin', name: 'adminClient', group: 'Admin 插件', signature: 'adminClient(options?: AdminClientOptions): BetterAuthClientPlugin', purpose: '给 createAuthClient 增加类型安全的 admin 命名空间和权限检查方法。', when: '管理后台前端需要调用已启用的 Admin 服务端插件', call: `const authClient = createAuthClient({\n  plugins: [adminClient()],\n})`, returns: '扩展客户端方法与推断类型的 Admin 客户端插件。', principle: '客户端插件只提供调用体验，不授予任何管理权限。', risk: '服务端未安装对应插件会导致端点不存在；前端可见方法不能作为授权依据。', url: DOCS.admin }),
  ...adminOps.map(([id, name, signature, call, purpose, when, returns, principle, risk]) => betterAuthApi({ id: `admin-${id}`, name, group: 'Admin 插件', signature, call, purpose, when, returns, principle, risk, url: DOCS.admin })),
]

const organizationOps = [
  ['create', 'organization.create / auth.api.createOrganization', 'organization.create({ name, slug, logo?, metadata?, userId? })', `await authClient.organization.create({ name: 'AI Lab', slug: 'ai-lab' })`, '创建组织并把创建者加入为 owner。', 'SaaS 租户开户或团队工作区创建', '返回 organization。', '组织是授权与数据隔离边界，不只是展示分组。', 'slug 需唯一，服务端要控制谁可创建以及数量上限。'],
  ['list', 'organization.list / auth.api.listOrganizations', 'organization.list()', `const orgs = await authClient.organization.list()`, '列出当前用户所属组织。', '组织切换器和工作区首页', '返回当前用户可见组织列表。', '可见性来自 member 关系而非客户端传入 userId。', '列表要避免跨用户或跨租户泄露。'],
  ['get-full', 'organization.getFullOrganization / auth.api.getFullOrganization', 'organization.getFullOrganization({ organizationId? | organizationSlug? })', `const org = await authClient.organization.getFullOrganization({ query: { organizationId } })`, '读取组织及成员、邀请等完整上下文。', '组织设置页需要聚合详情', '返回完整 organization 数据。', '聚合读取仍需组织成员身份和细粒度权限。', '不要默认把所有成员邮箱和邀请信息暴露给普通成员。'],
  ['update', 'organization.update / auth.api.updateOrganization', 'organization.update({ data, organizationId? })', `await authClient.organization.update({ data: { name: 'AI Platform' }, organizationId })`, '更新组织名称、slug、logo 或 metadata。', '组织管理员维护工作区资料', '返回更新后的 organization。', '资料更新与租户身份变更要分开授权。', 'metadata 字段需白名单，slug 变更还会影响路由和外部引用。'],
  ['delete', 'organization.delete / auth.api.deleteOrganization', 'organization.delete({ organizationId })', `await authClient.organization.delete({ organizationId })`, '删除组织及其授权关系。', '组织所有者关闭租户', '返回删除状态。', '删除组织是跨多表的高风险租户级操作。', '只允许 owner/明确权限，需确认级联、账单、导出和保留策略。'],
  ['check-slug', 'organization.checkSlug / auth.api.checkOrganizationSlug', 'organization.checkSlug({ slug })', `const available = await authClient.organization.checkSlug({ slug: 'ai-lab' })`, '检查组织 slug 是否可用。', '创建或修改组织时提供即时反馈', '返回可用性结果。', '可用性检查只是体验优化，最终唯一性必须由数据库约束保证。', '需要限流并处理检查后被他人抢占的竞态。'],
  ['set-active', 'organization.setActive / auth.api.setActiveOrganization', 'organization.setActive({ organizationId? | organizationSlug? | null })', `await authClient.organization.setActive({ organizationId })`, '把某组织写入当前会话作为 activeOrganization。', '用户在多个工作区之间切换', '返回激活组织或 null。', 'active organization 是上下文选择，不自动等于对其所有资源有权限。', '必须验证当前用户仍是成员，服务端查询也不能只信 session 中的 id。'],
  ['use-active', 'useActiveOrganization', 'authClient.useActiveOrganization()', `const { data: activeOrg } = authClient.useActiveOrganization()`, '响应式读取当前会话激活的组织。', '导航、面包屑和组织级页面渲染', '返回响应式 active organization 状态。', 'hook 驱动 UI，上游授权仍由服务端成员关系决定。', '切换期间处理 pending，避免短暂用旧组织发请求。'],
  ['invite', 'organization.inviteMember / auth.api.createInvitation', 'organization.inviteMember({ email, role, organizationId?, resend?, teamId? })', `await authClient.organization.inviteMember({ email, role: 'member', organizationId })`, '向邮箱发送带角色的组织邀请。', '管理员邀请新成员', '返回 invitation。', '邀请 token 是加入租户的短期能力，角色应在创建时受邀请者权限上限约束。', '防止管理员邀请比自己更高角色，链接需过期、防重放并避免邮箱枚举。'],
  ['get-invitation', 'organization.getInvitation / auth.api.getInvitation', 'organization.getInvitation({ query: { id } })', `const invite = await authClient.organization.getInvitation({ query: { id } })`, '读取一条邀请的组织、角色和状态。', '邀请接受页展示上下文', '返回 invitation。', '读取邀请不应泄露整个组织或成员目录。', '邀请 id/token 不要进入日志，过期和已消费状态要处理。'],
  ['list-invitations', 'organization.listInvitations / auth.api.listInvitations', 'organization.listInvitations({ organizationId? })', `const invites = await authClient.organization.listInvitations({ query: { organizationId } })`, '列出组织发出的待处理邀请。', '管理员管理邀请', '返回 invitation 列表。', '邀请列表包含邮箱等个人信息，应按组织权限最小披露。', '不可仅用 organizationId 查询，必须校验当前成员权限。'],
  ['cancel-invitation', 'organization.cancelInvitation / auth.api.cancelInvitation', 'organization.cancelInvitation({ invitationId })', `await authClient.organization.cancelInvitation({ invitationId })`, '取消尚未接受的组织邀请。', '邀请错误、人员变化或安全撤回', '返回取消状态。', '取消应立即使 token 失效并保持幂等。', '确认邀请属于当前可管理组织并记录操作者。'],
  ['accept-invitation', 'organization.acceptInvitation / auth.api.acceptInvitation', 'organization.acceptInvitation({ invitationId })', `await authClient.organization.acceptInvitation({ invitationId })`, '当前登录用户接受邀请并创建成员关系。', '用户确认加入组织', '返回 membership/organization 结果。', '接受邀请要绑定当前登录邮箱、邀请状态和组织成员唯一约束。', '不能让其他账号消费不属于自己的邀请；重复请求应安全。'],
  ['reject-invitation', 'organization.rejectInvitation / auth.api.rejectInvitation', 'organization.rejectInvitation({ invitationId })', `await authClient.organization.rejectInvitation({ invitationId })`, '当前用户拒绝一条组织邀请。', '用户不希望加入组织', '返回拒绝状态。', '拒绝与取消的主体不同：前者是受邀者，后者是组织管理者。', '校验邀请归属并避免泄露其他人的邀请。'],
  ['list-members', 'organization.listMembers / auth.api.listMembers', 'organization.listMembers({ organizationId?, limit?, offset?, sortBy? })', `const members = await authClient.organization.listMembers({ query: { organizationId, limit: 20 } })`, '分页列出组织成员及角色。', '成员目录和权限管理页', '返回 members 和 total。', '成员目录必须从 membership 关系查询并遵守组织可见性策略。', '限制分页、排序字段和个人信息，不可跨组织读取。'],
  ['get-active-member', 'organization.getActiveMember / auth.api.getActiveMember', 'organization.getActiveMember()', `const member = await authClient.organization.getActiveMember()`, '读取当前用户在 active organization 中的成员记录。', '显示本人组织身份、加入时间和角色', '返回 member 或 null。', 'user 身份和 member 身份分层，后者携带组织上下文中的角色。', '没有 active organization 或已被移除时必须处理 null。'],
  ['get-active-role', 'organization.getActiveMemberRole / auth.api.getActiveMemberRole', 'organization.getActiveMemberRole()', `const { data } = await authClient.organization.getActiveMemberRole()`, '读取当前用户在激活组织中的角色。', 'UI 展示角色或初步决定菜单', '返回 role。', '角色是权限集合的命名，不应代替服务端对具体 resource/action 的判断。', '不要只在前端按 role 保护操作。'],
  ['add-member', 'organization.addMember / auth.api.addMember', 'organization.addMember({ userId, role, organizationId?, teamId? })', `await auth.api.addMember({ body: { userId, role: 'member', organizationId } })`, '服务端直接添加已有用户为组织成员，不经过邮件邀请。', '受信后台同步成员或迁移数据', '返回 member。', '直接添加绕过用户确认，因此只适合明确授权的后台流程。', '它是 server-only 高权动作，必须审计并避免误加跨租户用户。'],
  ['remove-member', 'organization.removeMember / auth.api.removeMember', 'organization.removeMember({ memberIdOrEmail, organizationId? })', `await authClient.organization.removeMember({ memberIdOrEmail: memberId, organizationId })`, '从组织移除一个成员。', '离职、访问撤销或成员管理', '返回移除状态。', '移除 membership 后应使组织权限立即失效，并处理其 active context。', '保护最后一个 owner，清理团队关系并考虑会话/缓存失效。'],
  ['update-member-role', 'organization.updateMemberRole / auth.api.updateMemberRole', 'organization.updateMemberRole({ memberId, role, organizationId? })', `await authClient.organization.updateMemberRole({ memberId, role: 'admin', organizationId })`, '修改组织成员角色。', '升降级成员权限', '返回更新后的 member。', '角色变更属于组织级授权变更，应检查操作者能否授予目标角色。', '防止自提权、越权授予和移除最后一个 owner。'],
  ['leave', 'organization.leave / auth.api.leaveOrganization', 'organization.leave({ organizationId })', `await authClient.organization.leave({ organizationId })`, '当前成员主动退出组织。', '用户离开不再参与的工作区', '返回离开状态。', '自助离开与管理员移除的主体、审计语义不同。', '最后一个 owner 不应直接离开；先转移所有权并清理 active organization。'],
  ['has-permission', 'organization.hasPermission / auth.api.hasPermission', 'organization.hasPermission({ permissions, organizationId? })', `const allowed = await authClient.organization.hasPermission({ permissions: { member: ['delete'] } })`, '检查当前成员在组织中是否拥有指定资源动作权限。', '服务端执行组织敏感动作前', '返回权限判断结果。', '基于 resource/action 的授权比散落 role 判断更可组合，也支持自定义角色。', '客户端结果可能过期，只能做界面提示；服务端必须按最新成员关系再检查。'],
  ['create-team', 'organization.createTeam / auth.api.createTeam', 'organization.createTeam({ name, organizationId? })', `await authClient.organization.createTeam({ name: 'Platform', organizationId })`, '在组织内创建团队。', '组织需要更细分的小组协作边界', '返回 team。', 'team 是组织内分组，不应越过 organization 租户边界。', '校验团队创建权限、名称约束和所属组织。'],
  ['list-teams', 'organization.listTeams / auth.api.listOrganizationTeams', 'organization.listTeams({ organizationId? })', `const teams = await authClient.organization.listTeams({ query: { organizationId } })`, '列出组织中的团队。', '团队选择器和组织结构页', '返回 team 列表。', '团队可见性可能与普通成员可见性不同，应按产品策略授权。', '不能仅凭 organizationId 跨租户读取。'],
  ['update-team', 'organization.updateTeam / auth.api.updateTeam', 'organization.updateTeam({ teamId, data })', `await authClient.organization.updateTeam({ teamId, data: { name: 'Core Platform' } })`, '更新团队资料。', '团队管理员修改名称', '返回更新后的 team。', '更新必须同时验证 team 所属组织和操作者权限。', '防止通过猜测 teamId 修改其他租户团队。'],
  ['remove-team', 'organization.removeTeam / auth.api.removeTeam', 'organization.removeTeam({ teamId })', `await authClient.organization.removeTeam({ teamId })`, '删除组织中的团队。', '团队解散或结构调整', '返回删除状态。', '删除 team 需明确成员关系和业务资源是否级联。', '先检查所属组织、影响范围并保证操作幂等。'],
  ['add-team-member', 'organization.addTeamMember / auth.api.addTeamMember', 'organization.addTeamMember({ teamId, userId })', `await authClient.organization.addTeamMember({ teamId, userId })`, '把组织成员加入指定团队。', '维护小组成员关系', '返回 team membership。', '用户必须先是同一 organization 的成员，团队关系不能跨租户。', '不要只校验 teamId 与 userId 分别存在，还要校验它们同属一个组织。'],
  ['remove-team-member', 'organization.removeTeamMember / auth.api.removeTeamMember', 'organization.removeTeamMember({ teamId, userId })', `await authClient.organization.removeTeamMember({ teamId, userId })`, '从团队移除一个成员，但不一定移除其组织成员身份。', '小组调整或撤销团队资源访问', '返回移除状态。', 'team membership 与 organization membership 是两层关系，撤销范围不同。', '清理团队级缓存和授权，避免误删整个组织成员。'],
  ['list-team-members', 'organization.listTeamMembers / auth.api.listTeamMembers', 'organization.listTeamMembers({ teamId })', `const members = await authClient.organization.listTeamMembers({ query: { teamId } })`, '列出指定团队成员。', '团队成员页或授权设置', '返回 team member 列表。', '查询必须先由 team 反查 organization 并验证当前用户可见性。', '防止枚举 teamId 获取其他租户成员资料。'],
  ['set-active-team', 'organization.setActiveTeam / auth.api.setActiveTeam', 'organization.setActiveTeam({ teamId? | null })', `await authClient.organization.setActiveTeam({ teamId })`, '在当前会话中选择激活团队，或传 null 清除。', '同一组织内切换小组工作上下文', '返回 active team 或 null。', 'active team 只是上下文指针，权限仍来自实时团队与组织成员关系。', '验证团队属于 active organization 且当前用户仍是成员。'],
] as const

const organizationApis = [
  betterAuthApi({ id: 'organization-plugin', name: 'organization', group: 'Organization 插件', signature: 'organization(options?: OrganizationOptions): BetterAuthPlugin', purpose: '在服务端加入组织、成员、邀请、角色、权限和团队等多租户端点与数据模型。', when: 'B2B SaaS、团队工作区或需要组织级 RBAC', call: `export const auth = betterAuth({\n  plugins: [organization({ allowUserToCreateOrganization: true })],\n})`, returns: '可加入 betterAuth plugins 的 Organization 服务端插件。', principle: 'organization/member/team 把用户身份映射到租户上下文，是数据隔离和授权模型的核心。', risk: '启用前设计 owner 生命周期、租户边界和权限语句；插件不会自动过滤你的业务表。', url: DOCS.organization }),
  betterAuthApi({ id: 'organization-client-plugin', name: 'organizationClient', group: 'Organization 插件', signature: 'organizationClient(options?: OrganizationClientOptions): BetterAuthClientPlugin', purpose: '给客户端增加 organization 命名空间、组织 hooks 和自定义角色类型推断。', when: '前端需要管理组织、成员、邀请和团队', call: `const authClient = createAuthClient({\n  plugins: [organizationClient({ ac, roles })],\n})`, returns: '扩展 AuthClient 的 Organization 客户端插件。', principle: '客户端和服务端共享 access-control 类型能减少拼写错误，但真正判定仍在服务端。', risk: '两端角色配置要同步，不能把客户端 permissions 当可信结论。', url: DOCS.organization }),
  betterAuthApi({ id: 'create-access-control', name: 'createAccessControl', group: 'Organization 插件', signature: 'createAccessControl(statements): AccessControl', purpose: '用资源到动作的只读语句创建类型安全访问控制器，随后由它定义组织角色。', when: '默认 owner/admin/member 权限不足以表达业务资源', call: `const ac = createAccessControl({\n  project: ['create', 'read', 'update', 'delete'],\n} as const)`, returns: '可创建角色并供 Organization 两端配置复用的 AccessControl。', principle: 'permission 是 resource/action 原子能力，role 是这些能力的集合。', risk: 'statement 必须 as const 保留字面量类型；新增动作默认不应自动授予旧角色。', url: DOCS.organization }),
  betterAuthApi({ id: 'access-control-new-role', name: 'AccessControl.newRole', group: 'Organization 插件', signature: 'ac.newRole(statements): Role', purpose: '基于访问控制语句创建一个类型安全自定义角色及其权限集合。', when: '定义 reviewer、billing、developer 等领域角色', call: `const reviewer = ac.newRole({\n  project: ['read'],\n})`, returns: '可注册到 organization/organizationClient roles 的 Role。', principle: '角色应按职责最小授权，避免一个万能 admin 承载所有运营任务。', risk: '不要只更新客户端角色；服务端配置和已有成员数据也需迁移。', url: DOCS.organization }),
  betterAuthApi({ id: 'check-role-permission', name: 'organization.checkRolePermission', group: 'Organization 插件', signature: 'organization.checkRolePermission({ role, permissions })', purpose: '在客户端本地判断某个角色定义理论上是否包含指定权限，不读取当前用户会话。', when: '构建角色编辑器或预览一个角色会允许哪些动作', call: `const canDelete = authClient.organization.checkRolePermission({\n  role: 'admin', permissions: { member: ['delete'] },\n})`, returns: '同步布尔值。', principle: '静态角色能力检查与当前成员实时授权检查不同，前者不考虑组织、封禁或成员变更。', risk: '绝不能用它保护服务端资源；执行动作要调用 hasPermission 或在端点内校验。', url: DOCS.organization }),
  ...organizationOps.map(([id, name, signature, call, purpose, when, returns, principle, risk]) => betterAuthApi({ id: `organization-${id}`, name, group: 'Organization 插件', signature, call, purpose, when, returns, principle, risk, url: DOCS.organization })),
]

const passkeyApis = [
  betterAuthApi({ id: 'passkey-plugin', name: 'passkey', group: 'Passkey 插件', signature: 'passkey(options?: PasskeyOptions): BetterAuthPlugin', purpose: '在服务端注册基于 WebAuthn/FIDO2 的 passkey 挑战、验证、凭据存储和登录端点。', when: '提供抗钓鱼的无密码登录或作为高强度登录方式', call: `import { passkey } from '@better-auth/passkey'\n\nexport const auth = betterAuth({\n  plugins: [passkey({ rpID: 'example.com', origin: 'https://example.com' })],\n})`, returns: '可加入 betterAuth plugins 的 Passkey 服务端插件。', principle: '私钥留在认证器，服务端只存公钥；challenge、origin、RP ID 和签名共同抵御重放与钓鱼。', risk: '生产必须准确配置 origin/rpID 与 HTTPS，并为账号恢复和多设备凭据设计流程。', url: DOCS.passkey }),
  betterAuthApi({ id: 'passkey-client-plugin', name: 'passkeyClient', group: 'Passkey 插件', signature: 'passkeyClient(): BetterAuthClientPlugin', purpose: '给浏览器客户端增加 passkey 注册、登录、列出、重命名和删除方法，并封装 WebAuthn 浏览器调用。', when: '前端需要调用已启用的 Passkey 服务端插件', call: `const authClient = createAuthClient({\n  plugins: [passkeyClient()],\n})`, returns: '扩展 AuthClient 的 Passkey 客户端插件。', principle: '浏览器负责与认证器交互，服务端负责产生 challenge 并验证响应，两端缺一不可。', risk: 'WebAuthn 依赖安全上下文和浏览器能力；要处理用户取消、超时和无可用认证器。', url: DOCS.passkey }),
  betterAuthApi({ id: 'passkey-add', name: 'passkey.addPasskey / auth.api.addPasskey', group: 'Passkey 插件', signature: 'passkey.addPasskey({ name?, authenticatorAttachment?, extensions? })', purpose: '为已登录用户发起 WebAuthn 注册仪式并保存新的公钥凭据。', when: '安全设置中添加设备 passkey 或安全密钥', call: `await authClient.passkey.addPasskey({\n  name: 'MacBook Touch ID',\n  authenticatorAttachment: 'platform',\n})`, returns: '注册后的 passkey 记录以及可选 WebAuthn 扩展结果。', principle: '注册 challenge 必须随机、短期并绑定当前用户和 RP；服务端验证 attestation/响应后只保存公钥。', risk: '这是 client-only WebAuthn 流程，不能靠服务端伪造浏览器响应；还要防止已劫持旧会话静默加凭据。', url: DOCS.passkey }),
  betterAuthApi({ id: 'passkey-sign-in', name: 'signIn.passkey / auth.api.signInPasskey', group: 'Passkey 插件', signature: 'signIn.passkey({ autoFill?, extensions?, returnWebAuthnResponse? })', purpose: '发起 WebAuthn authentication，让用户用生物识别、PIN 或安全密钥证明持有私钥并登录。', when: '登录页按钮或 Conditional UI 自动填充', call: `await authClient.signIn.passkey({\n  autoFill: true,\n  extensions: { credProps: true },\n})`, returns: '验证成功后的用户与会话，失败时返回包含 error 的数据对象。', principle: '认证器对服务端 challenge 签名；服务端校验签名、origin、RP ID 和计数器后才创建会话。', risk: 'autoFill 要先检查浏览器 Conditional Mediation 支持；不能把用户取消当系统故障无限重试。', url: DOCS.passkey }),
  betterAuthApi({ id: 'passkey-list', name: 'passkey.listUserPasskeys / auth.api.listPasskeys', group: 'Passkey 插件', signature: 'passkey.listUserPasskeys(): Promise<Passkey[]>', purpose: '列出当前用户注册的 passkey，用于展示名称、设备类型、创建时间和备份状态。', when: '凭据管理页面', call: `const { data: passkeys } = await authClient.passkey.listUserPasskeys()`, returns: '当前用户的 passkey 公共元数据列表，不包含私钥。', principle: '服务端永远不应拥有私钥；AAGUID 只能辅助显示认证器类型，不能可靠识别具体设备或用户。', risk: '列表必须按当前会话 userId 查询，且不要把 credential 数据用于跨站追踪。', url: DOCS.passkey }),
  betterAuthApi({ id: 'passkey-update', name: 'passkey.updatePasskey / auth.api.updatePasskey', group: 'Passkey 插件', signature: 'passkey.updatePasskey({ id, name })', purpose: '修改当前用户某个 passkey 的友好名称，方便区分多台设备。', when: '用户重命名已注册凭据', call: `await authClient.passkey.updatePasskey({\n  id: passkeyId, name: 'Work YubiKey',\n})`, returns: '更新后的 passkey 或成功状态。', principle: '名称只是用户可见 metadata，不参与 WebAuthn 密码学验证。', risk: '必须校验 passkey 属于当前用户，并限制名称长度和内容。', url: DOCS.passkey }),
  betterAuthApi({ id: 'passkey-delete', name: 'passkey.deletePasskey / auth.api.deletePasskey', group: 'Passkey 插件', signature: 'passkey.deletePasskey({ id })', purpose: '删除当前用户的一条 passkey 公钥凭据，使对应认证器以后无法再登录此账号。', when: '设备丢失、密钥停用或用户清理旧凭据', call: `await authClient.passkey.deletePasskey({ id: passkeyId })`, returns: '凭据删除成功状态。', principle: '删除服务端公钥即可撤销该登录凭据；认证器本地记录可能仍存在但无法通过服务器验证。', risk: '不要允许删除最后一种可用登录/恢复方式；敏感场景要求新鲜会话或二次认证。', url: DOCS.passkey }),
]

export const BETTER_AUTH_FRAMEWORK_API_EXPECTED_COUNT = 93

export const betterAuthFrameworkApis: FrameworkApiReference[] = [
  ...foundationApis,
  ...coreAuthApis,
  ...sessionApis,
  ...adminApis,
  ...organizationApis,
  ...passkeyApis,
]

if (betterAuthFrameworkApis.length !== BETTER_AUTH_FRAMEWORK_API_EXPECTED_COUNT) {
  throw new Error(`Better Auth framework API catalog expected ${BETTER_AUTH_FRAMEWORK_API_EXPECTED_COUNT} entries, received ${betterAuthFrameworkApis.length}`)
}
