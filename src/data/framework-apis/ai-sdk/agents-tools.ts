import { AI_SDK_CORE, aiSdkApi } from '../helpers'

export const aiSdkAgentToolApis = [
  aiSdkApi({ name: 'Agent', group: 'Agent 与工具', kind: 'interface', maturity: 'type-only', signature: 'interface Agent<CALL_OPTIONS, TOOLS, OUTPUT>', beginner: 'Agent 是一份统一合同：只要一个对象能 generate 和 stream，就能被其他 AI SDK 工具以同一种方式调用。', whenToUse: '封装自研 Agent、第三方链或需要替换 ToolLoopAgent 的实现时。', example: `const agent = customAgent satisfies Agent<any, any, any>\nconst result = await agent.generate({ prompt: '检查订单状态' })`, returns: '这是 TypeScript 接口，本身不执行；实现类提供 generate、stream、tools 和 output。', interview: '说明接口隔离让调用方依赖能力合同而不是具体 Agent 实现，便于测试和替换。', pitfall: '自定义实现也要遵守 abortSignal、消息互斥输入和工具类型约束。', officialUrl: `${AI_SDK_CORE}agent` }),
  aiSdkApi({ name: 'ToolLoopAgent', group: 'Agent 与工具', kind: 'class', signature: 'new ToolLoopAgent({ model, instructions, tools, stopWhen? })', beginner: '它会重复执行“模型思考 → 调工具 → 把工具结果交回模型”，直到满足停止条件。', whenToUse: '任务需要多步工具调用，但又不需要持久化图工作流时。', example: `const agent = new ToolLoopAgent({\n  model, instructions, tools,\n  stopWhen: stepCountIs(8),\n})\nconst result = await agent.generate({ prompt })`, returns: '一个实现 Agent 接口的对象，可 generate/stream，并返回步骤、工具调用和最终输出。', interview: '要主动讲停止条件、最大步数、工具副作用幂等、上下文增长和人工确认，而不是只说“自动调用工具”。', pitfall: 'SDK 默认用 stepCountIs(20) 防止无限执行，但生产中仍应设置更小的业务终止条件、权限边界和幂等保护，避免重复副作用与成本失控。', officialUrl: `${AI_SDK_CORE}tool-loop-agent` }),
  aiSdkApi({ name: 'createAgentUIStream', group: 'Agent 与工具', kind: 'function', signature: 'createAgentUIStream({ agent, uiMessages, ...options }): Promise<AsyncIterableStream<UIMessageChunk>>', beginner: '把 Agent 内部的文本、工具步骤和状态转换成前端认识的 UIMessage 数据流。', whenToUse: '要自己逐项消费 Agent UI chunk、接到 WebSocket 或组合自定义发送逻辑时。', example: `const stream = await createAgentUIStream({ agent, uiMessages })\nfor await (const chunk of stream) console.log(chunk.type)`, returns: 'Promise，解析后得到 AsyncIterableStream<UIMessageChunk>，可用 for await 消费。', interview: '它先验证 uiMessages，再转换为模型消息并运行 agent.stream，最后输出 UIMessageChunk；它不负责 HTTP 响应头。', pitfall: '参数名必须是 uiMessages；不要把返回值误当同步 ReadableStream，也不要丢弃工具 part 和 metadata。', officialUrl: `${AI_SDK_CORE}create-agent-ui-stream` }),
  aiSdkApi({ name: 'createAgentUIStreamResponse', group: 'Agent 与工具', kind: 'function', signature: 'createAgentUIStreamResponse({ agent, uiMessages, ...options }): Promise<Response>', beginner: '它是“开箱即用版”：验证 UI 消息、运行 Agent，并异步生成浏览器能接收的流式 Response。', whenToUse: 'Next.js、Hono 或 Web 标准 Route Handler 想最少代码返回 Agent UI 流时。', example: `export async function POST(request: Request) {\n  const { messages: uiMessages } = await request.json()\n  return await createAgentUIStreamResponse({ agent, uiMessages })\n}`, returns: 'Promise<Response>，解析后得到带正确 UI message stream headers 和流式 body 的 Web Response。', interview: '对比 createAgentUIStream：前者只生成异步可迭代流，本 API 连 HTTP Response 一起构造。', pitfall: '参数必须叫 uiMessages；仍要在调用前完成认证、消息来源校验和资源授权。', officialUrl: `${AI_SDK_CORE}create-agent-ui-stream-response` }),
  aiSdkApi({ name: 'pipeAgentUIStreamToResponse', group: 'Agent 与工具', kind: 'function', signature: 'pipeAgentUIStreamToResponse({ response, agent, uiMessages, ...options }): Promise<void>', beginner: '把 Agent 的 UI 流直接写进 Node.js 原生 ServerResponse，适配非 Web Response 的服务框架。', whenToUse: 'Express、Node HTTP 或仍使用 res 对象的服务器。', example: `await pipeAgentUIStreamToResponse({\n  response: res, agent, uiMessages,\n})`, returns: 'Promise<void>；等待写流建立与消费过程，业务结果通过 Node response 持续发送。', interview: '解释 Web Response 与 Node ServerResponse 是两套边界，这个异步函数是桥接器。', pitfall: '参数必须叫 uiMessages；写流后不要再调用 res.json，客户端断开时要传播取消。', officialUrl: `${AI_SDK_CORE}pipe-agent-ui-stream-to-response` }),
  aiSdkApi({ name: 'tool', group: 'Agent 与工具', kind: 'function', signature: 'tool({ description?, inputSchema, execute? }): Tool', beginner: '把普通函数包装成模型可理解、可生成参数并可执行的“工具”。inputSchema 同时给模型提示并校验参数。', whenToUse: '模型需要查询数据库、调用业务服务或执行受控动作。', example: `const getWeather = tool({\n  description: '查询城市天气',\n  inputSchema: z.object({ city: z.string() }),\n  execute: ({ city }) => weatherPort.get(city),\n})`, returns: '保留输入/输出类型推断的 Tool 定义。', interview: '必须说 execute 里仍要鉴权；schema 只验证数据形状，不证明调用者权限。', pitfall: '工具过大、描述模糊或把身份参数交给模型都会降低安全性与命中率。', officialUrl: `${AI_SDK_CORE}tool` }),
  aiSdkApi({ name: 'dynamicTool', group: 'Agent 与工具', kind: 'function', signature: 'dynamicTool({ description?, inputSchema, execute? }): DynamicTool', beginner: '当工具的输入类型在编译时无法确定时，用它接受 unknown，再在运行时按 schema 校验。', whenToUse: '工具来自数据库、插件市场、MCP 或运行时配置，TypeScript 无法提前推断固定 schema。', example: `const pluginTool = dynamicTool({\n  inputSchema: runtimeSchema,\n  execute: async input => {\n    const safeInput = runtimeSchema.parse(input)\n    return runPlugin(safeInput)\n  },\n})`, returns: '输入输出为动态类型的 Tool，工具调用会标记 dynamic。', interview: '对比 tool：tool 提供编译期类型安全，dynamicTool 用运行时灵活性换掉一部分静态安全。', pitfall: 'execute 必须把 input 当不可信数据重新收窄，不能直接解构假定字段。', officialUrl: `${AI_SDK_CORE}dynamic-tool` }),
  aiSdkApi({ name: 'createMCPClient', group: 'MCP 与消息协议', kind: 'function', signature: 'createMCPClient({ transport, name?, onUncaughtError? }): Promise<MCPClient>', beginner: '连接一个 MCP Server，把远程暴露的工具、资源或提示转换成 AI SDK 能使用的能力。', whenToUse: '要接入标准 MCP 服务，而不是为每个外部系统手写 Tool 适配。', example: `const client = await createMCPClient({ transport })\ntry {\n  const tools = await client.tools()\n  console.log(Object.keys(tools))\n} finally {\n  await client.close()\n}`, returns: 'MCPClient，可列出工具并在结束时 close。', interview: '要解释 MCP 是能力协议，不自动解决授权；客户端仍需限制允许的 server、tool 和资源。', pitfall: '务必在 finally 中 close；不要把不可信 MCP Server 的全部工具无筛选暴露给 Agent。', officialUrl: `${AI_SDK_CORE}create-mcp-client` }),
  aiSdkApi({ name: 'Experimental_StdioMCPTransport', group: 'MCP 与消息协议', kind: 'transport', maturity: 'experimental', signature: 'new Experimental_StdioMCPTransport({ command, args?, env? })', beginner: '通过本机启动一个子进程，并用标准输入/输出与 MCP Server 通信。', whenToUse: '本地开发、桌面程序或受控服务器进程需要连接 stdio MCP 服务。', example: `const transport = new Experimental_StdioMCPTransport({ command: 'npx', args: ['-y', 'my-mcp-server'] })`, returns: '可传给 createMCPClient 的实验性 transport。', interview: '说明 stdio 适合本机进程，远程服务通常使用 HTTP transport；进程启动权限风险更高。', pitfall: 'command/args 不能来自未经校验的用户输入；要限制环境变量和工作目录。', officialUrl: `${AI_SDK_CORE}mcp-stdio-transport` }),
  aiSdkApi({ name: 'jsonSchema', group: 'MCP 与消息协议', kind: 'function', signature: 'jsonSchema(schema, options?): Schema', beginner: '把普通 JSON Schema 包装成 AI SDK 认识的 schema，同时可以附加自定义验证函数。', whenToUse: 'schema 来自 OpenAPI、MCP、配置文件或不是 Zod 编写。', example: `const schema = jsonSchema<{ city: string }>({ type: 'object', properties: { city: { type: 'string' } }, required: ['city'] })`, returns: '带静态泛型信息和运行时 JSON Schema 的 AI SDK Schema。', interview: 'JSON Schema 更通用、可跨语言；TypeScript 泛型只是声明，运行时仍以 schema/validate 为准。', pitfall: '不要用错误泛型“骗过”编译器；外部 schema 必须验证 required、additionalProperties 等细节。', officialUrl: `${AI_SDK_CORE}json-schema` }),
  aiSdkApi({ name: 'zodSchema', group: 'MCP 与消息协议', kind: 'function', signature: 'zodSchema(zodSchema, options?): Schema', beginner: '把 Zod 规则转换成 AI SDK 的统一 Schema，保留 TypeScript 类型推断和运行时校验。', whenToUse: '工具输入或结构化输出已经使用 Zod 定义。', example: `const schema = zodSchema(z.object({ score: z.number().min(0).max(100) }))`, returns: 'AI SDK Schema，类型从 Zod 自动推断。', interview: '说明 Zod 同时服务开发期类型推断和运行时验证，但复杂 refine 不一定能完整翻译为模型可理解的 JSON Schema。', pitfall: '模型看不懂只有代码意义的 refine；关键规则要写进 description 并在服务端再次校验。', officialUrl: `${AI_SDK_CORE}zod-schema` }),
  aiSdkApi({ name: 'valibotSchema', group: 'MCP 与消息协议', kind: 'function', signature: 'valibotSchema(valibotSchema, options?): Schema', beginner: '作用与 zodSchema 相似，只是输入来自 Valibot，适合已经使用 Valibot 的项目。', whenToUse: '应用希望使用更轻量的 Valibot schema，同时接入 AI SDK。', example: `const schema = valibotSchema(v.object({ name: v.string() }))`, returns: '兼容 AI SDK 的 Schema 与推断类型。', interview: '面试重点不是背库名，而是解释“统一 schema adapter”如何隔离上层 API 与验证库差异。', pitfall: '确认 Valibot 版本和 JSON Schema 转换能力；复杂验证仍要回到业务层。', officialUrl: `${AI_SDK_CORE}valibot-schema` }),
  aiSdkApi({ name: 'ModelMessage', group: 'MCP 与消息协议', kind: 'type', maturity: 'type-only', signature: 'type ModelMessage = SystemModelMessage | UserModelMessage | AssistantModelMessage | ToolModelMessage', beginner: '这是发给模型的消息格式，关注模型真正需要的角色、文本、图片、文件、工具调用和工具结果。', whenToUse: '服务端组装 prompt、保存可重放模型上下文或调用 generateText/streamText。', example: `const messages: ModelMessage[] = [{ role: 'user', content: '解释闭包' }]`, returns: 'TypeScript 联合类型，不产生运行时结果。', interview: '一定要区分 ModelMessage 与 UIMessage：前者面向模型，后者面向界面和持久化展示。', pitfall: '不要把 UI 临时状态直接传给模型；先用 convertToModelMessages 并做权限与字段裁剪。', officialUrl: `${AI_SDK_CORE}model-message` }),
  aiSdkApi({ name: 'UIMessage', group: 'MCP 与消息协议', kind: 'type', maturity: 'type-only', signature: 'interface UIMessage<METADATA, DATA_PARTS, TOOLS>', beginner: '这是前端聊天消息格式，除了文本，还能保存工具状态、自定义数据、文件和页面需要的 metadata。', whenToUse: '聊天 UI、消息持久化、恢复流和前端工具渲染。', example: `type ChatMessage = UIMessage<{ createdAt: number }, MyDataParts, MyTools>`, returns: '强类型的 UI 消息合同。', interview: '说明 UIMessage 是界面协议，不应直接当模型 prompt；转模型前要过滤数据和工具 part。', pitfall: '持久化时必须做版本迁移和 validateUIMessages，不能相信数据库里的旧消息永远符合当前类型。', officialUrl: `${AI_SDK_CORE}ui-message` }),
  aiSdkApi({ name: 'validateUIMessages', group: 'MCP 与消息协议', kind: 'function', signature: 'validateUIMessages({ messages, tools?, dataSchemas?, metadataSchema? }): Promise<UIMessage[]>', beginner: '像严格门卫：消息中只要有一项不合法就抛错，阻止坏数据进入模型或界面。', whenToUse: '接收客户端消息、加载持久化历史或恢复旧会话时。', example: `const safeMessages = await validateUIMessages({ messages: body.messages, tools })`, returns: '验证并收窄后的 UIMessage 数组；失败抛 TypeValidationError。', interview: '边界输入应先验证再 convert；工具调用还要校验 toolName、input 和已有结果。', pitfall: '不要只依赖 TypeScript 类型，因为 HTTP/数据库数据在运行时完全可能不符合类型。', officialUrl: `${AI_SDK_CORE}validate-ui-messages` }),
  aiSdkApi({ name: 'safeValidateUIMessages', group: 'MCP 与消息协议', kind: 'function', signature: 'safeValidateUIMessages(options): Promise<SafeValidateResult>', beginner: '它与 validateUIMessages 做同样的检查，但失败时返回 success/error，而不是抛异常。', whenToUse: '接口需要自己映射 400 错误、记录验证问题或提供兼容迁移提示。', example: `const checked = await safeValidateUIMessages({ messages })\nif (!checked.success) return Response.json({ error: 'invalid_messages' }, { status: 400 })`, returns: '成功时 data，失败时 error 的判别联合。', interview: '对比 throw 与 result 两种错误风格；在 HTTP 边界 safe 版本往往更容易精确映射状态码。', pitfall: '失败后不要继续使用原 messages；日志也不要输出完整敏感内容。', officialUrl: `${AI_SDK_CORE}safe-validate-ui-messages` }),
]
