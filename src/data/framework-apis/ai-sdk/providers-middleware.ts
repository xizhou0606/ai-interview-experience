import { AI_SDK_CORE, aiSdkApi } from '../helpers'

export const aiSdkProviderMiddlewareApis = [
  aiSdkApi({ name: 'createProviderRegistry', group: 'Provider 与模型管理', kind: 'function', signature: 'createProviderRegistry(providers, options?): ProviderRegistry', beginner: '把多个模型供应商放进一个带名字的注册表，以后通过“provider:model”字符串选择模型。', whenToUse: '同一应用要支持 OpenAI、Anthropic、阿里云等多个 Provider，并希望配置层统一选型。', example: `const registry = createProviderRegistry({ openai, anthropic })\nconst chat = registry.languageModel('openai:gpt-4.1')\nconst embedding = registry.embeddingModel('openai:text-embedding-3-small')\nconst image = registry.imageModel('openai:dall-e-3')`, returns: '可解析 languageModel、embeddingModel、imageModel 等的 ProviderRegistry。', interview: '注册表解决模型发现和切换，不解决业务路由策略；成本、降级和能力匹配仍要单独设计。', pitfall: '模型 id 必须白名单化，不要让客户端任意选择高成本或未授权模型。', officialUrl: `${AI_SDK_CORE}provider-registry` }),
  aiSdkApi({ name: 'customProvider', group: 'Provider 与模型管理', kind: 'function', signature: 'customProvider({ languageModels?, fallbackProvider?, ... }): Provider', beginner: '创建一个“虚拟供应商”，把友好模型别名映射到真实模型，也可以给原 Provider 加默认或替换配置。', whenToUse: '想用 stable/fast/reasoning 这类业务别名，而不是在全站散落真实模型 id。', example: `const appModels = customProvider({\n  languageModels: { fast: openai('gpt-4.1-mini') },\n  fallbackProvider: openai,\n})`, returns: '符合 Provider 接口的自定义 Provider。', interview: '说明别名隔离能让业务代码不感知模型迁移；生产还需配置审计和能力探针。', pitfall: '别名变更可能改变质量和成本，应版本化并做回归评测。', officialUrl: `${AI_SDK_CORE}custom-provider` }),
  aiSdkApi({ name: 'cosineSimilarity', group: 'Provider 与模型管理', kind: 'function', signature: 'cosineSimilarity(vector1, vector2): number', beginner: '计算两个向量方向有多相似，结果越接近 1 通常表示语义越接近。', whenToUse: '小规模本地相似度、测试 embedding 或实现简单去重。', example: `const score = cosineSimilarity(queryEmbedding, documentEmbedding)`, returns: '-1 到 1 附近的相似度数值。', interview: '余弦相似度比较方向而非长度；大规模检索应交给向量数据库索引，不要在应用层全表遍历。', pitfall: '两个向量维度必须一致，且必须来自可比较的模型/版本。', officialUrl: `${AI_SDK_CORE}cosine-similarity` }),
  aiSdkApi({ name: 'wrapLanguageModel', group: 'Middleware', kind: 'function', signature: 'wrapLanguageModel({ model, middleware, modelId?, providerId? }): LanguageModel', beginner: '像给模型套一层拦截器：不改业务调用方式，也能统一改请求、响应、日志或默认行为。', whenToUse: '统一缓存、观测、敏感词处理、默认设置或兼容 Provider 差异。', example: `const wrapped = wrapLanguageModel({ model, middleware: defaultSettingsMiddleware({ settings: { temperature: 0.2 } }) })`, returns: '仍符合 LanguageModel 接口的包装模型。', interview: '这是装饰器/中间件模式；要讲顺序、透明性、错误传播和避免记录敏感 prompt。', pitfall: '多层 middleware 顺序会影响行为；不要让观测层吞掉错误或改变业务语义。', officialUrl: `${AI_SDK_CORE}wrap-language-model` }),
  aiSdkApi({ name: 'wrapImageModel', group: 'Middleware', kind: 'function', signature: 'wrapImageModel({ model, middleware, modelId?, providerId? }): ImageModel', beginner: '与 wrapLanguageModel 相似，只是对象变成图像模型，可统一处理 prompt、尺寸、元数据和响应。', whenToUse: '多图像 Provider 需要统一默认值、审计或兼容适配。', example: `const safeImageModel = wrapImageModel({ model: imageModel, middleware })`, returns: '包装后的 ImageModel。', interview: '图像中间件常用于 provenance、审核、成本记录和 Provider 参数规范化。', pitfall: '不要在 middleware 中保存未授权参考图；二进制日志会放大隐私和存储风险。', officialUrl: `${AI_SDK_CORE}wrap-image-model` }),
  aiSdkApi({ name: 'LanguageModelV3Middleware', group: 'Middleware', kind: 'interface', maturity: 'type-only', lifecycle: 'experimental', signature: "type LanguageModelV3Middleware = { specificationVersion: 'v3'; transformParams?; wrapGenerate?; wrapStream?; overrideProvider?; overrideModelId?; overrideSupportedUrls? }", beginner: '这是 AI SDK 6 标记为实验性的语言模型中间件类型合同：先声明 v3 规范版本，再选择改参数、包住生成/流，或覆盖 provider、model id 与支持的 URL。', whenToUse: '自己实现缓存、审计、回退、模拟、内容转换或模型元数据覆盖中间件，并能承担实验性合同升级成本时。', example: `const middleware: LanguageModelV3Middleware = {\n  specificationVersion: 'v3',\n  transformParams: async ({ params }) => ({\n    ...params, temperature: 0.2,\n  }),\n}`, returns: 'TypeScript 中间件合同，本身不运行；交给 wrapLanguageModel 后才参与请求。', interview: 'transformParams 发生在调用前；wrapGenerate/wrapStream 包裹执行。V3 是协议版本，而 middleware 功能本身另被官方标为 experimental；流式包装必须保留所有 chunk 与取消语义。', pitfall: '不能漏掉 specificationVersion；自定义流转换也不能丢 tool-call、usage、finish 等非文本 chunk，升级 AI SDK 时还要核对实验性合同变化。', officialUrl: `${AI_SDK_CORE}language-model-v2-middleware` }),
  aiSdkApi({ name: 'extractReasoningMiddleware', group: 'Middleware', kind: 'middleware', signature: 'extractReasoningMiddleware({ tagName?, separator?, startWithReasoning? }): LanguageModelV3Middleware', beginner: '当 Provider 把推理内容混在文本标签里时，它把推理和最终回答拆成两个独立字段。', whenToUse: '兼容使用 <think> 等标签返回 reasoning 的模型。', example: `const middleware = extractReasoningMiddleware({ tagName: 'think' })`, returns: '可传给 wrapLanguageModel 的 middleware。', interview: '推理内容可能敏感且不适合直接展示；拆分是协议适配，不代表应该把 chain-of-thought 暴露给用户。', pitfall: '标签不完整或模型格式漂移会导致解析失败，必须监控 warnings 并准备降级。', officialUrl: `${AI_SDK_CORE}extract-reasoning-middleware` }),
  aiSdkApi({ name: 'simulateStreamingMiddleware', group: 'Middleware', kind: 'middleware', signature: 'simulateStreamingMiddleware(): LanguageModelV3Middleware', beginner: '把一次性完整结果切成多个 chunk，看起来像流式返回，常用于不支持流的模型或测试。', whenToUse: 'Provider 只有 generate 能力，但上层 UI 统一要求 stream 接口。', example: `const model = wrapLanguageModel({ model: nonStreamingModel, middleware: simulateStreamingMiddleware() })`, returns: '模拟 doStream 的 middleware。', interview: '要明确它不能降低首字真实延迟，只是统一接口和演示效果。', pitfall: '不要把模拟流宣传成 Provider 原生流；取消与 usage 时序也可能不同。', officialUrl: `${AI_SDK_CORE}simulate-streaming-middleware` }),
  aiSdkApi({ name: 'defaultSettingsMiddleware', group: 'Middleware', kind: 'middleware', signature: 'defaultSettingsMiddleware({ settings }): LanguageModelV3Middleware', beginner: '给模型调用补一组默认参数；调用方明确传入的值通常仍可覆盖默认值。', whenToUse: '全站统一 temperature、maxOutputTokens、providerOptions 或其他稳定默认配置。', example: `const middleware = defaultSettingsMiddleware({ settings: { temperature: 0.2, maxOutputTokens: 800 } })`, returns: '应用默认设置的语言模型 middleware。', interview: '默认设置属于组合根策略，不应散落在业务函数；但不同任务可能需要专用配置。', pitfall: '不要用一个 temperature 适配所有任务；结构化抽取和创意写作目标不同。', officialUrl: `${AI_SDK_CORE}default-settings-middleware` }),
  aiSdkApi({ name: 'addToolInputExamplesMiddleware', group: 'Middleware', kind: 'middleware', signature: 'addToolInputExamplesMiddleware(): LanguageModelV3Middleware', beginner: '把工具里定义的 inputExamples 注入模型可见信息，帮助模型更准确地产生工具参数。', whenToUse: 'Provider 不原生支持工具输入示例，但复杂 schema 只靠 description 命中率不够。', example: `const model = wrapLanguageModel({ model, middleware: addToolInputExamplesMiddleware() })`, returns: '增强工具描述的 middleware。', interview: '示例能提升参数生成质量，但也会增加 prompt token；应选少量代表性边界例子。', pitfall: '示例不能包含密钥、真实个人数据或会诱导模型复制的过期值。', officialUrl: `${AI_SDK_CORE}add-tool-input-examples-middleware` }),
  aiSdkApi({ name: 'extractJsonMiddleware', group: 'Middleware', kind: 'middleware', signature: 'extractJsonMiddleware(): LanguageModelV3Middleware', beginner: '当模型把 JSON 包在 Markdown 代码块里时，它先去掉围栏，让后续结构化解析更容易成功。', whenToUse: '兼容不能稳定原生 JSON mode、却常返回 ```json 的模型。', example: `const model = wrapLanguageModel({ model, middleware: extractJsonMiddleware() })`, returns: '清理文本内容的 middleware。', interview: '这是兼容性补丁，不等于 schema 校验；清理后仍必须验证字段和业务规则。', pitfall: '不要用字符串截取替代严格解析；回答中含多个代码块时要 fail closed。', officialUrl: `${AI_SDK_CORE}extract-json-middleware` }),
  aiSdkApi({ name: 'stepCountIs', group: '循环控制与测试', kind: 'function', signature: 'stepCountIs(count): StopCondition', beginner: '生成执行到指定步骤数后返回 true，让工具循环停止。', whenToUse: '给 ToolLoopAgent 或 generateText/streamText 的多步工具调用设置硬上限。', example: `const result = await generateText({ model, tools, stopWhen: stepCountIs(8) })`, returns: '检查 steps 数量的 StopCondition。', interview: '它是成本和死循环的最后保险，但业务完成条件通常还要配合 hasToolCall 或自定义条件。', pitfall: '上限太小任务做不完，太大成本失控；需要结合任务复杂度和监控调优。', officialUrl: `${AI_SDK_CORE}step-count-is` }),
  aiSdkApi({ name: 'hasToolCall', group: '循环控制与测试', kind: 'function', signature: 'hasToolCall(toolName): StopCondition', beginner: '它会检查最后一个生成步骤是否调用了指定工具；如果调用过，就通知 Agent 停止继续循环。', whenToUse: '有 finish/submit/escalate 等明确结束工具的 Agent。', example: `stopWhen: [stepCountIs(8), hasToolCall('submitReport')]`, returns: '按最后一步工具名判断的 StopCondition。', interview: '这是显式终止协议，比让模型在自然语言里说“完成了”更可靠。', pitfall: '终止工具的 execute 仍要幂等；被调用不一定代表副作用成功。', officialUrl: `${AI_SDK_CORE}has-tool-call` }),
  aiSdkApi({ name: 'simulateReadableStream', group: '循环控制与测试', kind: 'function', signature: 'simulateReadableStream({ chunks, initialDelayInMs?, chunkDelayInMs? }): ReadableStream', beginner: '用固定 chunk 和延迟造一个可预测的 ReadableStream，方便测试加载、取消和增量渲染。', whenToUse: '单元测试 UI stream 消费逻辑，不想真实调用模型。', example: `const stream = simulateReadableStream({ chunks: ['你', '好'], chunkDelayInMs: 20 })`, returns: '按配置发出 chunks 的 ReadableStream。', interview: '测试流式 UI 应使用确定性 fake，覆盖跨 chunk 边界、取消和异常，而不是依赖真实模型。', pitfall: '只测理想 chunk 会漏掉半包、空包和错误终止场景。', officialUrl: `${AI_SDK_CORE}simulate-readable-stream` }),
  aiSdkApi({ name: 'smoothStream', group: '循环控制与测试', kind: 'function', signature: 'smoothStream(options?): StreamTextTransform', beginner: '把忽快忽慢的大块文本重新切成更平滑的小块，让界面显示更自然。', whenToUse: 'Provider chunk 粒度很不均匀，影响打字机体验。', example: `streamText({ model, prompt, experimental_transform: smoothStream({ delayInMs: 15 }) })`, returns: '传给 streamText transform 的流转换器。', interview: '它优化展示节奏，不改变模型内容和真实生成速度；transform 发生在回调与结果解析前。', pitfall: '过度平滑会人为增加延迟；需要在首字速度和阅读体验之间平衡。', officialUrl: `${AI_SDK_CORE}smooth-stream` }),
  aiSdkApi({ name: 'generateId', group: '循环控制与测试', kind: 'function', signature: 'generateId(size?): string', beginner: '生成一个适合消息、工具调用或请求关联的随机 ID。', whenToUse: '不需要业务可读性、只需要高概率唯一的客户端/服务端标识。', example: `const messageId = generateId()`, returns: '返回指定长度的高熵随机字符串 ID，可直接用作消息或请求标识。', interview: 'ID 负责关联和幂等键的一部分，但真正幂等还需要数据库唯一约束和请求语义。', pitfall: '不要把随机 ID 当权限证明；公开资源还要授权或高熵 capability 设计。', officialUrl: `${AI_SDK_CORE}generate-id` }),
  aiSdkApi({ name: 'createIdGenerator', group: '循环控制与测试', kind: 'function', signature: 'createIdGenerator({ prefix?, size?, alphabet? }): () => string', beginner: '先定制 ID 的前缀、长度和字符集，再得到一个可重复调用的生成函数。', whenToUse: '希望 message_、tool_ 等 ID 可从日志快速识别，或测试需要固定格式。', example: `const nextMessageId = createIdGenerator({ prefix: 'msg', size: 16 })\nconst id = nextMessageId()`, returns: '每次调用返回新 ID 的函数。', interview: '前缀提高可观测性，不能降低随机部分的熵；格式化与唯一性是两件事。', pitfall: '自定义 alphabet/size 过小会增加碰撞概率。', officialUrl: `${AI_SDK_CORE}create-id-generator` }),
  aiSdkApi({ name: 'DefaultGeneratedFile', group: '循环控制与测试', kind: 'class', signature: 'new DefaultGeneratedFile({ data, mediaType })', beginner: 'AI SDK 对生成二进制文件的默认实现，可用 base64 或 Uint8Array 访问文件内容。', whenToUse: '自定义 Provider、测试图像/语音结果或需要构造 GeneratedFile。', example: `const file = new DefaultGeneratedFile({ data: bytes, mediaType: 'image/png' })`, returns: '实现 GeneratedFile 的对象，暴露 base64、uint8Array 和 mediaType。', interview: '二进制应通过对象存储和流式响应管理，GeneratedFile 只是进程内文件合同。', pitfall: '频繁 base64 转换会增加约 33% 体积并放大内存峰值。', officialUrl: `${AI_SDK_CORE}default-generated-file` }),
]
