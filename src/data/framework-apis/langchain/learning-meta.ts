import type { FrameworkApiLearningMeta, FrameworkApiLearningLevel, FrameworkApiParameter, FrameworkApiRuntime } from '../types'

const p = (name: string, type: string, required: boolean, description: string, defaultValue?: string): FrameworkApiParameter => ({ name, type, required, description, ...(defaultValue === undefined ? {} : { defaultValue }) })
const meta = (learningLevel: FrameworkApiLearningLevel, runtime: FrameworkApiRuntime, parameters: FrameworkApiParameter[], expectedOutput: string, condition: string, handling: string, relatedApis: string[]): FrameworkApiLearningMeta => ({ learningLevel, runtime, parameters, expectedOutput, errorCases: [{ condition, handling }], relatedApis })

export const langChainLearningMeta = {
  createAgent: meta('core', 'server', [p('model', 'LanguageModelLike', true, '负责决策和生成回复的聊天模型实例或供应商模型名。'), p('tools', 'Tool[]', false, '当前智能体可以发现并调用的最小权限工具集合。'), p('middleware', 'AgentMiddleware[]', false, '按声明顺序编排鉴权、路由、重试和审计等横切逻辑。'), p('responseFormat', 'ProviderStrategy | ToolStrategy', false, '用于验证并写入 structuredResponse 的结构化输出策略。')], '返回已编译的 ReactAgent 状态图，可通过 invoke 或 stream 运行模型—工具循环并取得最终状态。', '模型包未安装、工具 schema 无效、循环超限或中间件抛错', '启动时校验 provider 与工具目录；限制模型/工具步数并对可恢复错误分类处理。', ['ReactAgent', 'createMiddleware', 'tool']),
  ReactAgent: meta('core', 'server', [p('input.messages', 'BaseMessageLike[]', true, '每次运行写入智能体状态的用户与历史消息集合。'), p('config.context', 'Context', false, '注入工具和中间件但不暴露给模型的请求级身份与依赖。'), p('config.configurable', 'Record<string, unknown>', false, '线程、检查点或运行时扩展读取的可配置字段。')], '运行后返回包含 messages、可选 structuredResponse 与自定义状态字段的智能体最终状态对象。', '状态 schema 不兼容、运行被中断或工具调用无法恢复', '保留稳定线程标识和检查点；按状态字段判断完成、中断与失败并提供恢复入口。', ['createAgent', 'BuiltInState', 'Runtime']),
  BuiltInState: meta('reference', 'server', [p('messages', 'BaseMessage<TMessageStructure>[]', true, '智能体对话和工具交互消息；泛型 TMessageStructure 约束消息内容结构。'), p('__interrupt__', 'Interrupt[]', false, '当前运行产生、等待恢复处理的中断信息。'), p('jumpTo', 'JumpToTarget', false, 'afterModel 中间件可写入的一次性路由目标：model_request 或 tools，使用后自动清除。')], '这是 TypeScript 状态类型，不产生运行时对象；它只定义 messages、__interrupt__ 和 jumpTo 三类内置字段。', '把泛型误当 structuredResponse 类型、把 jumpTo 写成数组，或用普通字符串覆盖消息', '按当前 Reference 扩展 schema；结构化结果通过 Agent 的响应格式类型处理，不要伪造 BuiltInState 字段。', ['ReactAgent', 'createMiddleware', 'Runtime']),
  Runtime: meta('core', 'server', [p('context', 'Context', true, '一次运行内不可变的用户身份、租户信息和受控依赖。'), p('store', 'BaseStore', false, '供工具和中间件访问的长期共享存储接口。'), p('streamWriter', 'StreamWriter', false, '向调用方发送自定义结构化进度事件的写入器。')], '这是运行时上下文类型；实际实例由 invoke 或 stream 配置创建并注入执行图的各个节点。', 'contextSchema 与实际值不符或把密钥写入可持久化 state', '在入口校验 contextSchema；敏感依赖只放 Runtime，并限制日志与序列化字段。', ['ToolRuntime', 'ReactAgent', 'Runnable.invoke']),
  ToolRuntime: meta('core', 'server', [p('context', 'Context', true, '从当前 Runtime 继承且不会进入模型参数 schema 的身份信息。'), p('state', 'State', true, '工具调用时可读取的当前智能体状态快照。'), p('toolCallId', 'string', true, '框架生成并用于关联 ToolMessage 的工具调用编号。')], '这是框架自动注入工具实现的隐藏运行参数，提供状态、身份、存储、配置和调用关联信息。', '把 runtime 字段暴露进工具 schema 或信任模型传入 toolCallId', '运行参数只由框架注入；工具业务 schema 与权限上下文保持严格分离。', ['Runtime', 'tool', 'ToolMessage']),
  providerStrategy: meta('core', 'server', [p('schema', 'InteropZodType | JSON Schema', true, '模型原生 structured output 必须满足的结果结构与字段约束。'), p('provider capability', 'boolean', true, '目标模型是否支持原生 JSON Schema 约束输出。'), p('validation policy', 'strict', true, '解析后仍需执行的服务端 schema 验证策略。')], '返回 ProviderStrategy 配置；成功时验证后的对象写入智能体状态的 structuredResponse 字段。', '模型不支持原生结构化输出或返回内容无法通过 schema', '启动前检查模型能力；验证失败可改用 toolStrategy 或受控重试，禁止伪造结果。', ['toolStrategy', 'createAgent', 'BuiltInState']),
  toolStrategy: meta('core', 'server', [p('schema', 'InteropZodType | JSON Schema', true, '虚拟结构化输出工具所承载的目标结果结构。'), p('options.handleErrors', 'boolean | string | function', false, '验证失败时反馈模型、重试或终止的错误策略。'), p('tool message policy', 'ToolStrategyOptions', false, '控制结构化工具消息和多调用冲突的行为。')], '返回 ToolStrategy 配置；模型通过工具调用参数产生并验证最终 structuredResponse 对象。', '模型重复调用结构化工具、参数验证失败或与业务工具同名', '使用唯一名称并限制重试次数；将验证错误作为受控反馈返回模型并记录原始输出。', ['providerStrategy', 'createAgent', 'StructuredTool']),
  createMiddleware: meta('core', 'server', [p('options.name', 'string', true, '中间件在执行图、日志与追踪中的稳定唯一名称。'), p('before/after hooks', 'Agent hooks', false, '在模型或工具节点前后读取并更新状态的钩子。'), p('wrap hooks', 'wrapModelCall | wrapToolCall', false, '围绕真实调用实现鉴权、重试、路由、短路和审计。')], '返回可加入 createAgent.middleware 的 AgentMiddleware，所有钩子会按顺序编入智能体执行图。', '多个包装器顺序冲突、异步副作用未等待或错误被错误吞掉', '固定并测试中间件顺序；所有异步操作 await，并让不可恢复错误继续向上传播。', ['createAgent', 'modelRetryMiddleware', 'toolRetryMiddleware']),

  Runnable: meta('core', 'server', [p('RunInput', 'generic', true, 'Runnable 接受的类型化输入契约，决定组合链的上游要求。'), p('RunOutput', 'generic', true, '执行完成或流式产出的类型化输出契约。'), p('CallOptions', 'RunnableConfig', false, '标签、metadata、回调、取消与并发等横切配置。')], '抽象类定义统一的 invoke、batch、stream、配置和组合协议，具体子类执行后产生 RunOutput。', '直接实例化抽象类或 LCEL 上下游输入输出类型不兼容', '使用具体 Runnable 子类，并为每个 pipe 边界编写类型与运行时契约测试。', ['Runnable.invoke', 'Runnable.pipe', 'RunnableSequence.from']),
  'Runnable.invoke': meta('core', 'server', [p('input', 'RunInput', true, '必须满足当前 Runnable 输入泛型和运行时 schema 的单条数据。'), p('options.tags', 'string[]', false, '随执行树传播并用于追踪过滤的低基数标签。'), p('options.metadata', 'Record<string, unknown>', false, '随运行记录的脱敏业务版本和诊断字段。')], '返回 Promise<RunOutput>，具体结果可以是 AIMessage、Document 数组或解析后的结构化对象。', '输入契约错误、远程调用超时或执行链任一节点抛出异常', '在调用边界校验输入并传播取消信号；仅对最小可幂等节点实施分类重试。', ['Runnable', 'Runnable.batch', 'Runnable.stream']),
  'Runnable.batch': meta('core', 'server', [p('inputs', 'RunInput[]', true, '保持顺序的一组独立输入，输出位置与其一一对应。'), p('options.maxConcurrency', 'number', false, '保护供应商限流和连接池的最大并发数量。'), p('batchOptions.returnExceptions', 'boolean', false, '是否把逐项异常放回对应输出位置而非整体抛出。')], '返回与 inputs 顺序一致的 RunOutput 数组；启用 returnExceptions 后失败位置可能保存 Error。', '输入过多造成并发放大或调用方忽略逐项 Error', '分批并限制并发；启用部分失败模式后逐项判错、重试并保存输入关联 id。', ['Runnable.invoke', 'BaseChatModel.batch', 'Embeddings.embedDocuments']),
  'Runnable.stream': meta('core', 'both', [p('input', 'RunInput', true, '启动本次流式执行的类型化输入数据。'), p('options.signal', 'AbortSignal', false, '客户端断线或用户停止时向上游传播的取消信号。'), p('consumer', 'AsyncIterable consumer', true, '必须按实际 chunk 类型处理背压、错误和完成状态。')], '返回可用 for await 消费的 IterableReadableStream，支持的组件会增量产出 RunOutput 块。', '中途断线、慢消费者积压或把非字符串 chunk 当文本渲染', '传播取消并限制缓冲；按 chunk 类型解析，断线后用业务运行 id 查询最终状态。', ['Runnable.invoke', 'Runnable.streamEvents', 'BaseChatModel.stream']),
  'Runnable.streamEvents': meta('advanced', 'both', [p('input', 'RunInput', true, '要观察完整执行树的原始运行输入。'), p('options.version', '"v1" | "v2"', true, '事件 schema 版本，v2 包含更完整父运行关系。'), p('streamOptions', 'EventStreamCallbackHandlerOptions', false, '按名称、标签和类型筛选高价值事件的选项。')], '返回包含开始、增量、结束和自定义事件的 StreamEvent 流，可用于重建模型、工具与子链时间线。', '事件量过大、事件包含敏感输入输出或客户端重复消费', '生产设置 include/exclude 过滤和脱敏；携带 run_id 与序号以支持去重和诊断。', ['Runnable.stream', 'Runnable.withListeners', 'ReactAgent']),
  'Runnable.withConfig': meta('advanced', 'server', [p('config', 'RunnableConfig', true, '要预绑定到后续所有调用并向子链传播的运行配置。')], '返回合并了固定 tags、metadata、callbacks 或 configurable 的新 Runnable，原对象保持不变。', 'metadata 泄露敏感信息或调用级配置覆盖规则理解错误', '只允许低敏字段进入追踪，并用测试确认预绑定配置与 invoke 配置的合并结果。', ['Runnable.invoke', 'Runnable.getGraph', 'Runnable.withListeners']),
  'Runnable.withRetry': meta('advanced', 'server', [p('fields.stopAfterAttempt', 'number', false, '包含首次调用在内允许的最大尝试次数。')], '返回输出类型不变的 RunnableRetry；超过策略仍失败时向调用方抛出最后一次异常。', '重试非幂等链、与 SDK 内部重试叠加或超过总时限', '只包装最小幂等远程调用；统一重试预算、指数退避并遵守总截止时间。', ['Runnable.withFallbacks', 'modelRetryMiddleware', 'toolRetryMiddleware']),
  'Runnable.withFallbacks': meta('advanced', 'server', [p('fields.fallbacks', 'RunnableLike[]', true, '与主 Runnable 具有相同输入输出契约的降级候选列表。')], '返回按顺序尝试备用实现的 RunnableWithFallbacks，全部失败后才抛出最终异常。', '备用链能力不兼容、隐藏程序错误或重复产生副作用', '仅对明确可降级错误触发；校验契约并记录实际命中路径、质量等级和副作用。', ['Runnable.withRetry', 'modelFallbackMiddleware', 'Runnable.pipe']),
  'Runnable.pipe': meta('core', 'server', [p('coerceable', 'RunnableLike<RunOutput, NewOutput>', true, '接收当前输出并产生新输出的下游 RunnableLike。'), p('upstream output', 'RunOutput', true, '必须满足下游输入契约的当前组件输出。'), p('stream behavior', 'transform capability', false, '中间节点是否能保持增量流式转换的能力。')], '返回输入类型来自首节点、输出类型来自末节点的组合 RunnableSequence，不会立即执行。', '上下游数据形状不匹配或中间 Lambda 阻断流式输出', '为每个边界声明类型并验证样例；需要流式时使用支持 transform 的增量组件。', ['RunnableSequence.from', 'Runnable.assign', 'RunnableLambda.from']),
  'Runnable.assign': meta('core', 'server', [p('mapping', 'RunnableMapLike', true, '基于当前对象输入计算并新增字段的命名 Runnable 映射。'), p('input object', 'Record<string, unknown>', true, '会原样保留并与新字段合并的对象输入。'), p('field collision', 'merge policy', false, '新字段与现有键重名时需要明确覆盖语义。')], '返回保留原输入字段并附加 mapping 计算字段的新 Runnable，适合并行补充 LCEL 上下文。', '输入不是对象、映射字段失败或新键意外覆盖原数据', '在 assign 前规范化对象形状；给并行字段独立容错并避免使用已有关键键名。', ['RunnablePassthrough.assign', 'RunnableParallel.from', 'Runnable.pipe']),
  'Runnable.asTool': meta('advanced', 'server', [p('fields', 'RunnableToolLike', true, '定义模型可见的工具名、描述和输入 schema。')], '返回包装当前 Runnable 的 DynamicStructuredTool，使 Agent 能通过工具调用协议执行这条链。', 'Runnable 输入 schema 不完整或工具执行缺少权限边界', '提供严格 schema 与清晰描述；在链入口读取 Runtime 身份并再次执行服务端授权。', ['tool', 'DynamicStructuredTool', 'Runnable.invoke']),
  'Runnable.getGraph': meta('reference', 'server', [p('config', 'RunnableConfig', false, '解析可配置分支时使用的运行配置。')], '返回表示 Runnable 节点和边关系的 Graph，可用于可视化、调试和静态结构检查。', '误把静态图当作真实运行轨迹或图中包含敏感配置', '运行诊断仍以 streamEvents/trace 为准；展示前过滤节点 metadata 和内部配置。', ['Runnable.streamEvents', 'RunnableSequence.from', 'RunnableParallel.from']),
  'Runnable.withListeners': meta('advanced', 'server', [p('onStart/onEnd/onError', 'RunnableListenerConfig', true, '在运行生命周期边界接收可观察 Run 信息的监听器。')], '返回附加生命周期监听器的新 Runnable，业务输出类型与原组件保持一致且原对象不变。', '监听器耗时阻塞主调用、抛错污染业务或记录敏感内容', '监听器只投递有界异步队列并隔离异常；对输入输出实施采样和脱敏。', ['Runnable.streamEvents', 'Runnable.withConfig', 'BaseChatModel.invoke']),
  'RunnableLambda.from': meta('core', 'server', [p('func', 'RunnableFunc<RunInput, RunOutput>', true, '把输入转换成输出或另一个 Runnable 的类型化函数。'), p('config', 'RunnableConfig', false, '函数需要读取的取消、回调与 configurable 配置。'), p('idempotency', 'function contract', true, '函数若被重试必须明确是否安全重复执行。')], '返回可参与 invoke、batch、stream 和 pipe 的 RunnableLambda，把普通函数接入 LCEL 协议。', '函数返回类型漂移、隐藏副作用被重试或同步计算阻塞事件循环', '为输入输出加 schema；副作用显式幂等，重计算任务转移到受控工作线程或队列。', ['Runnable', 'Runnable.pipe', 'RunnableSequence.from']),
  'RunnableSequence.from': meta('core', 'server', [p('steps', 'RunnableLike[]', true, '按顺序执行且相邻输入输出兼容的至少两个步骤。'), p('name', 'string', false, '用于 trace 和图展示的稳定序列名称。'), p('error boundary', 'step policy', true, '每个远程步骤独立定义重试、回退和观测边界。')], '返回依次执行全部步骤的 RunnableSequence，首步决定输入类型，末步决定最终输出类型。', '步骤不足、相邻契约不匹配或整链重试重复前序副作用', '构建期检查步骤数量和类型；只在最小失败节点配置重试并记录步骤级 trace。', ['Runnable.pipe', 'RunnableParallel.from', 'RunnableLambda.from']),
  'RunnableParallel.from': meta('core', 'server', [p('steps', 'RunnableMapLike', true, '接收同一输入并按字段名并行计算的 Runnable 映射。'), p('maxConcurrency', 'RunnableConfig', false, '限制模型、检索和外部服务同时调用数量。'), p('partial failure policy', 'application policy', true, '一个分支失败时是否整体失败或使用可识别降级值。')], '返回并行执行映射并以相同键名聚合结果对象的 RunnableParallel。', '瞬时并发放大、一个分支拖慢全部结果或部分失败被隐藏', '设置并发与超时；为非关键分支提供显式降级标识，关键失败则取消其余任务。', ['Runnable.assign', 'RunnablePassthrough.assign', 'RunnableSequence.from']),
  'RunnablePassthrough.assign': meta('core', 'server', [p('mapping', 'RunnableMapLike', true, '从对象输入派生并合并的新字段映射。'), p('original input', 'Record<string, unknown>', true, '保持不变传给下游的原始对象字段。'), p('derived keys', 'string[]', true, '必须避免覆盖身份、权限或来源等受保护字段。')], '返回 RunnableAssign，在保留原对象的同时并行计算和合并派生字段。', '输入不是对象或派生键覆盖受保护字段', '在入口校验对象 schema；对 mapping 键设允许列表并在合并后再次验证输出。', ['Runnable.assign', 'RunnableParallel.from', 'Runnable.pipe']),
  'RunnableBranch.from': meta('core', 'server', [p('branches', 'Array<[Condition, RunnableLike]>', true, '按顺序判断的条件与对应执行分支。'), p('default', 'RunnableLike', true, '所有条件未命中时必须执行的最后兜底分支。'), p('condition input', 'RunInput', true, '每个条件读取但不应修改或产生副作用的输入。')], '返回按首个命中条件路由，并保证存在默认路径的 RunnableBranch。', '多个条件同时成立、条件抛错或各分支输出契约不同', '测试优先级与边界输入；条件保持纯函数，并用统一输出 schema 收敛所有分支。', ['RunnableSequence.from', 'RunnableParallel.from', 'Runnable.pipe']),

  initChatModel: meta('core', 'server', [p('model', 'string', false, '采用 provider:model 形式的模型名或延迟选择占位。'), p('fields.modelProvider', 'string', false, '无法从模型名推断时显式指定的供应商标识。'), p('fields.temperature', 'number', false, '控制采样随机性的模型通用配置。')], '异步返回对应供应商的 BaseChatModel，可继续 invoke、stream、bindTools 或结构化输出。', 'provider 包未安装、环境密钥缺失或模型名无法解析', '启动时预热并检查依赖/凭据；失败提供明确配置错误而不是自动切换未知模型。', ['BaseChatModel', 'BaseChatModel.invoke', 'BaseChatModel.bindTools']),
  BaseChatModel: meta('core', 'server', [p('CallOptions', 'generic', true, '供应商调用参数、工具与取消信号的类型契约。'), p('input', 'BaseMessageLike[]', true, '模型每次调用所消费的规范化消息序列。'), p('output', 'AIMessage', true, '包含文本、工具调用、usage 和响应 metadata 的标准结果。')], '抽象类定义聊天模型的 Runnable 协议；具体 provider 子类把输入消息转换成 AIMessage 或消息块。', '直接实例化抽象类或假设所有供应商支持相同能力', '使用具体集成或 initChatModel；调用前检测 tools、stream 与 structured output 能力。', ['initChatModel', 'BaseChatModel.invoke', 'BaseChatModel.stream']),
  'BaseChatModel.invoke': meta('core', 'server', [p('input', 'BaseMessageLike[] | string', true, '字符串或按角色组织的完整模型输入消息。'), p('options', 'CallOptions', false, '工具、停止词、取消、回调和供应商级参数。'), p('timeout/retry', 'CallOptions', false, '本次远程调用的总时限和瞬时错误策略。')], '返回完整 AIMessage，其中可能同时包含文本内容、tool_calls、usage_metadata 和响应 metadata。', '限流、超时、内容过滤或工具调用参数不合法', '区分可重试与业务拒绝；遵守 Retry-After，并保存 finish reason 与 request id 供排障。', ['BaseChatModel.stream', 'AIMessage', 'BaseChatModel.bindTools']),
  'BaseChatModel.stream': meta('core', 'both', [p('input', 'BaseMessageLike[] | string', true, '启动模型流生成的消息或字符串输入。'), p('options.signal', 'AbortSignal', false, '断线或用户停止时取消供应商请求。'), p('chunk accumulator', 'AIMessageChunk', true, '消费端需要按顺序 concat 的增量消息块。')], '返回 AIMessageChunk 的可异步迭代流，合并后可得到完整文本、工具调用和 usage 信息。', '中途断线、工具调用参数跨块分片或 chunk 顺序丢失', '按顺序 concat 并处理结束/错误事件；传播取消，必要时保存已完成部分与运行标识。', ['AIMessageChunk.concat', 'BaseChatModel.invoke', 'Runnable.stream']),
  'BaseChatModel.batch': meta('core', 'server', [p('inputs', 'BaseMessageLike[][] | string[]', true, '保持顺序的多组独立模型输入。'), p('options.maxConcurrency', 'number', false, '供应商限流范围内允许的最大并发。'), p('batchOptions.returnExceptions', 'boolean', false, '是否在对应位置返回逐项异常。')], '返回与 inputs 位置一致的 AIMessage 数组，具体 provider 可能优化为原生批处理。', '批量限流、部分输入失败或调用方将 Error 当 AIMessage', '限制批次和并发；逐项保存输入 id、错误类型并只重试失败且安全的项目。', ['Runnable.batch', 'BaseChatModel.invoke', 'AIMessage']),
  'BaseChatModel.bindTools': meta('core', 'server', [p('tools', 'BindToolsInput[]', true, '转换为供应商 tool schema 的受控工具集合。'), p('kwargs.tool_choice', 'string | object', false, '自动、强制或禁用某个工具的选择策略。'), p('authorization', 'application policy', true, '模型可见之外仍需在执行层检查的主体权限。')], '返回输入为消息、输出为 AIMessage 的 Runnable；模型可生成 tool_calls，但不会自动执行业务工具。', '供应商不支持工具、schema 超限或把 tool call 误认为已执行', '在绑定前按权限过滤；验证 tool_calls 后由受控执行器调用并写回 ToolMessage。', ['tool', 'ToolMessage', 'createAgent']),
  'BaseChatModel.withStructuredOutput': meta('core', 'server', [p('schema', 'InteropZodType | JSON Schema', true, '模型输出必须满足的业务字段与类型约束。'), p('config.method', 'StructuredOutputMethod', false, '指定 provider 原生、工具调用或 JSON 模式。'), p('config.includeRaw', 'boolean', false, '是否同时保留原始 AIMessage 便于诊断。')], '返回输入消息到验证后 RunOutput 对象的 Runnable，可直接参与 LCEL 管道。', '模型能力不支持、输出验证失败或 schema 过度复杂', '根据 provider 能力选择方法；限制结构复杂度并对失败做受控重试或人工降级。', ['providerStrategy', 'StructuredOutputParser.fromZodSchema', 'Runnable.pipe']),

  BaseMessage: meta('reference', 'both', [p('content', 'MessageContent', true, '文本或多模态内容块组成的标准消息载荷。')], '抽象消息基类统一 role/type、content、id、name 与 additional_kwargs 等跨供应商字段。', '把普通对象误当完整消息或序列化时丢失类型信息', '使用具体消息类或官方转换入口，并在跨网络传输后重新执行消息规范化。', ['SystemMessage', 'HumanMessage', 'AIMessage']),
  SystemMessage: meta('core', 'both', [p('content', 'MessageContent', true, '定义模型角色、目标和行为约束的高优先级系统内容。'), p('id', 'string', false, '用于状态更新、审计与移除操作的稳定消息标识。'), p('additional_kwargs', 'Record<string, unknown>', false, '供应商扩展字段，写入前必须限制和脱敏。')], '创建 role 为 system 的标准消息实例，可传给模型或放入 ChatPromptTemplate。', '把用户文本直接拼入高权限系统消息或写入密钥', '分隔不可信内容并用模板变量转义；真正安全边界放在工具授权和数据访问层。', ['HumanMessage', 'ChatPromptTemplate.fromMessages', 'dynamicSystemPromptMiddleware']),
  HumanMessage: meta('core', 'both', [p('content', 'MessageContent', true, '用户本轮文本或多模态输入内容。'), p('id', 'string', false, '支持消息级更新、过滤和审计的稳定标识。'), p('name', 'string', false, '多参与者场景中经过校验的用户显示标识。')], '创建 role 为 human 的标准消息实例，供模型、Agent 和消息处理函数统一消费。', '超大输入、恶意提示注入或多模态 URL 未校验', '限制大小与媒体类型；把用户内容视为不可信数据，并在工具执行层保持权限隔离。', ['SystemMessage', 'AIMessage', 'filterMessages']),
  AIMessage: meta('core', 'both', [p('content', 'MessageContent', true, '模型生成的文本或多模态内容。'), p('tool_calls', 'ToolCall[]', false, '模型提出但尚未执行业务动作的结构化调用意图。'), p('usage_metadata', 'UsageMetadata', false, '本次请求输入、输出和总 token 用量。')], '创建或表示模型完整响应，统一保存内容、工具调用、usage、响应 metadata 与消息 id。', '把 tool_calls 当执行结果、忽略 finish reason 或泄露 provider 原始字段', '先验证并执行工具再生成 ToolMessage；记录必要诊断字段并裁剪敏感响应 metadata。', ['ToolMessage', 'AIMessageChunk.concat', 'BaseChatModel.invoke']),
  'AIMessageChunk.concat': meta('core', 'both', [p('other', 'AIMessageChunk', true, '同一次模型流中紧接当前块的下一条消息增量。'), p('order', 'stream sequence', true, '必须保持供应商发送顺序，不能跨请求混合。'), p('tool call chunks', 'ToolCallChunk[]', false, '需要按索引和 id 合并的分片工具参数。')], '返回合并后的新 AIMessageChunk，逐步累积文本、工具调用分片、usage 与响应 metadata。', '跨运行混合 chunk、顺序错乱或不完整 JSON 工具参数被提前解析', '按 run id 和序号隔离；直到流结束或参数完整后再验证并执行工具调用。', ['AIMessage', 'BaseChatModel.stream', 'ToolMessage']),
  ToolMessage: meta('core', 'both', [p('content', 'MessageContent', true, '工具执行后返回给模型的受控结果或错误摘要。'), p('tool_call_id', 'string', true, '必须与 AIMessage.tool_calls 中的调用 id 精确对应。'), p('status', 'success | error', false, '供模型与观察系统区分工具成功和失败状态。')], '创建 role 为 tool 的标准消息，将真实工具执行结果关联回对应模型 tool call。', 'tool_call_id 错配、结果过大或把内部异常堆栈返回模型', '使用执行器保存的调用 id；对结果截断脱敏，并把异常转换为可操作的受控错误。', ['AIMessage', 'ToolRuntime', 'tool']),
  RemoveMessage: meta('advanced', 'server', [p('id', 'string', true, '要从支持消息归并的状态中删除的目标消息 id。')], '创建消息删除指令，状态归并器处理后移除指定 id 的历史消息而非追加普通内容。', '目标消息没有 id、id 不存在或删除破坏工具调用配对', '为持久化消息生成稳定 id；删除前检查 AIMessage 与 ToolMessage 的关联完整性。', ['BuiltInState', 'filterMessages', 'trimMessages']),
  filterMessages: meta('advanced', 'both', [p('messages', 'BaseMessage[]', true, '要按名称、类型或 id 过滤的原始消息序列。'), p('options.include/exclude', 'FilterMessagesOptions', false, '明确保留或排除消息的条件集合。')], '同步返回满足过滤规则的新 BaseMessage 数组，原始消息数组和消息对象保持不变。', 'include 与 exclude 规则冲突或过滤掉关键 system/tool 配对', '用代表性对话测试规则；保留安全系统消息，并检查每个工具调用仍有对应结果。', ['mergeMessageRuns', 'trimMessages', 'RemoveMessage']),
  mergeMessageRuns: meta('advanced', 'both', [p('messages', 'BaseMessage[]', true, '包含可能连续同类型消息的原始序列。'), p('options.chunkSeparator', 'string', false, '合并连续文本内容时使用的明确分隔符。')], '返回合并连续同类型消息后的新数组，可减少模型请求中的消息数量并保留总体顺序。', '错误合并工具消息、丢失消息 id 或内容边界不清', '对 system、human、AI 和 tool 分别测试；使用清晰分隔符并保留必要来源 metadata。', ['filterMessages', 'trimMessages', 'BaseMessage']),
  trimMessages: meta('core', 'both', [p('messages', 'BaseMessage[]', true, '需要在 token 预算内裁剪的完整对话序列。'), p('options.maxTokens', 'number', true, '裁剪后允许保留的最大 token 数。'), p('options.strategy', 'first | last', true, '从开头或结尾优先保留消息的策略。'), p('options.tokenCounter', 'TokenCounter', true, '与目标模型近似一致的 token 计数实现。')], '异步返回满足 token 预算且尽量保持对话合法边界的新 BaseMessage 数组。', '错误计数导致超上下文、裁掉 system 消息或拆断 tool call 配对', '使用目标模型计数器；配置 startOn/endOn/includeSystem，并对复杂工具对话做边界测试。', ['filterMessages', 'mergeMessageRuns', 'summarizationMiddleware']),

  tool: meta('core', 'server', [p('func', 'RunnableFunc<T, ToolReturn>', true, '真实执行业务动作并接收已验证输入的函数。'), p('fields.name', 'string', true, '模型选择工具和追踪调用使用的稳定唯一名称。'), p('fields.description', 'string', true, '明确能力、适用条件和禁止场景的模型可见描述。'), p('fields.schema', 'InteropZodType<T>', true, '运行时验证模型生成参数的严格输入 schema。')], '返回 DynamicStructuredTool 或 DynamicTool，可绑定给模型或交由 Agent 工具节点安全执行。', '参数虽通过 schema 但越权、重复副作用或外部调用超时', 'func 内再次鉴权；副作用使用幂等键、超时和审计，返回值进行脱敏与大小限制。', ['StructuredTool', 'DynamicStructuredTool', 'ToolRuntime']),
  Tool: meta('reference', 'server', [p('name', 'string', true, '模型可见且在工具集合中唯一的工具名称。')], '抽象字符串输入输出工具基类；具体子类实现调用逻辑并继承 Runnable 与工具协议。', '直接实例化抽象类或用字符串输入承载复杂未验证对象', '使用具体 Tool 子类；复杂参数优先 StructuredTool 并通过 schema 做字段级校验。', ['StructuredTool', 'tool', 'Runnable.asTool']),
  StructuredTool: meta('core', 'server', [p('schema', 'TSchema', true, '定义并验证工具结构化输入字段的运行时 schema。'), p('name', 'string', true, '工具目录、模型调用和 trace 使用的唯一名称。'), p('description', 'string', true, '帮助模型准确选择工具且说明输入与副作用边界。')], '抽象结构化工具基类；具体实现接收验证后的对象输入并产生类型化 TOutput。', 'schema 与实现假设不一致或把描述文本当权限控制', '实现入口使用解析后的参数；权限、幂等与业务约束必须在执行层再次校验。', ['DynamicStructuredTool', 'tool', 'BaseChatModel.bindTools']),
  DynamicStructuredTool: meta('core', 'server', [p('fields.name', 'string', true, '模型与执行器识别该工具的稳定名称。'), p('fields.description', 'string', true, '说明工具能力、触发条件和关键限制。'), p('fields.schema', 'InteropZodType', true, '验证调用参数并提供 TypeScript 推断的输入 schema。'), p('fields.func', 'ToolImplementation', true, '执行已验证输入并返回受控结果的异步实现。')], '构造可直接运行和绑定模型的结构化工具实例，输入输出类型由 schema 与实现共同确定。', '实现抛出内部错误、调用取消未传播或返回敏感数据', '捕获并分类外部错误；传播 signal，裁剪日志和返回内容并记录工具调用审计。', ['tool', 'StructuredTool', 'HeadlessTool.implement']),
  'HeadlessTool.implement': meta('advanced', 'server', [p('func', 'ToolImplementation', true, '为已有无实现工具定义注入的受控执行函数。')], '返回带具体实现的 ClientTool，使预声明的工具 schema 能在当前客户端或服务端真正执行。', '工具定义版本与实现不兼容或客户端实现越过服务端权限', '校验工具 id/schema 版本；高风险动作仍通过受认证服务端执行并保留审计记录。', ['DynamicStructuredTool', 'tool', 'ToolRuntime']),

  'PromptTemplate.fromTemplate': meta('core', 'both', [p('template', 'string', true, '包含受控变量占位符的单段字符串提示模板。'), p('options.templateFormat', 'f-string | mustache', false, '决定占位符解析与转义规则的模板格式。'), p('inputVariables', 'Record<string, unknown>', true, '调用 format/invoke 时必须完整提供并通过业务校验的变量。')], '返回可格式化为字符串提示的 PromptTemplate，并自动推断模板中的输入变量名称。', '缺少变量、模板注入或把未转义用户内容拼入指令', '使用变量绑定而非字符串拼接；入口校验变量并限制用户可控制的模板片段。', ['ChatPromptTemplate.fromMessages', 'Runnable.pipe', 'StringOutputParser']),
  'ChatPromptTemplate.fromMessages': meta('core', 'both', [p('messages', 'ChatPromptTemplateInput[]', true, '按角色和顺序组织的静态、模板或 placeholder 消息。'), p('extra.partialVariables', 'Record<string, unknown>', false, '预先绑定且调用时无需重复提供的安全变量。'), p('runtime variables', 'Record<string, unknown>', true, '每次 invoke 时填入消息模板的业务输入。')], '返回输入变量到 PromptValue 的 Runnable ChatPromptTemplate，可直接 pipe 到聊天模型。', '角色顺序错误、变量缺失或历史消息被当普通字符串转义', '用 MessagesPlaceholder 注入历史；构建期检查变量集合，并测试最终消息角色与顺序。', ['MessagesPlaceholder', 'BaseChatModel', 'PromptTemplate.fromTemplate']),
  MessagesPlaceholder: meta('core', 'both', [p('variableName', 'string', true, '运行时消息数组在模板输入对象中的字段名称。'), p('optional', 'boolean', false, '缺少历史字段时是否允许使用空消息列表。'), p('nMessages', 'number', false, '注入前只保留最后若干条历史消息的数量限制。')], '创建聊天模板中的消息槽位，格式化时把 BaseMessage 数组原样插入指定位置。', '传入字符串而非消息数组或无限历史撑爆模型上下文', '运行时验证消息类型；结合 trimMessages、nMessages 和 token 预算限制历史长度。', ['ChatPromptTemplate.fromMessages', 'trimMessages', 'BaseMessage']),
  FewShotPromptTemplate: meta('advanced', 'both', [p('examples', 'Record<string, unknown>[]', true, '用于示范任务输入输出规律的高质量代表性样例。'), p('examplePrompt', 'PromptTemplate', true, '把每个样例格式化为提示片段的子模板。'), p('prefix/suffix', 'string', true, '样例前的任务说明和样例后的真实输入模板。')], '返回将说明、样例和真实输入组合成最终字符串提示的 Runnable 模板。', '样例污染、样例过多超预算或样例格式与真实任务不一致', '版本化并评测样例集；限制数量、清理敏感内容，并验证最终变量和输出格式。', ['PromptTemplate.fromTemplate', 'Runnable.pipe', 'StructuredOutputParser.fromZodSchema']),
  StringOutputParser: meta('core', 'both', [p('input', 'AIMessage | AIMessageChunk', true, '从模型完整响应或流式消息块中提取文本内容。'), p('stream order', 'AIMessageChunk sequence', true, '流式解析时必须保持同一次运行的消息块顺序。'), p('non-text content', 'MessageContent', false, '遇到多模态内容时需要明确的文本选择策略。')], '返回模型响应中的文本字符串，并可在 LCEL 流中逐块转换为字符串输出。', '响应没有文本、混入多模态块或错误假设工具调用就是答案', '检查 content 类型和 finish reason；工具调用走专用执行路径，不能交给文本解析器。', ['JsonOutputParser', 'BaseChatModel.invoke', 'Runnable.pipe']),
  JsonOutputParser: meta('core', 'both', [p('input', 'string | AIMessageChunk', true, '模型生成的 JSON 文本或流式累积内容。'), p('target type', 'T', true, '解析后调用方期望使用的 TypeScript 结果形状。'), p('validation', 'application schema', true, 'JSON.parse 后仍需执行的业务 schema 校验。')], '返回从模型文本解析得到的 JSON 对象；流式模式可逐步产出累积结构。', 'JSON 语法错误、部分流被提前使用或对象缺少业务字段', '等待完整结果或处理增量语义；解析后再用 Zod 验证，失败记录原始输出并受控重试。', ['StructuredOutputParser.fromZodSchema', 'StringOutputParser', 'OutputParserException']),
  'StructuredOutputParser.fromZodSchema': meta('core', 'both', [p('schema', 'InteropZodType<T>', true, '定义模型输出字段、类型和业务约束的 Zod schema。'), p('format instructions', 'string', true, '由解析器生成并应加入提示词的结构化格式说明。'), p('raw output', 'string', true, '模型返回且需要 JSON 解析和 schema 验证的原始文本。')], '返回能生成格式说明并把模型文本验证为 T 类型对象的 StructuredOutputParser。', '模型输出不是 JSON、字段验证失败或 schema 错误过于复杂', '将格式说明放入 prompt；捕获 OutputParserException，并限制修复重试次数和原文日志。', ['JsonOutputParser', 'OutputParserException', 'BaseChatModel.withStructuredOutput']),
  CommaSeparatedListOutputParser: meta('reference', 'both', [p('input', 'string', true, '模型生成并预期由逗号分隔的单行列表文本。')], '返回去除多余空白后的字符串数组，适合结构简单且无嵌套字段的列表结果。', '列表项本身包含逗号、空输出或模型额外生成解释文字', '仅用于简单值；复杂列表改用 JSON schema，并在解析后过滤空项和数量上限。', ['StringOutputParser', 'JsonOutputParser', 'OutputParserException']),
  XMLOutputParser: meta('advanced', 'both', [p('fields.tags', 'string[]', false, '允许模型输出并由解析器接受的 XML 标签集合。')], '返回把模型 XML 文本转换成嵌套对象结构的转换解析器，并支持流式累积。', '非法 XML、外部实体风险或模型生成未允许标签', '禁用危险实体并限制深度/大小；解析后校验允许标签和目标业务 schema。', ['JsonOutputParser', 'OutputParserException', 'StringOutputParser']),
  OutputParserException: meta('reference', 'both', [p('message', 'string', true, '面向开发者说明解析失败原因的错误消息。'), p('llmOutput', 'string', false, '用于诊断但必须脱敏和截断的原始模型输出。'), p('sendToLLM', 'boolean', false, '是否允许把 observation 反馈给模型尝试自我修复。')], '构造可携带原始输出与修复提示的标准解析异常，供链路统一识别输出格式失败。', '把敏感原文写日志或让模型无限自我修复', '截断脱敏 llmOutput；仅对格式错误有限次反馈，业务安全错误不得自动修复。', ['StructuredOutputParser.fromZodSchema', 'JsonOutputParser', 'Runnable.withRetry']),

  summarizationMiddleware: meta('core', 'server', [p('options.model', 'LanguageModelLike', true, '达到阈值后负责压缩旧对话的总结模型。'), p('options.trigger', 'SummarizationTrigger', true, '按 token 或消息数量触发上下文压缩的条件。'), p('options.keep', 'SummarizationKeep', false, '总结后仍保留的最近消息或 token 窗口。')], '返回在上下文过长时生成摘要并替换旧消息的 AgentMiddleware，帮助控制后续模型 token。', '摘要丢失关键事实、工具配对被破坏或总结成本反复发生', '关键业务事实结构化保存；保留原文引用并评测摘要，确保消息边界合法。', ['createMiddleware', 'trimMessages', 'contextEditingMiddleware']),
  humanInTheLoopMiddleware: meta('core', 'server', [p('options.interruptOn', 'Record<string, boolean | config>', true, '声明哪些工具调用必须暂停并等待人工决策。'), p('approval decision', 'approve | edit | reject', true, '恢复时由已认证操作者提交的审批结果。'), p('checkpoint/thread', 'durable config', true, '跨请求暂停与恢复所需的持久化线程标识。')], '返回在高风险工具执行前中断 Agent 的 Middleware，并把审批请求保存为可恢复状态。', '没有持久化检查点、未授权用户审批或重复恢复导致副作用', '配置 durable 状态；审批接口验证主体和版本，使用幂等键保证一次决策只执行一次。', ['createAgent', 'toolCallLimitMiddleware', 'ToolRuntime']),
  dynamicSystemPromptMiddleware: meta('core', 'server', [p('fn', '(request: ModelRequest) => string | SystemMessage', true, '根据 Runtime context 生成本次模型调用系统提示的函数。'), p('request.runtime.context', 'Context', true, '提供租户、角色与任务信息的受控运行上下文。'), p('prompt policy', 'application policy', true, '禁止密钥、跨租户数据和未转义输入进入系统消息。')], '返回每次模型调用前动态计算并注入系统提示的 AgentMiddleware。', '全局缓存串租户、用户输入提升为系统权限或敏感规则泄露', '按请求生成并严格分隔不可信数据；日志只记录 prompt 版本与哈希而非原文。', ['SystemMessage', 'createMiddleware', 'Runtime']),
  llmToolSelectorMiddleware: meta('advanced', 'server', [p('options.model', 'LanguageModelLike', false, '用于从完整工具目录预选相关工具的轻量模型。'), p('options.maxTools', 'number', false, '每次模型调用最多暴露的候选工具数量。')], '返回先用模型缩小工具集合、再让主模型决策的 Middleware，可降低上下文与误选概率。', '选择器漏掉必要工具、选择结果越权或额外模型成本过高', '授权过滤必须先于语义选择；保留兜底工具并评测召回率、延迟与费用。', ['BaseChatModel.bindTools', 'providerToolSearchMiddleware', 'tool']),
  piiMiddleware: meta('advanced', 'server', [p('type', 'PIIDetectionType', true, '要检测的邮箱、电话、信用卡或自定义 PII 类型。'), p('options.strategy', 'block | redact | mask | hash', false, '检测命中后阻止、替换或哈希内容的处理策略。')], '返回在 Agent 消息路径上检测并处理指定 PII 类型的 Middleware。', '误报阻断正常请求、漏报新格式或哈希仍可被关联识别', '使用业务语料评测精确率/召回率；高风险字段分层策略并记录不含原文的命中指标。', ['piiRedactionMiddleware', 'createMiddleware', 'HumanMessage']),
  piiRedactionMiddleware: meta('advanced', 'server', [p('options.rules', 'Record<string, RegExp>', false, '旧中间件用于生成 REDACTED 占位符的命名正则规则；options 默认空对象。')], '返回已弃用的 PIIRedactionMiddleware：模型调用前替换匹配值，模型响应后恢复占位符以供工具执行，并打印弃用警告。', '旧配置使用不存在的 piiTypes/strategy、占位符未恢复，或 PII 已进入 checkpoint/trace/工具结果', '新代码迁移到 piiMiddleware；维护旧逻辑时覆盖消息、tool call 参数与 structured output 恢复测试，并对存储和日志另做治理。', ['piiMiddleware', 'createMiddleware', 'BaseMessage']),
  contextEditingMiddleware: meta('advanced', 'server', [p('options.edits', 'ContextEdit[]', false, '达到阈值时应用的消息裁剪、工具结果清理等编辑策略。')], '返回在模型调用前编辑上下文的 Middleware，用有界内容替换过长或低价值消息。', '裁掉关键指令、破坏工具消息配对或编辑不可追溯', '保护 system 与最近工具交互；记录编辑策略版本、前后 token 和被移除消息 id。', ['summarizationMiddleware', 'trimMessages', 'createMiddleware']),
  toolCallLimitMiddleware: meta('core', 'server', [p('options.runLimit', 'number', false, '单次 Agent 运行允许的工具总调用上限。'), p('options.threadLimit', 'number', false, '同一持久化线程跨运行允许的工具调用上限。'), p('options.exitBehavior', 'error | end', false, '达到限制时抛错或结束 Agent 的明确策略。')], '返回统计并限制工具调用次数的 Middleware，超限时按配置阻止新的工具执行。', '限制过低误伤任务、线程计数未持久化或超限后仍有并发调用', '按工具风险分层设限；持久化原子计数，并在执行前再次检查预算。', ['modelCallLimitMiddleware', 'humanInTheLoopMiddleware', 'tool']),
  todoListMiddleware: meta('advanced', 'server', [p('options', 'TodoListMiddlewareOptions', false, '控制待办工具名称、描述与状态写入方式的配置。')], '返回为 Agent 注入任务清单管理能力的 Middleware，帮助复杂任务显式规划和更新进度。', '模型把计划当完成事实、待办无限增长或状态与真实副作用不一致', '待办只作协调信号；限制数量并以真实工具结果和持久化记录确认完成状态。', ['createMiddleware', 'toolCallLimitMiddleware', 'ReactAgent']),
  modelCallLimitMiddleware: meta('core', 'server', [p('options.runLimit', 'number', false, '单次 Agent 运行允许的模型调用总数。'), p('options.threadLimit', 'number', false, '同一线程跨运行累积的模型调用上限。'), p('options.exitBehavior', 'error | end', false, '达到预算后选择抛错或正常结束智能体运行的终止方式。')], '返回统计并限制模型调用次数的 Middleware，避免 Agent 循环造成无限 token 和费用。', '计数并发竞争、线程预算重启丢失或限制未覆盖子调用', '使用持久化原子计数；所有模型入口共享预算，并同时设置时长和 token 上限。', ['toolCallLimitMiddleware', 'modelRetryMiddleware', 'createAgent']),
  modelFallbackMiddleware: meta('advanced', 'server', [p('models', 'LanguageModelLike[]', true, '主模型失败后按顺序尝试且能力兼容的备用模型。')], '返回模型调用失败时顺序降级的 Middleware，并保持 Agent 状态与调用协议不变。', '备用模型不支持工具/结构化输出或降级质量未被记录', '启动时检查能力兼容；只对可降级错误触发并记录实际模型、原因和质量等级。', ['Runnable.withFallbacks', 'modelRetryMiddleware', 'BaseChatModel']),
  modelRetryMiddleware: meta('advanced', 'server', [p('options.maxRetries', 'number', false, '模型瞬时失败后允许的额外尝试次数。'), p('options.backoffFactor', 'number', false, '控制后续等待时间增长速度的退避因子。')], '返回围绕模型调用执行分类重试与退避的 Middleware，成功后继续原 Agent 流程。', '重试内容过滤错误、与 SDK 重试叠加或超过请求截止时间', '仅重试限流和网络瞬断；统一尝试预算并遵守 Retry-After 与总 deadline。', ['Runnable.withRetry', 'modelFallbackMiddleware', 'BaseChatModel.invoke']),
  toolRetryMiddleware: meta('advanced', 'server', [p('options.maxRetries', 'number', false, '工具执行瞬时失败后允许的重试次数。'), p('options.tools', 'string[] | predicate', false, '明确允许自动重试的幂等工具范围。')], '返回在符合策略的工具调用失败后进行退避重试的 Middleware，并保留原 toolCallId 关联。', '重复支付/发信等非幂等副作用或重试掩盖参数错误', '默认仅允许只读工具；写操作要求幂等键，验证和权限错误直接失败。', ['Runnable.withRetry', 'modelRetryMiddleware', 'ToolRuntime']),
  toolEmulatorMiddleware: meta('advanced', 'server', [p('options.model', 'LanguageModelLike', false, '在模型不支持原生 tool calling 时模拟工具调用格式的模型配置。')], '返回用提示和解析模拟 tool calling 协议的 Middleware，供缺少原生能力的模型参与 Agent。', '模拟格式不稳定、错误工具名或参数无法通过 schema', '只用于低风险兼容场景；严格验证调用并限制重试，高风险任务使用原生工具模型。', ['BaseChatModel.bindTools', 'toolStrategy', 'StructuredTool']),
  providerToolSearchMiddleware: meta('advanced', 'server', [p('config.searchableTools', 'Tool[]', false, '要标记为 defer_loading、交给 provider 按需发现的工具；工具也可在 extras 中自行标记。')], '返回 provider-side tool search Middleware；当前要求 OpenAI gpt-5.4+ 或 Anthropic Claude Sonnet 4+/Opus 4+/Haiku 4.5+。', '使用非 OpenAI/Anthropic provider、模型版本太旧、搜索漏召回或未授权工具进入候选集', '启动时检查 provider 与模型代际并提供本地选择回退；先授权过滤，再评测召回率、延迟和成本。', ['llmToolSelectorMiddleware', 'BaseChatModel.bindTools', 'tool']),

  Document: meta('core', 'both', [p('fields.pageContent', 'string', true, '用于切分、embedding 和检索的文档正文内容。'), p('fields.metadata', 'TMetadata', false, '保存来源、版本、租户、权限和引用定位的结构化字段。'), p('fields.id', 'string', false, '支持增量 upsert、删除和来源追踪的稳定文档标识。')], '创建统一 Document 对象，供 Loader、Splitter、Retriever 与 VectorStore 跨组件传递内容和来源。', '正文为空、metadata 泄露敏感字段或 id 不稳定造成重复索引', '入库前校验内容；metadata 白名单并用来源版本和内容哈希生成稳定 id。', ['BaseDocumentLoader', 'TextSplitter.splitDocuments', 'VectorStore.addDocuments']),
  Embeddings: meta('core', 'server', [p('model', 'provider-specific', true, '决定向量维度、语义空间和成本的 embedding 模型。'), p('batchSize', 'number', false, '文档编码时符合供应商限制的批次大小。'), p('maxConcurrency', 'number', false, '保护限流和连接池的并发请求上限。')], '抽象类定义 embedDocuments 与 embedQuery，将文本编码为可比较的数值向量。', '直接实例化抽象类、查询和入库模型不一致或模型升级维度变化', '使用具体 provider；记录模型/维度版本，升级时新建索引并批量重嵌入。', ['Embeddings.embedDocuments', 'Embeddings.embedQuery', 'VectorStore']),
  'Embeddings.embedDocuments': meta('core', 'server', [p('documents', 'string[]', true, '按顺序批量编码并保留稳定文档或 chunk id 的文本。'), p('batch policy', 'provider limit', true, '根据供应商最大条数和字节数进行分批。'), p('retry mapping', 'chunk id map', true, '部分失败重试时维持输入与向量位置对应。')], '返回与 documents 顺序一致的 number[][]，每个内层数组是对应文本的 embedding。', '批次过大、部分失败或重试后向量与文档错位', '自适应分批；保存 chunkId 到位置映射，只重试失败批次并以稳定 id upsert。', ['Embeddings.embedQuery', 'VectorStore.addDocuments', 'TextSplitter.splitDocuments']),
  'Embeddings.embedQuery': meta('core', 'server', [p('document', 'string', true, '用户查询或检索意图的单条文本表示。'), p('model version', 'string', true, '必须与目标索引入库时使用的 embedding 模型一致。'), p('timeout', 'AbortSignal', false, '交互检索链的远程编码截止时间。')], '返回单个 number[] 查询向量，可与同一语义空间中的文档向量计算相似度。', '空查询、限流或向量维度与索引不匹配', '校验和截断查询；设置超时重试，检索前核对索引记录的模型与维度版本。', ['Embeddings.embedDocuments', 'VectorStore.similaritySearch', 'BaseRetriever.invoke']),
  BaseRetriever: meta('core', 'server', [p('input', 'string', true, '代表用户信息需求并驱动文档召回的查询文本。'), p('output', 'Document[]', true, '按检索策略返回且保留来源 metadata 的候选文档。'), p('RunnableConfig', 'RunnableConfig', false, '向检索运行传播 tags、metadata、callbacks 与取消信号。')], '抽象类把查询字符串到 Document 数组的检索能力统一为 Runnable，可组合、批处理和转工具。', '直接实例化抽象类或检索结果未做租户权限过滤', '使用具体 Retriever；在数据源查询阶段强制服务端权限条件并保留引用来源。', ['BaseRetriever.invoke', 'VectorStoreRetriever', 'VectorStore.asRetriever']),
  'BaseRetriever.invoke': meta('core', 'server', [p('input', 'string', true, '要召回相关文档的规范化查询文本。'), p('options.signal', 'AbortSignal', false, '超时或上游取消时终止检索请求。'), p('options.metadata', 'Record<string, unknown>', false, '用于 trace 的脱敏查询版本和租户诊断字段。')], '返回 Promise<Document[]>，结果数量、排序和 metadata 由具体 Retriever 策略决定。', '检索超时、返回空结果或越权文档进入模型上下文', '传播取消；空结果走明确降级，服务端过滤租户并在送模型前再次核对权限标签。', ['BaseRetriever', 'VectorStoreRetriever', 'Runnable.invoke']),
  VectorStore: meta('core', 'server', [p('embeddings', 'Embeddings', true, '负责文档与查询使用同一模型生成向量的编码器。'), p('index', 'provider-specific', true, '维度、距离度量和命名空间明确的向量索引。'), p('tenant policy', 'metadata filter', true, '所有读写都必须携带的服务端租户隔离策略。')], '抽象类统一文档写入、删除、相似度搜索和 Retriever 适配，不同后端由具体子类实现。', '索引维度错误、过滤语义不一致或跨租户读写', '启动时验证索引契约；集成测试适配器过滤，并禁止客户端覆盖租户条件。', ['VectorStore.addDocuments', 'VectorStore.similaritySearch', 'VectorStore.asRetriever']),
  'VectorStore.addDocuments': meta('core', 'server', [p('documents', 'Document[]', true, '已清洗、切分并携带来源和权限 metadata 的文档块。'), p('options.ids', 'string[]', false, '与文档按位置对应并支持幂等 upsert 的稳定 id。'), p('embedding version', 'string', true, '与当前索引维度和模型一致的编码版本。')], '返回后端生成的 id 数组或 void；成功后文档可通过向量与 metadata 条件检索。', '部分写入、重复索引、向量生成失败或 metadata 超限', '使用稳定 id 和批次账本；记录逐项结果，只重试失败项并支持按来源版本回滚。', ['Document', 'Embeddings.embedDocuments', 'VectorStore.delete']),
  'VectorStore.delete': meta('advanced', 'server', [p('params', 'Record<string, unknown>', true, '由具体后端定义的 ids、filter 或命名空间删除条件。')], '返回 Promise<void>；完成后目标向量记录被删除或进入后端最终一致的删除流程。', '条件过宽误删、后端异步删除未完成或派生缓存仍存在', '先预览和授权删除范围；审计请求并以 tombstone/状态轮询确认所有派生数据清理。', ['VectorStore.addDocuments', 'VectorStore.similaritySearch', 'Document']),
  'VectorStore.similaritySearch': meta('core', 'server', [p('query', 'string', true, '会通过当前 Embeddings 编码的用户检索问题。'), p('k', 'number', false, '返回最相似候选文档的数量上限。'), p('filter', 'VectorStoreFilterType', false, '服务端生成的租户、权限、时间和分类过滤条件。')], '返回按后端相似度排序的 Document 数组，不包含显式分数但保留每条来源 metadata。', 'k 太小漏召回、太大引入噪声或 filter 被客户端绕过', '按评测集调 k；服务端强制合并权限过滤，并在送入模型前去重和限制上下文。', ['VectorStore.similaritySearchWithScore', 'VectorStore.asRetriever', 'Embeddings.embedQuery']),
  'VectorStore.similaritySearchWithScore': meta('core', 'server', [p('query', 'string', true, '要进行语义匹配并观察分数分布的查询文本。'), p('k', 'number', false, '返回文档与原始分数元组的最大数量。'), p('filter', 'VectorStoreFilterType', false, '查询阶段强制执行的数据与权限过滤。')], '返回 [Document, number][]，每项包含候选文档及该向量后端定义的相似度或距离分数。', '误判分数方向/范围或把跨后端阈值直接复用', '阅读适配器评分定义；用标注集校准阈值并记录后端、距离度量和模型版本。', ['VectorStore.similaritySearch', 'VectorStore.maxMarginalRelevanceSearch', 'VectorStoreRetriever']),
  'VectorStore.maxMarginalRelevanceSearch': meta('advanced', 'server', [p('query', 'string', true, '需要兼顾相关性与结果多样性的检索问题。'), p('options.k', 'number', true, '最终返回的多样化文档数量。'), p('options.fetchK', 'number', true, '先从向量库召回并供 MMR 重排的候选数量。'), p('options.lambda', 'number', false, '平衡查询相关性和候选间差异的系数。')], '后端支持时返回经 MMR 去重和多样化后的 Document 数组；该方法在部分实现上不存在。', '方法未实现、fetchK 不足或 lambda 过度牺牲相关性', '调用前检测能力；用真实问题评测 k/fetchK/lambda，并提供普通相似度搜索回退。', ['VectorStore.similaritySearch', 'VectorStore.asRetriever', 'VectorStoreRetriever']),
  'VectorStore.asRetriever': meta('core', 'server', [p('options.k', 'number', false, 'Retriever 每次默认返回的文档数量。'), p('options.searchType', 'similarity | mmr', false, '选择基础相似度或多样化 MMR 检索。'), p('options.filter', 'VectorStoreFilterType', false, '固定并与请求权限条件安全合并的过滤器。')], '返回输入 string、输出 Document[] 的 VectorStoreRetriever，可继续 invoke、batch、pipe 或 asTool。', '配置 MMR 但后端不支持或固定 filter 覆盖请求权限', '启动时检查能力；权限过滤使用不可覆盖的服务端合并策略并做越权集成测试。', ['VectorStoreRetriever', 'BaseRetriever', 'VectorStore.similaritySearch']),
  VectorStoreRetriever: meta('core', 'server', [p('vectorStore', 'VectorStore', true, '真正执行查询并承载索引契约的向量存储。'), p('k', 'number', true, '每次检索的候选文档数量。'), p('searchType', 'similarity | mmr', true, '当前 Retriever 固定使用的查询策略。')], '类实例以 Runnable 协议把查询字符串转换为 Document 数组，并保存搜索和追踪配置。', '默认 k/策略未经评测或 metadata filter 未正确应用', '用任务数据评测召回率；在构造与调用两层确认租户过滤和后端能力。', ['VectorStore.asRetriever', 'BaseRetriever.invoke', 'VectorStore.similaritySearch']),
  BaseDocumentLoader: meta('reference', 'server', [p('source config', 'provider-specific', true, '具体 Loader 读取文件、网页或外部系统所需的来源配置。')], '抽象类定义 load() 契约，具体加载器把来源数据转换为带 pageContent 和 metadata 的 Document 数组。', '直接实例化抽象类或加载阶段丢失来源版本', '使用具体 Loader；为每批导入保存 source、etag/mtime、解析器版本和错误清单。', ['BaseDocumentLoader.load', 'TextLoader', 'PDFLoader']),
  'BaseDocumentLoader.load': meta('core', 'server', [p('无显式参数', 'never', false, '读取构造 Loader 时已经配置并授权的数据来源。'), p('source version', 'implicit', true, '增量导入和重试去重所需的来源版本标识。'), p('resource limits', 'implicit', true, '网络超时、文件大小和解析内存的安全限制。')], '返回 Promise<Document[]>，文档数量、初始粒度和 metadata 由具体 Loader 实现决定。', '网络/文件读取失败、编码错误、空文档或部分页面解析失败', '按来源分类重试并保存逐项错误；限制资源，空内容不入库且保留导入状态。', ['BaseDocumentLoader', 'TextSplitter.splitDocuments', 'Document']),
  TextLoader: meta('core', 'both', [p('filePathOrBlob', 'string | Blob', true, 'Node 文件路径或浏览器 Blob 形式的纯文本来源。'), p('encoding', 'runtime/default', false, '读取文本时必须与真实文件编码相匹配的解码策略。'), p('size limit', 'number', true, '避免一次把超大文本完全加载进内存的字节上限。')], '创建纯文本 Loader；load 通常返回包含完整文本和 source metadata 的 Document 数组。', '路径不可用、编码乱码、大文件耗尽内存或文本内容不可信', '按运行环境选择路径/Blob；限制大小和编码，加载后交给 Splitter 并隔离提示注入。', ['BaseDocumentLoader.load', 'TextSplitter', 'Document']),
  PDFLoader: meta('core', 'both', [p('filePathOrBlob', 'string | Blob', true, '待解析 PDF 的服务端路径或浏览器 Blob。'), p('options.splitPages', 'boolean', false, '是否按页创建 Document 并保留页码来源。'), p('parser limits', 'PDFLoaderOptions', true, '页数、大小、密码和解析资源的安全边界。')], '创建 PDF Loader；load 返回提取文本和页码等 metadata 的 Document 数组。', '扫描件无文本、加密文件、布局顺序错误或解析资源耗尽', '检测文本覆盖率并对扫描件走 OCR；限制页数/大小，抽样验证表格和阅读顺序。', ['BaseDocumentLoader.load', 'TextSplitter.splitDocuments', 'Document']),
  CheerioWebBaseLoader: meta('core', 'server', [p('webPath', 'string', true, '服务端请求并提取正文的目标网页 URL。'), p('options.selector', 'string', false, '只保留主内容并排除导航脚本的 CSS 选择器。'), p('network policy', 'allowlist + timeout', true, '防止 SSRF、重定向绕过和无限等待的访问策略。')], '创建静态 HTML Loader；load 返回提取正文并携带 source URL metadata 的 Document。', 'SSRF、动态页面无正文、请求限流或导航噪声进入索引', 'URL allowlist 与 DNS/IP 校验；设置超时/限速，选择 main 区域并清洗重复模板文本。', ['BaseDocumentLoader.load', 'Document', 'TextSplitter.splitDocuments']),
  TextSplitter: meta('core', 'server', [p('chunkSize', 'number', true, '按长度函数衡量的目标文档块大小。'), p('chunkOverlap', 'number', true, '相邻块为保持边界上下文而重复的长度。'), p('lengthFunction', '(text) => number', false, '决定大小单位是字符、token 或自定义成本的函数。')], '抽象类定义 splitText、createDocuments 和 splitDocuments，把长内容转换为可检索的小块。', '直接实例化抽象类或默认参数不适合目标语言和模型', '选具体 Splitter；用真实问题评测块大小、重叠、长度分布和召回效果。', ['TextSplitter.splitDocuments', 'RecursiveCharacterTextSplitter', 'TokenTextSplitter']),
  'TextSplitter.splitDocuments': meta('core', 'server', [p('documents', 'Document[]', true, '需要切分且已经带来源、权限 metadata 的原始文档。'), p('chunk identity', 'application policy', true, '为每块生成支持重建与删除的稳定来源位置标识。'), p('metadata propagation', 'Record<string, unknown>', true, '每个块必须继承的来源、租户和版本字段。')], '返回切分后的 Document 数组，每块具有较小 pageContent 并继承原文 metadata。', '切块后来源丢失、稳定 id 缺失或重复重建产生重复记录', '写入 source/chunkIndex/configVersion；以稳定 id upsert，并在重建前清理旧版本。', ['TextSplitter', 'VectorStore.addDocuments', 'Document']),
  'TextSplitter.splitText': meta('core', 'server', [p('text', 'string', true, '需要按当前 Splitter 规则切分的单段长文本。'), p('chunkSize', 'configured number', true, '当前实例配置并用于控制结果长度的块上限。'), p('chunkOverlap', 'configured number', true, '当前实例配置的相邻块重叠长度。')], '返回保持原文顺序的 string[] 文本块，不创建 Document 也不会保留来源 metadata。', '无意丢失来源信息、单块超限或字符长度误当 token', '生产入库优先 splitDocuments；检查长度分布，并用目标 tokenizer 复核上下文预算。', ['TextSplitter.splitDocuments', 'TextSplitter.createDocuments', 'TokenTextSplitter']),
  'TextSplitter.createDocuments': meta('core', 'server', [p('texts', 'string[]', true, '按顺序切分并转换为 Document 的多段原始文本。'), p('metadatas', 'Record<string, unknown>[]', false, '必须与 texts 按索引一一对应的来源 metadata。'), p('identity policy', 'application policy', true, '为生成块建立来源、序号和配置版本标识。')], '返回由多段文本切分得到的 Document 数组，每个块继承对应原文的 metadata。', 'texts 与 metadatas 错位或巨大 metadata 被复制到每个块', '验证数组长度与来源 id；metadata 只保留必要字段，大对象改存外部引用。', ['TextSplitter.splitDocuments', 'Document', 'VectorStore.addDocuments']),
  RecursiveCharacterTextSplitter: meta('core', 'server', [p('fields.chunkSize', 'number', true, '递归合并后期望满足的最大块长度。'), p('fields.chunkOverlap', 'number', true, '自然边界附近保留上下文的重叠长度。'), p('fields.separators', 'string[]', false, '从强到弱尝试的段落、换行、空格和字符边界。')], '创建通用递归字符切分器，尽量沿自然分隔边界生成满足大小约束的文本块。', '中文/代码分隔符不适配、超长无分隔内容或重叠过大', '按语料定制 separators；检查最大块与重复率，并用检索评测选择参数。', ['TextSplitter', 'RecursiveCharacterTextSplitter.fromLanguage', 'MarkdownTextSplitter']),
  'RecursiveCharacterTextSplitter.fromLanguage': meta('advanced', 'server', [p('language', 'SupportedTextSplitterLanguage', true, '用于选择代码或标记语言启发式分隔符的语言标识。'), p('options.chunkSize', 'number', false, '语言分隔边界下仍需遵守的块大小上限。')], '返回预配置语言 separators 的 RecursiveCharacterTextSplitter，优先在函数、类或标题边界切分。', '语言标识错误或启发式边界仍切断复杂语法结构', '根据文件扩展名和内容检测语言；代码检索保留符号 metadata，必要时改用 AST 切分。', ['RecursiveCharacterTextSplitter', 'TextSplitter.splitDocuments', 'CharacterTextSplitter']),
  CharacterTextSplitter: meta('advanced', 'server', [p('fields.separator', 'string', false, '结构稳定文本中用于切开段落或记录的单一分隔符。'), p('fields.chunkSize', 'number', false, '合并分段后的目标最大块长度。')], '创建使用单一分隔符的字符切分器，结果边界比递归策略更直接且可预测。', '输入缺少分隔符导致超大块或 separator 被移除后语义粘连', '先统计段落长度；保留必要分隔符，超长记录使用递归或 token 级后备策略。', ['RecursiveCharacterTextSplitter', 'TextSplitter.splitText', 'TokenTextSplitter']),
  TokenTextSplitter: meta('advanced', 'server', [p('fields.encodingName', 'string', true, '应近似匹配目标模型 tokenizer 的编码名称。'), p('fields.chunkSize', 'number', true, '按 token 数而非字符数计算的块窗口。'), p('fields.chunkOverlap', 'number', false, '相邻 token 窗口之间重复的 token 数量。')], '创建按 tokenizer 计数和切分的 TextSplitter，使结果大小更接近模型真实上下文预算。', 'encoding 不匹配、硬边界破坏 Unicode/代码或块语义不完整', '选择目标模型编码并验证解码完整性；优先结构切分，再用 token 限制处理超长块。', ['TextSplitter', 'RecursiveCharacterTextSplitter', 'BaseChatModel']),
  MarkdownTextSplitter: meta('advanced', 'server', [p('fields.chunkSize', 'number', false, 'Markdown 结构边界下的目标块大小。'), p('fields.chunkOverlap', 'number', false, '章节和代码块边界附近保留的上下文重叠。')], '创建预配置 Markdown 标题、代码块、水平线和段落分隔符的递归切分器。', '标题层级未进入 metadata、长代码块被切断或重复内容过多', '预解析并保存标题路径；代码块保留语言和行号，评测 overlap 带来的重复率。', ['RecursiveCharacterTextSplitter', 'TextSplitter.createDocuments', 'Document']),
} satisfies Record<string, FrameworkApiLearningMeta>
