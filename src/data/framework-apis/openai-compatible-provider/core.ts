import type { FrameworkApiReference } from '../types'
import { openAiJavascriptSdkLearningMeta } from './learning-meta'

const API = 'https://developers.openai.com/api/reference/resources'
const SDK = 'https://github.com/openai/openai-node'
const SDK_README = `${SDK}/tree/v6.47.0#readme`

type OpenAiApiInput = Omit<FrameworkApiReference, 'slug' | 'technologySlug' | 'maturity' | 'exampleLanguage'> & {
  slug: string
  maturity?: FrameworkApiReference['maturity']
}

function openAiApi(input: OpenAiApiInput): FrameworkApiReference {
  const learningMeta = openAiJavascriptSdkLearningMeta[input.name as keyof typeof openAiJavascriptSdkLearningMeta]
  if (!learningMeta) throw new Error(`Missing structured OpenAI JavaScript SDK learning metadata for ${input.name}`)
  return {
    ...input,
    ...learningMeta,
    slug: `openai-js-${input.slug}`,
    technologySlug: 'openai-javascript-sdk',
    maturity: input.maturity ?? 'stable',
    exampleLanguage: 'ts',
  }
}

/**
 * Learning denominator: 77 public, application-facing APIs from the official
 * OpenAI JavaScript SDK. It covers the client and documented transport helpers,
 * Responses, Chat Completions, Embeddings, Images, Audio, Files/Uploads, Models,
 * Moderations, Batches, and Vector Stores. Generated resource internals, Azure-
 * only APIs, exhaustive Realtime WebSocket events, deprecated Assistants, fine-
 * tuning, videos, webhooks, and administration endpoints are intentionally out
 * of scope so this chapter stays focused on the provider surface used by the
 * audited AI applications.
 */
export const openAiJavascriptSdkApis: FrameworkApiReference[] = [
  openAiApi({
    slug: 'client', name: 'new OpenAI', group: '客户端与传输', kind: 'class', signature: 'new OpenAI({ apiKey?, baseURL?, timeout?, maxRetries?, fetch?, defaultHeaders? })',
    beginner: 'OpenAI 客户端是所有资源 API 的入口。它读取 API Key，统一拼接地址、认证头、超时和重试，然后暴露 responses、files、vectorStores 等资源对象。',
    whenToUse: '应用启动时创建一个可复用客户端，并通过依赖注入交给业务服务；兼容服务可通过 baseURL 切换。',
    example: `import OpenAI from 'openai'\n\nconst openai = new OpenAI({\n  apiKey: process.env.OPENAI_API_KEY,\n  timeout: 30_000,\n  maxRetries: 2,\n})`,
    returns: '返回有状态配置但可安全复用的 OpenAI 客户端实例；真正的网络请求在调用资源方法时发生。',
    interview: '客户端封装的是 HTTP 传输策略而不是模型会话。生产实践是复用实例、从服务端环境变量注入密钥，并把超时、重试和请求 ID 纳入可观测性。',
    pitfall: '不要在浏览器前端暴露标准 API Key，也不要每个请求都重新创建客户端；兼容 baseURL 只代表协议形状相似，不保证所有参数和事件完全一致。', officialUrl: SDK_README,
  }),
  openAiApi({
    slug: 'with-options', name: 'client.withOptions', group: '客户端与传输', kind: 'function', signature: 'client.withOptions(options: Partial<ClientOptions>): OpenAI',
    beginner: 'withOptions 从现有客户端派生一个配置稍有不同的新客户端，例如某个慢任务使用更长超时，而不修改共享实例。',
    whenToUse: '某一类请求需要独立超时、重试、请求头或 baseURL，但仍想复用其余默认配置时。',
    example: `const longRunning = openai.withOptions({ timeout: 120_000 })\nconst result = await longRunning.responses.create({\n  model: 'gpt-5.4', input: '分析长文',\n})`,
    returns: '返回保留原配置并覆盖指定选项的新 OpenAI 客户端，不会原地修改原客户端。',
    interview: '它体现不可变配置派生：全局策略保持稳定，局部请求策略显式覆盖，能避免并发请求互相污染。',
    pitfall: '不要把 withOptions 当作单次请求参数；资源方法本身也接受 RequestOptions，只有一组请求长期共享配置时才值得派生客户端。', officialUrl: `${SDK}/blob/v6.47.0/src/client.ts`,
  }),
  openAiApi({
    slug: 'api-error', name: 'OpenAI.APIError', group: '客户端与传输', kind: 'class', signature: 'err instanceof OpenAI.APIError',
    beginner: 'APIError 是 SDK 对连接失败和非 2xx 响应的统一错误基类，能读取 status、request_id、headers、code 等诊断信息。',
    whenToUse: '集中记录失败原因，并按 401、429、5xx 或网络错误决定提示、告警和是否重试时。',
    example: `try {\n  await openai.responses.create({ model: 'gpt-5.4', input: 'hi' })\n} catch (err) {\n  if (err instanceof OpenAI.APIError) {\n    console.error(err.status, err.request_id, err.code)\n  }\n}`,
    returns: '错误对象携带 HTTP 状态、响应头、请求 ID 和服务端错误体；具体子类包括 RateLimitError、AuthenticationError 等。',
    interview: '错误处理应区分可重试的瞬时故障与确定性参数错误，并记录 request_id 形成端到端追踪；重试有副作用操作前还要考虑幂等性。',
    pitfall: '不要捕获后只返回“模型失败”；429 可能是速率或额度限制，401 不应重试，超时也可能发生在服务端已接收请求之后。', officialUrl: `${SDK}#handling-errors`,
  }),
  openAiApi({
    slug: 'api-promise-as-response', name: 'APIPromise.asResponse', group: '客户端与传输', kind: 'function', signature: 'request.asResponse(): Promise<Response>',
    beginner: 'asResponse 跳过 SDK 的 JSON 解析，直接拿到 Web 标准 Response，适合读取状态、响应头或自行消费二进制/流。',
    whenToUse: '需要自定义解析、检查限流头，或端点返回文件而不是普通 JSON 时。',
    example: `const raw = await openai.responses\n  .create({ model: 'gpt-5.4', input: 'hi' })\n  .asResponse()\nconsole.log(raw.status, raw.headers.get('x-request-id'))`,
    returns: '返回原始 fetch Response；方法在成功响应头到达后即可返回，不会替你消费响应体。',
    interview: 'SDK 的 APIPromise 把传输和解析分层；asResponse 给应用保留协议级控制权，适合代理、诊断和特殊内容类型。',
    pitfall: '响应体通常只能消费一次；调用 asResponse 后自行读取 body 时，不要又期待同一请求被 SDK 再解析。', officialUrl: `${SDK}#accessing-raw-response-data-eg-headers`,
  }),
  openAiApi({
    slug: 'api-promise-with-response', name: 'APIPromise.withResponse', group: '客户端与传输', kind: 'function', signature: 'request.withResponse(): Promise<{ data, response, request_id }>',
    beginner: 'withResponse 同时给你 SDK 已解析的数据和原始 Response，最常见用途是业务继续用强类型结果，同时记录请求 ID 与响应头。',
    whenToUse: '需要正常解析结果，又要采集 request_id、状态或限流头用于日志和排障时。',
    example: `const { data, response, request_id } = await openai.responses\n  .create({ model: 'gpt-5.4', input: 'hi' })\n  .withResponse()\nconsole.log(data.output_text, response.status, request_id)`,
    returns: '返回 data、Web Response 与 x-request-id 的组合对象；此时响应体已经被解析。',
    interview: '请求 ID 是跨客户端、网关和供应商支持定位问题的关联键，建议进入结构化日志和 trace span。',
    pitfall: 'withResponse 会等待并消费响应体；只想尽早转发原始流时应使用 asResponse。', officialUrl: `${SDK}#request-ids`,
  }),
  openAiApi({
    slug: 'page-async-iterator', name: 'PagePromise async iterator', group: '客户端与传输', kind: 'interface', signature: 'for await (const item of client.resource.list(params))',
    beginner: '所有分页 list 方法都能直接用 for await 遍历；SDK 在当前页耗尽后自动请求下一页，让调用者像遍历单个列表一样处理全量数据。',
    whenToUse: '要顺序扫描所有文件、批任务或向量库对象，并希望 SDK 自动维护 cursor 时。',
    example: `for await (const file of openai.files.list({ limit: 100 })) {\n  console.log(file.id, file.filename)\n}`,
    returns: '异步逐个产出资源项；网络上仍按页请求，不会一次性把所有页都载入内存。',
    interview: '自动分页是惰性拉取和背压：消费速度决定下一页请求时机，适合流式处理大集合。',
    pitfall: '全量遍历可能触发很多请求；在线接口应设置业务上限、取消信号或只读取一页，避免延迟和配额失控。', officialUrl: `${SDK}#auto-pagination`,
  }),
  openAiApi({
    slug: 'page-has-next', name: 'AbstractPage.hasNextPage', group: '客户端与传输', kind: 'function', signature: 'page.hasNextPage(): boolean',
    beginner: 'hasNextPage 检查服务端返回的 cursor 信息，告诉你当前页之后是否还有数据。',
    whenToUse: '需要自己控制分页节奏、把下一页放到后台任务，或向前端返回“还有更多”状态时。',
    example: `const page = await openai.files.list({ limit: 20 })\nif (page.hasNextPage()) console.log('还有下一页')`,
    returns: '返回布尔值；只有当前页存在项目且能计算出下一页请求参数时才为 true。',
    interview: '显式分页允许应用把 cursor 作为状态传递，避免全量扫描；cursor 不应被解释为稳定页码。',
    pitfall: '不要仅凭 data.length === limit 推断下一页，也不要长期缓存 cursor 后假设集合完全不变。', officialUrl: `${SDK}/blob/v6.47.0/src/core/pagination.ts`,
  }),
  openAiApi({
    slug: 'page-get-next', name: 'AbstractPage.getNextPage', group: '客户端与传输', kind: 'function', signature: 'page.getNextPage(): Promise<Page>',
    beginner: 'getNextPage 使用当前页保存的 cursor 和原请求选项，显式获取下一页对象。',
    whenToUse: '分页由按钮、队列或批处理 checkpoint 驱动，不能直接自动遍历全部数据时。',
    example: `let page = await openai.batches.list({ limit: 20 })\nwhile (page.hasNextPage()) {\n  page = await page.getNextPage()\n  console.log(page.data.length)\n}`,
    returns: '返回同类型的下一页实例，包含 data、has_more 以及继续分页所需的信息。',
    interview: '手动分页让应用在页边界做 checkpoint、限速和失败恢复，适合长时间同步任务。',
    pitfall: '在 hasNextPage 为 false 时调用会抛错；每一页仍消耗一次 API 请求，应遵守并发和速率限制。', officialUrl: `${SDK}/blob/v6.47.0/src/core/pagination.ts`,
  }),
  openAiApi({
    slug: 'stream-iterator', name: 'Stream async iterator', group: '客户端与传输', kind: 'interface', signature: 'for await (const event of stream)',
    beginner: '开启 stream 后，SDK 把 SSE 数据帧解析成有类型的事件，通过异步迭代器一条条交给应用。',
    whenToUse: '聊天界面要尽快显示增量文字、工具参数或状态事件，而不是等待完整响应时。',
    example: `const stream = await openai.responses.create({\n  model: 'gpt-5.4', input: '讲个故事', stream: true,\n})\nfor await (const event of stream) {\n  if (event.type === 'response.output_text.delta') process.stdout.write(event.delta)\n}`,
    returns: '异步产出 ResponseStreamEvent 或 ChatCompletionChunk；迭代结束表示服务端流正常结束或被取消。',
    interview: 'HTTP 流式通常使用 SSE：首字节延迟更低但完整性由事件序列决定，应用要处理断流、重复展示、取消和最终持久化。',
    pitfall: '不要把每个 delta 当完整 JSON 或完整回答；业务副作用应等待完成事件，网络中断时也不能假定请求从未执行。', officialUrl: `${SDK}#streaming-responses`,
  }),
  openAiApi({
    slug: 'stream-abort', name: 'Stream.controller.abort', group: '客户端与传输', kind: 'function', signature: 'stream.controller.abort(reason?)',
    beginner: '每个底层 Stream 都带 AbortController；调用 abort 会中止仍在进行的网络读取，适合用户点击“停止生成”。',
    whenToUse: '用户离开页面、请求超时、上游断开连接或已经得到足够内容，需要尽快释放连接时。',
    example: `const stream = await openai.responses.create({ model: 'gpt-5.4', input: '长文', stream: true })\nsetTimeout(() => stream.controller.abort(), 5_000)\nfor await (const event of stream) console.log(event.type)`,
    returns: '没有业务返回值；它向底层 fetch 发送取消信号，后续迭代会结束或抛出用户取消错误。',
    interview: '取消是协作式信号，不等于服务端事务回滚；计费和后台执行是否停止取决于端点和信号到达时机。',
    pitfall: '不要把 abort 当作 Responses background cancel；后者是持久化任务的服务端 API，前者只控制当前客户端连接。', officialUrl: `${SDK}#streaming-responses`,
  }),
  openAiApi({
    slug: 'to-file', name: 'toFile', group: '客户端与传输', kind: 'function', signature: 'toFile(value, name?, options?): Promise<File>',
    beginner: 'toFile 把 Buffer、Uint8Array、Blob 或流转换为 SDK 能上传的 Web File，解决不同运行时文件类型不一致的问题。',
    whenToUse: '没有本地 fs.ReadStream，文件内容来自内存、HTTP 下载或浏览器 Blob 时。',
    example: `import OpenAI, { toFile } from 'openai'\nconst file = await toFile(Buffer.from('{"x":1}\n'), 'batch.jsonl')\nawait openai.files.create({ file, purpose: 'batch' })`,
    returns: '返回带文件名和 MIME 信息的 File；流输入会先被缓冲后再生成文件对象。',
    interview: 'multipart 上传既需要字节也需要元数据；toFile 是跨运行时适配层，不改变文件生命周期和服务端 purpose 校验。',
    pitfall: '大流会被 toFile 缓冲进内存；超大文件优先使用 fs.createReadStream 或 toStreamingFile，并设置正确文件名与类型。', officialUrl: `${SDK}#file-uploads`,
  }),
  openAiApi({
    slug: 'to-streaming-file', name: 'toStreamingFile', group: '客户端与传输', kind: 'function', signature: 'toStreamingFile(stream, name?, options?): Uploadable',
    beginner: 'toStreamingFile 把任意 Web、Node 或云存储流包装成可上传对象，上传时边读边发，不必先把全部内容放入内存。',
    whenToUse: '文件很大或数据来自对象存储流，希望降低内存峰值时。',
    example: `import { toStreamingFile } from 'openai'\nconst uploadable = toStreamingFile(sourceStream, 'data.jsonl', { type: 'application/jsonl' })\nawait openai.files.create({ file: uploadable, purpose: 'batch' })`,
    returns: '返回 SDK multipart 层可识别的流式上传对象；数据会在请求发送过程中被消费。',
    interview: '流式上传用背压控制内存，但牺牲了天然重放能力；自动重试前必须确认源流是否可再次读取。',
    pitfall: '流通常只能消费一次，网络失败时未必能安全自动重试；还要确保服务端或代理支持未知长度/分块传输。', officialUrl: `${SDK}#file-uploads`,
  }),

  openAiApi({
    slug: 'responses-create', name: 'responses.create', group: 'Responses API', kind: 'function', signature: 'openai.responses.create(params, options?): APIPromise<Response | Stream<ResponseStreamEvent>>',
    beginner: 'Responses 是推荐的统一生成入口：同一个请求可接收文本、图片和历史项，调用函数、Web/File Search 等工具，并返回结构化 output items。',
    whenToUse: '新建智能体、工具调用、推理、多模态或需要延续 response 状态的应用时优先使用。',
    example: `const response = await openai.responses.create({\n  model: 'gpt-5.4',\n  input: '用三句话解释向量检索',\n})\nconsole.log(response.output_text)`,
    returns: 'stream=false 时返回 Response，并由 SDK补充便捷的 output_text；stream=true 时返回事件异步流。',
    interview: 'Responses 把消息、工具调用和多模态输出统一为有类型的 item 序列；工具执行仍在应用侧完成，模型只提出调用。',
    pitfall: '不要只读取 output[0] 假定它一定是文本；输出可能包含多个消息、工具调用或推理项，生产代码应按 type 分派。', officialUrl: `${API}/responses/methods/create`,
  }),
  openAiApi({
    slug: 'responses-retrieve', name: 'responses.retrieve', group: 'Responses API', kind: 'function', signature: 'openai.responses.retrieve(responseId, query?, options?): APIPromise<Response | Stream<ResponseStreamEvent>>',
    beginner: 'retrieve 按 response ID 读取之前存储或后台执行的响应，也能用 stream 查询参数继续接收事件。',
    whenToUse: '后台响应尚未完成需要轮询，或稍后恢复/审计一个已保存响应时。',
    example: `const response = await openai.responses.retrieve('resp_123')\nconsole.log(response.status, response.output_text)`,
    returns: '返回当前 Response 状态与已产生的 output；请求 stream=true 时返回 ResponseStreamEvent 流。',
    interview: 'background 模式把请求生命周期从单个 HTTP 连接解耦，response_id 成为恢复、查询和取消的稳定句柄。',
    pitfall: 'retrieve 不是重放生成；不要高频无退避轮询，也不要把 response_id 暴露给无权限用户。', officialUrl: `${API}/responses/methods/retrieve`,
  }),
  openAiApi({
    slug: 'responses-delete', name: 'responses.delete', group: 'Responses API', kind: 'function', signature: 'openai.responses.delete(responseId, options?): APIPromise<void>',
    beginner: 'delete 删除一个已存储的 Response 资源，用于数据生命周期和隐私清理。',
    whenToUse: '业务保留期到期、用户发起删除请求，或不再需要服务端保存的响应时。',
    example: `await openai.responses.delete('resp_123')`,
    returns: '成功时 Promise 完成且没有业务响应体；不存在或无权限时抛出 APIError。',
    interview: '删除是资源生命周期操作，不等于取消正在运行的后台生成，也不替代应用数据库、日志和缓存的同步清理。',
    pitfall: '删除通常不可逆；先确认 ID 所属租户，并把应用侧引用与衍生数据纳入同一删除流程。', officialUrl: `${API}/responses/methods/delete`,
  }),
  openAiApi({
    slug: 'responses-cancel', name: 'responses.cancel', group: 'Responses API', kind: 'function', signature: 'openai.responses.cancel(responseId, options?): APIPromise<Response>',
    beginner: 'cancel 请求服务端停止一个使用 background:true 创建且仍在执行的 Response。',
    whenToUse: '长任务用户撤销、预算保护或业务超时后，不希望后台继续消耗资源时。',
    example: `const cancelled = await openai.responses.cancel('resp_123')\nconsole.log(cancelled.status)`,
    returns: '返回取消后的 Response 状态快照；只有后台响应能使用该操作。',
    interview: '服务端 cancel 与客户端断开不同：它以持久化任务 ID 发出状态转换，但仍是竞争操作，任务可能已在取消到达前完成。',
    pitfall: '不要对普通同步响应调用；取消结果要按最终 status 判断，不能仅因 HTTP 成功就假设没有任何输出或费用。', officialUrl: `${API}/responses/methods/cancel`,
  }),
  openAiApi({
    slug: 'responses-compact', name: 'responses.compact', group: 'Responses API', kind: 'function', signature: 'openai.responses.compact(params, options?): APIPromise<CompactedResponse>',
    beginner: 'compact 把过长的会话输入压缩成可继续传给模型的加密/不透明 compaction item，帮助管理上下文窗口。',
    whenToUse: '长会话持续累积 input items，接近上下文限制且希望保留关键状态时。',
    example: `const compacted = await openai.responses.compact({\n  model: 'gpt-5.4', input: previousItems,\n})\nconsole.log(compacted.output)`,
    returns: '返回 CompactedResponse，其中 output 可作为后续 Responses 输入的一部分。',
    interview: '压缩是有损的上下文管理策略：降低 token 占用但不能保证保留所有细节，关键业务状态应外置并结构化存储。',
    pitfall: '不要把 compaction 当长期数据库或可人工阅读摘要；压缩前后都要测试工具状态和关键约束是否仍能正确延续。', officialUrl: `${API}/responses/methods/compact`,
  }),
  openAiApi({
    slug: 'responses-parse', name: 'responses.parse', group: 'Responses API', kind: 'function', signature: 'openai.responses.parse(paramsWithSchema, options?): APIPromise<ParsedResponse<T>>',
    beginner: 'parse 是 SDK 的结构化输出 helper：把 schema 交给模型请求，并把符合 schema 的结果放到 output_parsed。',
    whenToUse: '希望模型返回可直接进入程序的对象，而不是自己从自然语言或 JSON 字符串中猜测结构时。',
    example: `import { zodTextFormat } from 'openai/helpers/zod'\nconst result = await openai.responses.parse({\n  model: 'gpt-5.4', input: 'Alice 今年 30 岁',\n  text: { format: zodTextFormat(PersonSchema, 'person') },\n})\nconsole.log(result.output_parsed)`,
    returns: '返回 ParsedResponse<T>；解析成功时 output_parsed 是类型化对象，拒答或无可解析输出时可能为 null。',
    interview: '结构化输出把格式约束推进解码阶段，比“提示模型只输出 JSON”可靠；但 schema 校验不等于业务语义正确。',
    pitfall: '必须处理 refusal、incomplete 和 output_parsed=null；不要用 schema 把任意模型事实自动视为可信业务数据。', officialUrl: `${SDK}/blob/v6.47.0/helpers.md#structured-outputs-parsing-helpers`,
  }),
  openAiApi({
    slug: 'responses-stream', name: 'responses.stream', group: 'Responses API', kind: 'function', signature: 'openai.responses.stream(params, options?): ResponseStream<T>',
    beginner: 'stream 是比 create({stream:true}) 更高层的 helper：既发出细粒度事件，也累积最终 Response，方便监听文本和拿完整结果。',
    whenToUse: '界面需要增量展示，同时在结束时还要得到完整、解析后的响应对象时。',
    example: `const stream = openai.responses.stream({ model: 'gpt-5.4', input: '解释 SSE' })\nstream.on('response.output_text.delta', (e) => process.stdout.write(e.delta))\nconst final = await stream.finalResponse()`,
    returns: '立即返回 ResponseStream 事件运行器；可监听事件、异步迭代，并通过 finalResponse 等 helper 获取累积结果。',
    interview: '高层 stream runner 在原始 SSE 上维护状态机和快照，易用但会额外累积内存；只需逐事件转发时 create 流更轻量。',
    pitfall: '不要同时用多种消费方式重复处理同一事件；取消或异常时 finalResponse 会拒绝，必须清理 UI 的“生成中”状态。', officialUrl: `${SDK}/blob/v6.47.0/helpers.md#responses-streaming`,
  }),
  openAiApi({
    slug: 'responses-input-items-list', name: 'responses.inputItems.list', group: 'Responses API', kind: 'function', signature: 'openai.responses.inputItems.list(responseId, query?, options?): PagePromise<ResponseItemsPage, ResponseItem>',
    beginner: 'inputItems.list 查看某个 Response 实际使用的输入项，包括消息和工具相关 item，并支持 cursor 分页。',
    whenToUse: '审计会话上下文、调试 previous_response_id 链路，或恢复服务端保存的输入历史时。',
    example: `for await (const item of openai.responses.inputItems.list('resp_123')) {\n  console.log(item.type)\n}`,
    returns: '返回可自动分页的 ResponseItem 集合，项目顺序可由 order 参数控制。',
    interview: 'Responses 的会话不是简单字符串数组，而是类型化 item 图；查看输入项有助于解释工具结果如何进入下一轮推理。',
    pitfall: '输入项可能包含敏感内容，日志和调试页面要做租户鉴权、脱敏与访问审计。', officialUrl: `${API}/responses/subresources/input_items/methods/list`,
  }),
  openAiApi({
    slug: 'responses-input-tokens-count', name: 'responses.inputTokens.count', group: 'Responses API', kind: 'function', signature: 'openai.responses.inputTokens.count(params, options?): APIPromise<InputTokenCountResponse>',
    beginner: 'count 在真正生成前，按 Responses 的完整输入结构估算/计算输入 token 数，包括文本、图片和工具定义等。',
    whenToUse: '提交前做上下文窗口保护、预算预估，或决定是否裁剪/压缩历史时。',
    example: `const counted = await openai.responses.inputTokens.count({\n  model: 'gpt-5.4', input: longConversation,\n})\nconsole.log(counted.input_tokens)`,
    returns: '返回包含 input_tokens 的计数结果，不会创建模型输出。',
    interview: 'token 预算应基于真实序列化输入而非字符数；工具 schema、多模态和系统指令都会占上下文。',
    pitfall: '计数也需要网络调用，并且不能等价预测最终总费用；输出 token、工具后续轮次和缓存计费仍需另算。', officialUrl: `${API}/responses/subresources/input_tokens/methods/count`,
  }),

  openAiApi({
    slug: 'chat-create', name: 'chat.completions.create', group: 'Chat Completions', kind: 'function', signature: 'openai.chat.completions.create(params, options?): APIPromise<ChatCompletion | Stream<ChatCompletionChunk>>',
    beginner: 'Chat Completions 接收 messages 数组并生成 assistant 消息，是大量兼容提供商支持的经典接口；新 OpenAI 项目通常优先 Responses。',
    whenToUse: '维护现有 messages 架构、接入只兼容 /chat/completions 的供应商，或使用特定 Chat 参数时。',
    example: `const completion = await openai.chat.completions.create({\n  model: 'gpt-5.4',\n  messages: [{ role: 'user', content: '你好' }],\n})\nconsole.log(completion.choices[0]?.message.content)`,
    returns: '非流式返回 ChatCompletion 与 choices；stream:true 返回 ChatCompletionChunk 异步流。',
    interview: 'Chat 协议以 role 消息和 choices 为中心；兼容层最常见，但不同供应商对 tool_calls、reasoning、stream usage 的实现可能不同。',
    pitfall: '不要盲目访问 choices[0]；要处理空 choices、finish_reason、tool_calls 和 content=null，也不要把用户内容放进 developer 消息。', officialUrl: `${API}/chat/subresources/completions/methods/create`,
  }),
  openAiApi({
    slug: 'chat-retrieve', name: 'chat.completions.retrieve', group: 'Chat Completions', kind: 'function', signature: 'openai.chat.completions.retrieve(completionId, options?): APIPromise<ChatCompletion>',
    beginner: 'retrieve 读取一个创建时 store:true 的 Chat Completion；普通未存储完成无法稍后读取。',
    whenToUse: '需要服务端保存、审计或稍后展示特定 Chat Completion 时。',
    example: `const saved = await openai.chat.completions.retrieve('chatcmpl_123')\nconsole.log(saved.metadata)`,
    returns: '返回完整的已存储 ChatCompletion，包括 choices、usage 和 metadata。',
    interview: 'store 将一次性生成变成有生命周期的资源，但应用仍要设计租户关联、数据保留和删除策略。',
    pitfall: '只有 store:true 的对象可检索；不要把 completion ID 当作应用鉴权令牌。', officialUrl: `${API}/chat/subresources/completions/methods/retrieve`,
  }),
  openAiApi({
    slug: 'chat-update', name: 'chat.completions.update', group: 'Chat Completions', kind: 'function', signature: 'openai.chat.completions.update(completionId, { metadata }, options?): APIPromise<ChatCompletion>',
    beginner: 'update 只修改已存储 Chat Completion 的 metadata，不会改写模型生成内容。',
    whenToUse: '给存储的完成结果增加业务标签、评测状态或关联 ID 时。',
    example: `await openai.chat.completions.update('chatcmpl_123', {\n  metadata: { tenant: 'acme', reviewed: 'true' },\n})`,
    returns: '返回 metadata 已更新的 ChatCompletion 资源。',
    interview: '把可变 metadata 与不可变生成内容分开，有利于审计；更新接口不意味着能重新解释或编辑模型历史。',
    pitfall: '仅支持 metadata；并发更新应避免互相覆盖，敏感数据也不应随意塞进可检索标签。', officialUrl: `${API}/chat/subresources/completions/methods/update`,
  }),
  openAiApi({
    slug: 'chat-list', name: 'chat.completions.list', group: 'Chat Completions', kind: 'function', signature: 'openai.chat.completions.list(query?, options?): PagePromise<ChatCompletion>',
    beginner: 'list 分页列出 store:true 的 Chat Completions，可按模型、时间等条件筛选。',
    whenToUse: '后台管理、离线评测或数据生命周期任务需要扫描已存储完成时。',
    example: `for await (const item of openai.chat.completions.list({ limit: 50 })) {\n  console.log(item.id, item.model)\n}`,
    returns: '返回 CursorPage，可读取当前页 data 或通过异步迭代自动翻页。',
    interview: '列举接口适合控制面和离线任务，不应替代应用自己的查询数据库；分页与速率限制决定吞吐。',
    pitfall: '只会列出已存储对象；全量循环前要限定租户/时间范围并设计断点续跑。', officialUrl: `${API}/chat/subresources/completions/methods/list`,
  }),
  openAiApi({
    slug: 'chat-delete', name: 'chat.completions.delete', group: 'Chat Completions', kind: 'function', signature: 'openai.chat.completions.delete(completionId, options?): APIPromise<ChatCompletionDeleted>',
    beginner: 'delete 删除一个 store:true 的 Chat Completion，并返回删除确认对象。',
    whenToUse: '用户数据删除、保留期到期或测试数据清理时。',
    example: `const result = await openai.chat.completions.delete('chatcmpl_123')\nconsole.log(result.deleted)`,
    returns: '返回含 id、object 和 deleted 标志的确认对象。',
    interview: '远端删除只覆盖供应商保存的资源；完整合规流程还要处理应用缓存、数据库、副本和日志。',
    pitfall: '不可逆操作前校验租户归属；删除完成结果不会自动删除你已复制到自己系统的内容。', officialUrl: `${API}/chat/subresources/completions/methods/delete`,
  }),
  openAiApi({
    slug: 'chat-parse', name: 'chat.completions.parse', group: 'Chat Completions', kind: 'function', signature: 'openai.chat.completions.parse(paramsWithSchema, options?): APIPromise<ParsedChatCompletion<T>>',
    beginner: 'parse 根据结构化输出 schema 请求 Chat Completion，并把解析结果放入 message.parsed。',
    whenToUse: '仍使用 Chat Completions，但希望安全获得强类型 JSON 或自动解析工具参数时。',
    example: `const completion = await openai.chat.completions.parse({\n  model: 'gpt-5.4', messages,\n  response_format: zodResponseFormat(ResultSchema, 'result'),\n})\nconsole.log(completion.choices[0]?.message.parsed)`,
    returns: '返回 ParsedChatCompletion<T>，每个 choice 的 message 可带 parsed 数据与已解析工具参数。',
    interview: 'parse 是 SDK helper，不是额外 HTTP endpoint；可靠性来自服务端结构化输出约束与客户端类型解析的组合。',
    pitfall: '必须处理拒答、截断和 parsed 为空；schema 合法不代表内容满足业务规则，仍需域校验。', officialUrl: `${SDK}/blob/v6.47.0/helpers.md#structured-outputs-parsing-helpers`,
  }),
  openAiApi({
    slug: 'chat-run-tools', name: 'chat.completions.runTools', group: 'Chat Completions', kind: 'function', signature: 'openai.chat.completions.runTools(paramsWithRunnableTools, options?): ChatCompletionRunner',
    beginner: 'runTools 自动循环“请求模型→执行本地函数→把结果发回模型”，省去手写多轮 tool_calls 调度。',
    whenToUse: '工具数量不多、执行逻辑在同一进程，想快速搭建 Chat Completions 工具代理时。',
    example: `const runner = openai.chat.completions.runTools({\n  model: 'gpt-5.4', messages,\n  tools: [{ type: 'function', function: { name: 'weather', parse: JSON.parse, function: getWeather } }],\n})\nconst final = await runner.finalChatCompletion()`,
    returns: '返回 ChatCompletionRunner 或流式 runner，可监听事件并等待最终 completion。',
    interview: '工具调用是应用控制循环：模型只选择工具和参数，应用负责鉴权、校验、执行、幂等和把结果回填。',
    pitfall: '不要让模型参数直接触发高风险副作用；必须做 schema 校验、权限检查、最大轮数、超时和幂等键。', officialUrl: `${SDK}/blob/v6.47.0/helpers.md#automated-function-calls`,
  }),
  openAiApi({
    slug: 'chat-stream', name: 'chat.completions.stream', group: 'Chat Completions', kind: 'function', signature: 'openai.chat.completions.stream(params, options?): ChatCompletionStream<T>',
    beginner: 'stream 创建会累积快照的 Chat 流 runner，既能监听 content.delta，又能在结束后获取完整 ChatCompletion。',
    whenToUse: '需要事件监听和最终累积对象；只转发原始 chunk 时 create({stream:true}) 更轻。',
    example: `const stream = openai.chat.completions.stream({ model: 'gpt-5.4', messages })\nstream.on('content.delta', ({ delta }) => process.stdout.write(delta))\nconst final = await stream.finalChatCompletion()`,
    returns: '立即返回 ChatCompletionStream；它支持事件、异步迭代、abort 与 finalChatCompletion。',
    interview: 'runner 把 chunk 合并状态机封装起来，但协议仍可能在任意边界切分文本或工具 JSON，应用不能按单 chunk 解析。',
    pitfall: '累积长输出会占内存；异常和取消时最终 helper 会失败，UI 与持久化逻辑要区分完成、失败、取消。', officialUrl: `${SDK}/blob/v6.47.0/helpers.md#chat-streaming`,
  }),

  openAiApi({
    slug: 'embeddings-create', name: 'embeddings.create', group: 'Embeddings', kind: 'function', signature: 'openai.embeddings.create(params, options?): APIPromise<CreateEmbeddingResponse>',
    beginner: 'embeddings.create 把一个或多个文本转换成固定维度的数字向量；语义相近的文本在向量空间中通常距离更近。',
    whenToUse: '构建 RAG 检索、相似度搜索、聚类、去重或语义推荐，并需要自行保存向量时。',
    example: `const result = await openai.embeddings.create({\n  model: 'text-embedding-3-small',\n  input: ['什么是 RAG？', '解释检索增强生成'],\n})\nconst vectors = result.data.map((item) => item.embedding)`,
    returns: '返回按输入顺序排列的 embedding data、模型名和 token usage；每个 data 项包含 index 与数字数组。',
    interview: 'Embedding 是离线表示而非生成；检索质量取决于切片、同一模型/维度、距离度量、元数据过滤和召回后重排。',
    pitfall: '入库和查询必须使用兼容模型与维度；不要把向量距离直接当事实正确率，也要避免把敏感原文无治理地交给外部服务。', officialUrl: `${API}/embeddings/methods/create`,
  }),

  openAiApi({
    slug: 'images-generate', name: 'images.generate', group: 'Images', kind: 'function', signature: 'openai.images.generate(params, options?): APIPromise<ImagesResponse | Stream<ImageGenStreamEvent>>',
    beginner: 'images.generate 根据文字提示生成新图片；支持非流式结果，也可对支持的模型请求局部预览事件。',
    whenToUse: '从零生成插画、海报素材、产品概念图或多种视觉方案时。',
    example: `const result = await openai.images.generate({\n  model: 'gpt-image-1',\n  prompt: '极简风格的向量数据库示意图',\n  size: '1024x1024',\n})\nconsole.log(result.data?.[0]?.b64_json)`,
    returns: '非流式返回 ImagesResponse，图片可能以 base64 或 URL 提供；stream:true 返回图像生成事件流。',
    interview: '图像生成是高延迟高带宽任务，工程上要异步化、限制尺寸/数量、追踪内容安全并把二进制结果转存到受控对象存储。',
    pitfall: '返回格式依模型而变，不要永久依赖临时 URL；base64 会显著增大 JSON 与内存，应尽快解码并验证 MIME/大小。', officialUrl: `${API}/images/methods/generate`,
  }),
  openAiApi({
    slug: 'images-edit', name: 'images.edit', group: 'Images', kind: 'function', signature: 'openai.images.edit(params, options?): APIPromise<ImagesResponse | Stream<ImageEditStreamEvent>>',
    beginner: 'images.edit 以已有图片和文字指令为基础修改内容；可选 mask 通常指定允许重绘的透明区域。',
    whenToUse: '局部重绘、换背景、删除元素、扩图或保持主体的风格变换时。',
    example: `const edited = await openai.images.edit({\n  model: 'gpt-image-1',\n  image: createReadStream('room.png'),\n  prompt: '把墙面改成浅蓝色，其他元素不变',\n})`,
    returns: '非流式返回编辑后的图片数据；支持的模型也可返回增量编辑事件流。',
    interview: '编辑请求是 multipart 多模态输入，文件预处理、尺寸/格式约束和 mask 坐标语义比单纯 prompt 更容易出错。',
    pitfall: '先验证文件类型、大小和透明通道；用户上传内容要做权限与安全检查，不能假设指令会精确保留未遮罩细节。', officialUrl: `${API}/images/methods/edit`,
  }),
  openAiApi({
    slug: 'images-create-variation', name: 'images.createVariation', group: 'Images', kind: 'function', signature: 'openai.images.createVariation(params, options?): APIPromise<ImagesResponse>',
    beginner: 'createVariation 以输入图片为视觉起点生成相似但不同的变体，适用于支持该端点的图像模型。',
    whenToUse: '已有构图需要快速探索多个风格或细节候选，而不是精确局部编辑时。',
    example: `const variants = await openai.images.createVariation({\n  image: createReadStream('logo.png'),\n  n: 2, size: '1024x1024',\n})`,
    returns: '返回一个 ImagesResponse，data 中包含请求数量的变体图片信息。',
    interview: 'variation 与 edit 的控制粒度不同：前者探索相似分布，后者由 prompt/mask 指定修改目标；模型和格式支持也不同。',
    pitfall: '不要假定所有最新图像模型都支持 variation；应按模型文档检查端点兼容性，并妥善处理输入图片版权。', officialUrl: `${API}/images/methods/create_variation`,
  }),

  openAiApi({
    slug: 'audio-speech-create', name: 'audio.speech.create', group: 'Audio', kind: 'function', signature: 'openai.audio.speech.create(params, options?): APIPromise<Response>',
    beginner: 'speech.create 把文字合成为语音，返回的是二进制 HTTP Response，而不是普通 JSON 对象。',
    whenToUse: '生成旁白、无障碍朗读、语音提示或播客片段时。',
    example: `const audio = await openai.audio.speech.create({\n  model: 'gpt-4o-mini-tts', voice: 'alloy', input: '欢迎学习 AI 工程',\n})\nconst bytes = Buffer.from(await audio.arrayBuffer())`,
    returns: '返回 Web Response，可通过 arrayBuffer、body 或流式管道读取音频字节，格式由 response_format 决定。',
    interview: 'TTS 的输出是媒体流，服务层应正确设置 Content-Type、缓存与范围策略；低延迟播放可边接收边转发而非全部缓冲。',
    pitfall: '不要把 Response 直接 JSON.stringify；注意文本长度、音频格式、声音披露要求，并避免在内存中缓冲超长音频。', officialUrl: `${API}/audio/subresources/speech/methods/create`,
  }),
  openAiApi({
    slug: 'audio-transcriptions-create', name: 'audio.transcriptions.create', group: 'Audio', kind: 'function', signature: 'openai.audio.transcriptions.create(params, options?): APIPromise<Transcription | Stream<TranscriptionStreamEvent> | string>',
    beginner: 'transcriptions.create 把音频中的原语言语音转成文字；根据 response_format 和 stream 参数可返回对象、字幕文本或事件流。',
    whenToUse: '会议记录、语音输入、字幕、呼叫质检或音频内容检索时。',
    example: `const transcript = await openai.audio.transcriptions.create({\n  model: 'gpt-4o-transcribe',\n  file: createReadStream('meeting.mp3'),\n})\nconsole.log(transcript.text)`,
    returns: '返回 Transcription、字符串字幕，或开启 stream 后返回转写事件流；具体类型由参数联合类型决定。',
    interview: 'ASR 质量受采样率、噪声、说话人、语言提示和切片影响；长音频应异步分段并保留时间戳以便追溯。',
    pitfall: '上传前检查格式与大小；不要假设自动语言检测永远正确，也不要丢弃时间戳后再做精确字幕对齐。', officialUrl: `${API}/audio/subresources/transcriptions/methods/create`,
  }),
  openAiApi({
    slug: 'audio-translations-create', name: 'audio.translations.create', group: 'Audio', kind: 'function', signature: 'openai.audio.translations.create(params, options?): APIPromise<Translation | string>',
    beginner: 'translations.create 先识别非英语音频，再直接输出英语文本；它不是任意目标语言的通用翻译接口。',
    whenToUse: '需要把多语言语音统一转成英语文字供后续分析时。',
    example: `const translated = await openai.audio.translations.create({\n  model: 'whisper-1', file: createReadStream('zh.mp3'),\n})\nconsole.log(translated.text)`,
    returns: '默认返回含 text 的 Translation；请求 text/srt/vtt 等格式时可能直接返回字符串。',
    interview: '语音翻译把 ASR 与翻译合为一步，省调用但降低中间原文可见性；审计场景常需同时保留原语转写。',
    pitfall: '目标是英语且模型支持范围有限；不要把它误用为“指定任意 target_language”，专业术语还需人工或词表校验。', officialUrl: `${API}/audio/subresources/translations/methods/create`,
  }),

  openAiApi({
    slug: 'files-create', name: 'files.create', group: 'Files 与 Uploads', kind: 'function', signature: 'openai.files.create({ file, purpose, ... }, options?): APIPromise<FileObject>',
    beginner: 'files.create 通过 multipart 上传完整文件，并用 purpose 告诉平台它将用于 batch、fine-tune、assistants 等哪类流程。',
    whenToUse: '文件大小在单次上传限制内，要供 Batch、向量库或其他支持 File ID 的端点使用时。',
    example: `const file = await openai.files.create({\n  file: createReadStream('requests.jsonl'),\n  purpose: 'batch',\n})\nconsole.log(file.id, file.status)`,
    returns: '返回 FileObject，包含 file id、filename、bytes、purpose、created_at 和处理状态。',
    interview: 'File ID 是服务端资源句柄；上传、处理、引用和删除是独立生命周期，业务库应记录用途、租户、状态和到期时间。',
    pitfall: 'purpose 与后续端点必须匹配；上传成功不等于文件已可用，敏感文件还要限制访问并安排删除。', officialUrl: `${API}/files/methods/create`,
  }),
  openAiApi({
    slug: 'files-retrieve', name: 'files.retrieve', group: 'Files 与 Uploads', kind: 'function', signature: 'openai.files.retrieve(fileId, options?): APIPromise<FileObject>',
    beginner: 'retrieve 按 File ID 读取文件元数据和处理状态，不下载文件正文。',
    whenToUse: '确认上传是否处理完成、显示文件信息或排查失败原因时。',
    example: `const file = await openai.files.retrieve('file_123')\nconsole.log(file.filename, file.status, file.bytes)`,
    returns: '返回 FileObject 元数据；文件内容要另外调用 files.content。',
    interview: '把 metadata 查询与内容下载拆开能减少带宽，也便于在控制面轮询状态；应使用退避而非紧密轮询。',
    pitfall: '不要把 retrieve 结果误当文件内容；读取前要校验租户所有权，状态失败时记录服务端错误信息。', officialUrl: `${API}/files/methods/retrieve`,
  }),
  openAiApi({
    slug: 'files-list', name: 'files.list', group: 'Files 与 Uploads', kind: 'function', signature: 'openai.files.list(query?, options?): PagePromise<FileObjectsPage, FileObject>',
    beginner: 'list 分页列出项目中的 FileObject，可按 purpose、order、after 等条件缩小范围。',
    whenToUse: '文件管理后台、清理过期文件或核对批任务输入时。',
    example: `for await (const file of openai.files.list({ purpose: 'batch', limit: 100 })) {\n  console.log(file.id, file.filename)\n}`,
    returns: '返回可自动分页的 FileObject 集合；await 后也可只处理当前页 data。',
    interview: '远端文件列表是供应商控制面，不适合替代应用自己的租户索引；应用库应保存 File ID 与业务实体映射。',
    pitfall: '项目级列表可能跨多个业务租户；不要直接暴露给终端用户，全量清理前应先 dry-run 和设定时间边界。', officialUrl: `${API}/files/methods/list`,
  }),
  openAiApi({
    slug: 'files-delete', name: 'files.delete', group: 'Files 与 Uploads', kind: 'function', signature: 'openai.files.delete(fileId, options?): APIPromise<FileDeleted>',
    beginner: 'delete 删除平台保存的文件资源，并返回 deleted 确认。',
    whenToUse: '任务结束、保留期到期、上传错误或用户请求清除数据时。',
    example: `const removed = await openai.files.delete('file_123')\nconsole.log(removed.deleted)`,
    returns: '返回包含 id、object 和 deleted 布尔值的 FileDeleted 对象。',
    interview: '文件删除要考虑引用关系：批任务或向量库已引用时，业务应先理解对下游资源的影响并维护清理顺序。',
    pitfall: '删除不可逆且不会清除你自己的副本；确认任务不再依赖该 File ID，并实现租户级授权与审计。', officialUrl: `${API}/files/methods/delete`,
  }),
  openAiApi({
    slug: 'files-content', name: 'files.content', group: 'Files 与 Uploads', kind: 'function', signature: 'openai.files.content(fileId, options?): APIPromise<Response>',
    beginner: 'content 下载文件的实际字节，返回 Web Response，常用于读取 Batch 输出或错误 JSONL。',
    whenToUse: '文件是服务端生成的结果，或确实需要取回已上传内容时。',
    example: `const response = await openai.files.content('file_output_123')\nconst text = await response.text()\nfor (const line of text.trim().split('\n')) console.log(JSON.parse(line))`,
    returns: '返回原始 Response，可用 text、arrayBuffer 或 body 流消费内容。',
    interview: '结果文件可能很大，正确做法是流式解析、逐行处理并 checkpoint，而不是一次性 text() 进内存。',
    pitfall: '示例 text() 只适合小文件；生产中要验证 Content-Type/长度、处理压缩和断点，并防止把敏感输出写入普通日志。', officialUrl: `${API}/files/methods/content`,
  }),
  openAiApi({
    slug: 'files-wait-for-processing', name: 'files.waitForProcessing', group: 'Files 与 Uploads', kind: 'function', signature: 'openai.files.waitForProcessing(fileId, { pollInterval?, maxWait? }): Promise<FileObject>',
    beginner: 'waitForProcessing 是 SDK 轮询 helper：定期 retrieve 文件，直到状态为 processed/error 或超过最大等待时间。',
    whenToUse: '上传后下一个操作必须等平台处理完成，不想手写轮询和终止条件时。',
    example: `const ready = await openai.files.waitForProcessing(file.id, {\n  pollInterval: 2_000, maxWait: 60_000,\n})\nconsole.log(ready.status)`,
    returns: '处理完成时返回最终 FileObject；失败状态或超时会抛出错误。',
    interview: '轮询 helper 把最终一致性封装起来，但同步等待仍占用调用链；长任务更适合队列、状态机或 webhook 式通知。',
    pitfall: '这是 SDK helper，轮询间隔与总等待要受控；Web 请求中长时间等待会占连接，并可能在超时后仍继续后台处理。', officialUrl: `${SDK}/blob/v6.47.0/src/resources/files.ts`,
  }),

  openAiApi({
    slug: 'uploads-create', name: 'uploads.create', group: 'Files 与 Uploads', kind: 'function', signature: 'openai.uploads.create(params, options?): APIPromise<Upload>',
    beginner: 'uploads.create 建立一个分片上传会话，先声明最终文件名、字节数、MIME 和 purpose，再逐片上传。',
    whenToUse: '文件太大或网络环境需要断点/并行分片，单次 files.create 不合适时。',
    example: `const upload = await openai.uploads.create({\n  filename: 'dataset.jsonl', purpose: 'batch',\n  bytes: totalBytes, mime_type: 'application/jsonl',\n})`,
    returns: '返回 Upload 资源及过期时间和状态；随后要用 uploads.parts.create 添加所有分片。',
    interview: '分片上传把大对象拆成可重试单元，但完整性由最终 complete 的 part IDs 和总字节数校验保证。',
    pitfall: 'Upload 有有效期；声明 bytes/MIME 必须与实际一致，创建会话后忘记 complete 不会得到可用 File。', officialUrl: `${API}/uploads/methods/create`,
  }),
  openAiApi({
    slug: 'uploads-parts-create', name: 'uploads.parts.create', group: 'Files 与 Uploads', kind: 'function', signature: 'openai.uploads.parts.create(uploadId, { data }, options?): APIPromise<UploadPart>',
    beginner: 'parts.create 向上传会话加入一个字节分片，每片成功后会得到 part ID，最后按顺序提交这些 ID。',
    whenToUse: '执行大文件的分片、并行或失败重传时。',
    example: `const part = await openai.uploads.parts.create(upload.id, {\n  data: await toFile(chunk, 'part.bin'),\n})\npartIds.push(part.id)`,
    returns: '返回 UploadPart，包含 part id、创建时间和所属 upload id。',
    interview: '分片可独立重试，但重试要避免重复 part 被错误纳入 complete；应用应持久化每片校验值和顺序。',
    pitfall: '每片大小与总 part 数有限制；不要仅依靠客户端数组，长任务应把已成功 part IDs 做 checkpoint。', officialUrl: `${API}/uploads/subresources/parts/methods/create`,
  }),
  openAiApi({
    slug: 'uploads-complete', name: 'uploads.complete', group: 'Files 与 Uploads', kind: 'function', signature: 'openai.uploads.complete(uploadId, { part_ids, md5? }, options?): APIPromise<Upload>',
    beginner: 'complete 按 part_ids 的顺序组装所有分片并结束会话；成功后 Upload 中会关联可供其他 API 使用的 File。',
    whenToUse: '所有分片都上传并验证完成，准备把临时 upload 固化成文件时。',
    example: `const completed = await openai.uploads.complete(upload.id, {\n  part_ids: partIds,\n})\nconsole.log(completed.file?.id)`,
    returns: '返回状态为 completed 的 Upload，通常包含创建好的 FileObject。',
    interview: 'complete 是提交边界，part 顺序决定最终字节序；可选校验和用于检测传输/组装完整性。',
    pitfall: 'part_ids 顺序错误会生成错误文件；complete 失败时先按服务端状态判断是否可重试，避免重复创建上层业务记录。', officialUrl: `${API}/uploads/methods/complete`,
  }),
  openAiApi({
    slug: 'uploads-cancel', name: 'uploads.cancel', group: 'Files 与 Uploads', kind: 'function', signature: 'openai.uploads.cancel(uploadId, options?): APIPromise<Upload>',
    beginner: 'cancel 终止尚未完成的上传会话，之后不能再添加分片。',
    whenToUse: '用户撤销、源文件校验失败或任务异常，不想让临时上传继续占用生命周期时。',
    example: `const cancelled = await openai.uploads.cancel(upload.id)\nconsole.log(cancelled.status)`,
    returns: '返回状态为 cancelled 的 Upload 快照。',
    interview: '取消分片上传是控制面状态转换；应用还应停止本地并发上传 worker 并清理 checkpoint。',
    pitfall: '不能取消已经 completed 的 Upload；取消请求与仍在飞行的 part 请求可能竞争，要以最终服务端状态为准。', officialUrl: `${API}/uploads/methods/cancel`,
  }),

  openAiApi({
    slug: 'models-retrieve', name: 'models.retrieve', group: 'Models 与安全', kind: 'function', signature: 'openai.models.retrieve(modelId, options?): APIPromise<Model>',
    beginner: 'models.retrieve 读取一个模型 ID 的基础元数据，例如所有者和创建时间；它不返回价格、上下文窗口等完整能力表。',
    whenToUse: '启动检查模型是否对当前项目可见，或管理页展示基础模型记录时。',
    example: `const model = await openai.models.retrieve('gpt-5.4')\nconsole.log(model.id, model.owned_by)`,
    returns: '返回 Model 对象，主要包含 id、created、object 和 owned_by。',
    interview: '模型可见不代表所有端点/参数都兼容；能力发现应结合官方模型文档与实际契约测试。',
    pitfall: '不要用该元数据推断上下文、价格或工具支持；模型别名也可能更新，关键场景可固定快照并治理升级。', officialUrl: `${API}/models/methods/retrieve`,
  }),
  openAiApi({
    slug: 'models-list', name: 'models.list', group: 'Models 与安全', kind: 'function', signature: 'openai.models.list(options?): PagePromise<ModelsPage, Model>',
    beginner: 'models.list 列出当前 API Key 可访问的模型基础记录。',
    whenToUse: '运维诊断权限、内部模型选择器或部署前可用性检查时。',
    example: `for await (const model of openai.models.list()) {\n  console.log(model.id)\n}`,
    returns: '返回模型 Page；当前实现可按统一分页接口遍历。',
    interview: '动态列举适合发现，不应直接决定生产路由；路由还需能力、成本、延迟、区域和评测策略。',
    pitfall: '列表可能包含不适合你的端点或已计划弃用的模型；不要自动把任意新模型暴露给终端用户。', officialUrl: `${API}/models/methods/list`,
  }),
  openAiApi({
    slug: 'models-delete', name: 'models.delete', group: 'Models 与安全', kind: 'function', signature: 'openai.models.delete(modelId, options?): APIPromise<ModelDeleted>',
    beginner: 'models.delete 删除你拥有的 fine-tuned 模型；不能删除 OpenAI 提供的基础模型。',
    whenToUse: '自定义模型不再使用、保留期到期或需要停止后续调用时。',
    example: `const deleted = await openai.models.delete('ft:gpt-4.1:org:job')\nconsole.log(deleted.deleted)`,
    returns: '返回含 id 与 deleted 标志的 ModelDeleted 确认对象。',
    interview: '模型删除是资产生命周期控制，删除前要确认线上路由、评测、回滚和历史任务不再依赖该 ID。',
    pitfall: '这是破坏性操作且只适用于可删除的自有模型；不要把“删除模型”与“删除训练文件/数据”混为一谈。', officialUrl: `${API}/models/methods/delete`,
  }),
  openAiApi({
    slug: 'moderations-create', name: 'moderations.create', group: 'Models 与安全', kind: 'function', signature: 'openai.moderations.create(params, options?): APIPromise<ModerationCreateResponse>',
    beginner: 'moderations.create 检查文本或支持的图片是否命中安全类别，并返回 flagged、类别布尔值与分数。',
    whenToUse: '用户输入、公开输出、上传内容或高风险工作流需要安全分级与人工升级时。',
    example: `const moderation = await openai.moderations.create({\n  model: 'omni-moderation-latest', input: userText,\n})\nif (moderation.results[0]?.flagged) throw new Error('内容需审核')`,
    returns: '返回每个输入对应的 moderation result，包含 flagged、categories、category_scores 等字段。',
    interview: 'Moderation 是风险信号而非完整安全系统；阈值、上下文、地区政策、误报漏报和人工复核共同组成防护链。',
    pitfall: '不要只看总 flagged 而丢弃细分类别，也不要把模型分数当概率；批量输入结果必须按 index 与原输入正确对应。', officialUrl: `${API}/moderations/methods/create`,
  }),

  openAiApi({
    slug: 'batches-create', name: 'batches.create', group: 'Batch', kind: 'function', signature: 'openai.batches.create({ input_file_id, endpoint, completion_window, metadata? }, options?): APIPromise<Batch>',
    beginner: 'batches.create 用已上传的 JSONL 文件创建异步批处理任务，平台在时间窗口内离线执行每一行请求。',
    whenToUse: '大量非实时推理、评测、数据标注或 Embedding 任务，希望降低在线并发压力时。',
    example: `const batch = await openai.batches.create({\n  input_file_id: inputFile.id, endpoint: '/v1/responses',\n  completion_window: '24h', metadata: { job: 'eval-2026-07' },\n})`,
    returns: '返回 Batch 状态对象，之后通过 retrieve 查询进度，完成后从 output_file_id 下载逐行结果。',
    interview: 'Batch 是异步控制面：每行 custom_id 提供幂等关联，结果顺序不保证与输入一致，必须按 custom_id 回写。',
    pitfall: 'JSONL 每行必须满足目标端点格式且 custom_id 唯一；创建任务前先验证文件，否则大量错误只能在结果文件中发现。', officialUrl: `${API}/batches/methods/create`,
  }),
  openAiApi({
    slug: 'batches-retrieve', name: 'batches.retrieve', group: 'Batch', kind: 'function', signature: 'openai.batches.retrieve(batchId, options?): APIPromise<Batch>',
    beginner: 'retrieve 读取批任务当前状态、请求计数、错误、输出文件和过期时间。',
    whenToUse: '队列 worker 定期同步任务状态，或管理页展示完成/失败进度时。',
    example: `const batch = await openai.batches.retrieve('batch_123')\nconsole.log(batch.status, batch.request_counts, batch.output_file_id)`,
    returns: '返回最新 Batch 快照；终态可能是 completed、failed、expired 或 cancelled。',
    interview: '异步任务状态机要按终态驱动下游，并把轮询设计成可恢复、指数退避和幂等消费。',
    pitfall: 'completed 不代表每一行成功；必须同时解析 output 与 error 文件，并核对 request_counts。', officialUrl: `${API}/batches/methods/retrieve`,
  }),
  openAiApi({
    slug: 'batches-list', name: 'batches.list', group: 'Batch', kind: 'function', signature: 'openai.batches.list(query?, options?): PagePromise<BatchesPage, Batch>',
    beginner: 'list 按 cursor 分页列出项目中的批任务。',
    whenToUse: '运维后台、失败重试扫描或定期清理过期批任务记录时。',
    example: `for await (const batch of openai.batches.list({ limit: 100 })) {\n  console.log(batch.id, batch.status)\n}`,
    returns: '返回可自动分页的 Batch 集合。',
    interview: '远端 list 适合对账和恢复，不应是业务唯一状态库；本地表应持久化 batch_id、输入文件、状态和消费 checkpoint。',
    pitfall: '全量扫描昂贵且跨租户；使用 metadata/本地索引缩小范围，并防止多个 worker 重复消费同一完成任务。', officialUrl: `${API}/batches/methods/list`,
  }),
  openAiApi({
    slug: 'batches-cancel', name: 'batches.cancel', group: 'Batch', kind: 'function', signature: 'openai.batches.cancel(batchId, options?): APIPromise<Batch>',
    beginner: 'cancel 请求停止仍在运行的批任务；取消过程可能需要时间，已完成的行不会被撤销。',
    whenToUse: '输入数据错误、预算保护或业务不再需要未完成请求时。',
    example: `const batch = await openai.batches.cancel('batch_123')\nconsole.log(batch.status)`,
    returns: '返回进入 cancelling 或已 cancelled 的 Batch 状态快照。',
    interview: '取消是最终一致的状态转换，不是事务回滚；应用要继续观察终态并处理已经产生的部分输出。',
    pitfall: '不要因 cancel 返回成功就忽略 output_file_id；并发完成与取消存在竞争，费用和结果应按最终 request_counts 对账。', officialUrl: `${API}/batches/methods/cancel`,
  }),

  openAiApi({
    slug: 'vector-stores-create', name: 'vectorStores.create', group: 'Vector Stores', kind: 'function', signature: 'openai.vectorStores.create(params, options?): APIPromise<VectorStore>',
    beginner: 'vectorStores.create 建立一个托管检索库；文件加入后平台负责解析、切片和索引，供 file_search 或 search 使用。',
    whenToUse: '希望快速给 Responses 的 File Search 提供知识库，不自行维护向量数据库和切片管线时。',
    example: `const store = await openai.vectorStores.create({\n  name: '产品知识库',\n  expires_after: { anchor: 'last_active_at', days: 30 },\n})`,
    returns: '返回 VectorStore，包含 id、状态、文件计数、容量、metadata 和过期策略。',
    interview: '托管向量库把解析、Embedding、索引和检索封装起来，换来较少控制；仍需治理切片、元数据、更新延迟和租户隔离。',
    pitfall: '创建空库不代表 ready；应设置明确过期策略并按租户隔离，避免多个用户的敏感文件混在同一检索空间。', officialUrl: `${API}/vector_stores/methods/create`,
  }),
  openAiApi({
    slug: 'vector-stores-retrieve', name: 'vectorStores.retrieve', group: 'Vector Stores', kind: 'function', signature: 'openai.vectorStores.retrieve(vectorStoreId, options?): APIPromise<VectorStore>',
    beginner: 'retrieve 读取向量库元数据、总容量和文件处理计数，用于判断索引是否就绪。',
    whenToUse: '上传后检查 completed/failed/in_progress 文件数，或展示知识库状态时。',
    example: `const store = await openai.vectorStores.retrieve('vs_123')\nconsole.log(store.status, store.file_counts)`,
    returns: '返回最新 VectorStore 状态快照，不会返回检索结果或所有文件正文。',
    interview: '索引建立是异步的；库级 file_counts 是健康信号，但逐文件失败还要读取 vectorStores.files 状态。',
    pitfall: '不要只检查 store.status 就忽略 failed 文件；轮询要退避，并把失败原因反馈到上传工作流。', officialUrl: `${API}/vector_stores/methods/retrieve`,
  }),
  openAiApi({
    slug: 'vector-stores-update', name: 'vectorStores.update', group: 'Vector Stores', kind: 'function', signature: 'openai.vectorStores.update(vectorStoreId, params, options?): APIPromise<VectorStore>',
    beginner: 'update 修改向量库的名称、metadata 或过期策略，不会重新写入所有向量。',
    whenToUse: '给知识库增加业务标签、调整自动过期时间或重命名时。',
    example: `await openai.vectorStores.update('vs_123', {\n  metadata: { tenant: 'acme', environment: 'prod' },\n  expires_after: { anchor: 'last_active_at', days: 60 },\n})`,
    returns: '返回更新后的 VectorStore 元数据与当前状态。',
    interview: 'metadata 属于控制面索引，内容级过滤依赖文件 attributes；两层元数据职责不同。',
    pitfall: '更新过期策略可能影响数据生命周期；并发更新 metadata 时避免整对象覆盖，并不要把密钥放进 metadata。', officialUrl: `${API}/vector_stores/methods/update`,
  }),
  openAiApi({
    slug: 'vector-stores-list', name: 'vectorStores.list', group: 'Vector Stores', kind: 'function', signature: 'openai.vectorStores.list(query?, options?): PagePromise<VectorStoresPage, VectorStore>',
    beginner: 'list 按 cursor 分页列出项目下的托管向量库。',
    whenToUse: '知识库管理后台、过期清理或项目资源对账时。',
    example: `for await (const store of openai.vectorStores.list({ limit: 100 })) {\n  console.log(store.id, store.name, store.status)\n}`,
    returns: '返回可自动分页的 VectorStore 集合。',
    interview: '应用自己的数据库应保存 vector_store_id 与租户/权限映射；供应商 list 主要用于控制面恢复和审计。',
    pitfall: '不要向用户直接展示项目全量列表；跨租户资源泄漏风险高，批量删除前必须按本地映射校验。', officialUrl: `${API}/vector_stores/methods/list`,
  }),
  openAiApi({
    slug: 'vector-stores-delete', name: 'vectorStores.delete', group: 'Vector Stores', kind: 'function', signature: 'openai.vectorStores.delete(vectorStoreId, options?): APIPromise<VectorStoreDeleted>',
    beginner: 'delete 删除整个托管向量库和它的索引资源。',
    whenToUse: '知识库废弃、租户注销、测试环境清理或到期策略之外需要立即移除时。',
    example: `const result = await openai.vectorStores.delete('vs_123')\nconsole.log(result.deleted)`,
    returns: '返回含 vector store id 与 deleted 标志的确认对象。',
    interview: '删除向量库与删除原始 File 是不同生命周期；合规删除通常要明确两者是否都要清理。',
    pitfall: '这是破坏性操作，会让引用该库的 file_search 立即失效；先下线路由并确认是否还需单独删除 Files。', officialUrl: `${API}/vector_stores/methods/delete`,
  }),
  openAiApi({
    slug: 'vector-stores-search', name: 'vectorStores.search', group: 'Vector Stores', kind: 'function', signature: 'openai.vectorStores.search(vectorStoreId, params, options?): PagePromise<VectorStoreSearchResponsesPage, VectorStoreSearchResponse>',
    beginner: 'search 直接在向量库中检索与 query 相关的文本块，可设置过滤条件、最大结果数和排序选项。',
    whenToUse: '调试召回质量、自己编排 RAG，或不让模型自动调用 file_search 时。',
    example: `const result = await openai.vectorStores.search('vs_123', {\n  query: '退款期限是多少？', max_num_results: 8,\n  attribute_filter: { type: 'eq', key: 'region', value: 'cn' },\n})\nconsole.log(result.data[0]?.score, result.data[0]?.content)`,
    returns: '返回相关 chunk 的分页搜索响应，包含 score、content、filename、attributes 等信息。',
    interview: '检索阶段要区分召回与生成：过滤先缩小权限域，top-k/阈值控制上下文，最终回答还需引用和无法回答策略。',
    pitfall: '不要在检索后才做租户过滤，否则已发生越权召回；分数跨查询未必可直接比较，应通过评测校准阈值。', officialUrl: `${API}/vector_stores/methods/search`,
  }),

  openAiApi({
    slug: 'vector-store-files-create', name: 'vectorStores.files.create', group: 'Vector Store Files', kind: 'function', signature: 'openai.vectorStores.files.create(vectorStoreId, { file_id, attributes?, chunking_strategy? }, options?): APIPromise<VectorStoreFile>',
    beginner: 'files.create 把一个已上传的 File 关联到向量库，并启动解析、切片和索引。',
    whenToUse: 'File 已经通过 files.create 上传，希望精确控制 attributes 或切片策略时。',
    example: `const attached = await openai.vectorStores.files.create('vs_123', {\n  file_id: 'file_123', attributes: { region: 'cn', year: 2026 },\n})\nconsole.log(attached.status)`,
    returns: '立即返回 VectorStoreFile，通常先处于 in_progress，完成后才能稳定检索。',
    interview: '同一原始 File 与库内索引对象是两种资源；attributes 应在索引时绑定，才能在检索前做权限和业务过滤。',
    pitfall: 'create 成功只代表已受理；必须等待 completed，并为 failed 状态提供重试或提示。', officialUrl: `${API}/vector_stores/subresources/files/methods/create`,
  }),
  openAiApi({
    slug: 'vector-store-files-retrieve', name: 'vectorStores.files.retrieve', group: 'Vector Store Files', kind: 'function', signature: 'openai.vectorStores.files.retrieve(fileId, { vector_store_id }, options?): APIPromise<VectorStoreFile>',
    beginner: 'retrieve 读取某个 File 在特定向量库中的索引状态、attributes 和错误信息。',
    whenToUse: '等待单文件完成、展示处理进度或诊断解析失败时。',
    example: `const item = await openai.vectorStores.files.retrieve('file_123', {\n  vector_store_id: 'vs_123',\n})\nconsole.log(item.status, item.last_error)`,
    returns: '返回 VectorStoreFile，而不是原始 FileObject；同一 file 在不同库中可有不同状态。',
    interview: '复合资源由 vector_store_id + file_id 定位，体现原始对象和索引副本的分离。',
    pitfall: '当前 SDK 的参数顺序容易与直觉混淆；不要漏传 vector_store_id，也不要把索引状态当上传状态。', officialUrl: `${API}/vector_stores/subresources/files/methods/retrieve`,
  }),
  openAiApi({
    slug: 'vector-store-files-update', name: 'vectorStores.files.update', group: 'Vector Store Files', kind: 'function', signature: 'openai.vectorStores.files.update(fileId, { vector_store_id, attributes }, options?): APIPromise<VectorStoreFile>',
    beginner: 'update 修改库内文件的 attributes，便于后续 search 用条件过滤。',
    whenToUse: '文档所属部门、地区、版本或权限标签变化，但不需要重新上传正文时。',
    example: `await openai.vectorStores.files.update('file_123', {\n  vector_store_id: 'vs_123', attributes: { department: 'support', active: true },\n})`,
    returns: '返回 attributes 更新后的 VectorStoreFile。',
    interview: '检索元数据是授权边界的一部分；更新后要测试过滤是否立即一致，并保留业务数据库中的权威 ACL。',
    pitfall: '不要用可被终端用户任意修改的 attributes 作为唯一权限来源；并发更新可能覆盖未包含的标签。', officialUrl: `${API}/vector_stores/subresources/files/methods/update`,
  }),
  openAiApi({
    slug: 'vector-store-files-list', name: 'vectorStores.files.list', group: 'Vector Store Files', kind: 'function', signature: 'openai.vectorStores.files.list(vectorStoreId, query?, options?): PagePromise<VectorStoreFilesPage, VectorStoreFile>',
    beginner: 'list 分页列出一个向量库中的文件索引对象，可按 status 和 filter 条件筛选。',
    whenToUse: '知识库文件管理、失败文件巡检或等待一批索引全部完成时。',
    example: `for await (const file of openai.vectorStores.files.list('vs_123', { filter: 'failed' })) {\n  console.log(file.id, file.last_error)\n}`,
    returns: '返回可自动分页的 VectorStoreFile 集合。',
    interview: '库级清单是索引控制面；应用应把 file_id 对应的原始标题、ACL 和业务版本保存在自己的元数据系统。',
    pitfall: 'filter 是处理状态筛选，不等同 attributes 检索过滤；全量扫描前设置 limit 和取消策略。', officialUrl: `${API}/vector_stores/subresources/files/methods/list`,
  }),
  openAiApi({
    slug: 'vector-store-files-delete', name: 'vectorStores.files.delete', group: 'Vector Store Files', kind: 'function', signature: 'openai.vectorStores.files.delete(fileId, { vector_store_id }, options?): APIPromise<VectorStoreFileDeleted>',
    beginner: 'delete 把文件从某个向量库中移除，使其不再参与该库检索。',
    whenToUse: '文档下线、权限撤销、版本替换或错误文件需要从知识库移除时。',
    example: `await openai.vectorStores.files.delete('file_123', { vector_store_id: 'vs_123' })`,
    returns: '返回库内文件删除确认；原始 Files API 中的 File 通常仍然存在。',
    interview: '“从索引删除”和“删除原始文件”必须分开设计；前者控制召回，后者控制存储与数据保留。',
    pitfall: '只移除索引不会自动删除 File；若是隐私删除请求，还要清理原始文件、缓存和应用副本。', officialUrl: `${API}/vector_stores/subresources/files/methods/delete`,
  }),
  openAiApi({
    slug: 'vector-store-files-create-and-poll', name: 'vectorStores.files.createAndPoll', group: 'Vector Store Files', kind: 'function', signature: 'openai.vectorStores.files.createAndPoll(vectorStoreId, body, options?): Promise<VectorStoreFile>',
    beginner: 'createAndPoll 先关联 File，再自动轮询到 completed 或 failed，省去手写状态循环。',
    whenToUse: '脚本、后台 worker 或测试必须等索引完成后才能继续检索时。',
    example: `const ready = await openai.vectorStores.files.createAndPoll('vs_123', {\n  file_id: 'file_123',\n})\nconsole.log(ready.status)`,
    returns: '返回达到终态的 VectorStoreFile；失败或超时会抛出错误。',
    interview: '轮询 helper 提供易用的同步语义，但底层仍是多个 HTTP 请求；在线链路应评估超时并考虑异步任务。',
    pitfall: '默认轮询间隔不一定符合你的限流和延迟目标；不要在短超时 serverless 请求中无限等待。', officialUrl: `${SDK}/blob/v6.47.0/helpers.md#polling-helpers`,
  }),
  openAiApi({
    slug: 'vector-store-files-poll', name: 'vectorStores.files.poll', group: 'Vector Store Files', kind: 'function', signature: 'openai.vectorStores.files.poll(vectorStoreId, fileId, options?): Promise<VectorStoreFile>',
    beginner: 'poll 对已经存在的 VectorStoreFile 重复查询，直到它进入完成或失败状态。',
    whenToUse: '关联动作由其他服务完成，当前 worker 只负责等待索引终态时。',
    example: `const ready = await openai.vectorStores.files.poll('vs_123', 'file_123', {\n  pollIntervalMs: 2_000,\n})`,
    returns: '返回最终 VectorStoreFile 状态；内部会读取 poll-after 响应头或配置的间隔。',
    interview: '轮询要有退避、截止时间和可恢复 checkpoint；SDK helper 简化循环，但业务仍负责整体任务超时。',
    pitfall: '参数顺序与 retrieve 不完全相同，调用前核对签名；失败终态不应被自动当成功继续检索。', officialUrl: `${SDK}/blob/v6.47.0/src/resources/vector-stores/files.ts`,
  }),
  openAiApi({
    slug: 'vector-store-files-upload', name: 'vectorStores.files.upload', group: 'Vector Store Files', kind: 'function', signature: 'openai.vectorStores.files.upload(vectorStoreId, file, options?): Promise<VectorStoreFile>',
    beginner: 'upload 是两步便捷方法：先调用 Files API 上传原始文件，再把得到的 file_id 关联到向量库。',
    whenToUse: '手里只有本地/内存文件，希望一次调用完成上传和关联，但不需要等待索引完成时。',
    example: `const item = await openai.vectorStores.files.upload('vs_123', createReadStream('faq.pdf'))\nconsole.log(item.id, item.status)`,
    returns: '返回刚创建的 VectorStoreFile，通常仍处于 in_progress；原始 File 也已被创建。',
    interview: '便捷 helper 隐藏了两个资源创建动作；任一步失败都可能留下孤儿 File，因此生产系统要记录中间 ID 并补偿清理。',
    pitfall: '返回并不保证可检索；失败后检查是否已创建原始 File，避免重试不断产生重复文件。', officialUrl: `${SDK}/blob/v6.47.0/src/resources/vector-stores/files.ts`,
  }),
  openAiApi({
    slug: 'vector-store-files-upload-and-poll', name: 'vectorStores.files.uploadAndPoll', group: 'Vector Store Files', kind: 'function', signature: 'openai.vectorStores.files.uploadAndPoll(vectorStoreId, file, options?): Promise<VectorStoreFile>',
    beginner: 'uploadAndPoll 串联上传原始 File、加入向量库和轮询索引完成，是单文件入库最省代码的 helper。',
    whenToUse: '脚本、演示或后台任务需要确保一个文件已经可检索后再返回时。',
    example: `const ready = await openai.vectorStores.files.uploadAndPoll(\n  'vs_123', createReadStream('handbook.pdf'), { pollIntervalMs: 2_000 },\n)\nconsole.log(ready.status)`,
    returns: '成功返回状态为 completed 的 VectorStoreFile；上传、关联、处理任一步失败都会拒绝 Promise。',
    interview: '这是组合 helper 而非原子事务；可观测性应分别记录 upload、attach、index 三个阶段和耗时。',
    pitfall: '长文件可能超过 Web 请求超时；失败补偿要清理孤儿 File，并用内容哈希避免重复上传。', officialUrl: `${SDK}/blob/v6.47.0/helpers.md#bulk-upload-helpers`,
  }),
  openAiApi({
    slug: 'vector-store-files-content', name: 'vectorStores.files.content', group: 'Vector Store Files', kind: 'function', signature: 'openai.vectorStores.files.content(fileId, { vector_store_id }, options?): PagePromise<FileContentResponsesPage, FileContentResponse>',
    beginner: 'content 读取平台解析后用于索引的文本块，而不是下载原始文件字节。',
    whenToUse: '调试 PDF/文档解析结果、检查切片边界或解释为什么某段没有被召回时。',
    example: `const parsed = await openai.vectorStores.files.content('file_123', {\n  vector_store_id: 'vs_123',\n})\nconsole.log(parsed.data[0]?.text)`,
    returns: '返回解析后的内容分页响应，包含每个文本内容块及相关元数据。',
    interview: 'RAG 排障应逐层检查原文→解析→切片→向量→召回→生成；content 提供了解析/切片层可见性。',
    pitfall: '解析内容可能包含敏感正文，不应写入普通日志；它也可能与原始排版不同，不能作为原文件的权威替代。', officialUrl: `${API}/vector_stores/subresources/files/methods/content`,
  }),

  openAiApi({
    slug: 'vector-store-file-batches-create', name: 'vectorStores.fileBatches.create', group: 'Vector Store File Batches', kind: 'function', signature: 'openai.vectorStores.fileBatches.create(vectorStoreId, { file_ids, attributes?, chunking_strategy? }, options?): APIPromise<VectorStoreFileBatch>',
    beginner: 'fileBatches.create 一次把多个已上传 File 加入向量库，并用批对象跟踪整体处理计数。',
    whenToUse: '已有一组 File IDs，希望批量建索引并统一观察完成/失败数量时。',
    example: `const batch = await openai.vectorStores.fileBatches.create('vs_123', {\n  file_ids: ['file_a', 'file_b'], attributes: { source: 'handbook' },\n})\nconsole.log(batch.id, batch.file_counts)`,
    returns: '返回 VectorStoreFileBatch，包含状态和 completed/failed/in_progress 文件计数。',
    interview: '批关联减少控制面请求并提供聚合状态，但每个文件仍可能独立失败；业务必须支持部分成功。',
    pitfall: 'file_ids 数量有限制，且共享 attributes 未必适合每个文件；需要不同 ACL 时应分批或逐文件设置。', officialUrl: `${API}/vector_stores/subresources/file_batches/methods/create`,
  }),
  openAiApi({
    slug: 'vector-store-file-batches-retrieve', name: 'vectorStores.fileBatches.retrieve', group: 'Vector Store File Batches', kind: 'function', signature: 'openai.vectorStores.fileBatches.retrieve(batchId, { vector_store_id }, options?): APIPromise<VectorStoreFileBatch>',
    beginner: 'retrieve 查看文件批次的当前状态和各类文件数量。',
    whenToUse: '后台 worker 同步入库进度，或 UI 显示“一共多少、完成多少、失败多少”时。',
    example: `const batch = await openai.vectorStores.fileBatches.retrieve('vsfb_123', {\n  vector_store_id: 'vs_123',\n})\nconsole.log(batch.status, batch.file_counts)`,
    returns: '返回最新 VectorStoreFileBatch 聚合状态。',
    interview: '聚合完成不等于每个文件可用；部分失败需要用 listFiles(filter=failed) 下钻并决定重试。',
    pitfall: 'batch_id 只在所属 vector store 语境中定位；不要漏传库 ID，也不要高频无退避查询。', officialUrl: `${API}/vector_stores/subresources/file_batches/methods/retrieve`,
  }),
  openAiApi({
    slug: 'vector-store-file-batches-cancel', name: 'vectorStores.fileBatches.cancel', group: 'Vector Store File Batches', kind: 'function', signature: 'openai.vectorStores.fileBatches.cancel(batchId, { vector_store_id }, options?): APIPromise<VectorStoreFileBatch>',
    beginner: 'cancel 请求停止仍在处理的文件批次；已经完成的文件不会自动从库中移除。',
    whenToUse: '批量上传选择错误、权限发生变化或预算/时间保护需要停止剩余索引时。',
    example: `const batch = await openai.vectorStores.fileBatches.cancel('vsfb_123', {\n  vector_store_id: 'vs_123',\n})`,
    returns: '返回取消过程中的 VectorStoreFileBatch 状态快照。',
    interview: '取消批处理不是回滚事务；应用要列出已成功文件，决定保留还是逐一从库中删除。',
    pitfall: '取消与处理完成会竞争；不能因批状态 cancelled 就假设库中没有任何新增内容。', officialUrl: `${API}/vector_stores/subresources/file_batches/methods/cancel`,
  }),
  openAiApi({
    slug: 'vector-store-file-batches-create-and-poll', name: 'vectorStores.fileBatches.createAndPoll', group: 'Vector Store File Batches', kind: 'function', signature: 'openai.vectorStores.fileBatches.createAndPoll(vectorStoreId, body, options?): Promise<VectorStoreFileBatch>',
    beginner: 'createAndPoll 创建文件批次并自动等待它进入终态。',
    whenToUse: '后台脚本已经有 File IDs，并且后续步骤必须等整批索引完成时。',
    example: `const batch = await openai.vectorStores.fileBatches.createAndPoll('vs_123', {\n  file_ids: ['file_a', 'file_b'],\n})\nconsole.log(batch.file_counts)`,
    returns: '返回达到 completed、failed 或 cancelled 等终态的 VectorStoreFileBatch。',
    interview: 'helper 简化状态机，但部分失败仍是正常结果；应检查 file_counts，而不是只判断 Promise resolve。',
    pitfall: '大量文件处理可能很久，避免阻塞短生命周期请求；设置轮询间隔和上层 deadline。', officialUrl: `${SDK}/blob/v6.47.0/helpers.md#polling-helpers`,
  }),
  openAiApi({
    slug: 'vector-store-file-batches-list-files', name: 'vectorStores.fileBatches.listFiles', group: 'Vector Store File Batches', kind: 'function', signature: 'openai.vectorStores.fileBatches.listFiles(batchId, { vector_store_id, filter?, ... }, options?): PagePromise<VectorStoreFilesPage, VectorStoreFile>',
    beginner: 'listFiles 分页列出某个批次包含的具体 VectorStoreFile，并可只看失败或进行中的项目。',
    whenToUse: '批次部分失败时下钻定位文件，或对每个文件记录最终状态时。',
    example: `for await (const file of openai.vectorStores.fileBatches.listFiles('vsfb_123', {\n  vector_store_id: 'vs_123', filter: 'failed',\n})) console.log(file.id, file.last_error)`,
    returns: '返回可自动分页的 VectorStoreFile 集合。',
    interview: '批任务聚合状态必须能下钻到项目级错误；重试应针对失败项而不是无脑重跑整批。',
    pitfall: '只处理当前页会漏掉后续失败项；重试前先判断失败是否为永久格式错误。', officialUrl: `${API}/vector_stores/subresources/file_batches/methods/list_files`,
  }),
  openAiApi({
    slug: 'vector-store-file-batches-poll', name: 'vectorStores.fileBatches.poll', group: 'Vector Store File Batches', kind: 'function', signature: 'openai.vectorStores.fileBatches.poll(vectorStoreId, batchId, options?): Promise<VectorStoreFileBatch>',
    beginner: 'poll 等待一个已创建的文件批次达到终态，不重复创建批次。',
    whenToUse: '创建与等待由不同 worker 或不同重试阶段负责时。',
    example: `const done = await openai.vectorStores.fileBatches.poll('vs_123', 'vsfb_123', {\n  pollIntervalMs: 3_000,\n})`,
    returns: '返回最终 VectorStoreFileBatch 状态与文件计数。',
    interview: '把 create 和 poll 分开便于持久化 batch_id、进程重启后恢复等待，并避免重复提交。',
    pitfall: '要为总体任务设置最大等待和取消信号；helper 的一次异常不等于远端任务失败，可先 retrieve 再决定。', officialUrl: `${SDK}/blob/v6.47.0/src/resources/vector-stores/file-batches.ts`,
  }),
  openAiApi({
    slug: 'vector-store-file-batches-upload-and-poll', name: 'vectorStores.fileBatches.uploadAndPoll', group: 'Vector Store File Batches', kind: 'function', signature: 'openai.vectorStores.fileBatches.uploadAndPoll(vectorStoreId, { files, fileIds? }, options?): Promise<VectorStoreFileBatch>',
    beginner: 'uploadAndPoll 是批量入库组合 helper：并发上传本地文件、创建 file batch，再等索引处理结束。',
    whenToUse: '一次导入多份本地文档，并希望一条调用在后台任务中完成全部上传与索引时。',
    example: `const batch = await openai.vectorStores.fileBatches.uploadAndPoll('vs_123', {\n  files: [createReadStream('a.pdf'), createReadStream('b.pdf')],\n})\nconsole.log(batch.file_counts)`,
    returns: '返回终态 VectorStoreFileBatch；内部还会创建多个原始 File 和库内文件对象。',
    interview: '组合 helper 提升易用性但不是事务：部分上传、关联或索引失败会留下多种资源，生产系统需要幂等键、哈希去重和补偿清理。',
    pitfall: '不要在前端请求中同步等待大批文件；限制并发与总大小，记录每个创建的 file_id，失败后避免重复产生孤儿资源。', officialUrl: `${SDK}/blob/v6.47.0/helpers.md#bulk-upload-helpers`,
  }),
]

export const OPENAI_JAVASCRIPT_SDK_EXPECTED_COUNT = 77
