import type { ApiEntry } from '../types'

const AI_PM_COMMIT = 'b83a65cc23154aebe56b9ebcd2ebf67e5b3530c0'
type AiPmEntry = Omit<ApiEntry, 'project' | 'verifiedCommit'>
const api = (entry: AiPmEntry): ApiEntry => ({ ...entry, project: 'ai-pm', verifiedCommit: AI_PM_COMMIT })
const doctor = 'scripts/ai-index-doctor.ts'
const evalScript = 'scripts/ai-index-eval.ts'

export const aiPmKnowledgeApis: ApiEntry[] = [
  api({
    slug: 'ai-pm-dashscope-embedding', name: 'createDashScopeEmbedding', signature: 'createDashScopeEmbedding(): EmbeddingPort', kind: 'function', module: 'semantic-search', technology: 'DashScope text-embedding-v4 OpenAI-compatible API',
    summary: '用统一 EmbeddingPort 批量生成文档/查询向量，并核对每个输入 index 对应的非空向量。', whenToUse: '后台 chunk 入库和在线知识检索需要同一模型与维度时。', sourcePath: 'src/lib/ai/knowledge/dashscope-embedding.ts', sourceLine: 17,
    parameters: [{ name: 'texts', type: 'string[]', required: true, description: '文档批次或单条查询；调用方负责切块和批量上限。' }], returns: { type: 'EmbeddingPort', description: 'embedDocuments、embedQuery 和 getModelInfo。' },
    example: { language: 'ts', code: `const embedding = createDashScopeEmbedding()\nconst vectors = await embedding.embedDocuments(chunks.map(c => c.content))`, explanation: ['请求 /embeddings 并传 model/input/dimensions。', '查询和文档共用适配器，避免维度漂移。'] },
    effect: { title: '文本变成可检索向量', description: '业务记录和飞书文档 chunk 可写入 Qdrant，用户问题可转为查询向量。', metrics: [{ label: 'default model', value: 'text-embedding-v4' }, { label: 'protocol', value: 'OpenAI-compatible' }, { label: 'retry', value: 'worker-owned' }], output: [{ label: 'N inputs', value: 'N vectors', tone: 'good' }, { label: 'missing vector', value: 'hard error', tone: 'warn' }] },
    errors: [{ condition: 'AI_API_KEY 缺失', behavior: '立即抛错。', recovery: 'worker 保留失败任务，配置密钥后重试。' }, { condition: '响应缺某 index', behavior: '拒绝部分写入。', recovery: '记录批次并重试，必要时缩小批量。' }], bestPractices: ['collection 维度必须与模型配置一致。', '批量限制、超时和指数退避应显式配置。', '不要记录向量或原文到普通日志。'], tests: [{ path: doctor, proves: '运维脚本校验模型/维度配置；当前没有真实 embedding contract 自动测试。' }, { path: evalScript, proves: '端到端评测会真实复用该 embedding 生成查询向量。' }], officialSources: [{ label: 'DashScope text embedding', url: 'https://help.aliyun.com/zh/model-studio/text-embedding-synchronous-api' }, { label: 'OpenAI embeddings format', url: 'https://platform.openai.com/docs/api-reference/embeddings' }], related: ['ai-pm-embed-pending-chunks', 'ai-pm-knowledge-retriever', 'ai-pm-qdrant-vector-store'],
  }),
  api({
    slug: 'ai-pm-dashscope-reranker', name: 'createDashScopeReranker', signature: 'createDashScopeReranker(): RerankerPort', kind: 'function', module: 'semantic-search', technology: 'DashScope qwen3-rerank native API', implementationStatus: 'best-effort', statusNote: '生产检索默认尝试 qwen3-rerank；缺 key、HTTP/网络异常或空结果都按 Qdrant 混合分数 fallback，结果仍可用但排序质量降低。',
    summary: '把 query 与候选标题/小节/正文发给百炼精排；失败时稳定回退原始 score 排序。', whenToUse: 'Qdrant 先召回 4 倍候选后压缩到用户需要的 top K 时。', sourcePath: 'src/lib/ai/knowledge/dashscope-reranker.ts', sourceLine: 30,
    parameters: [{ name: 'query', type: 'string', required: true, description: '用户自然语言问题。' }, { name: 'candidates/limit', type: 'KnowledgeChunkCandidate[] / number', required: true, description: '候选片段和最终上限。' }], returns: { type: 'RerankerPort', description: '按 relevance_score 更新并截断的候选数组。' },
    example: { language: 'ts', code: `const ranked = await createDashScopeReranker().rerank({ query, candidates, limit: 6 })`, explanation: ['使用 DashScope native rerank input/parameters，而非 chat endpoint。', 'return_documents=false 避免重复传回正文。'] },
    effect: { title: '召回候选再精排', description: '语义相近但业务无关的片段被降权；供应商故障时仍返回向量结果。', metrics: [{ label: 'default model', value: 'qwen3-rerank' }, { label: 'fallback', value: 'score sort' }], output: [{ label: 'rerank available', value: 'relevance ordered', tone: 'good' }, { label: 'rerank unavailable', value: 'vector/sparse order', tone: 'neutral' }] },
    errors: [{ condition: 'HTTP/网络/空结果', behavior: 'console warning 后 fallback。', recovery: '监控 fallback 比例和质量回归。' }, { condition: '返回非法 index', behavior: '对应候选被过滤。', recovery: '验证 provider schema 并记录异常计数。' }], bestPractices: ['fallback 必须可见于指标，不能静默掩盖长期故障。', '评测同时记录 rerank 前后 Recall/MRR。', '限制候选正文长度。'], tests: [{ path: evalScript, proves: '评测链路真实调用 reranker 并计算最终 MRR；暂无 mock contract 单测。' }], officialSources: [{ label: 'DashScope text rerank', url: 'https://help.aliyun.com/zh/model-studio/text-rerank-api' }], related: ['ai-pm-knowledge-retriever', 'ai-pm-retrieval-eval', 'ai-pm-qdrant-vector-store'],
  }),
  api({
    slug: 'ai-pm-qdrant-vector-store', name: 'createQdrantVectorStore', signature: 'createQdrantVectorStore(): VectorStorePort', kind: 'function', module: 'semantic-search', technology: '@qdrant/js-client-rest 1.18.0',
    summary: '懒创建 Cosine collection，稳定映射 chunk cuid 为 UUID，执行 workspace 过滤的向量检索并融合 80% 向量与 20% 关键词分。', whenToUse: 'worker 写入/删除知识 chunk，或在线检索当前工作区候选时。', sourcePath: 'src/lib/ai/knowledge/qdrant-vector-store.ts', sourceLine: 66,
    parameters: [{ name: 'chunks', type: 'VectorChunk[]', required: false, description: 'upsert 的向量和 payload。' }, { name: 'VectorSearchInput', type: '{workspaceId, queryVector, sparseQuery?, limit}', required: false, description: '多租户查询条件。' }], returns: { type: 'VectorStorePort', description: 'upsertChunks、deleteSource、hybridSearch。' },
    example: { language: 'ts', code: `const store = createQdrantVectorStore()\nconst matches = await store.hybridSearch({ workspaceId, queryVector, sparseQuery: query, limit: 24 })`, explanation: ['所有 search 必须带 workspaceId payload filter。', '原 chunk id 保留在 payload，point id 使用稳定 UUID。'] },
    effect: { title: '多租户混合召回', description: '同一 collection 中只返回当前工作区片段，并用轻量关键词弥补纯向量。', metrics: [{ label: 'distance', value: 'Cosine' }, { label: 'fusion', value: '0.8 vector + 0.2 sparse' }, { label: 'write wait', value: 'true' }], output: [{ label: 'search', value: 'scored tenant-scoped chunks', tone: 'good' }, { label: 'missing QDRANT_URL', value: 'hard error', tone: 'warn' }] },
    errors: [{ condition: 'QDRANT_URL 缺失', behavior: '首次实际操作时抛错，不阻断构建。', recovery: '配置 URL 后跑 doctor/eval。' }, { condition: 'collection 维度已存在但配置改变', behavior: '写入失败。', recovery: '版本化 collection 并重建，不原地改维度。' }], bestPractices: ['tenant filter 是强制安全边界。', 'collection 名包含模型/维度版本。', 'upsert/delete 做真实 Qdrant contract 测试。'], tests: [{ path: doctor, proves: '调用 collectionExists 检查 Qdrant 连接和 collection；不覆盖写入/租户过滤。' }, { path: evalScript, proves: '真实评测覆盖 tenant-scoped search。' }], officialSources: [{ label: 'Qdrant JavaScript client', url: 'https://qdrant.tech/documentation/interfaces/web-ui/' }, { label: 'Qdrant filtering', url: 'https://qdrant.tech/documentation/concepts/filtering/' }], related: ['ai-pm-dashscope-embedding', 'ai-pm-knowledge-retriever', 'ai-pm-cleanup-knowledge-source'],
  }),
  api({
    slug: 'ai-pm-knowledge-retriever', name: 'createKnowledgeRetriever', signature: 'createKnowledgeRetriever(deps): KnowledgeRetrieverPort', kind: 'function', module: 'semantic-search', technology: 'Port/Adapter RAG orchestration',
    summary: '顺序编排 query embedding、Qdrant 4 倍召回、Rerank top K 和检索 trace，供 ChatBox knowledge tool 复用。', whenToUse: '需要以同一安全/质量协议检索版本、需求、Bug、任务和飞书文档时。', sourcePath: 'src/lib/ai/knowledge/retriever.ts', sourceLine: 12,
    parameters: [{ name: 'deps', type: 'EmbeddingPort + VectorStorePort + RerankerPort + TraceEvalPort', required: true, description: '可替换基础设施适配器。' }, { name: 'search', type: '{workspaceId, query, limit=8}', required: true, description: '租户、问题和最终数量。' }], returns: { type: 'KnowledgeRetrieverPort', description: 'search 返回精排 KnowledgeChunkCandidate[]。' },
    example: { language: 'ts', code: `const retriever = createKnowledgeRetriever({ embedding, vectorStore, reranker, traceEval })\nconst hits = await retriever.search({ workspaceId, query, limit: 6 })`, explanation: ['候选量为 max(limit*4,20)。', 'trace 只记录输入摘要和数量，不阻断答案。'] },
    effect: { title: 'ChatBox 获得可引用上下文', description: '模糊问题返回标题、小节、正文和相关度，模型再结合结构化工具下结论。', metrics: [{ label: 'candidate multiplier', value: '4x' }, { label: 'default topK', value: '8' }], output: [{ label: 'hit', value: 'ranked chunks', tone: 'good' }, { label: 'no hit', value: 'empty list', tone: 'neutral' }] },
    errors: [{ condition: 'embedding/vector store 失败', behavior: '向调用方抛错。', recovery: 'Chat tool 返回脱敏空片段，管理员检查索引。' }, { condition: 'trace 写失败', behavior: 'trace adapter fail-open。', recovery: '告警但不阻断用户回答。' }], bestPractices: ['检索层不拼最终答案。', 'workspaceId 必须贯穿每个 adapter。', '记录召回数、返回数和 latency 分位数。'], tests: [{ path: evalScript, proves: '端到端评测通过同一 retriever 计算 Recall@K 和 MRR。' }], officialSources: [{ label: 'Qdrant hybrid search concepts', url: 'https://qdrant.tech/documentation/concepts/hybrid-queries/' }], related: ['ai-pm-dashscope-embedding', 'ai-pm-qdrant-vector-store', 'ai-pm-dashscope-reranker', 'ai-pm-prisma-trace'],
  }),
  api({
    slug: 'ai-pm-index-business-entity', name: 'indexBusinessEntity', signature: 'indexBusinessEntity(job, queue): Promise<void>', kind: 'agent-task', module: 'background-jobs', technology: 'Prisma 7 + content-hash chunk pipeline',
    summary: '读取版本/需求/Bug/任务，生成统一 source 与 chunk；内容 hash 未变只刷新元数据，变化则事务替换 chunk 并投递 embed_chunks。', whenToUse: '业务记录创建/更新后的异步 index_entity job。', sourcePath: 'src/lib/ai/knowledge/source-builders.ts', sourceLine: 238,
    parameters: [{ name: 'job', type: 'ClaimedIndexJob', required: true, description: '带 workspace/entity/job/payload 的队列任务。' }, { name: 'queue', type: 'IndexQueuePort', required: true, description: '继续投递重型 embedding。' }], returns: { type: 'Promise<void>', description: 'MySQL source/chunk 就绪到 pending；embedding 尚未执行。' },
    example: { language: 'ts', code: `await indexBusinessEntity(job, queue) // saves chunks, then enqueue embed_chunks`, explanation: ['保存请求只轻量入队，不同步调模型。', 'dedupeKey 包 contentHash，确保内容变更可重新 embedding。'] },
    effect: { title: '业务保存后自动建索引', description: '版本、需求、Bug、任务最终成为可检索片段且重复保存不重复消耗模型。', metrics: [{ label: 'entity types', value: '4' }, { label: 'dedupe', value: 'contentHash' }, { label: 'DB write', value: 'transactional' }], output: [{ label: 'unchanged', value: 'metadata refresh only', tone: 'good' }, { label: 'changed', value: 'pending chunks + embed job', tone: 'good' }] },
    errors: [{ condition: '对象不存在/无文本', behavior: '任务失败并重试。', recovery: '清理孤儿任务或修复 source builder。' }, { condition: '入队在 DB commit 后失败', behavior: 'source 停在 indexing/pending。', recovery: '补偿扫描器重新投递。' }], bestPractices: ['数据库事务与外部队列之间必须有 outbox/补偿。', 'content hash 在标准化文本后计算。', 'source/chunk 状态机可观测。'], tests: [{ path: 'scripts/full-chain-assistant-action-smoke.ts', proves: '真实动作写入后断言创建索引刷新任务；未覆盖完整 chunk/embedding。' }, { path: 'scripts/full-chain-ai-index-admin-smoke.ts', proves: '管理员重建会为四类业务对象投递标准 index_entity。' }], officialSources: [{ label: 'Prisma transactions', url: 'https://www.prisma.io/docs/orm/prisma-client/queries/transactions' }, { label: 'Transactional outbox pattern', url: 'https://microservices.io/patterns/data/transactional-outbox.html' }], related: ['ai-pm-mastra-knowledge-workflow', 'ai-pm-embed-pending-chunks', 'ai-pm-index-compensation'],
  }),
  api({
    slug: 'ai-pm-sync-feishu-knowledge', name: 'syncFeishuDocument', signature: 'syncFeishuDocument(job, queue): Promise<void>', kind: 'agent-task', module: 'integrations', technology: 'Feishu docx/wiki + Prisma chunks',
    summary: '解析飞书链接、读取正文并关联需求/项目/版本，按内容 hash 幂等更新 source/chunk 后投递 embedding。', whenToUse: '需求绑定飞书 docx/wiki 或管理员重建其外部文档索引时。', sourcePath: 'src/lib/ai/knowledge/source-builders.ts', sourceLine: 369,
    parameters: [{ name: 'job.payload.documentLink', type: 'string', required: true, description: '支持的飞书文档链接。' }, { name: 'job.workspaceId/requirementId', type: 'string', required: true, description: '多租户关联。' }], returns: { type: 'Promise<void>', description: 'feishu_doc/wiki source 与 pending chunks；后续 embed job。' },
    example: { language: 'ts', code: `await syncFeishuDocument({ ...job, entityType: 'feishu_doc', payload: { documentLink, requirementId } }, queue)`, explanation: ['正文不变只刷新业务关联。', 'sourceUrl/token 保存用于后续重建。'] },
    effect: { title: '飞书文档进入知识库', description: 'ChatBox 可检索需求背后的正文而不只看结构化摘要。', metrics: [{ label: 'source types', value: 'docx + wiki' }, { label: 'dedupe', value: 'contentHash' }], output: [{ label: 'new content', value: 'chunks queued', tone: 'good' }, { label: 'unchanged', value: 'no embedding cost', tone: 'good' }] },
    errors: [{ condition: '链接/权限/文档读取失败', behavior: 'job 失败重试。', recovery: '检查飞书应用权限和链接类型。' }, { condition: '事务后入队失败', behavior: 'pending chunk 留存。', recovery: '补偿扫描器。' }], bestPractices: ['外部文档访问凭据只在服务端。', '原文 PII 按工作区权限和保留策略治理。', '支持 token/version 增量同步而非全量轮询。'], tests: [{ path: 'scripts/full-chain-ai-index-admin-smoke.ts', proves: '验证飞书已有 source 的 rebuild_source 与需求链接的 sync_feishu 入队上下文。' }, { path: 'scripts/full-chain-requirement-ai-smoke.ts', proves: '验证飞书链接解析和正文读取入口契约。' }], officialSources: [{ label: 'Feishu Docs API', url: 'https://open.feishu.cn/document/server-docs/docs/docs/docx-v1/document/get' }], related: ['ai-pm-index-business-entity', 'ai-pm-embed-pending-chunks', 'ai-pm-index-compensation'],
  }),
  api({
    slug: 'ai-pm-cleanup-knowledge-source', name: 'cleanupKnowledgeSource', signature: 'cleanupKnowledgeSource(job): Promise<void>', kind: 'agent-task', module: 'background-jobs', technology: 'Qdrant delete + Prisma cascade',
    summary: '按 workspace/entity 找到内部和需求关联的飞书 source，先删除 Qdrant 点再删除 MySQL 元数据。', whenToUse: '业务对象删除后的 cleanup_source job。', sourcePath: 'src/lib/ai/knowledge/source-builders.ts', sourceLine: 535,
    parameters: [{ name: 'job', type: 'ClaimedIndexJob', required: true, description: 'workspaceId、entityType、entityId。' }], returns: { type: 'Promise<void>', description: '向量与 source/chunk 元数据均删除。' },
    example: { language: 'ts', code: `await cleanupKnowledgeSource({ ...job, jobType: 'cleanup_source' })`, explanation: ['需求删除同时覆盖 requirement、feishu_doc、feishu_wiki。', '先外部后内部，失败可重试。'] },
    effect: { title: '删除后不再被 RAG 召回', description: '业务删除最终同步到向量库，避免陈旧/越权片段。', metrics: [{ label: 'order', value: 'Qdrant -> MySQL' }, { label: 'requirement sources', value: '3 types' }], output: [{ label: 'success', value: 'no searchable vectors', tone: 'good' }, { label: 'Qdrant failure', value: 'DB metadata retained', tone: 'warn' }] },
    errors: [{ condition: 'Qdrant 删除失败', behavior: 'MySQL 不删，job 可重试。', recovery: '恢复 Qdrant 后重放。' }, { condition: 'Qdrant 成功/MySQL 失败', behavior: '元数据残留但向量已删。', recovery: '重试幂等删除。' }], bestPractices: ['删除链路必须审计和补偿。', '数据保留/GDPR 删除覆盖所有派生向量。', '删除 API 需幂等。'], tests: [{ path: 'scripts/full-chain-assistant-action-smoke.ts', proves: '验证业务动作会产生索引刷新；当前无真实 Qdrant 删除 contract 测试。' }], officialSources: [{ label: 'Qdrant delete points', url: 'https://qdrant.tech/documentation/concepts/points/#delete-points' }], related: ['ai-pm-qdrant-vector-store', 'ai-pm-index-compensation'],
  }),
  api({
    slug: 'ai-pm-embed-pending-chunks', name: 'embedPendingChunks', signature: 'embedPendingChunks(job): Promise<void>', kind: 'agent-task', module: 'background-jobs', technology: 'DashScope Embedding + Qdrant + Prisma',
    summary: 'worker 批量读取 pending chunks，生成向量并等待 Qdrant upsert，随后事务标记 chunk/source ready 和模型元数据。', whenToUse: 'Mastra 路由到 embed_chunks 的重型后台步骤。', sourcePath: 'src/lib/ai/knowledge/embedding-workflow.ts', sourceLine: 15,
    parameters: [{ name: 'job.sourceId', type: 'string', required: true, description: '目标索引源。' }], returns: { type: 'Promise<void>', description: '外部向量和 MySQL 状态完成；无 pending chunk 时只把 source 标 ready。' },
    example: { language: 'ts', code: `await embedPendingChunks(job) // model -> Qdrant -> DB ready`, explanation: ['绝不在业务保存请求同步调用。', 'Qdrant 完成后才推进 DB ready。'] },
    effect: { title: '索引从 pending 变为可检索', description: '后台完成后 ChatBox knowledge tool 可召回这些片段。', metrics: [{ label: 'execution', value: 'worker only' }, { label: 'Qdrant wait', value: 'true' }], output: [{ label: 'success', value: 'chunks/source ready', tone: 'good' }, { label: 'provider failure', value: 'job retry', tone: 'warn' }] },
    errors: [{ condition: 'sourceId 缺失', behavior: '硬失败。', recovery: '修复任务生产者。' }, { condition: 'Qdrant 成功但 DB 事务失败', behavior: '向量已新、DB 仍 pending。', recovery: '稳定 point id 让重试覆盖写。' }], bestPractices: ['外部写 idempotent。', '记录模型名、维度和索引时间。', '大批次分段避免模型/DB 超时。'], tests: [{ path: evalScript, proves: '评测只消费 ready source，间接验证最终状态；暂无独立 embedding workflow contract 测试。' }], officialSources: [{ label: 'Qdrant upsert points', url: 'https://qdrant.tech/documentation/concepts/points/' }], related: ['ai-pm-dashscope-embedding', 'ai-pm-qdrant-vector-store', 'ai-pm-mastra-knowledge-workflow'],
  }),
  api({
    slug: 'ai-pm-prisma-trace', name: 'createPrismaTraceEval', signature: 'createPrismaTraceEval(): TraceEvalPort', kind: 'function', module: 'semantic-search', technology: 'Prisma MySQL trace adapter', implementationStatus: 'fallback', statusNote: '文件名提到 Langfuse，但当前 commit 没有 Langfuse SDK/exporter；生产实现仅把检索 trace 写入 ai_index_traces，createNoopTraceEval 甚至是同一函数别名。',
    summary: '把 knowledge.search 的 workspace、输入摘要、召回/返回数量和 scores 写入 MySQL；写失败 fail-open。', whenToUse: 'RAG 检索需要最低限度的可追溯数据，部署尚未接 Langfuse 时。', sourcePath: 'src/lib/ai/knowledge/langfuse-trace.ts', sourceLine: 7,
    parameters: [{ name: 'TraceEvalEvent', type: '{workspaceId, traceId?, name, input?, output?, scores?}', required: true, description: '结构化追踪事件。' }], returns: { type: 'TraceEvalPort', description: 'record(event) 异步写库但吞掉错误。' },
    example: { language: 'ts', code: `await traceEval.record({ workspaceId, name: 'knowledge.search', input: { query, limit }, output: { retrieved, returned } })`, explanation: ['trace 失败不阻断用户回答。', '当前不是分布式 tracing，也没有 latency/span。'] },
    effect: { title: '管理员看到最近检索轨迹', description: '索引状态 API 返回最近十条 trace，辅助判断是否有召回。', metrics: [{ label: 'backend', value: 'MySQL' }, { label: 'Langfuse', value: 'not wired' }, { label: 'failure', value: 'fail-open' }], output: [{ label: 'record success', value: 'ai_index_traces row', tone: 'good' }, { label: 'record fail', value: 'console error only', tone: 'warn' }] },
    errors: [{ condition: '数据库写失败', behavior: '记录 error 后继续回答。', recovery: '增加异步缓冲和 trace 写失败指标。' }], bestPractices: ['不要把 fallback 命名成已接入 Langfuse。', 'trace 传播 request/conversation id。', '生产接 OpenTelemetry/Langfuse 时保留 Port。'], tests: [{ path: 'scripts/full-chain-ai-index-admin-smoke.ts', proves: '状态端点契约检查 recentTraces；当前不验证 trace 写入。' }, { path: evalScript, proves: '真实评测会调用该 adapter 写 trace。' }], officialSources: [{ label: 'Langfuse tracing', url: 'https://langfuse.com/docs/observability/features/traces' }, { label: 'OpenTelemetry traces', url: 'https://opentelemetry.io/docs/concepts/signals/traces/' }], related: ['ai-pm-knowledge-retriever', 'ai-pm-get-index-status', 'ai-pm-retrieval-eval'],
  }),
  api({
    slug: 'ai-pm-retrieval-eval', name: 'runKnowledgeRetrievalEval', signature: 'runKnowledgeRetrievalEval({ workspaceId, cases?, limit?, topK? }): Promise<KnowledgeEvalResult>', kind: 'workflow', module: 'semantic-search', technology: 'Recall@K + MRR retrieval eval', implementationStatus: 'best-effort', statusNote: '可运行的生产运维脚本，但默认评测集由 ready source 的标题反查自身自动生成，只能发现配置/召回回归，不能证明真实用户问题和引用正确率。',
    summary: '对固定或自动生成的 query/source 对执行完整 embedding→Qdrant→rerank，计算 Recall@K、MRR 和每例命中名次。', whenToUse: '上线、模型/维度升级或工作区重建后做 RAG 冒烟质量门禁。', sourcePath: 'src/lib/ai/knowledge/eval.ts', sourceLine: 59,
    parameters: [{ name: 'workspaceId', type: 'string', required: true, description: '评测租户。' }, { name: 'cases/topK', type: 'KnowledgeEvalCase[] / number', required: false, description: '测试集与 K，默认 5。' }], returns: { type: 'Promise<KnowledgeEvalResult>', description: 'total/evaluated/recallAtK/mrr 与逐例 rank。' },
    example: { language: 'sh', code: `AI_INDEX_EVAL_WORKSPACE_ID=ws_x pnpm ai-index:eval`, explanation: ['脚本显式要求 workspaceId。', '退出前断开 Prisma。'] },
    effect: { title: '检索质量可量化', description: '部署者能在变更前后比较召回率和排序，而非只看接口 200。', metrics: [{ label: 'metrics', value: 'Recall@K + MRR' }, { label: 'default K', value: '5' }, { label: 'golden dataset', value: 'missing by default' }], output: [{ label: 'healthy smoke', value: 'high recall / MRR', tone: 'good' }, { label: '0 cases', value: '0 metrics', tone: 'warn' }] },
    errors: [{ condition: '基础设施/模型失败', behavior: '脚本非零退出。', recovery: '先跑 doctor，再检查 collection/维度/密钥。' }], bestPractices: ['提交人工标注的真实查询与期望 source。', '分别报告 rerank 前后指标。', 'CI/发布门禁固定数据版本和阈值。'], tests: [{ path: evalScript, proves: '提供真实执行入口、参数和结果输出；该脚本本身就是评测而非单元测试。' }], officialSources: [{ label: 'Information retrieval evaluation', url: 'https://nlp.stanford.edu/IR-book/html/htmledition/evaluation-of-ranked-retrieval-results-1.html' }], related: ['ai-pm-knowledge-retriever', 'ai-pm-prisma-trace', 'ai-pm-dashscope-reranker'],
  }),
  api({
    slug: 'ai-pm-index-compensation', name: 'runKnowledgeIndexCompensation', signature: 'runKnowledgeIndexCompensation(options): Promise<CompensationResult>', kind: 'workflow', module: 'background-jobs', technology: 'Prisma reconciliation scanner',
    summary: '按小批量扫描工作区，发现缺 source、failed 或长时间 pending/indexing 的记录，再投递标准索引/重建任务。', whenToUse: '常驻 ai-index worker 周期补偿数据库提交与队列/模型/向量库之间的裂缝时。', sourcePath: 'src/lib/ai/knowledge/compensation.ts', sourceLine: 255,
    parameters: [{ name: 'queue/workerId', type: 'IndexQueuePort/string', required: true, description: '目标队列和观察标识。' }, { name: 'maxWorkspaces/perEntityLimit', type: 'number', required: false, description: '限制每轮扫描成本。' }], returns: { type: 'Promise<{workspaces,enqueued}>', description: '扫描和补偿任务数量。' },
    example: { language: 'ts', code: `await runKnowledgeIndexCompensation({ queue, workerId, maxWorkspaces: 20 })`, explanation: ['按日 run key 构造去重键。', '补偿只入队，不在扫描器同步 embedding。'] },
    effect: { title: '最终一致的自动索引', description: '短暂崩溃、外部失败或历史漏索引会被下一轮发现。', metrics: [{ label: 'scan', value: 'bounded batches' }, { label: 'dedupe', value: 'daily run key' }], output: [{ label: 'healthy', value: '0 enqueued', tone: 'good' }, { label: 'drift', value: 'repair jobs queued', tone: 'warn' }] },
    errors: [{ condition: '扫描查询失败', behavior: '本轮 worker 记录错误。', recovery: '下个周期重试。' }, { condition: '全量工作区增长', behavior: '固定从 id 起始可能饿死尾部。', recovery: '持久化游标/分片扫描。' }], bestPractices: ['补偿扫描有游标、批次和 SLO。', '去重周期与故障恢复目标一致。', '记录发现原因而不只记录数量。'], tests: [{ path: 'scripts/ai-index-worker.ts', proves: '生产 worker 定时启动补偿循环；当前没有独立扫描场景自动测试。' }, { path: 'scripts/full-chain-ai-index-admin-smoke.ts', proves: '验证补偿最终投递所复用的标准 job 协议。' }], officialSources: [{ label: 'Saga / compensating transaction', url: 'https://microservices.io/patterns/data/saga.html' }], related: ['ai-pm-index-business-entity', 'ai-pm-sync-feishu-knowledge', 'ai-pm-index-queue-selector'],
  }),
]
