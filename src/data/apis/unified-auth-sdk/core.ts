import type { ApiEntry, ApiParameter } from '../types'

const COMMIT = '37986a1774ca8d3f0e3aea491ba6dde6ac9fceb4'
const AI_BOUNDARY = '直接 AI 能力分母为 0：固定提交没有模型调用、Agent、RAG、向量库或 MCP；本条只作为 AI 应用的认证、会话与租户安全底座。'

const BETTER_AUTH = { label: 'Better Auth 基础用法', url: 'https://better-auth.com/docs/basic-usage' }
const BETTER_AUTH_SECURITY = { label: 'Better Auth 安全说明', url: 'https://better-auth.com/docs/reference/security' }
const SESSION = { label: 'Better Auth Session Management', url: 'https://better-auth.com/docs/concepts/session-management' }
const OAUTH = { label: 'Better Auth OAuth', url: 'https://better-auth.com/docs/concepts/oauth' }
const GENERIC_OAUTH = { label: 'Better Auth Generic OAuth', url: 'https://better-auth.com/docs/plugins/generic-oauth' }
const DRIZZLE_ADAPTER = { label: 'Better Auth Drizzle Adapter', url: 'https://better-auth.com/docs/adapters/drizzle' }
const DRIZZLE_SCHEMA = { label: 'Drizzle ORM PostgreSQL Schema', url: 'https://orm.drizzle.team/docs/sql-schema-declaration' }
const FETCH = { label: 'MDN Fetch API', url: 'https://developer.mozilla.org/en-US/docs/Web/API/Fetch_API' }
const URL_API = { label: 'MDN URL API', url: 'https://developer.mozilla.org/en-US/docs/Web/API/URL' }
const NEXT_ROUTE = { label: 'Next.js Route Handlers', url: 'https://nextjs.org/docs/app/getting-started/route-handlers' }
const NODE_HTTP = { label: 'Node.js HTTP', url: 'https://nodejs.org/api/http.html' }
const POSTGRES_SCHEMA = { label: 'PostgreSQL Schemas', url: 'https://www.postgresql.org/docs/current/ddl-schemas.html' }

const p = (name: string, type: string, required: boolean, description: string, defaultValue?: string): ApiParameter => ({ name, type, required, description, defaultValue })
const effect = (title: string, description: string, metrics: ApiEntry['effect']['metrics'], output: ApiEntry['effect']['output']): ApiEntry['effect'] => ({ title, description, metrics, output })
const api = (input: Omit<ApiEntry, 'project' | 'verifiedCommit' | 'statusNote'> & { statusNote?: string }): ApiEntry => ({
  ...input,
  project: 'unified-auth-sdk',
  verifiedCommit: COMMIT,
  statusNote: input.statusNote ?? AI_BOUNDARY,
})
const sdkTest = (proves: string) => ({ path: 'packages/sdk/src/service-client.test.ts', proves })
const hostedTest = (proves: string) => ({ path: 'packages/hosted-service/src/hosted-service.test.ts', proves })
const cliTest = (proves: string) => ({ path: 'packages/hosted-service/src/cli-config.test.ts', proves })
const noDirectTest = (path: string, boundary: string) => ({ path, proves: `${boundary}；固定提交没有覆盖这一运行时边界的独立行为测试，不能据此推断生产可用性。` })

export const unifiedAuthSdkCoreApis: ApiEntry[] = [
  api({
    slug: 'unified-auth-create-service-client',
    name: 'createAuthServiceClient',
    signature: 'createAuthServiceClient(options?: CreateAuthServiceClientOptions) → AuthServiceClient',
    kind: 'function',
    module: 'auth-sdk',
    technology: 'TypeScript ESM + Fetch + cookie session',
    implementationStatus: 'production',
    summary: '解析显式参数、统一配置、环境变量或浏览器 origin，生成只负责身份查询与 hosted login 跳转的黑盒客户端；业务成员关系和权限表仍由业务应用持有。',
    whenToUse: 'AI 应用要接入统一身份服务，但不应把 Better Auth、数据库或 OAuth secret 打进业务前端时。',
    sourcePath: 'packages/sdk/src/service-client.ts',
    sourceLine: 162,
    parameters: [p('authBaseURL/config/env', 'string | config objects', false, '身份服务地址与应用配置，按显式参数→config→env→浏览器 origin 取值。'), p('clientId', 'string', false, '每次 URL 和查询都携带的应用 ID。', 'default'), p('fetcher', 'typeof fetch', false, '可注入测试或服务端 fetch。', 'global fetch')],
    returns: { type: 'AuthServiceClient', description: '包含 7 个公开方法：3 个身份查询、2 个 URL 构造和 2 个浏览器导航；本审计以 7/7 为 SDK 方法分母。' },
    example: { language: 'ts', code: `const auth = createAuthServiceClient({\n  authBaseURL: 'https://auth.example.com',\n  clientId: 'ai-workbench',\n  defaultRedirectURI: 'https://app.example.com/'\n})`, explanation: ['客户端不持有 secret。', '跨源 cookie 查询使用 credentials=include，因此服务端 CORS 与 Cookie 属性必须匹配。'] },
    effect: effect('AI 应用获得统一身份边界', '模型额度、知识库和工具调用可在业务层以 authUserId 归属，而不把业务授权塞进 SDK。', [{ label: '公开客户端方法', value: '7/7' }, { label: '直接 AI API', value: '0' }], [{ label: '成功', value: '稳定 AuthServiceClient', tone: 'good' }, { label: '配置缺失', value: '构造时抛错', tone: 'warn' }]),
    errors: [{ condition: '没有可解析的 auth base URL', behavior: '工厂同步抛出 requires ... 错误。', recovery: '部署启动时校验 origin、clientId 和 redirect URI，禁止静默使用错误租户。' }, { condition: '服务端返回非 2xx', behavior: '查询方法只抛含 HTTP status 的通用 Error。', recovery: '调用层区分 401、403、429 和 5xx，并附 request ID，不回显敏感正文。' }],
    bestPractices: ['SDK 只承载身份，不承载 workspace/project 权限。', '前端只配置公开 base URL/clientId，不注入 OAuth secret。', 'AI 工具执行前在服务端重新校验 session 和资源权限。', '给 fetch 设置超时、取消、重试上限和可观测 request ID。'],
    tests: [sdkTest('验证 URL、client_id、身份查询以及 env/config 两条配置解析路径；没有真实 Cookie/CORS 集成测试。')],
    officialSources: [FETCH, SESSION, BETTER_AUTH_SECURITY],
    related: ['unified-auth-client-navigation', 'unified-auth-client-identity-queries', 'unified-auth-create-server'],
  }),
  api({
    slug: 'unified-auth-client-navigation',
    name: 'getLoginURL / getLogoutURL / login / logout',
    signature: 'getLoginURL(LoginOptions?) / getLogoutURL(LogoutOptions?) / login() / logout() → string',
    kind: 'method',
    module: 'auth-sdk',
    technology: 'WHATWG URL + browser navigation',
    implementationStatus: 'production',
    summary: '为 hosted login/logout 统一追加 client_id、provider 和 redirect_uri；get* 只返回 URL，login/logout 在浏览器中调用 location.assign 后也返回同一 URL。',
    whenToUse: '登录按钮、退出按钮、服务端重定向或测试需要同一套确定性 URL 生成规则时。',
    sourcePath: 'packages/sdk/src/service-client.ts',
    sourceLine: 227,
    parameters: [p('provider', 'AuthProviderId', false, '登录页可直接转入的提供方。'), p('redirectURI', 'string', false, '登录/退出后的回跳地址；hosted service 必须再做白名单校验。')],
    returns: { type: 'string', description: '绝对 hosted-auth URL；SSR 下 login/logout 不导航但仍返回 URL。' },
    example: { language: 'ts', code: `const url = auth.getLoginURL({ provider: 'github', redirectURI: 'https://app.example.com/' })\n// 用户确认后再执行 auth.login(...)`, explanation: ['getLoginURL 适合服务端与可访问链接。', '不要把任意用户输入直接作为 redirectURI。'] },
    effect: effect('可预测的登录跳转', '用户从 AI 应用进入统一登录页，回跳仍保留应用上下文。', [{ label: '覆盖方法', value: '4/4' }, { label: '导航方式', value: 'location.assign' }], [{ label: 'URL', value: 'client_id + redirect_uri', tone: 'good' }, { label: '开放重定向', value: '由服务端防守', tone: 'warn' }]),
    errors: [{ condition: '调用方传入未登记 redirect_uri', behavior: '客户端仍会生成 URL，服务端随后返回 400。', recovery: '客户端使用固定部署配置，服务端始终精确白名单比对。' }, { condition: 'SSR 调用 login/logout', behavior: '不会访问 window，仅返回 URL。', recovery: '服务端用框架 redirect，浏览器事件内才用导航方法。' }],
    bestPractices: ['优先用相对回跳或部署时固定的绝对 URL。', '回跳白名单必须做规范化后精确匹配。', '退出需让 Better Auth 撤销会话，再重定向。'],
    tests: [sdkTest('精确断言 login/logout URL 的 client_id、provider 与 redirect_uri 编码。')],
    officialSources: [URL_API, BETTER_AUTH_SECURITY],
    related: ['unified-auth-create-service-client', 'unified-auth-http-login', 'unified-auth-http-logout'],
  }),
  api({
    slug: 'unified-auth-client-identity-queries',
    name: 'getAuthContext / getCurrentUser / getSession',
    signature: 'getAuthContext() / getCurrentUser() / getSession() → Promise<AuthContext | AuthUser | AuthSession | null>',
    kind: 'method',
    module: 'auth-sdk',
    technology: 'Fetch credentials=include + JSON identity contract',
    implementationStatus: 'production',
    summary: '通过 context、me 和 session 三个 hosted 端点读取当前身份，所有请求都附 client_id 与浏览器 Cookie，但不提供业务 RBAC 判断。',
    whenToUse: '服务端或客户端需要显示当前用户、判断是否有会话，或把 authUserId 传给业务授权层时。',
    sourcePath: 'packages/sdk/src/service-client.ts',
    sourceLine: 195,
    parameters: [],
    returns: { type: 'Promise<AuthContext | AuthUser | AuthSession | null>', description: 'context 同时返回 user/session；匿名时为 null，而非把匿名当网络错误。' },
    example: { language: 'ts', code: `const { user, session } = await auth.getAuthContext()\nif (!user || !session) return showLogin()\nawait loadWorkspaceFor(user.id)`, explanation: ['身份存在不等于具备某个 AI 工具权限。', '授权查询必须在可信服务端完成。'] },
    effect: effect('会话映射到业务主体', 'UI 可展示登录态，后端可用 user.id 查询自己的 workspace/team/project membership。', [{ label: '覆盖查询', value: '3/3' }, { label: 'Cookie', value: 'credentials=include' }], [{ label: '已登录', value: 'user + session', tone: 'good' }, { label: '匿名', value: 'null', tone: 'neutral' }]),
    errors: [{ condition: '跨源 Cookie/CORS 配置不匹配', behavior: '可能得到匿名或请求失败。', recovery: '优先同源部署；跨源时显式配置 Secure、SameSite、credentials 与允许 origin。' }, { condition: '只在前端检查身份后直接调用高权限 AI 工具', behavior: '前端状态可被绕过。', recovery: '工具、模型与数据端点每次都服务端鉴权授权。' }],
    bestPractices: ['把 401/匿名与网络故障分开展示。', '不要缓存敏感 auth context 到 localStorage。', '高风险操作要求 fresh session 或二次验证。', '日志记录 userId/clientId，不记录 Cookie/token。'],
    tests: [sdkTest('验证 me 查询携带 client_id；hosted-service 测试另验证 Better Auth session 到 SDK context 的映射与 Set-Cookie 透传。'), hostedTest('验证 context 的 session/user 字段、metadata 筛选及刷新 Cookie。')],
    officialSources: [SESSION, FETCH, BETTER_AUTH_SECURITY],
    related: ['unified-auth-http-context', 'unified-auth-http-session', 'unified-auth-http-user'],
  }),
  api({
    slug: 'unified-auth-redirect-helpers',
    name: 'sanitizeCallbackURL / readCallbackURL / toAbsoluteUrl',
    signature: 'sanitizeCallbackURL(raw?) → string; readCallbackURL(params) → string; toAbsoluteUrl(pathOrUrl, baseURL?) → string',
    kind: 'function',
    module: 'auth-security',
    technology: 'Open-redirect defense + WHATWG URL',
    implementationStatus: 'production',
    summary: '只接受以单斜杠开头的站内 callback，拒绝协议相对与绝对外站 URL，并在 OAuth 客户端需要时转换成绝对地址。',
    whenToUse: '登录页读取 callbackURL/returnTo 或构造 OAuth 回调地址时。',
    sourcePath: 'packages/sdk/src/redirect.ts',
    sourceLine: 3,
    parameters: [p('raw', 'string | undefined', false, '不可信回跳参数。'), p('baseURL', 'string', false, '把站内路径转换为绝对 URL 的基址；否则浏览器 origin 或 localhost。')],
    returns: { type: 'string', description: '安全站内路径或绝对 URL；非法输入回落 /。' },
    example: { language: 'ts', code: `const callback = sanitizeCallbackURL(search.get('returnTo') ?? undefined)\n// https://evil.example → '/'；//evil.example → '/'`, explanation: ['拒绝开放重定向。', 'toAbsoluteUrl 仍应只接收先净化的路径。'] },
    effect: effect('收口登录回跳', '攻击者不能借应用可信域把用户转到钓鱼站点。', [{ label: 'helper', value: '3/3' }, { label: '危险外站', value: '回落 /' }], [{ label: '站内路径', value: '保留', tone: 'good' }, { label: '绝对外站', value: '拒绝', tone: 'warn' }]),
    errors: [{ condition: '调用方绕过 sanitize 直接 toAbsoluteUrl', behavior: '绝对外站 URL 会原样保留。', recovery: '在边界建立 SafeRedirect 类型，只允许净化后的值进入转换器。' }],
    bestPractices: ['拒绝 //、反斜杠和控制字符等浏览器解析歧义。', '服务端再做允许 origin/path 校验。', '测试编码、双重编码和 Unicode 边界。'],
    tests: [{ path: 'packages/sdk/src/redirect.test.ts', proves: '覆盖站内路径、外站/协议相对拒绝、callbackURL/returnTo 优先级和绝对 URL 构造。' }],
    officialSources: [URL_API, BETTER_AUTH_SECURITY],
    related: ['unified-auth-client-navigation', 'unified-auth-http-login'],
  }),
  api({
    slug: 'unified-auth-create-server',
    name: 'createAuthServer',
    signature: 'createAuthServer(options: CreateAuthServerOptions) → AuthServer',
    kind: 'function',
    module: 'better-auth-runtime',
    technology: 'Better Auth + Drizzle PostgreSQL + admin + Generic OAuth',
    implementationStatus: 'best-effort',
    summary: '把 PostgreSQL Drizzle adapter、realm schema、7 天会话、admin 插件、飞书 Generic OAuth、Google/GitHub social provider 和生产 Cookie 默认值装配成 Better Auth server。',
    whenToUse: 'AI 应用同源嵌入身份服务，或独立 Auth Service 需要统一的 Better Auth 配置入口时。',
    sourcePath: 'packages/sdk/src/server.ts',
    sourceLine: 116,
    parameters: [p('database', 'DrizzleDatabase', true, 'Better Auth 的 PostgreSQL 数据库实例。'), p('secret/baseURL/trustedOrigins', 'string/string[]', false, '会话签名与可信来源；可从统一 config/env 解析。'), p('realmId/schema', 'string | Drizzle schema', false, '身份表隔离命名空间或自定义 schema。'), p('providers', 'Feishu/Google/GitHub options', false, '只有凭据完整的 provider 才启用。')],
    returns: { type: 'AuthServer', description: 'Better Auth handler/API 实例；仓库没有真实数据库、代理与 OAuth 控制台的端到端测试。' },
    example: { language: 'ts', code: `const auth = createAuthServer({ database: db, config })\nexport const { GET, POST } = createHostedAuthRouteHandlers({ auth, config })`, explanation: ['生产环境默认 Secure Cookie 与 trusted proxy headers。', '默认密码注册关闭，但密码登录开启；应确认这符合产品策略。'] },
    effect: effect('集中身份生命周期', '登录、账号绑定、会话刷新和封禁交给 Better Auth，业务只消费稳定身份。', [{ label: '默认 session', value: '7 天' }, { label: '刷新间隔', value: '5 分钟' }, { label: '直接 AI', value: '0' }], [{ label: '已配置 provider', value: '注册', tone: 'good' }, { label: '缺凭据 provider', value: '跳过', tone: 'neutral' }]),
    errors: [{ condition: '生产 secret 缺失、baseURL/trustedOrigins 错配', behavior: '构造或运行期认证失败，可能影响 Cookie/CSRF 判断。', recovery: '启动时强制 secret 长度、HTTPS base URL 和明确 trusted origins。' }, { condition: '默认 localhost 被带入错误环境', behavior: '可能扩大可信 origin。', recovery: '生产配置显式覆盖并在启动自检中拒绝 localhost。' }],
    bestPractices: ['生产 secret 放密钥管理服务并轮换。', '只启用实际使用的 provider 与最小 scope。', '高风险 AI 管理操作配合 fresh session。', '记录认证事件但不记录密码、token 或 OAuth code。'],
    tests: [noDirectTest('packages/sdk/src/server.ts', '源码证明装配选项，schema.test 只验证表结构；未启动真实 Better Auth server')],
    officialSources: [BETTER_AUTH, SESSION, DRIZZLE_ADAPTER, GENERIC_OAUTH, BETTER_AUTH_SECURITY],
    related: ['unified-auth-create-drizzle-schema', 'unified-auth-create-next-handlers', 'unified-auth-build-feishu-provider'],
  }),
  api({
    slug: 'unified-auth-create-drizzle-schema',
    name: 'normalizeAuthRealmId / getAuthRealmSchemaName / createAuthDrizzleSchema',
    signature: 'createAuthDrizzleSchema(realmId?) → { user, session, account, verification }',
    kind: 'function',
    module: 'auth-persistence',
    technology: 'Drizzle ORM pgSchema + PostgreSQL constraints',
    implementationStatus: 'production',
    summary: '把 realm 规整成 auth_* PostgreSQL schema，并创建 Better Auth 期望的 user/session/account/verification 四表、外键、唯一索引与飞书租户字段。',
    whenToUse: '同一数据库承载多个隔离身份域，或 Better Auth Drizzle adapter 需要显式 schema 映射时。',
    sourcePath: 'packages/sdk/src/schema.ts',
    sourceLine: 6,
    parameters: [p('realmId', 'string | undefined', false, '被转小写、非字母数字替换为下划线，并添加 auth_ 前缀。', 'default')],
    returns: { type: 'AuthDrizzleSchema', description: '键名严格为 account/session/user/verification 的 Drizzle 表对象。' },
    example: { language: 'ts', code: `const schema = createAuthDrizzleSchema('AI PM')\n// PostgreSQL namespace: auth_ai_pm`, explanation: ['不同 realm 是表命名空间隔离，不自动等于数据库级租户授权。', '业务表仍应通过 authUserId 关联。'] },
    effect: effect('身份表按 realm 隔离', '不同 AI 产品可共享数据库实例但使用独立 Better Auth 表集。', [{ label: '表', value: '4' }, { label: 'realm 示例', value: 'AI PM → auth_ai_pm' }], [{ label: 'schema keys', value: 'Better Auth 对齐', tone: 'good' }, { label: '隔离层级', value: 'namespace，不是 RLS', tone: 'warn' }]),
    errors: [{ condition: '两个不同原始 realm 归一化为同名', behavior: '会意外共享身份表。', recovery: '部署清单预先检测碰撞，realm 采用稳定短 ID 而非展示名。' }, { condition: '手工迁移与 Drizzle schema 漂移', behavior: '运行时缺列/索引。', recovery: 'CI 比对迁移计划，发布前运行 doctor 和备份演练。' }],
    bestPractices: ['realm 是稳定标识，禁止运行中改名。', '数据库角色只授予所需 auth_* schema。', 'token/refreshToken 列按敏感数据做加密、备份和访问审计。', '业务租户隔离仍需业务表约束或 RLS。'],
    tests: [{ path: 'packages/sdk/src/schema.test.ts', proves: '验证 realm 规整、schema 前缀、四个 Better Auth model key、飞书字段和 impersonatedBy 列。' }],
    officialSources: [DRIZZLE_SCHEMA, DRIZZLE_ADAPTER, POSTGRES_SCHEMA],
    related: ['unified-auth-create-server', 'unified-auth-migrate-database', 'unified-auth-build-database-plan'],
  }),
  api({
    slug: 'unified-auth-create-next-handlers',
    name: 'createNextAuthHandlers',
    signature: 'createNextAuthHandlers(options) → { GET, POST, auth }',
    kind: 'function',
    module: 'framework-adapters',
    technology: 'Web Request/Response + Next.js App Router',
    implementationStatus: 'best-effort',
    summary: '创建一次 AuthServer，并让 GET/POST Route Handler 都直接委托 auth.handler，不把 Next.js 作为运行时依赖。',
    whenToUse: 'Next.js App Router 项目直接暴露 Better Auth 标准路由时；若还要 hosted login/context 语义，应改用 hosted route handlers。',
    sourcePath: 'packages/sdk/src/next.ts',
    sourceLine: 14,
    parameters: [p('options', 'CreateAuthServerOptions', true, '完整 server/database/provider 配置。')],
    returns: { type: 'NextAuthHandlers', description: 'GET、POST 和底层 auth 实例。' },
    example: { language: 'ts', code: `export const { GET, POST, auth } = createNextAuthHandlers({ database: db, config })`, explanation: ['适用于 app/api/auth/[...all]/route.ts。', '它不提供 /login 页面或 client_id 校验。'] },
    effect: effect('框架路由接入', 'Next.js 将标准 Web Request 直接交给 Better Auth。', [{ label: 'handler', value: 'GET + POST' }, { label: 'Next 直接依赖', value: '0' }], [{ label: '标准 auth 路由', value: '可用', tone: 'good' }, { label: 'hosted UI', value: '不包含', tone: 'neutral' }]),
    errors: [{ condition: '部署路径没有捕获全部 Better Auth 子路由', behavior: 'OAuth callback 或 session 端点 404。', recovery: '使用 catch-all Route Handler 并对照 provider 控制台回调。' }],
    bestPractices: ['只实例化一次 auth，避免每请求重复装配。', '保留标准 Request/Response 便于边缘/Node 适配。', '对 callback URL 做部署环境自动化验收。'],
    tests: [noDirectTest('packages/sdk/src/next.ts', '源码只有薄委托，仓库未安装/启动 Next.js 做路由集成验证')],
    officialSources: [NEXT_ROUTE, BETTER_AUTH],
    related: ['unified-auth-create-server', 'unified-auth-create-hosted-route-handlers'],
  }),
  api({
    slug: 'unified-auth-create-browser-client',
    name: 'createAuthClient',
    signature: 'createAuthClient(options?: { baseURL?: string }) → AuthClient',
    kind: 'function',
    module: 'better-auth-runtime',
    technology: 'Better Auth React client + admin/genericOAuth plugins',
    implementationStatus: 'best-effort',
    summary: '创建 Better Auth React 客户端并装配 adminClient、genericOAuthClient 与飞书租户附加字段推断；这是 Auth Service 内部能力，不是黑盒业务客户端首选。',
    whenToUse: '身份服务自己的 React 管理页或登录页需要 Better Auth 原生 hooks/actions 时。',
    sourcePath: 'packages/sdk/src/client.ts',
    sourceLine: 24,
    parameters: [p('baseURL', 'string', false, 'Better Auth origin；浏览器默认当前 origin，SSR 默认 localhost。')],
    returns: { type: 'AuthClient', description: 'Better Auth React client，具体方法由插件和服务端 schema 决定。' },
    example: { language: 'ts', code: `const authClient = createAuthClient({ baseURL: 'https://auth.example.com' })`, explanation: ['业务应用只需 hosted identity 时优先 createAuthServiceClient。', 'admin client 必须配合服务端角色授权。'] },
    effect: effect('原生 Better Auth 客户端能力', 'Auth Service UI 可使用会话 hooks、OAuth 与管理插件。', [{ label: '插件', value: '3' }, { label: 'AI 能力', value: '0' }], [{ label: '类型', value: '附加字段推断', tone: 'good' }, { label: '权限', value: '仍由服务端执行', tone: 'warn' }]),
    errors: [{ condition: '把 adminClient 当成前端授权', behavior: '攻击者仍可直接调用服务端端点。', recovery: '服务端 admin plugin/RBAC 是唯一可信边界。' }, { condition: 'SSR 未传 baseURL', behavior: '回落 localhost:3000。', recovery: '服务端渲染总是显式注入部署 origin。' }],
    bestPractices: ['业务前端与 Auth Service 内部客户端分包。', '不要把 admin UI 的可见性当授权。', '生成客户端与服务端插件保持一致。'],
    tests: [noDirectTest('packages/sdk/src/client.ts', '源码证明插件装配；没有 React DOM、真实服务端或管理权限行为测试')],
    officialSources: [BETTER_AUTH, GENERIC_OAUTH, BETTER_AUTH_SECURITY],
    related: ['unified-auth-create-server', 'unified-auth-sign-in-actions'],
  }),
  api({
    slug: 'unified-auth-sign-in-actions',
    name: 'signInWithGoogle / signInWithFeishu / signInWithEmailPassword / createSignInActions',
    signature: 'createSignInActions(authClient, options?) → { email, feishu, google }',
    kind: 'function',
    module: 'auth-sdk',
    technology: 'Better Auth social/oauth2/email sign-in',
    implementationStatus: 'production',
    summary: '把三种 Better Auth 登录方法压成稳定 action：Google 走 social、飞书走 oauth2、邮箱登录先 trim email；同时集中默认成功/错误回跳。',
    whenToUse: 'Auth Service 自有 UI 需要统一登录按钮事件，而不是使用 hosted login URL 时。',
    sourcePath: 'packages/sdk/src/sign-in.ts',
    sourceLine: 41,
    parameters: [p('authClient', 'SignInClient', true, '同时支持 social/oauth2/email 的 Better Auth client。'), p('callback/error/provider options', 'CreateSignInActionsOptions', false, '默认成功回 /，错误回 /login?error=provider。')],
    returns: { type: 'SignInActions', description: 'email、feishu、google 三个异步 action，返回 Better Auth error envelope。' },
    example: { language: 'ts', code: `const actions = createSignInActions(authClient, { callbackURL: '/workspace' })\nconst result = await actions.feishu()\nif (result.error) showError(result.error.message)`, explanation: ['helper 不会自动抛出 Better Auth 业务错误。', '回调参数仍需走安全净化。'] },
    effect: effect('登录 UI 复用同一契约', '三类按钮拥有一致默认回跳和错误处理入口。', [{ label: 'provider action', value: '3' }, { label: '底层公开 helper', value: '4/4' }], [{ label: '成功', value: '进入 OAuth/会话', tone: 'good' }, { label: 'error envelope', value: '调用方展示', tone: 'warn' }]),
    errors: [{ condition: '调用方忽略 result.error', behavior: 'UI 可能停留无反馈。', recovery: '统一解析错误码，展示安全文案并记录 request ID。' }, { condition: 'callback 是外站绝对 URL', behavior: 'Google helper 会转为绝对地址但不负责白名单。', recovery: '只传净化后的站内 callback，服务端再次校验。' }],
    bestPractices: ['邮箱密码只在明确需要时启用。', '错误文案不泄露账号是否存在。', 'OAuth state/PKCE/callback 验证交给 Better Auth。'],
    tests: [{ path: 'packages/sdk/src/sign-in.test.ts', proves: '验证 email trim、三类 action 默认值，以及 callback/provider 覆盖。' }],
    officialSources: [OAUTH, GENERIC_OAUTH, BETTER_AUTH_SECURITY],
    related: ['unified-auth-create-browser-client', 'unified-auth-build-feishu-provider', 'unified-auth-redirect-helpers'],
  }),
  api({
    slug: 'unified-auth-build-feishu-provider',
    name: 'buildFeishuOAuthProvider',
    signature: 'buildFeishuOAuthProvider(opts: FeishuOAuthProviderOptions) → GenericOAuthConfig',
    kind: 'function',
    module: 'oauth-providers',
    technology: 'Better Auth Generic OAuth + Feishu OAuth v2',
    implementationStatus: 'best-effort',
    summary: '定义飞书授权、token 和 user_info 交换，额外用 tenant_access_token 查询组织名，并把 open_id、邮箱、头像与 tenant metadata 映射到 Better Auth 用户。',
    whenToUse: '企业 AI 应用需要用飞书身份登录，并希望在身份 profile 中保留 tenant key/name 时。',
    sourcePath: 'packages/sdk/src/providers/feishu.ts',
    sourceLine: 111,
    parameters: [p('appId/appSecret', 'string', true, '飞书应用凭据，只能存在服务端。'), p('providerId', 'string', true, 'Better Auth Generic OAuth provider 唯一标识。')],
    returns: { type: 'GenericOAuthConfig', description: '包含 getToken、getUserInfo、scope 与端点的 Better Auth provider 配置。' },
    example: { language: 'ts', code: `const provider = buildFeishuOAuthProvider({\n  appId: env.FEISHU_APP_ID, appSecret: env.FEISHU_APP_SECRET, providerId: 'feishu'\n})`, explanation: ['tenant token 以内存 Map 缓存并提前 60 秒失效。', '缺邮箱时合成 open_id@feishu.local，不能当已验证真实邮箱。'] },
    effect: effect('飞书身份映射', '企业用户完成 OAuth 后，Auth User 带 tenant key/name 可供业务做组织绑定。', [{ label: '默认 scope', value: '2' }, { label: 'tenant token cache', value: '进程内' }], [{ label: '用户资料', value: 'Better Auth User', tone: 'good' }, { label: 'fallback email', value: '未验证', tone: 'warn' }]),
    errors: [{ condition: 'token 交换非 2xx 或无 access_token', behavior: '抛出含飞书 code/msg 的 Error。', recovery: '对外转换为通用错误；内部安全日志记录 provider request ID，不记录 token/code。' }, { condition: 'tenant 查询失败', behavior: '静默降级为无 organizationName。', recovery: '记录降级指标，组织授权不要只依赖可选名称。' }, { condition: '并发多实例', behavior: 'tenant token cache 不共享。', recovery: '接受少量重复请求或使用带 TTL 的共享缓存和 singleflight。' }],
    bestPractices: ['生产凭据来自 secret manager。', 'providerId 与控制台 callback 精确一致。', '只请求最小 scope。', '身份主键使用稳定 open_id/union_id 语义，邮件不作为唯一跨租户主键。'],
    tests: [noDirectTest('packages/sdk/src/providers/feishu.ts', 'hosted-service 测试只验证飞书 start 被映射到 Generic OAuth；未 mock 飞书 token/user/tenant 网络链')],
    officialSources: [GENERIC_OAUTH, BETTER_AUTH_SECURITY],
    related: ['unified-auth-create-server', 'unified-auth-sign-in-actions', 'unified-auth-http-provider-start'],
  }),
  api({
    slug: 'unified-auth-define-config',
    name: 'defineUnifiedAuthConfig / resolveUnifiedAuthConfigValue',
    signature: 'defineUnifiedAuthConfig(config) → config; resolveUnifiedAuthConfigValue(value) → T | undefined',
    kind: 'function',
    module: 'hosted-config',
    technology: 'Typed TypeScript config + lazy secret values',
    implementationStatus: 'production',
    summary: '以 unified-auth.config.ts 作为 app/auth/database/providers/realm 的单一配置源，并允许 secret/database URL 在运行时通过函数延迟读取。',
    whenToUse: '业务应用要让 runtime、CLI migrate 和 doctor 共享同一份声明式配置时。',
    sourcePath: 'packages/hosted-service/src/config.ts',
    sourceLine: 35,
    parameters: [p('config', 'UnifiedAuthConfig', true, '应用 origin/redirect、Auth Service、数据库、provider 与 realm。'), p('lazy value', 'T | (() => T | undefined)', false, '适合在环境文件加载后读取 secret。')],
    returns: { type: 'UnifiedAuthConfig / T | undefined', description: '不修改配置；resolver 在调用时执行函数。' },
    example: { language: 'ts', code: `export default defineUnifiedAuthConfig({\n  app: { id: 'ai-workbench', origin: 'https://app.example.com', redirectURI: 'https://app.example.com/' },\n  auth: { secret: () => process.env.BETTER_AUTH_SECRET },\n  database: { url: () => process.env.DATABASE_URL },\n  providers: ['github'], realm: 'ai-workbench'\n})`, explanation: ['secret 不应写进配置文件。', 'CLI 会先加载环境文件再导入配置。'] },
    effect: effect('配置漂移减少', '应用、路由和数据库工具从相同 app/realm/origin 事实派生。', [{ label: '配置文件', value: '1' }, { label: 'secret 支持', value: 'lazy' }], [{ label: '类型检查', value: '编译期', tone: 'good' }, { label: '运行值校验', value: '仍需 doctor', tone: 'warn' }]),
    errors: [{ condition: 'lazy function 返回 undefined', behavior: '下游到运行时才报缺配置。', recovery: '生产启动先运行强校验/doctor，不把缺 secret 留到用户请求。' }],
    bestPractices: ['配置文件可提交，secret 值不可提交。', 'CI 与本地使用同一解析路径。', 'origin/redirect 使用完整 HTTPS URL。', 'realm/app.id 改动按数据迁移处理。'],
    tests: [cliTest('验证生成、加载、缺配置诊断，以及 .env/.env.local 与 shell env 的优先级。')],
    officialSources: [BETTER_AUTH_SECURITY, POSTGRES_SCHEMA],
    related: ['unified-auth-create-server', 'unified-auth-create-hosted-route-handlers', 'unified-auth-run-cli'],
  }),
  api({
    slug: 'unified-auth-create-hosted-route-handlers',
    name: 'createHostedAuthRouteHandlers / handleHostedAuthRequest',
    signature: 'createHostedAuthRouteHandlers(options) → { GET, POST, handle, service }',
    kind: 'function',
    module: 'hosted-http',
    technology: 'Web Request/Response dispatcher + Better Auth passthrough',
    implementationStatus: 'best-effort',
    summary: '创建 hosted service，并让 GET/POST 共用同一分派器：覆盖 9 个明确路径、1 个 /api/auth/* catch-all 和 7 个路由族。',
    whenToUse: 'Next.js 或任何支持标准 Request/Response 的 AI 业务应用要同源嵌入 hosted auth 时。',
    sourcePath: 'packages/hosted-service/src/hosted-service/routes.ts',
    sourceLine: 13,
    parameters: [p('options.auth', 'HostedBetterAuthServer', true, '底层 Better Auth handler。'), p('app/client/redirect/provider config', 'HostedAuthServiceOptions', false, '单应用 hosted auth 配置。')],
    returns: { type: 'HostedAuthRouteHandlers', description: 'GET、POST、通用 handle 与 service；异常被转换为纯文本 500。' },
    example: { language: 'ts', code: `export const { GET, POST } = createHostedAuthRouteHandlers({ auth, config })`, explanation: ['7/7 路由族均在本文件被本审计覆盖。', '未知路径返回 404。'] },
    effect: effect('同源 hosted auth 网关', '登录页、身份查询、provider start 和 Better Auth callback 使用一个部署 origin。', [{ label: '分派覆盖', value: '9 路径 + 1 catch-all' }, { label: '路由族', value: '7/7' }], [{ label: '已知路由', value: 'service handler', tone: 'good' }, { label: '异常', value: '500 文本', tone: 'warn' }]),
    errors: [{ condition: 'handler 抛出 Error', behavior: '直接把 error.message 写入 500 body。', recovery: '生产返回通用错误码，详细信息只进受控日志并附 correlation ID。' }, { condition: '任意 HTTP method', behavior: 'GET/POST 导出相同 dispatcher，底层分支不显式限制 method。', recovery: '每个自定义路由验证方法，其他返回 405 + Allow。' }],
    bestPractices: ['错误正文不可泄露内部配置。', '在网关加安全头、限流和访问日志。', 'callback 透传必须保留 Set-Cookie。', '显式测试部署 base path 与反向代理 header。'],
    tests: [hostedTest('验证嵌入路由分派、callback passthrough、配置/env fallback 和未知 client 拒绝；未覆盖异常信息脱敏。')],
    officialSources: [BETTER_AUTH, NEXT_ROUTE, BETTER_AUTH_SECURITY],
    related: ['unified-auth-http-login', 'unified-auth-http-context', 'unified-auth-http-auth-passthrough'],
  }),
  api({
    slug: 'unified-auth-http-login',
    name: 'GET /login',
    signature: 'GET /login?client_id&redirect_uri&provider? → HTML | 302 | 400',
    kind: 'http',
    module: 'hosted-http',
    technology: 'Hosted login HTML + redirect allowlist',
    implementationStatus: 'production',
    summary: '校验单应用 client_id 和 redirect_uri；指定受支持 provider 时转到 start 路由，否则渲染启用 provider 的统一登录页。',
    whenToUse: '用户从 AI 应用点击登录，或 SDK getLoginURL 生成可访问入口时。',
    sourcePath: 'packages/hosted-service/src/hosted-service/service.ts',
    sourceLine: 61,
    parameters: [p('client_id', 'string', false, '必须等于已配置应用；缺省使用配置 clientId。'), p('redirect_uri', 'string', false, '必须精确位于 allowedRedirectURIs。'), p('provider', 'feishu | google | github', false, '存在时跳过选择页。'), p('error', 'string', false, '显示在登录页；当前需要额外控制泄露与长度。')],
    returns: { type: '200 text/html | 302 | 400 JSON', description: 'HTML 登录页、provider start 重定向或客户端/回跳校验错误。' },
    example: { language: 'http', code: 'GET /login?client_id=ai-workbench&redirect_uri=https%3A%2F%2Fapp.example.com%2F&provider=github', explanation: ['provider=github 会 302 到 /api/auth/github/start。', 'redirect URI 在真正 OAuth 前已校验。'] },
    effect: effect('用户看到统一登录入口', '页面展示应用品牌与启用的飞书/Google/GitHub provider。', [{ label: 'provider', value: '最多 3' }, { label: 'redirect 校验', value: '精确包含' }], [{ label: '选择页', value: 'HTML', tone: 'good' }, { label: '非法 client/redirect', value: '400', tone: 'warn' }]),
    errors: [{ condition: 'error 查询参数来自上游未净化正文', behavior: '渲染器做 HTML escape，但仍可能显示敏感信息。', recovery: '只传错误码并映射固定用户文案，限制长度。' }, { condition: 'allowedRedirectURIs 为空', behavior: 'isRedirectAllowed 会允许任意 URI。', recovery: '生产配置强制至少一个 HTTPS 白名单，不允许空数组表示全放行。' }],
    bestPractices: ['生产必须配置非空回跳白名单。', '登录页加 CSP、frame-ancestors 和 Referrer-Policy。', 'provider error 只展示安全文案。'],
    tests: [hostedTest('验证默认/定制/自定义登录页、provider 链接、配置派生与显式禁用 provider。')],
    officialSources: [OAUTH, BETTER_AUTH_SECURITY],
    related: ['unified-auth-client-navigation', 'unified-auth-http-provider-start', 'unified-auth-create-login-page-component'],
  }),
  api({
    slug: 'unified-auth-http-logout',
    name: 'GET /logout',
    signature: 'GET /logout?redirect_uri → 302',
    kind: 'http',
    module: 'hosted-http',
    technology: 'Better Auth sign-out + Set-Cookie passthrough',
    implementationStatus: 'best-effort',
    summary: '内部构造 POST /api/auth/sign-out 交给 Better Auth，复制响应头后重定向到 redirect_uri 或 /。',
    whenToUse: '浏览器从业务应用退出统一会话时。',
    sourcePath: 'packages/hosted-service/src/hosted-service/better-auth.ts',
    sourceLine: 33,
    parameters: [p('redirect_uri', 'string', false, '退出后的重定向；当前分支没有复用 allowlist 校验。', '/')],
    returns: { type: '302 Response', description: '携带 Better Auth sign-out 的 Set-Cookie 等响应头。' },
    example: { language: 'http', code: 'GET /logout?redirect_uri=https%3A%2F%2Fapp.example.com%2F', explanation: ['内部真正 sign-out 使用 POST。', '当前 redirect_uri 未校验是需要修复的开放重定向风险。'] },
    effect: effect('会话退出并回到应用', 'Better Auth 清理 Cookie 后浏览器收到 302。', [{ label: '内部 auth 请求', value: 'POST sign-out' }, { label: '外部响应', value: '302' }], [{ label: 'Cookie', value: '透传清理', tone: 'good' }, { label: 'redirect', value: '未白名单校验', tone: 'warn' }]),
    errors: [{ condition: '攻击者构造外站 redirect_uri', behavior: '当前会原样写入 Location，形成开放重定向。', recovery: '与 /login 共用 getRedirectURI + isRedirectAllowed，拒绝或回落安全首页。' }, { condition: 'Better Auth sign-out 返回失败', behavior: '当前仍使用其 headers 并 302，没有检查 response.ok。', recovery: '失败时返回安全错误页并记录退出失败指标。' }],
    bestPractices: ['立即修复退出开放重定向。', '退出操作采用 POST+CSRF 或 SameSite 防护，GET 只做兼容入口。', '验证 Cookie 已撤销后再跳转。'],
    tests: [noDirectTest('packages/hosted-service/src/hosted-service/better-auth.ts', 'hosted 测试未覆盖 logout 成功、失败或恶意 redirect_uri')],
    officialSources: [SESSION, BETTER_AUTH_SECURITY],
    related: ['unified-auth-client-navigation', 'unified-auth-http-login', 'unified-auth-http-auth-passthrough'],
  }),
  api({
    slug: 'unified-auth-http-context',
    name: 'GET /api/auth/context',
    signature: 'GET /api/auth/context?client_id → AuthContext | 400',
    kind: 'http',
    module: 'hosted-http',
    technology: 'Better Auth get-session adapter',
    implementationStatus: 'production',
    summary: '验证 client_id，向 Better Auth /api/auth/get-session 透传 Cookie，将结果压成 { session, user } 的稳定 SDK identity contract，并保留刷新 Set-Cookie。',
    whenToUse: '业务应用一次请求同时需要当前用户和会话时。',
    sourcePath: 'packages/hosted-service/src/hosted-service/better-auth.ts',
    sourceLine: 22,
    parameters: [p('client_id', 'string', false, '必须匹配单应用配置。'), p('Cookie', 'HTTP Cookie', false, 'Better Auth session cookie。')],
    returns: { type: '200 AuthContext | 400 { error }', description: '匿名返回 user/session 为 null；client 错误为 400。' },
    example: { language: 'http', code: 'GET /api/auth/context?client_id=ai-workbench\nCookie: better-auth.session=…', explanation: ['响应可能带刷新后的 Set-Cookie。', 'metadata 排除 Better Auth 常见基础字段，保留扩展字段。'] },
    effect: effect('身份上下文标准化', '业务代码不依赖 Better Auth 原始 session 形状。', [{ label: '下游请求', value: '1 次 get-session' }, { label: '返回对象', value: 'user + session' }], [{ label: '登录', value: '完整 context', tone: 'good' }, { label: '匿名', value: 'null/null', tone: 'neutral' }]),
    errors: [{ condition: 'Better Auth 非 2xx 或返回非 JSON', behavior: '被归一为匿名 null，而不是错误。', recovery: '区分真实匿名与上游故障，故障返回 502/503 并可观测。' }],
    bestPractices: ['身份查询失败不能静默降级成匿名。', 'metadata 建立显式 allowlist，避免新敏感字段自动外泄。', '响应设置 private/no-store。'],
    tests: [hostedTest('验证 Better Auth session/user 映射、metadata 和刷新 Set-Cookie。')],
    officialSources: [SESSION, BETTER_AUTH_SECURITY],
    related: ['unified-auth-client-identity-queries', 'unified-auth-http-session', 'unified-auth-http-user'],
  }),
  api({
    slug: 'unified-auth-http-session',
    name: 'GET /api/auth/session',
    signature: 'GET /api/auth/session?client_id → AuthSession | null | 400',
    kind: 'http',
    module: 'hosted-http',
    technology: 'Better Auth session projection',
    implementationStatus: 'production',
    summary: '读取 Better Auth 会话并只暴露 id、userId、expiresAt 与当前 clientId，隐藏 token、IP、user-agent 等内部字段。',
    whenToUse: '客户端只需要会话存在性/过期时间而不需要用户 profile 时。',
    sourcePath: 'packages/hosted-service/src/hosted-service/better-auth.ts',
    sourceLine: 70,
    parameters: [p('client_id', 'string', false, '应用上下文。'), p('Cookie', 'HTTP Cookie', false, 'Better Auth session cookie。')],
    returns: { type: '200 AuthSession | null | 400', description: '没有有效 userId 时返回 null；expiresAt 统一为 ISO string/null。' },
    example: { language: 'http', code: 'GET /api/auth/session?client_id=ai-workbench', explanation: ['不返回 session token。', '会话存在仍不代表具备业务资源权限。'] },
    effect: effect('最小会话视图', '前端能判断登录态与过期时间，减少不必要 profile 暴露。', [{ label: '公开字段', value: '4' }, { label: 'token 暴露', value: '0' }], [{ label: '有效', value: 'AuthSession', tone: 'good' }, { label: '无效', value: 'null', tone: 'neutral' }]),
    errors: [{ condition: '上游 get-session 故障', behavior: '当前可能静默返回 null。', recovery: '与匿名分开建模并记录上游 status/latency。' }],
    bestPractices: ['使用 no-store。', '高风险操作重新服务端校验，不信任此客户端投影。', '过期时间只作 UI 提示，真实有效性由服务端判定。'],
    tests: [hostedTest('context 映射测试间接验证 session 字段与 expiresAt；没有 session 独立端点断言。')],
    officialSources: [SESSION, BETTER_AUTH_SECURITY],
    related: ['unified-auth-client-identity-queries', 'unified-auth-http-context'],
  }),
  api({
    slug: 'unified-auth-http-user',
    name: 'GET /api/auth/me | GET /api/auth/user',
    signature: 'GET /api/auth/{me|user}?client_id → AuthUser | null | 400',
    kind: 'http',
    module: 'hosted-http',
    technology: 'Better Auth user projection',
    implementationStatus: 'production',
    summary: '两个兼容路径共用同一 handler，从 session user 投影 id、name、email、avatarUrl 和额外 metadata。',
    whenToUse: '业务 UI 只需要当前身份 profile；新代码优先使用 /me，/user 作为兼容别名。',
    sourcePath: 'packages/hosted-service/src/hosted-service/routes.ts',
    sourceLine: 57,
    parameters: [p('client_id', 'string', false, '应用上下文。'), p('Cookie', 'HTTP Cookie', false, 'Better Auth session cookie。')],
    returns: { type: '200 AuthUser | null | 400', description: '匿名为 null；扩展字段进入 metadata。' },
    example: { language: 'http', code: 'GET /api/auth/me?client_id=ai-workbench', explanation: ['/api/auth/user 返回相同结果。', 'email 可能为空或未验证，不能天然作为授权主键。'] },
    effect: effect('最小用户 profile', 'AI 应用显示头像/名称，并用稳定 id 关联业务权限与用量。', [{ label: '兼容路径', value: '2' }, { label: '主键', value: 'user.id' }], [{ label: '登录', value: 'AuthUser', tone: 'good' }, { label: '匿名', value: 'null', tone: 'neutral' }]),
    errors: [{ condition: 'Better Auth 新增敏感 user 扩展字段', behavior: '当前过滤基础字段后其余全部进入 metadata。', recovery: '改成显式 metadata allowlist，例如只允许 feishuTenantKey/Name。' }],
    bestPractices: ['授权关联使用 user.id。', 'emailVerified=false 时不要信任邮箱归属。', 'PII 响应使用 no-store 并限制日志。'],
    tests: [hostedTest('context 测试验证 user 字段和 metadata；路由分派测试覆盖 embedded handler，但未分别断言 /me 与 /user。')],
    officialSources: [SESSION, BETTER_AUTH_SECURITY],
    related: ['unified-auth-client-identity-queries', 'unified-auth-http-context'],
  }),
  api({
    slug: 'unified-auth-http-provider-start',
    name: 'GET /api/auth/{feishu|google|github}/start',
    signature: 'GET /api/auth/:provider/start?client_id&redirect_uri → 302 | 400',
    kind: 'http',
    module: 'hosted-http',
    technology: 'OAuth initiation + Better Auth social/generic OAuth',
    implementationStatus: 'production',
    summary: '覆盖 3/3 provider start 路径：先校验 client 与回跳白名单/启用状态，再把飞书映射到 sign-in/oauth2、Google/GitHub 映射到 sign-in/social，保留 state Cookie 后 302。',
    whenToUse: '统一登录页的 provider 按钮开始 OAuth 流程时。',
    sourcePath: 'packages/hosted-service/src/hosted-service/better-auth.ts',
    sourceLine: 42,
    parameters: [p('provider', 'feishu | google | github', true, '由路径确定。'), p('client_id', 'string', false, '必须匹配配置。'), p('redirect_uri', 'string', false, '必须在白名单。')],
    returns: { type: '302 provider authorization URL | 400 JSON', description: '成功保留 Better Auth Set-Cookie；上游失败重定向回登录页并携带 error。' },
    example: { language: 'http', code: 'GET /api/auth/github/start?client_id=ai-workbench&redirect_uri=https%3A%2F%2Fapp.example.com%2F', explanation: ['GitHub 默认请求 read:user 与 user:email。', 'Google 默认 openid/email/profile。'] },
    effect: effect('OAuth state 流程启动', '用户被重定向至提供方，Better Auth state Cookie 随响应返回。', [{ label: 'provider path', value: '3/3' }, { label: '回跳校验', value: 'start 前' }], [{ label: '成功', value: '302 provider URL', tone: 'good' }, { label: '非法 client/redirect', value: '400', tone: 'warn' }]),
    errors: [{ condition: 'Better Auth 返回的 error/message 含内部信息', behavior: '当前拼进 /login?error=...。', recovery: '只传错误码，详细 provider 正文仅写受控日志。' }, { condition: '默认 scope 超出业务需要', behavior: '用户授权面扩大。', recovery: '按 provider 和用途显式最小 scope，并定期审计。' }],
    bestPractices: ['OAuth state、PKCE 与 callback 验证交给成熟库。', '保留多个 Set-Cookie，不要错误合并。', 'provider start 加 IP/账号维度速率限制。', '回调控制台 URL 与部署 origin 自动比对。'],
    tests: [hostedTest('分别验证 GitHub/Google social body、默认 scope、飞书 generic OAuth provider mapping、Set-Cookie 和非法 client。')],
    officialSources: [OAUTH, GENERIC_OAUTH, BETTER_AUTH_SECURITY],
    related: ['unified-auth-http-login', 'unified-auth-build-feishu-provider', 'unified-auth-http-auth-passthrough'],
  }),
  api({
    slug: 'unified-auth-http-auth-passthrough',
    name: 'ANY /api/auth/*',
    signature: 'Request /api/auth/* → auth.handler(request)',
    kind: 'http',
    module: 'hosted-http',
    technology: 'Better Auth catch-all callback/session routes',
    implementationStatus: 'production',
    summary: '自定义 context/provider 路由之后的 catch-all，把 Better Auth 标准登录、callback、session 与插件端点原样委托给底层 handler。',
    whenToUse: 'OAuth provider 回调和 Better Auth 客户端调用标准 API 时；应用不应自行复制 callback 验证。',
    sourcePath: 'packages/hosted-service/src/hosted-service/routes.ts',
    sourceLine: 63,
    parameters: [p('request', 'Request', true, '完整 method、URL、headers 和 body。')],
    returns: { type: 'Response', description: 'Better Auth 原始响应，包括状态、body 与 Set-Cookie。' },
    example: { language: 'http', code: 'GET /api/auth/oauth2/callback/feishu?code=…&state=…', explanation: ['飞书 Generic OAuth callback 和 Google/GitHub callback 都走 passthrough。', '实际端点集合由 Better Auth 配置/插件决定，不应硬编码为本仓库自建逻辑。'] },
    effect: effect('成熟认证协议留在库内', 'state 校验、code 交换、账号绑定和 session cookie 由 Better Auth 完成。', [{ label: '自建 callback', value: '0' }, { label: '透传测试 callback', value: '2' }], [{ label: '协议处理', value: 'Better Auth', tone: 'good' }, { label: '暴露面', value: '随插件变化', tone: 'warn' }]),
    errors: [{ condition: '启用 admin 或其他插件后新增端点', behavior: 'catch-all 会自动暴露。', recovery: '维护路由资产清单、权限测试和升级差异审计。' }, { condition: '反向代理改写 host/proto', behavior: 'callback URL、Secure Cookie 或 origin 校验可能错误。', recovery: '只信任受控 proxy headers，并做真实部署 E2E。' }],
    bestPractices: ['Better Auth 版本升级先审计新增端点。', '对敏感端点做速率限制与审计日志。', '完整透传 Set-Cookie。', 'OAuth code/state/token 禁止进入普通日志。'],
    tests: [hostedTest('验证飞书 generic OAuth 与 Google callback 两条路径原样到达底层 handler。')],
    officialSources: [BETTER_AUTH, OAUTH, GENERIC_OAUTH, BETTER_AUTH_SECURITY],
    related: ['unified-auth-create-hosted-route-handlers', 'unified-auth-http-provider-start', 'unified-auth-http-logout'],
  }),
  api({
    slug: 'unified-auth-create-login-page-component',
    name: 'renderLoginPage / createHostedAuthLoginPageComponent / defaultHostedAuthLoginPageComponent',
    signature: 'createHostedAuthLoginPageComponent(defaultConfig?) → (props) => string',
    kind: 'function',
    module: 'hosted-ui',
    technology: 'Server-rendered HTML + explicit escaping',
    implementationStatus: 'production',
    summary: '把品牌、hero、panel、provider 顺序、背景和 footer 配置合成可复用服务端 HTML component；也允许调用方接管整个字符串渲染。',
    whenToUse: 'hosted auth 需要业务品牌但仍复用 SDK 生成的 provider href、clientId 和 redirect URI 模型时。',
    sourcePath: 'packages/hosted-service/src/hosted-service/login-page/index.ts',
    sourceLine: 28,
    parameters: [p('defaultConfig', 'HostedAuthLoginPageConfig', false, '品牌、背景、主 provider 与文案。'), p('props.model', 'HostedAuthLoginPageModel', true, '服务端生成的可信路由模型。')],
    returns: { type: 'HTML string / LoginPageComponent', description: '完整登录文档或可复用 component。' },
    example: { language: 'ts', code: `const loginPageComponent = createHostedAuthLoginPageComponent({\n  brandName: 'AI Workbench', primaryProvider: 'github', providers: ['github']\n})`, explanation: ['provider href 仍由 SDK 生成。', '自定义 component 必须自行 escape 所有不可信字符串。'] },
    effect: effect('可品牌化统一登录页', '用户看到与 AI 产品一致的品牌，同时认证协议仍保持黑盒。', [{ label: '内置 provider', value: '3' }, { label: '渲染位置', value: '服务端' }], [{ label: '默认 component', value: '转义 HTML', tone: 'good' }, { label: '自定义 component', value: '调用方负责安全', tone: 'warn' }]),
    errors: [{ condition: '自定义 component 直接插入 error/appName', behavior: '可能产生 XSS。', recovery: '提供安全模板 API/escaping helper，并用 CSP 作为纵深防御。' }, { condition: '远程背景/logo 未受控', behavior: '可能泄露访问或加载恶意内容。', recovery: '限制可信 CDN origin，设置 CSP img-src。' }],
    bestPractices: ['登录页优先安全与可信感，不放第三方脚本。', '自定义渲染必须 escape。', '加 CSP、frame-ancestors、nosniff 与 Referrer-Policy。', '保持键盘可达与清晰错误态。'],
    tests: [hostedTest('验证默认、配置化和完全自定义三种渲染，provider 筛选、背景/Logo 与链接。')],
    officialSources: [BETTER_AUTH_SECURITY],
    related: ['unified-auth-http-login', 'unified-auth-http-provider-start'],
  }),
  api({
    slug: 'unified-auth-create-node-server',
    name: 'createHostedAuthNodeServer',
    signature: 'createHostedAuthNodeServer(options: HostedAuthNodeServerOptions) → http.Server',
    kind: 'function',
    module: 'framework-adapters',
    technology: 'node:http ↔ Web Request/Response adapter',
    implementationStatus: 'best-effort',
    summary: '把 IncomingMessage 转为标准 Request，交给 hosted handlers，再把 status/headers/body 缓冲回 ServerResponse。',
    whenToUse: '不依赖 Next.js 的独立 Node Auth Service 原型或简单部署。',
    sourcePath: 'packages/hosted-service/src/hosted-service-node.ts',
    sourceLine: 39,
    parameters: [p('options', 'HostedAuthNodeServerOptions', true, 'hosted service 选项；port 字段由调用方 listen 时自行使用。')],
    returns: { type: 'node:http.Server', description: '尚未 listen 的 Node server。' },
    example: { language: 'ts', code: `createHostedAuthNodeServer({ auth, config }).listen(config.auth?.port ?? 3004)`, explanation: ['adapter 读取整个 Response body 到内存。', '请求 body 当前没有从 IncomingMessage 传入 Request，是 POST 流程的关键缺口。'] },
    effect: effect('独立 Node 运行入口', '标准 hosted handlers 可以在原生 Node HTTP 上启动。', [{ label: '框架依赖', value: '0' }, { label: '响应模式', value: '全量 buffer' }], [{ label: 'GET', value: '可委托', tone: 'good' }, { label: 'POST body', value: '当前丢失', tone: 'warn' }]),
    errors: [{ condition: 'Better Auth POST 需要请求 body', behavior: 'toRequest 没有读取 req body，登录/回调相关 POST 可能失败。', recovery: '按 method 流式桥接 body，并设置 duplex/大小上限。' }, { condition: '大响应或慢请求', behavior: 'arrayBuffer 全量占内存且无超时。', recovery: '使用流式 pipe、server timeouts、body limits 和 abort 传播。' }, { condition: '伪造 x-forwarded-proto', behavior: '未判断可信代理就用于 URL。', recovery: '只在受控代理后信任该头，否则使用服务端配置 origin。' }],
    bestPractices: ['修复请求 body 桥接后再用于生产。', '设置 headers/request/keepAlive timeout。', '限制请求/响应体并处理客户端断开。', '生产 TLS 放在受控网关并校验代理头。'],
    tests: [noDirectTest('packages/hosted-service/src/hosted-service-node.ts', '仓库没有启动 Node server 的 GET/POST、body、Set-Cookie 或代理头集成测试')],
    officialSources: [NODE_HTTP, FETCH, BETTER_AUTH_SECURITY],
    related: ['unified-auth-create-hosted-route-handlers', 'unified-auth-http-auth-passthrough'],
  }),
  api({
    slug: 'unified-auth-run-cli',
    name: 'runCli / unified-auth init / doctor / db migrate',
    signature: 'runCli(argv: string[], output?) → Promise<0 | 1>',
    kind: 'workflow',
    module: 'auth-cli',
    technology: 'Node CLI + TypeScript config loader',
    implementationStatus: 'best-effort',
    summary: '解析 init、doctor、db migrate 和 --cwd/--config：init 只生成统一配置，doctor 汇总配置/数据库检查，migrate 执行增量计划并用退出码表达结果。',
    whenToUse: '首次接入、部署前自检和显式数据库迁移阶段。',
    sourcePath: 'packages/hosted-service/src/cli/index.ts',
    sourceLine: 27,
    parameters: [p('argv', 'string[]', true, '不含 node/bin 的 CLI 参数。'), p('output', 'console-like', false, '可注入测试日志收集器。', 'console')],
    returns: { type: 'Promise<0 | 1>', description: '成功/帮助为 0，配置、迁移或未知命令失败为 1。' },
    example: { language: 'bash', code: `unified-auth init\nunified-auth doctor\nunified-auth db migrate`, explanation: ['init 不生成 .env 文件。', 'doctor 只有 config.database.url 存在才连接数据库。'] },
    effect: effect('认证部署可检查', '开发者看到配置、provider、数据库对象与待迁移动作，而不是等登录请求失败。', [{ label: '命令', value: '3/3' }, { label: '退出码', value: '0/1' }], [{ label: '自检', value: '[ok]/[warn]/[fail]', tone: 'good' }, { label: '真实 OAuth', value: '不检查', tone: 'warn' }]),
    errors: [{ condition: 'doctor 未配置 database.url', behavior: '只做静态配置检查，仍可能退出 0。', recovery: '生产 profile 强制数据库与 secret readiness。' }, { condition: '动态 import 配置执行任意代码', behavior: 'CLI 以当前用户权限运行配置。', recovery: '只运行受信仓库配置，CI 依赖审查并限制 secret 输出。' }],
    bestPractices: ['部署前强制 doctor。', '迁移与应用启动分离。', '日志永不输出连接串密码/secret。', '增加真实 Auth Service health 与 provider callback smoke test。'],
    tests: [cliTest('覆盖 init、doctor、env 优先级、缺配置和数据库计划；没有真实 PostgreSQL/OAuth 集成。')],
    officialSources: [POSTGRES_SCHEMA, BETTER_AUTH_SECURITY],
    related: ['unified-auth-define-config', 'unified-auth-migrate-database', 'unified-auth-build-database-plan'],
  }),
  api({
    slug: 'unified-auth-migrate-database',
    name: 'migrateUnifiedAuthDatabase',
    signature: 'migrateUnifiedAuthDatabase(config) → Promise<AuthDatabaseMigrationResult>',
    kind: 'workflow',
    module: 'auth-cli',
    technology: 'PostgreSQL introspection + additive DDL',
    implementationStatus: 'best-effort',
    summary: '连接 PostgreSQL、检查 realm schema 与四张 Better Auth 表，拒绝不兼容类型/可空性，再顺序执行缺失 schema/table/column/constraint/index 的 DDL。',
    whenToUse: '部署新 realm 或 SDK schema 升级，需要幂等补齐认证数据库对象时。',
    sourcePath: 'packages/hosted-service/src/cli/database.ts',
    sourceLine: 156,
    parameters: [p('config.database.url', 'string | () => string', true, 'PostgreSQL 连接串。'), p('config.realm/app.id', 'string', false, '决定 auth_* schema 名。')],
    returns: { type: 'Promise<AuthDatabaseMigrationResult>', description: 'schemaName、actions、warnings 与 changed；不维护迁移版本表。' },
    example: { language: 'ts', code: `const result = await migrateUnifiedAuthDatabase(config)\nconsole.log(result.schemaName, result.changed)`, explanation: ['发现不兼容现有结构会在执行前抛错。', '缺失对象使用 IF NOT EXISTS 与 introspection 组合。'] },
    effect: effect('幂等补齐 auth schema', '新环境创建四表/索引/外键，已有兼容环境不重复变更。', [{ label: '表规范', value: '4' }, { label: '迁移记录表', value: '0' }], [{ label: '兼容缺失', value: '添加', tone: 'good' }, { label: '不兼容结构', value: '拒绝', tone: 'warn' }]),
    errors: [{ condition: '中途某条 DDL 失败', behavior: '没有显式事务，前序动作可能已提交。', recovery: '在 PostgreSQL 事务内执行可事务 DDL，并记录计划/已执行动作。' }, { condition: '多个实例并发 migrate', behavior: '可能竞争约束/索引创建。', recovery: '使用 advisory lock 或部署单例迁移作业。' }, { condition: '数据库连接串缺失', behavior: '构造 client 前抛错。', recovery: 'secret readiness 与连通性在部署前验证。' }],
    bestPractices: ['迁移前备份和 dry-run 展示计划。', '使用事务/advisory lock。', '数据库账号最小 DDL 权限，运行时账号不持有 DDL 权限。', 'SDK schema 与 CLI spec 建立自动差异测试。'],
    tests: [cliTest('纯函数计划覆盖缺 schema/table/column/index/constraint；没有真实 PostgreSQL 事务与失败恢复测试。')],
    officialSources: [POSTGRES_SCHEMA, DRIZZLE_ADAPTER],
    related: ['unified-auth-create-drizzle-schema', 'unified-auth-build-database-plan', 'unified-auth-run-cli'],
  }),
  api({
    slug: 'unified-auth-build-database-plan',
    name: 'buildAuthDatabasePlan / doctorUnifiedAuthDatabase',
    signature: 'buildAuthDatabasePlan(schemaName, inspection) → AuthDatabasePlan',
    kind: 'workflow',
    module: 'auth-cli',
    technology: 'PostgreSQL information_schema/pg_catalog drift detection',
    implementationStatus: 'best-effort',
    summary: '把数据库快照与四表规范比较：缺失对象转为可执行 action，类型/可空性冲突转为 warning；doctor 再将连接、schema、table 和 pending action 转成 pass/warn/fail。',
    whenToUse: '迁移前生成确定性计划，或部署检查认证库是否与 SDK 预期一致时。',
    sourcePath: 'packages/hosted-service/src/cli/database.ts',
    sourceLine: 182,
    parameters: [p('schemaName', 'string', true, '已规整 auth_* 名。'), p('inspection', 'AuthDatabaseInspection', true, 'schema、table、columns、indexes、constraints 的当前快照。')],
    returns: { type: 'AuthDatabasePlan / Promise<DoctorCheck[]>', description: '动作、警告与检查清单；不会自动判断数据内容或权限策略。' },
    example: { language: 'ts', code: `const plan = buildAuthDatabasePlan('auth_ai_workbench', inspection)\nif (plan.warnings.length) throw new Error('manual migration required')`, explanation: ['缺列会产生 ADD COLUMN IF NOT EXISTS。', '现有类型/nullable 不兼容只警告，不自动破坏性修改。'] },
    effect: effect('数据库漂移在上线前可见', '团队能看到待创建对象和需要人工处理的不兼容结构。', [{ label: '检查对象', value: 'schema + 4 tables' }, { label: '破坏性自动修改', value: '0' }], [{ label: '缺失对象', value: 'action', tone: 'good' }, { label: '冲突对象', value: 'warning/fail', tone: 'warn' }]),
    errors: [{ condition: '规范只比较类型与 nullable', behavior: '默认值、外键动作、索引定义变化可能漏检。', recovery: '扩展 inspection 到默认值、约束表达式、唯一性和 on delete。' }, { condition: 'doctor 能连库但运行时账号不同', behavior: '检查通过不代表应用实际可读写。', recovery: 'doctor 使用与应用相同角色，另设专用迁移角色。' }],
    bestPractices: ['计划生成保持纯函数并快照测试。', '输出 DDL 前做标识符转义。', 'doctor 区分结构健康、权限健康与 OAuth 健康。', '将检查结果接入发布门禁和可观测告警。'],
    tests: [cliTest('验证全新数据库与部分旧表两种 inspection 的 action/warning 结果；真实 catalog 查询未集成测试。')],
    officialSources: [POSTGRES_SCHEMA, DRIZZLE_ADAPTER],
    related: ['unified-auth-migrate-database', 'unified-auth-run-cli', 'unified-auth-create-drizzle-schema'],
  }),
]
