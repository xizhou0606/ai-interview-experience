import { defineInterviewHttp as d, err, p, SNAPSHOT_TEST, SNAPSHOT_BOUNDARY_TEST, ROUND_DAO_TEST, RECORDING_ROUTE_TEST, REPORT_ROUTE_TEST, TRANSITION_TEST, DRIZZLE_TX, S3, HTTP_CACHE, OWASP_IDOR } from './http-studio-interviews-shared'
import type { ApiEntry } from '../types'

const round = p('id', 'path roundId', true, '轮次 ID；先联合 organizationId 与 recruiting visibility 解析候选人。')
const auth = err('session、activeOrg 或所需权限缺失', '返回 401/403，业务读写不会执行。', '恢复有效工作区会话并申请最小权限。')

export const httpStudioInterviewCoreApisB: ApiEntry[] = [
  d({
    slug: 'get-studio-question-template-bindings-http', method: 'GET', path: '/api/w/:slug/studio/interviews/:id/question-template-bindings', source: 'detail-route.ts', line: 459, permission: 'interview.read',
    summary: '把 roundId 解析为候选人，先懒挂载该岗位当前适用但尚未绑定的题库模板，再返回候选人级模板绑定及启用状态。', when: '面试详情题库区需要展示完整适用模板，包括在候选人创建后新增的模板时。', parameters: [round], returnType: '200 InterviewQuestionTemplateBinding[]', returnDescription: '不可见轮次 404；成功前可能产生新的默认绑定写入。', exampleNotes: ['GET 具有 lazy-bind 写副作用，不应由爬虫或预取无意触发。', '该读取不自动刷新冻结 context snapshot。'], effectTitle: '补齐并展示题库绑定', effectDescription: '新建的适用模板会出现在配置区，但既有运行快照保持不变。', metrics: [{ label: 'read side effect', value: 'ensure bindings' }, { label: 'runtime snapshot', value: 'unchanged' }], outputs: [{ label: 'applicable templates', value: 'binding list', tone: 'good' }, { label: 'snapshot drift', value: 'explicit refresh needed', tone: 'neutral' }], errors: [auth, err('轮次不可见', '返回 404，不执行懒绑定。', '从可见详情进入。'), err('并发首次懒绑定', '若缺少唯一约束可能重复插入。', '对 interviewId+templateId 建唯一约束并使用 upsert。')], practices: ['把有写副作用的 ensure 拆到显式命令或保证严格幂等。', '清晰区分“当前绑定”与“冻结运行快照”。'], test: SNAPSHOT_TEST, proves: 'core 快照测试证明模板版本会冻结且 refresh 才 supersede；本 GET 的 lazy bind/upsert 与返回 DTO 无直测。', sources: [DRIZZLE_TX], related: ['put-studio-question-template-bindings-http', 'refresh-studio-interview-context-http'] }),
  d({
    slug: 'put-studio-question-template-bindings-http', method: 'PUT', path: '/api/w/:slug/studio/interviews/:id/question-template-bindings', source: 'detail-route.ts', line: 531, permission: 'interview.update',
    summary: '把 roundId 解析为候选人，在事务中用 enabledTemplateIds 全量替换题库绑定，并按候选人岗位校验适用性后回读绑定。', when: '用户保存完整的启用模板集合，而不是增量切换单个模板时。', parameters: [round, p('enabledTemplateIds', 'JSON string[]', true, '全量集合；每项非空，未包含的现有绑定会被禁用/替换。')], returnType: '200 InterviewQuestionTemplateBinding[]', returnDescription: 'body 缺失/非法 400；候选人不可见 404。', exampleNotes: ['PUT 是全量替换，客户端必须携带所有保留项。', '保存绑定不会自动刷新 context snapshot，需另行显式确认。'], effectTitle: '保存完整题库选择', effectDescription: '配置区反映新绑定集合；正在使用的冻结上下文不静默变化。', metrics: [{ label: 'semantics', value: 'full replace' }, { label: 'transaction', value: 'binding set' }], outputs: [{ label: 'valid set', value: 'updated bindings', tone: 'good' }, { label: 'omitted id', value: 'disabled/removed', tone: 'neutral' }], errors: [auth, err('enabledTemplateIds 缺失或含空 ID', 'validator 返回 400。', '提交去重后的完整非空 ID 数组。'), err('并发编辑题库集合', '后保存者全量覆盖先保存者。', '增加版本号/If-Match，并在 409 时合并。')], practices: ['全量 PUT 配合乐观并发版本。', '模板 ID 必须服务端验证组织、岗位 scope 与存在性。'], test: SNAPSHOT_TEST, proves: 'core 测试证明快照冻结模板版本；没有直测 replaceInterviewBindings 的跨租户拒绝、全量替换或并发覆盖。', sources: [DRIZZLE_TX, OWASP_IDOR], related: ['get-studio-question-template-bindings-http', 'refresh-studio-interview-context-http'] }),
  d({
    slug: 'get-studio-interview-recording-http', method: 'GET', path: '/api/w/:slug/studio/interviews/:id/recordings/:conversationId', source: 'routes/recordings/route.ts', line: 13, permission: 'interview.read',
    summary: '验证 round 可见、conversation 属于同一 round 且录像状态 completed 后，签发 600 秒 S3 GET 预签名 URL；静态 manifest 将其误标成 binary，实际是 JSON。', when: '详情页准备播放已完成录制的视频，需要短期对象访问能力时。', parameters: [round, p('conversationId', 'path string', true, '必须同时匹配当前组织与 roundId。')], returnType: '200 { url, expiresInSeconds:600 } JSON', returnDescription: '跨轮次/无文件 404，尚未完成 409，签名失败 500；不是二进制响应。', exampleNotes: ['先读取 JSON，再把 url 交给 video；不要把 endpoint 本身设为 src。', 'URL 是 bearer capability，不写入日志或长期缓存。'], effectTitle: '短期播放面试录像', effectDescription: 'completed 录像获得十分钟播放 URL；处理中状态显示稍后重试。', metrics: [{ label: 'transport', value: 'JSON (manifest corrected)' }, { label: 'URL TTL', value: '600s' }], outputs: [{ label: 'completed', value: 'presigned URL', tone: 'good' }, { label: 'processing', value: '409 + status', tone: 'neutral' }, { label: 'foreign conversation', value: '404', tone: 'warn' }], errors: [auth, err('conversation 不属于 round、对象 key 缺失', '统一 404。', '刷新报告/会话列表，不允许跨轮猜测。'), err('recordingStatus 非 completed', '409 并返回当前状态。', '按退避策略轮询或由事件推送刷新。'), err('S3 签名异常', '脱敏 500，内部记录 round/conversation。', '修复凭据/对象存储后重新签发。')], practices: ['预签名 URL 短 TTL、HTTPS、最小对象权限并从遥测脱敏。', '轮询加指数退避和终止状态。'], test: RECORDING_ROUTE_TEST, proves: 'direct 路由测试覆盖 200 JSON/600 秒、跨轮 404、跨组织时不读存储、未完成 409；签名 500 未覆盖。', technology: 'Hono 4.12.23 + AWS SDK S3 presigning', sources: [S3], related: ['get-studio-interview-reports-http', 'presign-recording-url'] }),
  d({
    slug: 'get-studio-interview-reports-http', method: 'GET', path: '/api/w/:slug/studio/interviews/:id/reports', source: 'routes/reports/route.ts', line: 9, permission: 'interview.read',
    summary: '先用 roundId 验证组织与招聘可见域，再按 scheduleEntryId 读取该轮全部 conversation 报告，并显式包含上下文快照元数据。', when: '详情页需要展示某一轮的多次会话评分、摘要与使用的 snapshot 版本时。', parameters: [round], returnType: '200 InterviewConversationReport[]', returnDescription: 'round 不存在/不可见 404；没有报告返回空数组。', exampleNotes: ['报告按 round 而不是候选人过滤，避免把其他轮次混入。', 'snapshot metadata 用于解释同候选人不同轮结果差异。'], effectTitle: '展示单轮 AI 报告历史', effectDescription: '用户看到该轮所有 conversation 报告与可追溯快照版本。', metrics: [{ label: 'scope key', value: 'scheduleEntryId' }, { label: 'snapshot metadata', value: 'included' }], outputs: [{ label: 'completed conversations', value: 'report list', tone: 'good' }, { label: 'none', value: '[]', tone: 'neutral' }], errors: [auth, err('round 不存在、跨组织或不可见', '在查询报告前返回 404。', '从当前列表打开详情。')], practices: ['报告查询必须先做对象级授权。', 'PII/模型原始输出使用字段白名单、保留策略与读取审计。'], test: REPORT_ROUTE_TEST, proves: 'direct 路由测试覆盖同组织 round 报告查询和跨组织 404；DAO 测试另证 snapshot metadata 仅显式请求时返回。', sources: [OWASP_IDOR], related: ['get-studio-interview-recording-http', 'get-studio-interview-round-http'] }),
  d({
    slug: 'reset-studio-interview-round-http', method: 'POST', path: '/api/w/:slug/studio/interviews/:id/reset', source: 'route.ts', line: 229, permission: 'interview.update',
    summary: '只允许候选人仍在 ai_interview 阶段时重置任意状态轮次；事务清空 conversation/LiveKit 热重连锚点、设 pending、刷新题库绑定与上下文快照并写 round_reset 审计。', when: '一次 AI 面试需从干净房间和最新上下文重新开始时。', parameters: [round], returnType: '200 StudioInterviewRoundDetail', returnDescription: 'round/candidate 404；候选人不在 AI 阶段实际返回 409。', exampleNotes: ['重置会丢弃旧会话锚点，UI 必须确认。', '成功回读含新 snapshot 与 pending 状态。'], effectTitle: '把轮次恢复为可重新发起', effectDescription: '旧 LiveKit/会话锚点清空，新上下文快照与审计原子生成。', metrics: [{ label: 'status', value: 'pending' }, { label: 'snapshot reason', value: 'reset' }], outputs: [{ label: 'AI stage', value: 'fresh round', tone: 'good' }, { label: 'late stage', value: '409', tone: 'warn' }], errors: [auth, err('轮次/候选人不存在', '404，事务不执行。', '刷新列表。'), err('候选人已离开 AI 阶段', '409 防止绕过 UI 回滚。', '先走显式阶段回退/重新激活。'), err('事务中快照或审计失败', '整个 reset 回滚。', '修复依赖后重试；不要部分清空锚点。')], practices: ['状态、锚点、快照、审计在一个事务。', '旧报告保留不可变历史，不随 reset 覆盖。'], test: SNAPSHOT_BOUNDARY_TEST, proves: 'core 源码测试直接约束 reset 是快照创建边界；未执行验证锚点清空、409、审计原子性。', sources: [DRIZZLE_TX], related: ['refresh-studio-interview-context-http', 'get-studio-interview-round-http'] }),
  d({
    slug: 'get-studio-interview-resume-http', method: 'GET', path: '/api/w/:slug/studio/interviews/:id/resume', source: 'detail-route.ts', line: 106, permission: 'interview.read',
    summary: '把可见 round 解析为候选人，读取 resumeStorageKey 并从 S3-compatible storage 流式 inline 返回原文件，带 private,max-age=300。', when: '受权招聘人员在详情页直接预览或下载原始简历文件。', parameters: [round], returnType: '200 binary stream', returnDescription: '返回原 Content-Type/Length 与编码文件名；无记录/key/object 404。', example: `const url = '/api/w/' + encodeURIComponent(slug) + '/studio/interviews/' + encodeURIComponent(roundId) + '/resume'\niframe.src = url`, exampleNotes: ['响应是二进制，不调用 json。', 'private 缓存仍会在本地保留五分钟，应结合敏感度评估。'], effectTitle: '流式展示原始简历', effectDescription: '浏览器无需把文件经过 JSON/base64 即可 inline 展示。', metrics: [{ label: 'transport', value: 'binary stream' }, { label: 'cache', value: 'private 300s' }], outputs: [{ label: 'object found', value: 'inline file', tone: 'good' }, { label: 'missing', value: '404', tone: 'warn' }], errors: [auth, err('round/candidate/key/object 缺失', '统一 404。', '重新上传简历或刷新记录。')], practices: ['PII 文件按对象级授权、加密和下载审计。', '设置 nosniff/安全 Content-Disposition，并支持流式 backpressure。'], test: ROUND_DAO_TEST, proves: 'family/core DAO 直测 round→candidate 的组织/可见域；未直测对象流、headers、404 或缓存策略。', technology: 'Hono 4.12.23 + AWS SDK S3 streaming', sources: [S3, HTTP_CACHE], related: ['get-studio-interview-resume-preview-http', 'put-object-bytes'] }),
  d({
    slug: 'get-studio-interview-resume-preview-http', method: 'GET', path: '/api/w/:slug/studio/interviews/:id/resume-preview.pdf', source: 'detail-route.ts', line: 152, permission: 'interview.read',
    summary: '授权 round 后把简历对象完整读为 bytes，并由预览适配器把 PDF/PPTX 统一返回 PDF；cacheKey 绑定 storageKey。', when: '原文件是 PPTX 或浏览器需要统一 PDF 预览体验时。', parameters: [round], returnType: '200 application/pdf binary', returnDescription: '记录/key/object 缺失 404；不支持格式或转换失败进入全局 500。', example: `window.open('/api/w/' + encodeURIComponent(slug) + '/studio/interviews/' + encodeURIComponent(roundId) + '/resume-preview.pdf', '_blank', 'noopener')`, exampleNotes: ['服务端会把整对象读入内存，不适合无上限文件。', '转换结果应按源对象 hash 缓存。'], effectTitle: '把简历统一为 PDF 预览', effectDescription: '招聘人员无需 Office 即可浏览 PPTX/PDF 简历。', metrics: [{ label: 'transport', value: 'PDF binary' }, { label: 'input loading', value: 'full bytes' }], outputs: [{ label: 'supported', value: 'preview PDF', tone: 'good' }, { label: 'large file', value: 'memory pressure', tone: 'warn' }], errors: [auth, err('文件缺失', '返回 404。', '重新上传原文件。'), err('格式不支持/转换器失败', '全局 500。', '回退原文件下载；隔离转换并记录格式/大小。')], practices: ['转换前限制大小/MIME、检测压缩炸弹并隔离执行。', '缓存产物绑定内容 hash，避免重复 CPU/内存消耗。'], test: ROUND_DAO_TEST, proves: 'family/core DAO 仅证明授权解析；没有直接覆盖 getObjectBytes、PPTX/PDF 分支、大小门禁或响应头。', technology: 'Hono + S3 + PPTX/PDF preview adapter', sources: [S3], related: ['get-studio-interview-resume-http', 'put-object-bytes'] }),
  d({
    slug: 'transition-studio-interview-candidate-http', method: 'POST', path: '/api/w/:slug/studio/interviews/:id/transition', source: 'route.ts', line: 338, permission: 'interview.update plus transition authorizer',
    summary: '以 candidateId 执行推进、结案或重新激活；Zod 强制 closed⇔终态 outcome，不允许非 closed 携带 closedMeta/reason，内部 authorizer 再检查目标转换权限并原子写审计。', when: '招聘流程从 screening/AI/真人/Offer 推进，或以明确 outcome 结案/重新激活时。', parameters: [p('id', 'path candidateId', true, '候选人 ID；与相邻 round 端点语义不同。'), p('pipelineStage/outcome/closedMeta/reactivationReason', 'JSON', true, 'stage 必填；终态、结案元数据与重激活原因受跨字段不变量约束。')], returnType: '200 { ok:true }', returnDescription: 'schema/业务非法 400，内部 authorizer 403，候选人 404。', exampleNotes: ['客户端使用共享 stage/outcome 枚举。', '响应后重新读取候选人/轮次，不能本地猜测派生元数据。'], effectTitle: '原子推进候选人生命周期', effectDescription: '候选人阶段、outcome、结案/重激活元数据与审计保持一致。', metrics: [{ label: 'invariant', value: 'closed ⇔ terminal outcome' }, { label: 'authorization', value: 'request authorizer' }], outputs: [{ label: 'valid transition', value: 'ok', tone: 'good' }, { label: 'invalid invariant', value: '400', tone: 'warn' }, { label: 'forbidden', value: '403', tone: 'warn' }], errors: [auth, err('stage/outcome/metadata 组合非法', 'validator 或 transitionCandidateStage 返回 400。', '按服务端错误调整目标状态。'), err('缺少目标阶段权限', '内部 authorizer 返回 403。', '申请正确 capability，不改 header 伪造。'), err('并发阶段变更', '事务/行锁决定一个顺序；后请求可能因来源状态失效。', '刷新最新阶段后让用户重新确认。')], practices: ['跨字段不变量同时由 schema 与数据库 CHECK 保护。', '所有转换记录 from/to、actor、reason 和 provenance。'], test: TRANSITION_TEST, proves: 'core DB/DAO 直测非法 stage/outcome CHECK、结案原子字段与重新激活清理；HTTP 403 与每条状态边未全覆盖。', sources: [DRIZZLE_TX], related: ['patch-studio-candidate-expectations-http', 'create-studio-human-interview-round-http'] }),
  d({
    slug: 'bulk-delete-studio-interview-rounds-http', method: 'POST', path: '/api/w/:slug/studio/interviews/bulk-delete', source: 'route.ts', line: 511, permission: 'interview.delete',
    summary: '校验非空 roundId 数组，在事务中联查并锁定所有 parent；任一候选人超过 AI 阶段就整批拒绝，否则删除当前组织内命中轮次并批量回退无剩余轮次的 AI 候选人。', when: '列表多选删除 AI 面试轮次，并要求晚期候选人不发生部分删除时。', parameters: [p('ids', 'JSON non-empty string[]', true, 'roundId 集合；未知/跨组织 ID 当前被静默忽略。')], returnType: '200 { success:true, deletedCount:number }', returnDescription: '空数组 400；任一已超过 AI 阶段实际 409；成功可 deletedCount < ids.length。', exampleNotes: ['必须以 deletedCount 对账，不假定全部 ID 命中。', '409 时整批未删除，可拆分后重试允许项。'], effectTitle: '原子批量删除允许的轮次', effectDescription: '晚期阶段保护为 all-or-nothing；允许时一次 SQL 删除并回退孤儿候选人。', metrics: [{ label: 'stage policy', value: 'reject whole batch' }, { label: 'result', value: 'deletedCount' }], outputs: [{ label: 'all allowed', value: 'batch deleted', tone: 'good' }, { label: 'one locked', value: '409 none deleted', tone: 'warn' }, { label: 'unknown ids', value: 'silently not counted', tone: 'neutral' }], errors: [auth, err('ids 为空', '400，不开始事务。', '至少选择一条。'), err('任一命中 parent 超过 AI 阶段', '409，整批回滚/不删。', '先回退阶段或拆分操作。'), err('部分 ID 不存在/跨组织', '当前不会报错，只导致 deletedCount 较小。', '客户端严格对账；高保证场景应返回 per-id result 或 404。')], practices: ['去重 IDs 并限制批量上限。', '锁顺序稳定排序，降低并发批量操作死锁。'], test: SNAPSHOT_TEST, proves: 'family 快照 DAO 测试不证明批量删除；需补 direct 事务测试覆盖 all-or-nothing 409、未知 ID、最后轮次回退和锁顺序。', sources: [DRIZZLE_TX], related: ['delete-studio-interview-round-http', 'transition-studio-interview-candidate-http'] }),
  d({
    slug: 'dedup-check-studio-interviews-http', method: 'POST', path: '/api/w/:slug/studio/interviews/dedup-check', source: 'route.ts', line: 157, permission: 'interview.read',
    summary: '用邮箱、姓名、电话和规范化 ResumeProfile 在当前组织执行精确+语义简历查重，返回包含 level、score、similarity 与 semanticReasons 的候选匹配。', when: '新建候选人提交前给 HR 展示潜在重复项并选择 create/skip/force 策略时。', parameters: [p('email/name/phone', 'JSON nullable strings', false, '分别限制 200/200/40 字符。'), p('resumeProfile', 'JSON ResumeProfile|null', false, '用于语义相似度；custom schema 当前只做类型层声明，需服务端防御性验证。')], returnType: '200 { matches: ResumeDuplicateMatch[] }', returnDescription: '非法基础字段 400；无匹配返回空数组。', exampleNotes: ['查重是提示，不应自动合并候选人。', '响应日志当前包含匹配 ID/分数，生产应控制敏感度。'], effectTitle: '提交前提示潜在重复候选人', effectDescription: 'HR 能看到精确与语义原因，选择安全的创建策略。', metrics: [{ label: 'signals', value: 'identity + semantic' }, { label: 'scope', value: 'organization' }], outputs: [{ label: 'matches', value: 'ranked reasons', tone: 'good' }, { label: 'none', value: '[]', tone: 'neutral' }], errors: [auth, err('字段超长或 body 非法', 'validator 返回 400。', '裁剪并按 schema 提交。'), err('embedding/vector 服务不可用', '可能进入全局 500 或降级取决于 dedup service。', '定义显式 degraded 响应和超时，允许仅精确查重。')], practices: ['语义阈值校准并向用户展示原因，不做无审核自动合并。', '对 PII/匹配结果日志脱敏，设置限流与超时。'], test: ROUND_DAO_TEST, proves: 'family/core 轮次 DAO 测试不覆盖 semantic dedup；需要 direct 测试覆盖组织隔离、阈值、无 embedding 降级与日志脱敏。', related: ['create-studio-interview-http', 'find-semantic-resume-duplicates'] }),
]
