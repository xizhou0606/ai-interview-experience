import { AI_INTERVIEW_COMMIT } from '../helpers'
import type { ApiEntry } from '../types'

export const httpWorkspaceCoreApis: ApiEntry[] = [
  {
    slug: 'match-chat-attachment-job-description', name: 'POST /api/w/:slug/chat/attachments/:id/match-job-description', signature: 'POST /api/w/:slug/chat/attachments/:id/match-job-description → 200 JobDescriptionMatchResult', kind: 'http', project: 'ai-interview', module: 'agent-tools', technology: 'Hono + Mastra structured generation + Drizzle', implementationStatus: 'best-effort', statusNote: '生产端点；附件解析与租户边界有直接测试，但 AI 匹配器失败会刻意降级为 matchedId:null，而不是让请求失败。', verifiedCommit: AI_INTERVIEW_COMMIT,
    summary: '读取当前用户、当前工作区的聊天附件，优先复用结构化简历；只有缓存缺失时才从 OCR 文本补结构化并按内容 hash 回填，再与全部在招 JD 做 best-effort 匹配。', whenToUse: '用户在招聘 Copilot 中上传简历后，希望自动预选最匹配岗位，同时不能让模型或网络抖动阻塞聊天主流程时。', sourcePath: 'apps/ai-recruitment-copilot-backend/src/server/routes/chat/routes/attachments/route.ts', sourceLine: 18,
    parameters: [{ name: 'slug', type: 'path string', required: true, description: 'workspace slug，由 workspaceMiddleware 解析为 activeOrg。' }, { name: 'id', type: 'path string', required: true, description: '附件 ID；读取时同时约束 userId 与 organizationId。' }], returns: { type: '200 { matchedId: string | null; reason: string | null }', description: '命中时给出岗位与原因；模型失败降级为空匹配；解析缓存不可用返回 422。' },
    example: { language: 'ts', code: `const response = await fetch(\n  \`/api/w/\${slug}/chat/attachments/\${attachmentId}/match-job-description\`,\n  { method: 'POST', signal },\n)\nconst match = response.ok ? await response.json() : null`, explanation: ['无需重复上传或提交简历正文，服务端从受租户约束的附件读取。', '客户端把 null 匹配呈现为“未自动匹配”，仍允许用户手选岗位。'] },
    effect: { title: '附件旁出现岗位建议', description: '结构化缓存命中时不再调用简历结构化模型；只有 OCR 文本的附件会补全缓存，之后同 hash 文件可复用。', metrics: [{ label: 'HTTP success', value: '200' }, { label: 'cache backfill', value: 'contentHash' }, { label: 'AI failure mode', value: 'null match' }], output: [{ label: 'matchedId', value: 'jd-frontend', tone: 'good' }, { label: 'reason', value: 'React 与岗位技能匹配', tone: 'neutral' }, { label: 'no profile', value: '422 · 重新上传', tone: 'warn' }] },
    errors: [{ condition: '附件不存在或跨用户/跨 workspace', behavior: '统一返回 404，避免泄露存在性。', recovery: '刷新附件列表，不允许客户端传 organizationId 绕过 scope。' }, { condition: '无结构化缓存且无可用 OCR 文本', behavior: '返回 422。', recovery: '提示重新上传并保留原消息草稿。' }, { condition: '匹配模型连接失败', behavior: 'helper 记录 warn 并返回空匹配。', recovery: '允许人工选岗，并记录降级率用于告警。' }], bestPractices: ['附件查找必须同时绑定 userId、organizationId 和 id。', '缓存键用服务端计算的内容 hash，不信任客户端声明。', '把“无匹配”和“系统降级”分开观测，避免静默质量退化。'],
    tests: [{ path: 'apps/ai-recruitment-copilot-backend/src/server/routes/chat/routes/attachments/__tests__/route.test.ts', proves: '端点直测：结构化缓存直用、OCR 文本按需结构化、contentHash 回填及匹配输入。' }, { path: 'apps/ai-recruitment-copilot-backend/src/server/routes/interview/__tests__/match-job-description.test.ts', proves: '直接覆盖匹配器成功与连接重置时 best-effort 空结果。' }], officialSources: [{ label: 'Hono routing', url: 'https://hono.dev/docs/api/routing' }, { label: 'Mastra structured output', url: 'https://mastra.ai/docs/agents/structured-output' }], related: ['preflight-chat-attachment-upload', 'upload-chat-attachment', 'match-interview-job-description'],
  },
  {
    slug: 'get-chat-attachment-preview', name: 'GET /api/w/:slug/chat/attachments/:previewId', signature: 'GET /api/w/:slug/chat/attachments/:id-preview.pdf → 200 application/pdf', kind: 'http', project: 'ai-interview', module: 'object-storage', technology: 'Hono + S3/R2 + LibreOffice headless', statusNote: '生产端点；仅接受 -preview.pdf 后缀且只转换 PPTX。当前附件 router 测试未直接覆盖预览、转换失败或缓存头。', verifiedCommit: AI_INTERVIEW_COMMIT,
    summary: '识别 `-preview.pdf` 虚拟资源，从对象存储读取原始 PPTX，使用 LibreOffice 转 PDF，并以进程内最多 20 项 LRU 缓存复用转换结果。', whenToUse: '浏览器需要内嵌预览 PPTX 简历而不能直接渲染 Office 文件时；PDF/图片应走原附件端点。', sourcePath: 'apps/ai-recruitment-copilot-backend/src/server/routes/chat/routes/attachments/route.ts', sourceLine: 63,
    parameters: [{ name: 'slug', type: 'path string', required: true, description: '当前工作区。' }, { name: 'previewId', type: '`${attachmentId}-preview.pdf`', required: true, description: '后缀决定进入预览分支；其他值会 next 到原文件路由。' }], returns: { type: '200 application/pdf', description: 'inline PDF、private max-age=300 与 Content-Length；非 PPTX 返回 415，转换失败返回 500。' },
    example: { language: 'html', code: `<iframe\n  src={\`/api/w/\${slug}/chat/attachments/\${id}-preview.pdf\`}\n  title="PPTX 简历预览"\n/>`, explanation: ['鉴权依然基于原 attachmentId、当前用户和 workspace。', '响应是私有缓存，不能交给共享 CDN 做 public cache。'] },
    effect: { title: 'PPTX 在浏览器内可读', description: '首次请求完成 Office→PDF 转换，随后同进程同 storageKey 命中内存缓存。', metrics: [{ label: 'conversion timeout', value: '30s' }, { label: 'memory cache', value: '20 items' }, { label: 'browser cache', value: '300s private' }], output: [{ label: 'PPTX', value: 'inline PDF', tone: 'good' }, { label: 'non-PPTX', value: '415', tone: 'warn' }, { label: 'soffice failure', value: '500', tone: 'warn' }] },
    errors: [{ condition: '对象或附件不存在', behavior: '404。', recovery: '刷新消息附件状态，不重试不存在对象。' }, { condition: 'LibreOffice 缺失、超时或输出损坏', behavior: '脱敏 500。', recovery: '提供原文件下载，并对转换依赖做 readiness/指标。' }, { condition: '大文件频繁转换', behavior: '占用 CPU、临时盘与内存。', recovery: '改为异步转换并把派生 PDF 持久化到对象存储。' }], bestPractices: ['派生预览按 storageKey+转换版本缓存。', '临时目录无论成功失败都清理。', '设置 private 缓存与 CSP/frame 策略，并限制转换资源。'],
    tests: [{ path: 'apps/ai-recruitment-copilot-backend/src/server/routes/chat/routes/attachments/__tests__/route.test.ts', proves: '同 router 的租户附件读取测试；当前不直接证明 preview/PPTX 分支。' }, { path: 'apps/ai-recruitment-copilot-backend/src/server/routes/studio/__tests__/pptx-preview.test.ts', proves: '转换工具仅直接覆盖 soffice 参数与生成 PDF bytes；不证明 HTTP 响应、LRU 缓存、失败或清理。' }], officialSources: [{ label: 'MDN Content-Disposition', url: 'https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Content-Disposition' }, { label: 'LibreOffice command line parameters', url: 'https://help.libreoffice.org/latest/en-US/text/shared/guide/start_parameters.html' }], related: ['get-chat-attachment', 'upload-chat-attachment', 'put-object-bytes'],
  },
  {
    slug: 'get-chat-attachment', name: 'GET /api/w/:slug/chat/attachments/:id', signature: 'GET /api/w/:slug/chat/attachments/:id → 200 ReadableStream<Uint8Array>', kind: 'http', project: 'ai-interview', module: 'object-storage', technology: 'Hono + AWS SDK S3 streaming', statusNote: '生产读取端点；当前附件 router 测试只直测岗位匹配，尚未直测二进制流、Content-Length 与私有缓存头。', verifiedCommit: AI_INTERVIEW_COMMIT,
    summary: '在用户与工作区双重 scope 下查附件元数据，再把 S3/R2 Body 原样流给浏览器，不把完整文件缓冲进 Node 堆。', whenToUse: '聊天消息需要内嵌 PDF/图片、下载原文件，或服务端工具需要通过已授权 URL 引用附件时。', sourcePath: 'apps/ai-recruitment-copilot-backend/src/server/routes/chat/routes/attachments/route.ts', sourceLine: 95,
    parameters: [{ name: 'slug', type: 'path string', required: true, description: 'workspace scope。' }, { name: 'id', type: 'path string', required: true, description: '附件 ID；服务端同时检查 userId。' }], returns: { type: '200 binary stream', description: 'Content-Type 取对象元数据或附件 mediaType，inline 文件名，可能含 Content-Length；缺失统一 404。' },
    example: { language: 'ts', code: `const response = await fetch(\`/api/w/\${slug}/chat/attachments/\${id}\`)\nif (!response.ok) throw new Error('附件不可用')\nconst objectUrl = URL.createObjectURL(await response.blob())`, explanation: ['浏览器会带同源 session cookie。', '使用完 object URL 后调用 URL.revokeObjectURL。'] },
    effect: { title: '消息内直接预览原附件', description: '服务端以流式传输控制内存，浏览器按真实 MIME 渲染。', metrics: [{ label: 'server buffering', value: '0 full-file copy' }, { label: 'cache', value: 'private 300s' }, { label: 'tenant predicates', value: 'user + org + id' }], output: [{ label: 'PDF/image', value: 'inline', tone: 'good' }, { label: 'object missing', value: '404', tone: 'warn' }] },
    errors: [{ condition: 'DB 有记录但对象丢失', behavior: '404，与附件不存在相同。', recovery: '记录 storage drift 指标并提供重新上传。' }, { condition: 'S3 流中途断开', behavior: '响应 body 截断。', recovery: '浏览器显示失败并允许幂等重取；服务端记录 request/object key。' }, { condition: '恶意 MIME/文件名', behavior: '可能触发浏览器内容嗅探或 header 混淆。', recovery: '白名单 MIME、编码文件名并增加 X-Content-Type-Options:nosniff。' }], bestPractices: ['对象读取保持 streaming，不先 arrayBuffer。', '404 不区分越权与不存在。', '私有附件禁止 public CDN 缓存，文件名与 MIME 必须安全化。'],
    tests: [{ path: 'apps/ai-recruitment-copilot-backend/src/server/routes/chat/routes/attachments/__tests__/route.test.ts', proves: '同 router 测试证明附件按当前用户上下文加载；未直接覆盖二进制 GET 分支。' }], officialSources: [{ label: 'AWS SDK v3 streaming', url: 'https://docs.aws.amazon.com/sdk-for-javascript/v3/developer-guide/javascript_s3_code_examples.html' }, { label: 'MDN MIME type verification', url: 'https://developer.mozilla.org/en-US/docs/Web/HTTP/Guides/MIME_types' }], related: ['get-chat-attachment-preview', 'upload-chat-attachment', 'put-object-bytes'],
  },
  {
    slug: 'list-chat-conversations', name: 'GET /api/w/:slug/chat/conversations', signature: 'GET /api/w/:slug/chat/conversations → 200 { conversations: ChatConversationSummary[] }', kind: 'http', project: 'ai-interview', module: 'agent-tools', technology: 'Hono RPC + Drizzle ORM', statusNote: '生产端点；返回当前用户全部会话且暂无分页。现有 conversationsRouter 测试未直接覆盖列表排序/序列化。', verifiedCommit: AI_INTERVIEW_COMMIT,
    summary: '按 userId 与 organizationId 列出会话摘要，按 createdAt 倒序，并把 Date 显式序列化成 ISO 字符串供侧边栏恢复。', whenToUse: '进入招聘 Copilot、切换 workspace 或会话写入后刷新侧边栏历史时。', sourcePath: 'apps/ai-recruitment-copilot-backend/src/server/routes/chat/routes/conversations/route.ts', sourceLine: 26,
    parameters: [{ name: 'slug', type: 'path string', required: true, description: '工作区租户边界；用户来自 session。' }], returns: { type: '200 { conversations: { id; title; isTitleGenerating; createdAt; updatedAt }[] }', description: '只含摘要，不加载消息正文。' },
    example: { language: 'ts', code: `const { conversations } = await rpcFetch(\n  rpc.api.w[':slug'].chat.conversations.$get({ param: { slug } }),\n  '加载会话列表失败',\n)`, explanation: ['列表不携带 messages，避免侧边栏请求放大。', 'isTitleGenerating 让刷新后仍能显示标题生成中。'] },
    effect: { title: '侧边栏恢复会话历史', description: '切换工作区后只出现当前用户在当前组织的会话摘要。', metrics: [{ label: 'sort', value: 'createdAt DESC' }, { label: 'message payload', value: 'excluded' }, { label: 'pagination', value: 'missing' }], output: [{ label: 'conversations', value: '最近会话优先', tone: 'good' }, { label: 'large history', value: 'unbounded', tone: 'warn' }] },
    errors: [{ condition: 'session/workspace 缺失', behavior: '401。', recovery: '重新登录或返回工作区选择。' }, { condition: '会话数量持续增长', behavior: '响应与数据库扫描线性增大。', recovery: '增加 cursor、limit 和 createdAt/id 复合索引。' }], bestPractices: ['列表只返回摘要。', '分页游标包含唯一 tie-breaker id。', '所有查询必须同时约束 userId 与 organizationId。'],
    tests: [{ path: 'apps/ai-recruitment-copilot-backend/src/server/routes/chat/routes/conversations/__tests__/route.test.ts', proves: '同 router 测试覆盖鉴权上下文与写入；未直接证明 GET 列表、排序和 ISO 序列化。' }], officialSources: [{ label: 'Hono RPC', url: 'https://hono.dev/docs/guides/rpc' }, { label: 'Drizzle select', url: 'https://orm.drizzle.team/docs/select' }], related: ['get-chat-conversation', 'upsert-chat-conversation', 'delete-chat-conversation'],
  },
  {
    slug: 'upsert-chat-conversation', name: 'POST /api/w/:slug/chat/conversations', signature: 'POST UpsertConversationPayload → 200 { ok: true }', kind: 'http', project: 'ai-interview', module: 'agent-tools', technology: 'Hono zValidator + Drizzle upsert policy', verifiedCommit: AI_INTERVIEW_COMMIT,
    summary: '使用客户端稳定 conversation id 创建或更新标题、JD 上下文、导入映射；先查所有权，已有跨用户/跨组织 ID 会 fail closed。', whenToUse: '新建 Copilot 会话，或在流开始前持久化会话级上下文，使刷新、后台流和多标签页能恢复。', sourcePath: 'apps/ai-recruitment-copilot-backend/src/server/routes/chat/routes/conversations/route.ts', sourceLine: 49,
    parameters: [{ name: 'id', type: 'string', required: true, description: '稳定会话 ID。' }, { name: 'createdAt', type: 'nonnegative integer', required: false, description: '客户端创建时间毫秒，仅创建时使用。' }, { name: 'title / isTitleGenerating', type: 'string / boolean', required: false, description: '侧边栏状态。' }, { name: 'jobDescriptionConfig / resumeImports', type: 'JobDescriptionConfig | null / Record<string,string>', required: false, description: '会话焦点上下文。' }], returns: { type: '200 { ok: true }', description: '创建或所有者更新成功；无效 schema 400；ID 属于其他 actor 时 403。' },
    example: { language: 'ts', code: `await upsertConversation(slug, {\n  id: crypto.randomUUID(), title: '前端候选人筛选',\n  jobDescriptionConfig: { mode: 'select', jobDescriptionId: 'jd-1', name: '前端', departmentName: '研发', prompt: '' },\n})`, explanation: ['服务端忽略客户端 userId/organizationId，统一取 session/workspace。', '相同 id 重投更新指定字段，不创建重复会话。'] },
    effect: { title: '刷新后会话上下文仍存在', description: '会话在发送消息前就有稳定 ID 与 JD/导入上下文。', metrics: [{ label: 'success', value: '200' }, { label: 'ownership', value: 'fail closed' }, { label: 'validation', value: 'Zod' }], output: [{ label: 'new id', value: 'insert', tone: 'good' }, { label: 'owned id', value: 'partial update', tone: 'good' }, { label: 'foreign id', value: '403', tone: 'warn' }] },
    errors: [{ condition: '缺 id 或 JD config 非法', behavior: '400 统一字符串错误。', recovery: '客户端保留表单并定位字段。' }, { condition: 'ID 冲突到其他用户/组织', behavior: '403。', recovery: '生成新 UUID；不得覆盖。' }, { condition: '并发首次创建同 ID', behavior: 'check-then-insert 存在竞态，可能触发唯一键错误。', recovery: '用原子 INSERT…ON CONFLICT 并在冲突后复核所有权。' }], bestPractices: ['ID 稳定但所有权只能由服务端判断。', '创建与更新最好拆分或返回 created/updated。', '为首次创建竞态增加数据库级冲突处理。'],
    tests: [{ path: 'apps/ai-recruitment-copilot-backend/src/server/routes/chat/routes/conversations/__tests__/route.test.ts', proves: '端点直测：无效创建返回规范化 400，成功写入明确返回 200 {ok:true}。' }], officialSources: [{ label: 'Hono validation', url: 'https://hono.dev/docs/guides/validation' }, { label: 'PostgreSQL INSERT ON CONFLICT', url: 'https://www.postgresql.org/docs/current/sql-insert.html' }], related: ['patch-chat-conversation', 'list-chat-conversations', 'upsert-chat-conversation-message'],
  },
  {
    slug: 'get-chat-conversation', name: 'GET /api/w/:slug/chat/conversations/:id', signature: 'GET /api/w/:slug/chat/conversations/:id → 200 { conversation: ChatConversationDetail }', kind: 'http', project: 'ai-interview', module: 'agent-tools', technology: 'Hono RPC + Drizzle ORM + ARC messages', statusNote: '生产端点；会话与消息分两次查询，现有 router 测试未直接覆盖读取和消息排序。', verifiedCommit: AI_INTERVIEW_COMMIT,
    summary: '按 id、userId、organizationId 加载会话元数据，再按 createdAt 正序加载 ARC 消息，用于刷新后完整恢复流式聊天状态。', whenToUse: '用户点击历史会话、刷新页面，或后台流完成后重新同步服务端事实时。', sourcePath: 'apps/ai-recruitment-copilot-backend/src/server/routes/chat/routes/conversations/route.ts', sourceLine: 81,
    parameters: [{ name: 'slug', type: 'path string', required: true, description: '工作区。' }, { name: 'id', type: 'path string', required: true, description: '会话 ID。' }], returns: { type: '200 { conversation: ChatConversationDetail }', description: '含 JD config、resumeImports 和有序 messages；不存在/越界统一 404。' },
    example: { language: 'ts', code: `const conversation = await fetchConversation(slug, id)\nif (!conversation) navigate({ to: '/w/$slug/chat', params: { slug } })\nelse chat.setMessages(conversation.messages)`, explanation: ['客户端 helper 把 404 安静映射为 null。', '消息按数据库 createdAt 排序，不信任客户端数组顺序。'] },
    effect: { title: '历史聊天可恢复', description: '标题、岗位上下文、附件导入状态和消息在刷新后重建。', metrics: [{ label: 'DB queries', value: '2' }, { label: 'message order', value: 'createdAt ASC' }, { label: 'not found', value: 'null in client' }], output: [{ label: 'conversation', value: 'metadata + messages', tone: 'good' }, { label: 'stale id', value: '404 → new chat', tone: 'neutral' }] },
    errors: [{ condition: '会话不存在或非当前 actor', behavior: '404。', recovery: '侧边栏移除陈旧条目并回新会话。' }, { condition: '消息量非常大', behavior: '单次读取完整消息导致延迟/内存增长。', recovery: '按消息游标分页或存摘要窗口。' }], bestPractices: ['详情和列表都使用相同租户谓词。', '消息排序增加 createdAt,id tie-breaker。', '长会话实行窗口化、摘要与附件懒加载。'],
    tests: [{ path: 'apps/ai-recruitment-copilot-backend/src/server/routes/chat/routes/conversations/__tests__/route.test.ts', proves: '同 router 测试覆盖 workspace actor 与所有权检查；未直接证明 GET 详情或消息排序。' }], officialSources: [{ label: 'Drizzle orderBy', url: 'https://orm.drizzle.team/docs/select#order-by' }, { label: 'Hono RPC', url: 'https://hono.dev/docs/guides/rpc' }], related: ['list-chat-conversations', 'upsert-chat-conversation-message', 'get-or-create-chat'],
  },
  {
    slug: 'patch-chat-conversation', name: 'PATCH /api/w/:slug/chat/conversations/:id', signature: 'PATCH PatchConversationPayload → 200 { ok: true }', kind: 'http', project: 'ai-interview', module: 'agent-tools', technology: 'Hono zValidator + Drizzle conditional update', statusNote: '生产端点；复用 upsertConversation，因此不存在的 id 会被创建，PATCH 并非严格“只更新”。当前测试未直测此语义。', verifiedCommit: AI_INTERVIEW_COMMIT,
    summary: '局部更新标题生成状态、JD 配置与简历导入映射；只有请求中出现的字段进入 SQL SET，但复用的 DAO 会在 id 不存在时插入新会话。', whenToUse: '自动标题结束、用户改名、切换 JD 或导入候选人后同步小范围会话元数据时。', sourcePath: 'apps/ai-recruitment-copilot-backend/src/server/routes/chat/routes/conversations/route.ts', sourceLine: 113,
    parameters: [{ name: 'id', type: 'path string', required: true, description: '会话 ID。' }, { name: 'body', type: 'PatchConversationPayload', required: true, description: '所有字段可选：title、isTitleGenerating、jobDescription、jobDescriptionConfig、resumeImports。' }], returns: { type: '200 { ok: true }', description: '更新成功；跨 actor 403；schema 无效 400。不存在 id 当前会创建空默认会话。' },
    example: { language: 'ts', code: `await patchConversation(slug, conversationId, {\n  title: '高级前端候选人',\n  isTitleGenerating: false,\n})`, explanation: ['undefined 字段不覆盖数据库值，null 只允许在 jobDescriptionConfig 中表达清空。', '客户端应只 patch 已成功创建的会话。'] },
    effect: { title: '标题和上下文即时同步', description: '侧边栏结束“生成中”状态，刷新后保持最新标题/JD。', metrics: [{ label: 'fields', value: '5 optional' }, { label: 'owned update', value: '200' }, { label: 'missing-id behavior', value: 'insert' }], output: [{ label: 'title', value: 'updated', tone: 'good' }, { label: 'foreign id', value: '403', tone: 'warn' }, { label: 'unknown id', value: 'created unexpectedly', tone: 'warn' }] },
    errors: [{ condition: '跨 actor id', behavior: '403。', recovery: '刷新会话列表。' }, { condition: 'PATCH 未知 id', behavior: '当前插入默认会话。', recovery: '改为 UPDATE…RETURNING，0 行返回 404；创建只留给 POST。' }, { condition: '空 body', behavior: '仍更新时间 updatedAt。', recovery: 'schema 至少要求一个字段或直接返回 no-op。' }], bestPractices: ['PATCH 不应隐式创建资源。', '返回版本/updatedAt 支持乐观并发。', '对多标签页更新使用 If-Match 或版本列。'],
    tests: [{ path: 'apps/ai-recruitment-copilot-backend/src/server/routes/chat/routes/conversations/__tests__/route.test.ts', proves: '同 router 直测 POST schema/写入与所有权；PATCH 路径及“缺失即创建”当前无直接测试。' }], officialSources: [{ label: 'RFC 5789 PATCH', url: 'https://www.rfc-editor.org/rfc/rfc5789' }, { label: 'Drizzle update', url: 'https://orm.drizzle.team/docs/update' }], related: ['upsert-chat-conversation', 'get-chat-conversation', 'list-chat-conversations'],
  },
  {
    slug: 'delete-chat-conversation', name: 'DELETE /api/w/:slug/chat/conversations/:id', signature: 'DELETE /api/w/:slug/chat/conversations/:id → 200 { ok: true } | 404', kind: 'http', project: 'ai-interview', module: 'agent-tools', technology: 'Hono RPC + Drizzle DELETE RETURNING', statusNote: '服务端第二次删除返回 404；前端 rpcFetch 明确 allow404:true，因此产品层把删除做成幂等。现有 router 测试未直测 DELETE。', verifiedCommit: AI_INTERVIEW_COMMIT,
    summary: '用 id、userId、organizationId 三条件删除会话，并通过 RETURNING 判断是否真的删除；客户端把 404 视为目标状态已达成。', whenToUse: '用户从侧边栏删除自己的 Copilot 会话，或重复操作/多标签页竞争时。', sourcePath: 'apps/ai-recruitment-copilot-backend/src/server/routes/chat/routes/conversations/route.ts', sourceLine: 145,
    parameters: [{ name: 'slug', type: 'path string', required: true, description: '工作区。' }, { name: 'id', type: 'path string', required: true, description: '待删除会话 ID。' }], returns: { type: '200 { ok: true } | 404 { error: "Not Found" }', description: '服务端严格表达是否删除；客户端封装把 404 归一为成功。' },
    example: { language: 'ts', code: `await deleteConversation(slug, conversationId)\nqueryClient.setQueryData(['chat', slug, 'conversations'], old =>\n  old.filter(item => item.id !== conversationId),\n)`, explanation: ['先完成或乐观执行 UI 移除，失败再回滚。', '第二次删除的 404 不应弹错误。'] },
    effect: { title: '侧边栏会话消失', description: '跨 workspace 或其他用户的同 ID 不会被删除。', metrics: [{ label: 'tenant predicates', value: '3' }, { label: 'server repeat', value: '404' }, { label: 'client repeat', value: 'success' }], output: [{ label: 'first delete', value: '200', tone: 'good' }, { label: 'repeat/stale', value: '404 accepted', tone: 'neutral' }] },
    errors: [{ condition: '不存在或越权', behavior: '404，不泄露资源归属。', recovery: '客户端视为已删除并刷新列表。' }, { condition: '会话关联消息未级联', behavior: '数据库可能拒绝或遗留孤儿。', recovery: '用 FK ON DELETE CASCADE 并加集成测试。' }], bestPractices: ['产品层删除应幂等。', '租户谓词直接放在 DELETE SQL。', '删除大量附件/对象时使用 outbox 异步清理。'],
    tests: [{ path: 'apps/ai-recruitment-copilot-backend/src/server/routes/chat/routes/conversations/__tests__/route.test.ts', proves: '同 router 测试覆盖 actor/ownership，但当前没有 DELETE 与级联的直接断言。' }], officialSources: [{ label: 'HTTP DELETE semantics', url: 'https://www.rfc-editor.org/rfc/rfc9110#name-delete' }, { label: 'Drizzle delete', url: 'https://orm.drizzle.team/docs/delete' }], related: ['list-chat-conversations', 'get-chat-conversation', 'upsert-chat-conversation'],
  },
  {
    slug: 'confirm-chat-recruiting-action', name: 'POST /api/w/:slug/chat/conversations/:id/actions/confirm', signature: 'POST { proposal } → 200 executed|noop / 409 failed', kind: 'http', project: 'ai-interview', module: 'agent-tools', technology: 'Hono + Zod discriminated union + Drizzle transaction + server authorization', verifiedCommit: AI_INTERVIEW_COMMIT,
    summary: '把 Agent 的只读建议升级为显式用户确认：先校验 resumeLibrary:update、会话所有权和候选人可见性，再执行绑定岗位、推进阶段或生成面试题，并写审计日志。', whenToUse: 'Copilot 产生结构化 proposal 后，用户点击“确认执行”；绝不能让模型工具直接调用数据库写操作。', sourcePath: 'apps/ai-recruitment-copilot-backend/src/server/routes/chat/routes/conversations/route.ts', sourceLine: 162,
    parameters: [{ name: 'id', type: 'path string', required: true, description: 'proposal 所属会话。' }, { name: 'proposal', type: 'bind_candidate_to_job | advance_candidate_stage | generate_interview_questions', required: true, description: '含稳定 proposal id、标题、解释和按类型校验的 payload。' }], returns: { type: 'ConfirmRecruitingActionResult', description: 'executed/noop 返回 200；业务失败映射 409；鉴权/所有权/可见性失败为 403/404。' },
    example: { language: 'ts', code: `const result = await confirmRecruitingAction(slug, conversationId, {\n  id: 'proposal-42', type: 'bind_candidate_to_job', title: '绑定岗位',\n  explanation: '技能吻合', payload: { resumeRecordId: 'resume-1', jobDescriptionId: 'jd-1' },\n})`, explanation: ['proposal 不是授权凭据，所有资源再次在服务端校验。', 'noop 是安全终态，重复确认不会重复业务变更。'] },
    effect: { title: '建议变成可审计业务动作', description: 'UI 展示 executed/noop/failed；成功动作更新候选人并记录 operator、proposal id 与来源。', metrics: [{ label: 'action types', value: '3' }, { label: 'permission', value: 'resumeLibrary:update' }, { label: 'business failure', value: '409' }], output: [{ label: 'executed', value: '已绑定/推进/生成', tone: 'good' }, { label: 'noop', value: '已是目标状态', tone: 'neutral' }, { label: 'failed', value: '409 + message', tone: 'warn' }] },
    errors: [{ condition: '会话或候选人不可见', behavior: '403/404，动作不执行。', recovery: '刷新当前 workspace 数据。' }, { condition: '重复 proposal', behavior: '绑定/阶段可 noop；生成题目仍可能重复生成并写审计。', recovery: '以 organizationId+proposalId 建唯一执行记录，返回既有结果。' }, { condition: 'DB 已提交但缓存失效/评价重置失败', behavior: '部分副作用可能不同步。', recovery: '事务外副作用改 outbox，并允许后台重放。' }], bestPractices: ['模型只能提出 proposal，用户确认后服务端重新授权。', 'proposalId 作为幂等键并持久化执行结果。', '业务写入、审计与 outbox 在同一事务。'],
    tests: [{ path: 'apps/ai-recruitment-copilot-backend/src/server/routes/chat/routes/conversations/__tests__/route.test.ts', proves: '端点直测：先检查会话所有权、传入服务端 Authorizer、成功返回；跨 workspace 不执行。' }, { path: 'apps/ai-recruitment-copilot-backend/src/server/routes/studio/routes/interviews/utils/candidate-stage-transition.test.ts', proves: '只直接覆盖被确认动作复用的候选人阶段状态机；不证明绑定岗位、生成题目或整个 HTTP 动作。' }], officialSources: [{ label: 'OWASP Authorization Cheat Sheet', url: 'https://cheatsheetseries.owasp.org/cheatsheets/Authorization_Cheat_Sheet.html' }, { label: 'Zod discriminated unions', url: 'https://zod.dev/api?id=discriminated-unions' }], related: ['create-recruiting-copilot-tools', 'create-workspace-authorizer', 'upsert-chat-conversation-message'],
  },
  {
    slug: 'upsert-chat-conversation-message', name: 'POST /api/w/:slug/chat/conversations/:id/messages', signature: 'POST { message: UIMessage } → 200 { ok: true }', kind: 'http', project: 'ai-interview', module: 'agent-tools', technology: 'Hono + AI SDK UI message adapter + Drizzle ON CONFLICT', statusNote: '生产持久化兜底；route 测试未直测 messages 端点。upsertChatMessage 以全局 message.id 冲突更新，数据库 schema 必须保证不会跨会话串写。', verifiedCommit: AI_INTERVIEW_COMMIT,
    summary: '校验最小 UIMessage 外壳、确认会话所有权、转换为 ARC message，再按 message id 幂等 upsert 并刷新 conversation.updatedAt。', whenToUse: '流中断、页面卸载或后台流结束时，把已有用户/助手消息 best-effort 写回服务端以支持恢复。', sourcePath: 'apps/ai-recruitment-copilot-backend/src/server/routes/chat/routes/conversations/route.ts', sourceLine: 214,
    parameters: [{ name: 'id', type: 'path string', required: true, description: '会话 ID。' }, { name: 'message', type: 'UIMessage-like { id; role; ... }', required: true, description: 'role 仅 system/user/assistant；loose 字段由 adapter 归一为 ARC。' }], returns: { type: '200 { ok: true }', description: '持久化成功；会话不存在 404、非所有者 403、数据库异常 500 Persist failed。' },
    example: { language: 'ts', code: `await upsertChatMessageOnServer(slug, conversationId, {\n  id: 'msg-42', role: 'assistant', parts: [{ type: 'text', text: partialText }],\n})`, explanation: ['相同 message id 更新内容，适合把 partial 补成 final。', '调用前必须先拥有会话，DAO 本身不会再次复核。'] },
    effect: { title: '中断后的部分回答可恢复', description: '刷新会话后能看到最后一次成功 upsert 的消息，侧边栏更新时间同步前移。', metrics: [{ label: 'idempotency key', value: 'message.id' }, { label: 'writes', value: 'message + conversation timestamp' }, { label: 'DB failure', value: '500' }], output: [{ label: 'first write', value: 'insert', tone: 'good' }, { label: 'same id', value: 'update', tone: 'good' }, { label: 'persist failure', value: '500', tone: 'warn' }] },
    errors: [{ condition: '消息 schema 无效', behavior: '400，不写库。', recovery: '修复 adapter 并保留本地消息。' }, { condition: '会话归属不符', behavior: '403/404。', recovery: '停止后台流并刷新会话。' }, { condition: '数据库暂时失败', behavior: '返回 500，当前代码只 console error。', recovery: '客户端有限重试；服务端增加 request id/指标，避免无限重放。' }], bestPractices: ['消息 ID 必须在会话作用域内唯一，冲突更新应同时约束 conversationId。', '流式增量不要每 token 写库，按阶段或终态节流。', '保留 partial/final 状态，避免把截断回答伪装成完整。'],
    tests: [{ path: 'apps/ai-recruitment-copilot-backend/src/server/routes/chat/routes/conversations/__tests__/route.test.ts', proves: '同 router 直接覆盖会话所有权边界，但当前未直测 messages handler、adapter 或 Persist failed。' }, { path: 'apps/ai-recruitment-copilot-backend/src/server/agents/mastra/__tests__/arc-message-adapter.test.ts', proves: '直接覆盖 UI/legacy message 到 ARC 消息的相邻转换，不是 HTTP 集成测试。' }], officialSources: [{ label: 'AI SDK UI messages', url: 'https://ai-sdk.dev/docs/ai-sdk-ui/chatbot-message-persistence' }, { label: 'Drizzle upsert', url: 'https://orm.drizzle.team/docs/insert#on-conflict-do-update' }], related: ['get-chat-conversation', 'get-or-create-chat', 'create-chat-transport'],
  },
  {
    slug: 'preflight-chat-attachment-upload', name: 'POST /api/w/:slug/chat/uploads/preflight', signature: 'POST UploadPreflightInput → 200 { hit:false } | { hit:true, ...UploadedAttachment }', kind: 'http', project: 'ai-interview', module: 'object-storage', technology: 'Web Crypto SHA-256 + Hono Zod + content-addressed cache', statusNote: '生产优化端点；客户端任意失败都会静默降级到 multipart。当前缓存按内容 hash 全局复用对象/解析结果，但会为当前 actor 新建独立附件行。', verifiedCommit: AI_INTERVIEW_COMMIT,
    summary: '在上传字节前用 SHA-256、文件名、MIME 和大小查询解析缓存；命中后复制元数据到当前用户/组织的新 attachment row，直接返回受租户约束的新 URL。', whenToUse: '支持的简历文件可能重复上传，需跳过大文件传输、OCR 与解析费用时；普通非简历附件直接走 multipart。', sourcePath: 'apps/ai-recruitment-copilot-backend/src/server/routes/chat/routes/uploads/route.ts', sourceLine: 66,
    parameters: [{ name: 'filename', type: 'string 1..255', required: true, description: '用于文件类型校验。' }, { name: 'hash', type: '64-char lowercase SHA-256 hex', required: true, description: '客户端预检键；真正上传时服务端会重算。' }, { name: 'mediaType', type: 'string', required: true, description: '与文件名共同判断支持类型。' }, { name: 'size', type: 'positive integer ≤ 20MiB', required: true, description: '快速拒绝超限。' }], returns: { type: 'UploadPreflightResponse', description: 'miss 只返回 hit:false；hit 返回新 attachment id、parseStatus、可选 parsed 与 workspace URL。' },
    example: { language: 'ts', code: `const hash = await sha256HexOfFile(file)\nconst cached = await apiFetch(\`/api/w/\${slug}/chat/uploads/preflight\`, {\n  method: 'POST', body: { filename: file.name, hash, mediaType: file.type, size: file.size },\n})\nif (!cached.hit) return uploadMultipart(file)`, explanation: ['预检失败不是上传失败，客户端自动退回 multipart。', '缓存命中仍创建当前用户自己的 attachment id，读路径不共享授权。'] },
    effect: { title: '重复简历秒传', description: '命中时跳过文件字节、S3 PUT 和 OCR，只写一条租户附件引用。', metrics: [{ label: 'hash', value: 'SHA-256' }, { label: 'max size', value: '20MiB' }, { label: 'fallback', value: 'multipart' }], output: [{ label: 'cache hit', value: 'new id + existing parse', tone: 'good' }, { label: 'cache miss/disabled', value: '{ hit:false }', tone: 'neutral' }] },
    errors: [{ condition: 'schema/文件类型无效', behavior: '400。', recovery: '客户端按真实限制预校验后走上传或提示。' }, { condition: '缓存服务/网络失败', behavior: '客户端 catch 并静默 multipart。', recovery: '记录 preflight fallback rate，不能长期静默。' }, { condition: '客户端伪造 hash', behavior: '只可能查询错误缓存；真正上传不信任该值。', recovery: '敏感跨租户解析结果需按内容安全模型审查，并可加入 HMAC/tenant scope。' }], bestPractices: ['客户端 hash 只用于优化，服务端上传必须重算。', '缓存命中创建独立授权记录。', '缓存开关、命中率、降级率与节省字节应可观测。'],
    tests: [{ path: 'apps/ai-recruitment-copilot-backend/src/server/routes/chat/routes/uploads/__tests__/route.test.ts', proves: '端点直测缓存禁用时返回 hit:false、不会读取 hash registry 或创建附件；命中分支仍需补直接测试。' }], officialSources: [{ label: 'Web Crypto digest', url: 'https://developer.mozilla.org/en-US/docs/Web/API/SubtleCrypto/digest' }, { label: 'OWASP File Upload Cheat Sheet', url: 'https://cheatsheetseries.owasp.org/cheatsheets/File_Upload_Cheat_Sheet.html' }], related: ['upload-chat-attachment', 'get-chat-attachment', 'match-chat-attachment-job-description'],
  },
  {
    slug: 'upload-chat-attachment', name: 'POST /api/w/:slug/chat/uploads', signature: 'multipart POST file → 200 UploadedAttachment', kind: 'http', project: 'ai-interview', module: 'object-storage', technology: 'Hono FormData + S3/R2 + SHA-256 + resume extraction', statusNote: '生产端点；S3 上传失败是致命 500，文本抽取失败却返回 200 parseStatus:failed。结构化抽取刻意延迟到真正需要时。', verifiedCommit: AI_INTERVIEW_COMMIT,
    summary: '服务端校验文件、重算 SHA-256，缓存命中则复制租户记录；miss 时并行 S3 PUT 与文本抽取，前者失败中止，后者失败以可见 parseStatus 降级。', whenToUse: 'preflight miss/失败，或上传不支持预检的附件，需要得到聊天消息可引用的稳定 attachment URL 时。', sourcePath: 'apps/ai-recruitment-copilot-backend/src/server/routes/chat/routes/uploads/route.ts', sourceLine: 114,
    parameters: [{ name: 'file', type: 'multipart File', required: true, description: '支持的简历文档，0 < size ≤ 20MiB；字段名固定为 file。' }], returns: { type: '200 { id; url; parseStatus; parsed? }', description: 'ready 时含 text/pageCount/textSource，structured 当前为 null；抽取失败仍 200 failed。' },
    example: { language: 'ts', code: `const form = new FormData()\nform.append('file', file, file.name)\nconst uploaded = await apiFetch(\`/api/w/\${slug}/chat/uploads\`, { method: 'POST', body: form })\nmessage.parts.push({ type: 'file', url: uploaded.url, mediaType: file.type })`, explanation: ['不要手动设置 multipart Content-Type，浏览器负责 boundary。', 'parseStatus=failed 时仍可保留附件并让模型/用户走降级路径。'] },
    effect: { title: '附件上传与文本抽取并行', description: '新文件一次请求获得可展示 URL 与解析状态；相同内容使用 hash 对象键，重复 PUT 幂等覆盖。', metrics: [{ label: 'max size', value: '20MiB' }, { label: 'parallel tasks', value: 'S3 + extract' }, { label: 'structured extraction', value: 'lazy' }], output: [{ label: 'ready', value: 'URL + parsed text', tone: 'good' }, { label: 'extract failed', value: '200 + failed', tone: 'warn' }, { label: 'storage failed', value: '500', tone: 'warn' }] },
    errors: [{ condition: '缺文件/不支持类型/超限', behavior: '400/415/413。', recovery: '客户端在上传前显示支持格式和大小。' }, { condition: 'S3 PUT 失败', behavior: '500，不创建附件行。', recovery: '幂等重试同 hash；增加对象存储熔断与告警。' }, { condition: '文本抽取失败', behavior: '记录截断错误并创建 failed 附件。', recovery: 'UI 明确状态，允许重解析或让模型读取原文件。' }], bestPractices: ['服务端校验 magic bytes，而不仅是扩展名/MIME。', '上传与解析状态分离，长解析改后台任务。', '对象、DB 行与后续清理使用 outbox/补偿避免孤儿。'],
    tests: [{ path: 'apps/ai-recruitment-copilot-backend/src/server/routes/chat/routes/uploads/__tests__/route.test.ts', proves: '端点直测缓存禁用 miss：服务端 hash、S3 PUT、文本抽取和租户附件写入均执行。' }], officialSources: [{ label: 'MDN FormData', url: 'https://developer.mozilla.org/en-US/docs/Web/API/FormData' }, { label: 'OWASP File Upload Cheat Sheet', url: 'https://cheatsheetseries.owasp.org/cheatsheets/File_Upload_Cheat_Sheet.html' }], related: ['preflight-chat-attachment-upload', 'get-chat-attachment', 'put-object-bytes'],
  },
  {
    slug: 'match-interview-job-description', name: 'POST /api/w/:slug/interview/match-job-description', signature: 'POST { resumeProfile, interviewRecordId? } → 200 JobDescriptionMatchResult', kind: 'http', project: 'ai-interview', module: 'agent-tools', technology: 'Hono + Zod + Drizzle tenant guard + best-effort AI matcher', implementationStatus: 'best-effort', statusNote: '生产端点；匹配模型失败会降级为空匹配。若提供 interviewRecordId，仅用于验证记录属于当前 workspace，不会从记录读取 profile。', verifiedCommit: AI_INTERVIEW_COMMIT,
    summary: '校验完整 ResumeProfile；可选地验证 interviewRecordId 的租户归属，然后加载当前组织全部 JD，调用 best-effort AI 匹配器返回岗位 ID 与理由。', whenToUse: '简历刚解析完成或 Studio 表单准备默认选岗时，已有结构化 profile 但附件并非聊天附件。', sourcePath: 'apps/ai-recruitment-copilot-backend/src/server/routes/interview/routes/analysis/route.ts', sourceLine: 88,
    parameters: [{ name: 'resumeProfile', type: 'ResumeProfile', required: true, description: '共享 schema 校验的结构化候选人档案。' }, { name: 'interviewRecordId', type: 'string', required: false, description: '传入时必须存在于当前 organization。' }], returns: { type: '200 { matchedId: string | null; reason: string | null }', description: '匹配失败降级为空；记录越界/不存在 404；workspace 缺失 403。' },
    example: { language: 'ts', code: `const match = await matchJobDescriptionForResume(slug, resumeProfile, { signal })\nif (match?.matchedId) form.setFieldValue('jobDescriptionId', match.matchedId)`, explanation: ['客户端 helper 对非 2xx 返回 null，不阻塞后续手工选择。', '服务端只返回当前 workspace JD 的 ID。'] },
    effect: { title: '创建候选人时自动选岗', description: '表单可预填匹配岗位与理由；模型不可用时保持未选择状态。', metrics: [{ label: 'input', value: 'validated ResumeProfile' }, { label: 'tenant check', value: 'optional record id' }, { label: 'AI fallback', value: 'null match' }], output: [{ label: 'matchedId', value: 'jd-1', tone: 'good' }, { label: 'reason', value: '技能与岗位要求匹配', tone: 'neutral' }, { label: 'matcher down', value: '200 null', tone: 'warn' }] },
    errors: [{ condition: 'resumeProfile 缺失/非法', behavior: '400 字符串错误。', recovery: '回到简历解析或修正字段。' }, { condition: 'record 不在当前 workspace', behavior: '404。', recovery: '刷新记录，不能改传 organizationId。' }, { condition: '模型/网络失败', behavior: '空匹配。', recovery: '人工选岗；记录 best-effort 降级原因和频率。' }], bestPractices: ['匹配结果只是建议，最终由用户确认。', 'JD 列表过大时先规则/向量召回再给模型精排。', '区分“真实无匹配”与“系统降级”的遥测。'],
    tests: [{ path: 'apps/ai-recruitment-copilot-backend/src/server/routes/interview/__tests__/match-job-description.test.ts', proves: '匹配 helper 直测成功和连接重置降级；HTTP schema/record tenant guard 尚无直接测试。' }], officialSources: [{ label: 'Hono validation', url: 'https://hono.dev/docs/guides/validation' }, { label: 'OWASP IDOR prevention', url: 'https://cheatsheetseries.owasp.org/cheatsheets/Insecure_Direct_Object_Reference_Prevention_Cheat_Sheet.html' }], related: ['match-chat-attachment-job-description', 'post-interview-parse-resume', 'generate-interview-questions-http'],
  },
  {
    slug: 'generate-interview-questions-http', name: 'POST /api/w/:slug/interview/generate-questions', signature: 'POST { resumeProfile } → 200 text/event-stream<AiRunEvent>', kind: 'http', project: 'ai-interview', module: 'workflows', technology: 'Hono SSE + Mastra workflow + Zod structured output', statusNote: '生产流端点；路由本身只依赖共享 workspace middleware，没有 handler 内再次读取 activeOrg。Agent/workflow 有直接测试，HTTP 鉴权与 abort 仍缺集成测试。', verifiedCommit: AI_INTERVIEW_COMMIT,
    summary: '把结构化简历交给面试题工作流，以统一 AiRun SSE 发出步骤事件、10 道带难度/考核点/追问方向的题目和单一终态。', whenToUse: 'Studio 发起面试前自动生成可编辑题目，并需要在耗时生成期间显示进度或支持取消时。', sourcePath: 'apps/ai-recruitment-copilot-backend/src/server/routes/interview/routes/analysis/route.ts', sourceLine: 141,
    parameters: [{ name: 'resumeProfile', type: 'ResumeProfile', required: true, description: '共享 Zod schema 验证的候选人档案。' }], returns: { type: 'text/event-stream<AiRunEvent>', description: 'step.*、run.completed 或 run.failed；完成 output 含 interviewQuestions。' },
    example: { language: 'ts', code: `const response = await rpc.api.w[':slug'].interview['generate-questions'].$post(\n  { json: { resumeProfile }, param: { slug } }, { init: { signal } },\n)\nawait readAiRunEventStream(response, event => {\n  if (event.type === 'run.completed') setQuestions(event.output.interviewQuestions)\n}, signal)`, explanation: ['不要用普通 JSON helper 消费 SSE。', 'dialog 关闭时 abort，避免迟到结果覆盖新候选人。'] },
    effect: { title: '面试题逐步生成并回填表单', description: '终态给出标准化题目，招聘人员可在启动面试前编辑。', metrics: [{ label: 'default question count', value: '10' }, { label: 'temperature', value: '0.3' }, { label: 'terminal events', value: '1' }], output: [{ label: 'run.completed', value: '10 questions', tone: 'good' }, { label: 'run.failed', value: '可重试错误', tone: 'warn' }] },
    errors: [{ condition: 'resumeProfile 非法', behavior: '400。', recovery: '修正解析结果后重试。' }, { condition: '模型/schema 失败', behavior: 'SSE run.failed。', recovery: '保留手工题目，提供显式重试。' }, { condition: '客户端取消', behavior: '读取停止；底层工作流是否完全取消需验证。', recovery: '将 request.signal 贯穿模型与 workflow。' }], bestPractices: ['统一事件协议且只发一个终态。', '流中失败不返回第二个 HTTP 状态。', '题目数量、重复率、编辑率和生成延迟应观测。'],
    tests: [{ path: 'apps/ai-recruitment-copilot-backend/src/server/agents/__tests__/resume-analysis-agent-questions.test.ts', proves: '核心流直测：结构化生成、10 题规范化和 run.completed；不是 HTTP 路由集成。' }, { path: 'apps/ai-recruitment-copilot-backend/src/server/agents/mastra/__tests__/ai-run-stream.test.ts', proves: '同族事件层直测心跳、错误与单终态。' }], officialSources: [{ label: 'Hono streaming', url: 'https://hono.dev/docs/helpers/streaming' }, { label: 'Mastra workflows', url: 'https://mastra.ai/docs/workflows/overview' }], related: ['stream-interview-questions-workflow', 'create-ai-run-event-stream', 'confirm-chat-recruiting-action'],
  },
  {
    slug: 'generate-resume-review-http', name: 'POST /api/w/:slug/interview/generate-review', signature: 'POST { resumeProfile, jobDescriptionId? } → 200 text/event-stream<AiRunEvent>', kind: 'http', project: 'ai-interview', module: 'workflows', technology: 'Hono SSE + Mastra three-agent review workflow', statusNote: '生产端点；不存在/越界的 jobDescriptionId 当前被 loadJobDescriptionText 静默当作“无 JD”继续生成，而不是 404。', verifiedCommit: AI_INTERVIEW_COMMIT,
    summary: '在当前组织加载可选 JD 文本，运行硬筛、定性评价、六维评分与代码加权组装工作流，并把步骤、预览和最终 structuredReview 流给前端。', whenToUse: '需要结构化评价与最终 Markdown 同时可靠生成，前端主要展示阶段进度而非逐字首屏时。', sourcePath: 'apps/ai-recruitment-copilot-backend/src/server/routes/interview/routes/analysis/route.ts', sourceLine: 153,
    parameters: [{ name: 'resumeProfile', type: 'ResumeProfile', required: true, description: '候选人结构化档案。' }, { name: 'jobDescriptionId', type: 'string | null', required: false, description: '当前 workspace JD；无效值当前降级为 null 文本。' }], returns: { type: 'text/event-stream<AiRunEvent>', description: '步骤事件与 run.completed { review, structuredReview }；硬筛可短路后续模型。' },
    example: { language: 'ts', code: `const result = await generateResumeReview({\n  workspaceSlug: slug, resumeProfile, jobDescriptionId, signal,\n  onEvent: setProgress, onDraftChange: setReviewDraft,\n})\nif (result) setStructuredReview(result.structuredReview)`, explanation: ['客户端同时处理 HTTP 非 2xx 与流内 run.failed。', 'abort 返回 null，不把取消显示为失败。'] },
    effect: { title: '可解释的结构化简历评价', description: '界面得到六维分、亮点、风险、偏差扫描、职级与下一步建议。', metrics: [{ label: 'pipeline', value: 'hard filter + 2 agents' }, { label: 'score', value: 'code weighted' }, { label: 'review max', value: '2000 chars' }], output: [{ label: 'structuredReview', value: 'six dimensions', tone: 'good' }, { label: 'hard filter', value: 'early reject', tone: 'warn' }, { label: 'invalid JD id', value: 'review without JD', tone: 'warn' }] },
    errors: [{ condition: 'workspace 缺失', behavior: '403。', recovery: '重新建立工作区上下文。' }, { condition: 'JD id 不存在/越界', behavior: '当前静默无 JD 生成。', recovery: '返回 404 或显式 warning，防止用户误以为已按岗位评价。' }, { condition: '工作流中模型失败', behavior: 'run.failed。', recovery: '保留已完成步骤，按安全阶段显式重试。' }], bestPractices: ['岗位上下文缺失必须对用户可见。', '总分由代码从版本化维度权重计算。', '流事件携带 runId，防止迟到流覆盖新请求。'],
    tests: [{ path: 'apps/ai-recruitment-copilot-backend/src/server/agents/__tests__/resume-analysis-agent-review.test.ts', proves: '核心流直测三代理事件、评分预览、Markdown 与 structuredReview 终态；非 HTTP 集成。' }, { path: 'apps/ai-recruitment-copilot-backend/src/server/agents/mastra/__tests__/resume-review-workflow.test.ts', proves: '工作流直接覆盖硬筛短路、步骤和组合结果。' }], officialSources: [{ label: 'Mastra workflows', url: 'https://mastra.ai/docs/workflows/overview' }, { label: 'Hono streaming', url: 'https://hono.dev/docs/helpers/streaming' }], related: ['generate-resume-review-markdown-http', 'stream-resume-review-workflow', 'recruitment-scorers'],
  },
  {
    slug: 'generate-resume-review-markdown-http', name: 'POST /api/w/:slug/interview/generate-review-markdown-stream', signature: 'POST { resumeProfile, jobDescriptionId? } → 200 text/event-stream<AiRunEvent>', kind: 'http', project: 'ai-interview', module: 'workflows', technology: 'Hono SSE + Mastra text stream + structured scorers', statusNote: '生产端点；先给 Markdown token，再串行结构化与评分，首字可见更快但总耗时可能更长。无效 JD id 同样静默退化为无 JD。', verifiedCommit: AI_INTERVIEW_COMMIT,
    summary: '先用文本 Agent 逐 chunk 输出评价 Markdown，截断至 2000 字后再把完整文本作为上下文做定性结构化和六维评分，最终返回同一 ResumeReview 契约。', whenToUse: '用户需要先看到评价逐字出现，同时后台继续收敛可入库的结构化评分时。', sourcePath: 'apps/ai-recruitment-copilot-backend/src/server/routes/interview/routes/analysis/route.ts', sourceLine: 168,
    parameters: [{ name: 'resumeProfile', type: 'ResumeProfile', required: true, description: '候选人结构化档案。' }, { name: 'jobDescriptionId', type: 'string | null', required: false, description: '当前组织岗位上下文。' }], returns: { type: 'text/event-stream<AiRunEvent>', description: 'markdown-review step.delta 先到；随后 qualitative/scoring；run.completed 含原 Markdown 与 structuredReview。' },
    example: { language: 'ts', code: `await generateResumeReviewMarkdownFirst({\n  workspaceSlug: slug, resumeProfile, jobDescriptionId, signal,\n  onDraftChange: markdown => previewRef.current?.render(markdown),\n  onEvent: event => updateStage(event.stepId),\n})`, explanation: ['draft 只用于即时预览，最终入库应等待 run.completed。', '最终 structuredReview 的定性/评分明确基于已展示 Markdown。'] },
    effect: { title: '评价文本先出现，评分随后收敛', description: '用户先看到 Markdown 增量，再看到六维评分与最终结构化评价。', metrics: [{ label: 'first stage', value: 'markdown stream' }, { label: 'follow-up agents', value: '2 sequential' }, { label: 'markdown cap', value: '2000 chars' }], output: [{ label: 'step.delta', value: '逐字评价', tone: 'good' }, { label: 'scoring preview', value: 'baseScore + dimensions', tone: 'good' }, { label: 'empty markdown', value: 'run.failed', tone: 'warn' }] },
    errors: [{ condition: '文本流为空/中断', behavior: 'run.failed，不进入可靠终态。', recovery: '保留 partial 并标注未完成；用户显式重试。' }, { condition: '文本成功但后续结构化/评分失败', behavior: '用户已见草稿但无 run.completed。', recovery: 'UI 标记“草稿，未完成评分”，不可当正式评价入库。' }, { condition: '无效 JD id', behavior: '静默按无 JD 生成。', recovery: '显式验证并返回 404/warning。' }], bestPractices: ['区分 partial draft 与 committed result。', '已输出 token 后不自动重放，避免重复内容/费用。', '将首 token、文本完成、评分完成分别计时。'],
    tests: [{ path: 'apps/ai-recruitment-copilot-backend/src/server/agents/__tests__/resume-analysis-agent-review.test.ts', proves: '核心流直测 Markdown delta 先于评分、Markdown 进入后续 prompts、最终结果一致；非 HTTP 集成。' }, { path: 'apps/ai-recruitment-copilot-backend/src/server/agents/mastra/__tests__/ai-run-stream.test.ts', proves: '同族事件边界的错误与单终态保证。' }], officialSources: [{ label: 'AI SDK text streaming', url: 'https://ai-sdk.dev/docs/ai-sdk-core/generating-text' }, { label: 'WHATWG Streams', url: 'https://streams.spec.whatwg.org/' }], related: ['generate-resume-review-http', 'create-ai-run-event-stream', 'recruitment-scorers'],
  },
]
