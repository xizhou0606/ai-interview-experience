import { AI_INTERVIEW_COMMIT } from '../helpers'
import type { ApiEntry } from '../types'

const READ_SOURCE = 'apps/ai-recruitment-copilot-backend/src/server/routes/studio/routes/resumes/read-route.ts'
const WRITE_SOURCE = 'apps/ai-recruitment-copilot-backend/src/server/routes/studio/routes/resumes/route.ts'
const ROUTE_TEST = 'apps/ai-recruitment-copilot-backend/src/server/routes/studio/routes/resumes/__tests__/route-behavior.test.ts'
const DAO_TEST = 'apps/ai-recruitment-copilot-backend/src/server/routes/studio/routes/resumes/__tests__/dao.test.ts'
const SKILL_TEST = 'apps/ai-recruitment-copilot-backend/src/server/routes/studio/routes/resumes/__tests__/skills.dao.test.ts'
const REVIEW_TEST = 'apps/ai-recruitment-copilot-backend/src/server/routes/studio/routes/resumes/utils/review-lifecycle.test.ts'
const hono = { label: 'Hono validation', url: 'https://hono.dev/docs/guides/validation' }
const drizzle = { label: 'Drizzle transactions', url: 'https://orm.drizzle.team/docs/transactions' }
const owasp = { label: 'OWASP object authorization', url: 'https://cheatsheetseries.owasp.org/cheatsheets/Insecure_Direct_Object_Reference_Prevention_Cheat_Sheet.html' }
const s3 = { label: 'AWS S3 object access', url: 'https://docs.aws.amazon.com/AmazonS3/latest/userguide/security-best-practices.html' }
const cache = { label: 'MDN HTTP caching', url: 'https://developer.mozilla.org/en-US/docs/Web/HTTP/Caching' }

type Spec = {
  slug: string; method: 'GET' | 'POST' | 'PATCH' | 'DELETE'; path: string; source: string; line: number; manifest: string; technology: string
  summary: string; use: string; params?: ApiEntry['parameters']; returns: string; result: string; code: string; visible: string
  error: string; recovery: string; practice: string; proves: string; related: string[]; test?: string; module?: string
  transport?: 'json' | 'binary'; sources?: ApiEntry['officialSources']; access?: string
}

function defineResumeHttp(s: Spec): ApiEntry {
  const endpoint = `${s.method} ${s.path}`
  const idParameter = s.path.includes('/:id') ? [{ name: 'id', type: 'path string', required: true, description: '当前 workspace 候选记录 ID；按 recruiting visibility 或明确的 workspace-member review 合同校验。' }] : []
  return {
    slug: s.slug, name: endpoint, signature: `${endpoint} → ${s.returns}`, kind: 'http', project: 'ai-interview', module: s.module ?? 'data-runtime', technology: s.technology,
    implementationStatus: 'production', statusNote: `生产端点；manifest 核对为 ${s.manifest}。${s.access ?? '由 auth + workspace + resumeLibrary permission 与 recruiting visibility 共同收口。'} ${s.proves}`,
    summary: s.summary, whenToUse: s.use, sourcePath: s.source, sourceLine: s.line, verifiedCommit: AI_INTERVIEW_COMMIT,
    parameters: [{ name: 'slug', type: 'path string', required: true, description: 'workspaceMiddleware 解析 activeOrg；客户端不能提交 organizationId 替代。' }, ...idParameter, ...(s.params ?? [])],
    returns: { type: s.returns, description: s.result },
    example: { language: 'ts', code: s.code, explanation: [`真实案例：${s.visible}`, '先按 HTTP 状态分支；binary 读取 Blob，JSON 再解析业务 payload。'] },
    effect: { title: s.visible, description: s.result, metrics: [{ label: 'transport', value: s.transport ?? 'json' }, { label: 'status', value: s.manifest }, { label: 'tenant boundary', value: 'activeOrg + visibility/id' }], output: [{ label: '成功', value: s.visible, tone: 'good' }, { label: '拒绝', value: s.error, tone: 'warn' }] },
    errors: [{ condition: s.error, behavior: `按 manifest/源码返回确定的 4xx 或全局 500；${s.proves}`, recovery: s.recovery }, { condition: '会话、组织、权限或对象可见范围失效', behavior: '401/404，跨租户记录不进入业务查询结果。', recovery: '恢复 workspace 会话并从有权列表重新选择；不要把 404 细分成可枚举的权限提示。' }],
    bestPractices: [s.practice, '所有 SQL 同时约束 organizationId 与 visibility/object id；缓存 key/tag 必须包含组织和可见范围，写后精确失效。', 'AI、对象存储、向量索引等跨系统副作用采用幂等键/outbox/重放与对账；记录延迟、失败类别和 actor 审计。'],
    tests: [{ path: s.test ?? ROUTE_TEST, proves: s.proves }], officialSources: [hono, drizzle, owasp, ...(s.sources ?? [])], related: s.related,
  }
}

export const studioResumesHttpApis: ApiEntry[] = [
  defineResumeHttp({
    slug: 'list-studio-resumes-http', method: 'GET', path: '/api/w/:slug/studio/resumes', source: READ_SOURCE, line: 76, manifest: 'JSON 200；错误 401', technology: 'Hono Zod + Drizzle secure pagination', summary: '按招聘可见范围分页检索候选人，支持 creator/JD/outcome/pipeline/skills CSV 过滤、搜索与排序；skills 使用交集语义并返回阶段聚合、重复摘要和业务分支进度。', use: '招聘台主列表、筛选器和分页 URL 状态同步。', params: [{ name: 'creatorIds/jdIds/outcomes/pipelineStages/skills', type: 'CSV query', required: false, description: 'CSV 被规范化；skills 是 AND 交集。' }, { name: 'page/pageSize/sortBy/sortOrder/search', type: 'query string', required: false, description: 'DAO 统一安全分页与 allowlist 排序。' }], returns: '200 PaginatedResult<ResumeListRecord> | 401', result: '列表显示候选人、技能、岗位、阶段、AI/人工面试和 offer 汇总；空可见范围返回空页。', code: `const qs = new URLSearchParams({ page: '1', pageSize: '20', skills: 'React,TypeScript', pipelineStages: 'screening' })\nconst page = await fetch('/api/w/acme/studio/resumes?' + qs, { credentials: 'include' }).then(r => r.json())`, visible: '招聘台按技能交集和阶段显示可分页候选人', error: '非法 query、无 activeOrg 或 visibility=none', recovery: '过滤变化重置第一页；客户端只使用共享排序字段并取消陈旧请求。', practice: '深分页改 cursor；search 做 debounce/trigram 索引，复杂聚合用 explain/analyze 与慢查询指标守护。', test: DAO_TEST, proves: 'DAO 测试直证组织隔离、visibility、搜索、技能交集、岗位/创建人过滤和阶段/offer 聚合；没有 HTTP validator/401 直测。', related: ['get-studio-resume-http', 'list-studio-resume-skill-suggestions-http'],
  }),
  defineResumeHttp({
    slug: 'create-studio-resume-http', method: 'POST', path: '/api/w/:slug/studio/resumes', source: WRITE_SOURCE, line: 132, manifest: 'JSON 201；错误 400/401', technology: 'Multipart + cached parse + best-effort AI review', module: 'workflows', summary: '接收手工字段、可选 PDF、客户端预解析 payload/评价；前置文件校验，按 payload→注册表缓存→现场解析复用，做语义去重，并 best-effort 生成结构化评价/筛选，写库后排队语义索引。', use: 'HR 手工新增候选人或把已预解析简历正式写入招聘台。', params: [{ name: 'resume / resumePayload / resumeReview', type: 'multipart File / JSON strings', required: false, description: '文件可选；payload 可复用 OCR/profile/questions，review 必须满足严格 v3 schema。' }, { name: 'candidateName / email / phone / JD / notes / targetRole / assessment', type: 'multipart fields', required: true, description: '创建 schema 校验；JD 必须属于 activeOrg。' }], returns: '201 ResumeDetail | 400 | 401 | 500', result: '详情立即出现；AI 失败不会阻断创建，而以 failed 状态/错误记录，语义索引异步补齐。', code: `const fd = new FormData(); fd.set('candidateName', '林岚'); fd.set('resume', file); fd.set('jobDescriptionId', jdId)\nconst detail = await fetch('/api/w/acme/studio/resumes', { method: 'POST', body: fd, credentials: 'include' }).then(r => r.json())`, visible: '新候选人入库并显示解析、AI 评价和重复状态', error: '非法/超大文件、跨租户岗位、严格 review schema 失败或存储/解析异常', recovery: '保留草稿与 content hash；400 就地修复，外部系统失败按状态重放，防止重复创建。', practice: '创建 API 接收 Idempotency-Key；对象上传、DB 写、重复快照、AI/索引分别可补偿，原始 OCR 文本不进入共享 DTO/日志。', proves: 'route-behavior 直证 POST 后持久化重复匹配；route/payload 测试直证预解析时只存对象和 questions DTO，未覆盖真实 AI/S3 故障。', related: ['find-semantic-resume-duplicates', 'generate-resume-review-http', 'put-object-bytes'],
  }),
  defineResumeHttp({
    slug: 'delete-studio-resume-http', method: 'DELETE', path: '/api/w/:slug/studio/resumes/:id', source: WRITE_SOURCE, line: 510, manifest: 'JSON 200；错误 401/404/409', technology: 'State guard + Drizzle delete + distributed cleanup', module: 'background-jobs', summary: '先按 recruiting visibility 加载记录；queued/processing 拒绝删除，删除数据库行后 best-effort 清理语义索引、重复快照、缓存和聊天 resumeImports 假状态。', use: '候选人记录可安全移除且没有进行中的解析任务时。', returns: '200 { success: true } | 401 | 404 | 409', result: '候选人从招聘台、语义搜索、重复匹配和聊天“已入库”状态消失。', code: `const response = await fetch('/api/w/acme/studio/resumes/' + id, { method: 'DELETE', credentials: 'include' })\nif (response.status === 409) showToast('解析中，稍后再删')`, visible: '单个候选人及跨功能残留被清理', error: '记录不可见或 resumeParseStatus 为 queued/processing', recovery: '409 等 worker 完成/取消后重试；清理失败由墓碑/outbox 对账重放。', practice: '删除前协调 worker 锁/取消令牌；数据库提交与索引/聊天清理之间用 durable outbox，DELETE 重试返回幂等成功。', proves: 'route-behavior 直证删除后调用语义与重复清理；未覆盖解析中 409、聊天 sweep 失败和跨系统补偿。', related: ['bulk-delete-studio-resumes-http', 'delete-studio-resume-pool-item-http'],
  }),
  defineResumeHttp({
    slug: 'get-studio-resume-http', method: 'GET', path: '/api/w/:slug/studio/resumes/:id', source: READ_SOURCE, line: 183, manifest: 'JSON 200；错误 401/404', technology: 'Drizzle visibility-scoped detail DTO', summary: '按 organizationId、id 和 recruiting visibility 返回候选人详情，包含可编辑资料、AI 评价、筛选、问题、阶段与文件存在标记，但不暴露 scheduleEntries 等内部集合。', use: '招聘台详情/编辑弹窗首次加载。', returns: '200 ResumeDetail | 401 | 404', result: '详情页用稳定 DTO 展示候选信息与业务状态；跨租户和超出可见范围统一 404。', code: `const detail = await fetch('/api/w/acme/studio/resumes/' + encodeURIComponent(id), { credentials: 'include' }).then(r => r.json())`, visible: '候选人详情、AI 评价和问题完整回显', error: '对象不存在、跨租户或不在 recruiting visibility', recovery: '移除详情缓存并回列表；不向用户泄漏对象真实归属。', practice: 'DTO 明确 PII 字段分级与最小化；详情缓存 private/no-store，并用 ETag/updatedAt 支持并发编辑。', test: DAO_TEST, proves: 'DAO 测试直证 detail 应用 recruiting visibility；route DTO 测试证明不返回 scheduleEntries，未直接覆盖本 GET 状态码。', related: ['patch-studio-resume-http', 'get-studio-resume-timeline-http'],
  }),
  defineResumeHttp({
    slug: 'patch-studio-resume-http', method: 'PATCH', path: '/api/w/:slug/studio/resumes/:id', source: WRITE_SOURCE, line: 315, manifest: 'JSON 200；错误 400/401/404/409', technology: 'Multipart whitelist update + Drizzle transaction', summary: '仅在解析可编辑状态接收表单/可选新文件；显式白名单更新身份、岗位、HR 评价和简历资料，事务同步技能与岗位变更审计，岗位变化再重置旧评价并排队语义索引。', use: 'HR 修改候选人资料、岗位绑定、人工评价或替换简历。', params: [{ name: 'multipart body', type: 'ResumeLibraryEditForm + resume? + resumeReview?', required: true, description: 'candidateName 必填；新文件再次走 PDF/20MB 校验；review 严格 v3。' }], returns: '200 ResumeDetail | 400 | 401 | 404 | 409', result: '详情回显新资料；替换文件重算技能/索引，换岗位写审计并清空不再适用的旧评价。', code: `const fd = new FormData(); fd.set('candidateName', draft.name); fd.set('jobDescriptionId', nextJdId)\nconst saved = await fetch('/api/w/acme/studio/resumes/' + id, { method: 'PATCH', body: fd, credentials: 'include' }).then(r => r.json())`, visible: '详情与列表同步新身份、岗位、技能和评价状态', error: '解析未 ready、字段/review 非法、跨租户岗位或并发覆盖', recovery: '409 等解析结束；400 保留草稿；增加 If-Match/version，冲突时展示差异而非 last-write-wins。', practice: 'PATCH 仍是表单快照语义，应显式文档化；用乐观锁把岗位/评价并发变化变成 409，外部索引异步按 record version 去陈旧。', proves: 'route-behavior 直证岗位变更审计与评价重置；parser/profile-sync/skills 测试覆盖白名单输入与技能同步，未覆盖乐观锁（当前不存在）。', related: ['get-studio-resume-http', 'reassess-studio-resume-http'],
  }),
  defineResumeHttp({
    slug: 'list-studio-resume-duplicate-matches-http', method: 'GET', path: '/api/w/:slug/studio/resumes/:id/duplicate-matches', source: READ_SOURCE, line: 200, manifest: 'JSON 200；错误 401/404', technology: 'Visibility guard + semantic match snapshot', module: 'semantic-search', summary: '先按 recruiting visibility 证明候选人可见，再用 organizationId、user owner、sourceType=studio_interview 与 id 读取重复快照。', use: '详情页解释候选人重复风险并提供合并/保留决策。', returns: '200 { matches: ResumeDuplicateMatch[] } | 401 | 404', result: '展示规则、语义分数和匹配来源，不在每次打开时重新 embedding。', code: `const { matches } = await fetch('/api/w/acme/studio/resumes/' + id + '/duplicate-matches', { credentials: 'include' }).then(r => r.json())`, visible: '重复风险抽屉显示可解释匹配证据', error: '记录超出 visibility 或快照/索引陈旧', recovery: '404 回列表；陈旧快照按 source version 重算并标记算法版本。', practice: '重复分数不能自动覆盖/删除候选人；保留人工决策、阈值/embedding 版本和误报指标。', proves: 'route-behavior 直证 visibility 检查先于 match 查询，并断言 org/user/source 过滤参数。', related: ['dedup-check-studio-resumes-http', 'find-semantic-resume-duplicates'],
  }),
  defineResumeHttp({
    slug: 'patch-studio-resume-evaluation-http', method: 'PATCH', path: '/api/w/:slug/studio/resumes/:id/evaluation', source: WRITE_SOURCE, line: 278, manifest: 'JSON 200；错误 401/404', technology: 'Zod status transition + audit-aware DAO', summary: '校验 resumeEvaluationUpdateSchema，在可见对象上更新人工简历评估状态并记录 operator，失效组织缓存后回读详情。', use: '有 update 权限的 HR 在招聘台纠正或清空评估状态。', params: [{ name: 'body.status', type: 'resume evaluation status | null', required: true, description: '共享 schema 限定合法状态。' }], returns: '200 ResumeDetail | 400 | 401 | 404', result: '候选人列表/详情立即显示新的人工评估；源码 validator 的 400 未被 manifest errorStatuses 静态收集。', code: `await fetch('/api/w/acme/studio/resumes/' + id + '/evaluation', { method: 'PATCH', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ status: 'passed' }), credentials: 'include' })`, visible: 'HR 评估标签更新并记录操作人', error: '非法 status、对象不可见或写时已删除', recovery: '400 回滚控件；404 刷新列表；并发修改用 version/If-Match 返回 409。', practice: '状态更新定义允许迁移、审计原因与乐观锁；不要把客户端标签直接写入不受控 text 字段。', proves: 'family route-behavior 覆盖一次性 review evaluation 而非此 permissioned PATCH；本 handler 的 validator/缓存失效尚无直接测试。', related: ['submit-studio-resume-review-evaluation-http', 'get-studio-resume-http'],
  }),
  defineResumeHttp({
    slug: 'launch-studio-resume-interview-http', method: 'POST', path: '/api/w/:slug/studio/resumes/:id/launch-interview', source: READ_SOURCE, line: 446, manifest: 'JSON 201；错误 400/401/404/409', technology: 'Pipeline state machine + transaction + context snapshot', module: 'workflows', summary: '验证候选人可见、未结案/未进入后续阶段且简历可启动；事务写回最多 50 道题、推进 ai_interview、新建默认轮次、审计和自动绑定题库，事务后创建活动上下文快照。', use: '从筛选阶段正式发起第一轮 AI 面试。', params: [{ name: 'body.interviewQuestions', type: 'StudioInterviewQuestion[0..50]', required: true, description: '可由用户编辑后提交；共享 client schema 校验。' }], returns: '201 InterviewRoundDetail | 400 | 401 | 404 | 409', result: '候选人阶段变为 ai_interview，出现默认轮次、审计事件和冻结上下文。', code: `const round = await fetch('/api/w/acme/studio/resumes/' + id + '/launch-interview', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ interviewQuestions }), credentials: 'include' }).then(r => r.json())`, visible: '招聘台出现可进入的首轮 AI 面试', error: 'closed/后续阶段、简历未 ready、无默认轮次或并发重复启动', recovery: '按 409 指引先重新激活/等待解析；用行锁/条件 UPDATE+唯一轮次约束把双击变成同一结果。', practice: '状态机判断与写入必须同一事务/锁内复核；快照创建若在事务后失败，需要 outbox 补偿，不能留下无上下文轮次。', proves: 'route-behavior 直证 201、审计、context snapshot 与后续阶段 409；source-boundary 测试限制快照创建入口，未覆盖并发双启。', related: ['load-studio-interviews-hydration-state', 'build-interview-dispatch-contract'],
  }),
  defineResumeHttp({
    slug: 'reassess-studio-resume-http', method: 'POST', path: '/api/w/:slug/studio/resumes/:id/reassess', source: READ_SOURCE, line: 248, manifest: 'JSON 200；错误 401/404/409/500', technology: 'Synchronous AI reassessment lifecycle', module: 'workflows', summary: '验证 update 权限与 visibility 后同步运行 reassessResumeRecord；已结案、未解析或无岗位返回 409，其他 AI 失败回读详情并返回 500，成功失效缓存返回 200。', use: 'HR 在岗位/简历变化后主动重跑 AI 筛选与结构化评价。', returns: '200 ResumeDetail | 401 | 404 | 409 | 500 ResumeDetail', result: '详情更新 screening/review；失败时仍返回记录，让 UI 保留上次成功结果并展示失败状态。', code: `const response = await fetch('/api/w/acme/studio/resumes/' + id + '/reassess', { method: 'POST', credentials: 'include' })\nconst detail = await response.json()`, visible: 'AI 筛选与评价刷新，失败仍保留上一版', error: 'closed、简历未 ready、未绑定岗位、模型失败或旧任务晚到', recovery: '409 修复前置条件；500 显式重试；用 assessment run id/JD version 忽略陈旧任务。', practice: '长 AI 调用更适合 202+job 状态/SSE；用 run version、超时、取消与“最后成功结果”保证并发重评不覆盖新岗位。', test: REVIEW_TEST, proves: 'lifecycle 测试直证成功原子更新、失败保留旧结果、岗位中途变化和陈旧 job 防覆盖；HTTP 200/409/500 没有直接测试。', related: ['generate-resume-review-http', 'enqueue-resume-review-generation-jobs'],
  }),
  defineResumeHttp({
    slug: 'get-studio-resume-file-http', method: 'GET', path: '/api/w/:slug/studio/resumes/:id/resume', source: READ_SOURCE, line: 556, manifest: 'binary 200；错误 JSON 401/404', technology: 'Visibility guard + S3/R2 stream', module: 'object-storage', transport: 'binary', sources: [s3, cache], summary: '先按 detail visibility 与 hasResumeFile 授权，再流式返回对象，设置 private max-age=300、inline 文件名、content type/length。', use: '招聘台详情内嵌原始简历。', returns: '200 binary stream | 401 | 404 JSON', result: '浏览器边下载边渲染，不把整个文件装入服务端内存。', code: `const response = await fetch('/api/w/acme/studio/resumes/' + id + '/resume', { credentials: 'include' })\nconst file = await response.blob()`, visible: '原始简历在详情中五分钟私有缓存预览', error: '对象不可见、无文件、storage key/对象丢失', recovery: '404 保留结构化资料并提示重新上传；对象清单任务对账 DB key。', practice: '授权后对象查询也应保留 organizationId 谓词，避免授权与读取间 TOCTOU；加 nosniff 和受控 MIME/文件名。', proves: 'family tests 未直接覆盖二进制流、header、S3 404；源码对象行二次查询只按 id，前置授权依赖全局唯一 id。', related: ['get-studio-resume-preview-http', 'put-object-bytes'],
  }),
  defineResumeHttp({
    slug: 'get-studio-resume-preview-http', method: 'GET', path: '/api/w/:slug/studio/resumes/:id/resume-preview.pdf', source: READ_SOURCE, line: 605, manifest: 'binary 200；错误 JSON 401/404', technology: 'Object bytes + PPTX PDF conversion', module: 'object-storage', transport: 'binary', sources: [s3, cache], summary: '授权后读取完整对象 bytes，通过 createPptxPreviewPdfResponse 统一生成/缓存 PDF 预览。', use: '原简历为 PPTX 等格式但 UI 统一使用 PDF viewer 时。', returns: '200 application/pdf | 401 | 404 JSON | 500', result: '用户在同一阅读器查看预览；转换异常进入全局 500。', code: `const response = await fetch('/api/w/acme/studio/resumes/' + id + '/resume-preview.pdf', { credentials: 'include' })\nconst previewUrl = URL.createObjectURL(await response.blob())`, visible: '异构简历统一为 PDF 预览', error: '文件缺失、转换超时/内存超限或缓存陈旧', recovery: '回退原文件下载；转换移到限额 worker 并按 object version 重试/缓存。', practice: '转换设文件大小、页数、CPU/内存和超时；缓存键绑定对象版本，覆盖文件后精准失效旧 PDF。', proves: 'family tests未直接覆盖 preview helper 的真实转换、资源上限、content type 与 500。', related: ['get-studio-resume-file-http', 'get-studio-resume-review-preview-http'],
  }),
  defineResumeHttp({
    slug: 'get-studio-resume-review-http', method: 'GET', path: '/api/w/:slug/studio/resumes/:id/review', source: READ_SOURCE, line: 236, manifest: 'JSON 200；错误 401/404', technology: 'Workspace-member review DTO', summary: '不要求 resumeLibrary.read action，只要已认证 workspace member 即按 organizationId+id 加载 review 详情；这是比招聘台 visibility 更宽的协作评审入口。', use: '普通 workspace 成员进入候选人评审页面。', returns: '200 ResumeDetail | 401 | 404', result: '评审者看到当前组织候选人资料与评价，不受 recruiting visibility role 过滤。', code: `const review = await fetch('/api/w/acme/studio/resumes/' + id + '/review', { credentials: 'include' }).then(r => r.json())`, visible: 'workspace 评审者打开候选人评审页', error: '非 workspace member、跨组织记录或 member-wide 策略配置错误', recovery: '401 回登录；404 回评审列表；权限模型变化必须同时迁移 review 子路由。', practice: '把 member-wide review 作为显式产品权限而非 middleware 漏配；PII/文件访问单独授权并用矩阵测试防回归。', access: '源码刻意只有 auth+workspace middleware，无 resumeLibrary action/visibility；按 workspace-member-wide 合同加载。', proves: 'route-behavior 直证 workspace review 可读并可一次性评估；应补角色矩阵测试证明该宽权限是产品意图。', related: ['submit-studio-resume-review-evaluation-http', 'get-studio-resume-review-timeline-http'],
  }),
  defineResumeHttp({
    slug: 'submit-studio-resume-review-evaluation-http', method: 'POST', path: '/api/w/:slug/studio/resumes/:id/review/evaluation', source: READ_SOURCE, line: 394, manifest: 'JSON 200；错误 401/404/409', technology: 'Atomic compare-and-set evaluation', summary: 'workspace member 提交 schema 校验的评估状态；DAO 只允许未评估记录更新一次，已评估返回 409，成功记录 operator 并失效缓存。', use: '协作评审者首次给候选人做通过/不通过等不可重复评价。', params: [{ name: 'body.status', type: 'ResumeEvaluationStatus', required: true, description: 'resumeEvaluationStatusSubmitSchema 校验的非空状态。' }], returns: '200 ResumeDetail | 400 | 401 | 404 | 409', result: '评审页显示首次提交结果；第二个并发提交者得到 409，不覆盖第一位。', code: `const response = await fetch('/api/w/acme/studio/resumes/' + id + '/review/evaluation', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ status: 'passed' }), credentials: 'include' })`, visible: '一次性评价落库，重复/并发提交明确冲突', error: '非法状态、记录不存在或 already_evaluated', recovery: '400 修正；409 刷新并展示已提交者/时间，不自动重试覆盖。', practice: '一次性写入用条件 UPDATE/唯一约束而非 read-then-write；保留 actor、时间、理由和管理员纠错的独立审计流程。', access: '与 review 页面一致为 workspace-member-wide，无 resumeLibrary.update action。', proves: 'route-behavior 直证 POST 200、operator/status 入参和一次性 DAO 调用；未直接覆盖两个并发请求只有一个成功。', related: ['get-studio-resume-review-http', 'patch-studio-resume-evaluation-http'],
  }),
  defineResumeHttp({
    slug: 'get-studio-resume-review-file-http', method: 'GET', path: '/api/w/:slug/studio/resumes/:id/review/resume', source: READ_SOURCE, line: 311, manifest: 'binary 200；错误 JSON 401/404', technology: 'Workspace-member file stream', module: 'object-storage', transport: 'binary', sources: [s3, cache], summary: '按 workspace-member detail 验证记录/hasResumeFile，再用 organizationId+id 获取 storage key 并流式返回原文件。', use: '评审页面查看候选人原始简历。', returns: '200 binary stream | 401 | 404 JSON', result: '评审者在浏览器中查看五分钟 private-cache 原文件。', code: `const response = await fetch('/api/w/acme/studio/resumes/' + id + '/review/resume', { credentials: 'include' })\nconst file = await response.blob()`, visible: '评审页内嵌原始简历', error: '跨组织、无文件、对象丢失或 member-wide PII 权限过宽', recovery: '404 回结构化评审；安全审计发现越权时收紧为专门 reviewResume.read action。', practice: '简历文件属于高敏 PII；member-wide 读取必须经过政策确认、访问日志、下载水印/限速与短私有缓存。', access: '只有 auth+workspace，未调用 resumeLibrary permission；对象 SQL 二次约束 organizationId。', proves: 'family tests未直接覆盖 member role 矩阵、文件流/headers 或对象存储失败。', related: ['get-studio-resume-review-preview-http', 'get-studio-resume-review-http'],
  }),
  defineResumeHttp({
    slug: 'get-studio-resume-review-preview-http', method: 'GET', path: '/api/w/:slug/studio/resumes/:id/review/resume-preview.pdf', source: READ_SOURCE, line: 355, manifest: 'binary 200；错误 JSON 401/404', technology: 'Workspace-member PDF preview conversion', module: 'object-storage', transport: 'binary', sources: [s3, cache], summary: 'workspace-member 授权后读取对象 bytes，统一转换/返回 PDF 预览。', use: '评审页用 PDF viewer 阅读 PPTX/异构简历。', returns: '200 application/pdf | 401 | 404 JSON | 500', result: '评审者获得统一 PDF 预览，转换失败不影响结构化评价数据。', code: `const response = await fetch('/api/w/acme/studio/resumes/' + id + '/review/resume-preview.pdf', { credentials: 'include' })\nconst preview = await response.blob()`, visible: '评审页统一 PDF 阅读体验', error: '文件/对象缺失、转换资源耗尽或访问策略不匹配', recovery: '回退原文件/结构化资料；worker 限额转换并按对象版本缓存。', practice: '把转换服务当不可信文件处理边界：沙箱、超时、反病毒、格式 allowlist；审计 member-wide PII 读取。', access: '只有 auth+workspace，明确继承 review member-wide 文件读取策略。', proves: 'family tests未覆盖真实转换、安全沙箱、资源上限或 role 矩阵。', related: ['get-studio-resume-review-file-http', 'get-studio-resume-preview-http'],
  }),
  defineResumeHttp({
    slug: 'list-studio-resume-review-rounds-http', method: 'GET', path: '/api/w/:slug/studio/resumes/:id/review/rounds', source: READ_SOURCE, line: 298, manifest: 'JSON 200；错误 401/404', technology: 'Workspace-member round query', summary: '先用 member-wide detail 验证组织内候选人，再按 candidate+organization 列出所有面试轮次。', use: '评审页面查看 AI/人工面试历史轮次。', returns: '200 InterviewRoundListRecord[] | 401 | 404', result: '评审者看到按 sortOrder 排列的轮次状态和结果。', code: `const rounds = await fetch('/api/w/acme/studio/resumes/' + id + '/review/rounds', { credentials: 'include' }).then(r => r.json())`, visible: '评审页展示候选人全部面试轮次', error: '候选人跨组织/不存在或轮次权限继承不明确', recovery: '404 回评审列表；若轮次包含敏感录制/报告，DTO 分层而非直接透传。', practice: '根候选人授权后，子资源查询仍带 organizationId；轮次 DTO 不携带录制密钥、内部 prompt 或不必要 PII。', access: 'workspace-member-wide review 合同；无 resumeLibrary.read action。', proves: 'family route test只覆盖 review/evaluation，不直接覆盖 rounds 排序、DTO 最小化和角色矩阵。', related: ['list-studio-resume-rounds-http', 'get-studio-resume-review-timeline-http'],
  }),
  defineResumeHttp({
    slug: 'get-studio-resume-review-timeline-http', method: 'GET', path: '/api/w/:slug/studio/resumes/:id/review/timeline', source: READ_SOURCE, line: 286, manifest: 'JSON 200；错误 401/404', technology: 'Workspace-member aggregated audit timeline', summary: '以 visibility={kind:all} 在当前 organization 内聚合候选人阶段、轮次和审计事件，为 member-wide review 呈现完整时间线。', use: '评审者理解候选人从入库到当前阶段的变更轨迹。', returns: '200 CandidateTimeline | 401 | 404', result: 'UI 显示带标题、描述、tone 和时间的完整审计/招聘事件流。', code: `const timeline = await fetch('/api/w/acme/studio/resumes/' + id + '/review/timeline', { credentials: 'include' }).then(r => r.json())`, visible: '评审页按时间回放阶段和操作事件', error: '对象跨组织或审计 detail 含不应向普通 member 展示的信息', recovery: '404 回列表；对 event detail 做字段级 presenter/redaction，不透传任意 JSON。', practice: '审计时间线按受众分级脱敏、稳定分页；事件 append-only 并记录 actor/原因，旧 detail schema 有兼容 renderer。', access: 'workspace-member-wide，显式传 kind:all；这是需安全评审的宽可见范围。', proves: '当前代表测试未直接覆盖 timeline 聚合、kind:all 边界和 event detail 脱敏。', related: ['get-studio-resume-timeline-http', 'get-studio-resume-review-http'],
  }),
  defineResumeHttp({
    slug: 'list-studio-resume-rounds-http', method: 'GET', path: '/api/w/:slug/studio/resumes/:id/rounds', source: READ_SOURCE, line: 425, manifest: 'JSON 200；错误 401/404', technology: 'Permission + visibility-scoped round list', summary: '要求 resumeLibrary.read，先按 recruiting visibility 验证候选人，再按 organizationId 列出候选人轮次。', use: '招聘台详情的“AI 面试”tab。', returns: '200 InterviewRoundListRecord[] | 401 | 404', result: '按 sortOrder 显示每轮状态、类型和结果，超出 visibility 的候选人不可枚举。', code: `const rounds = await fetch('/api/w/acme/studio/resumes/' + id + '/rounds', { credentials: 'include' }).then(r => r.json())`, visible: '招聘台详情展示所有 AI 面试轮次', error: '权限/visibility 不满足或候选人已删除', recovery: '刷新详情；轮次增长后改 cursor 并保留稳定 sortOrder+id。', practice: '子资源列表同时校验根对象与 org；大轮次数分页，聚合字段避免 N+1，录制/报告另行细粒度授权。', proves: 'family tests未直接覆盖此 handler；DAO 列表/详情测试只证明候选人 visibility，不证明轮次查询。', related: ['list-studio-resume-review-rounds-http', 'launch-studio-resume-interview-http'],
  }),
  defineResumeHttp({
    slug: 'get-studio-resume-timeline-http', method: 'GET', path: '/api/w/:slug/studio/resumes/:id/timeline', source: READ_SOURCE, line: 219, manifest: 'JSON 200；错误 401/404', technology: 'Visibility-scoped audit timeline', summary: '要求 resumeLibrary.read，以组织和 recruiting visibility 聚合候选人阶段、审计、轮次等事件并返回时间线 DTO。', use: '招聘台详情解释岗位变更、评价、面试和 offer 进展。', returns: '200 CandidateTimeline | 401 | 404', result: '有权 HR 看到可追溯事件序列，受限角色看不到不在 scope 的候选人。', code: `const timeline = await fetch('/api/w/acme/studio/resumes/' + id + '/timeline', { credentials: 'include' }).then(r => r.json())`, visible: '招聘台时间线回放候选人全流程', error: 'visibility 不满足、对象删除或旧 audit detail 无法解析', recovery: '404 回列表；timeline presenter 对未知旧事件使用安全兜底标题。', practice: '时间线查询分页/设上限；审计事件 append-only，detail schema 版本化并避免存储无必要 PII/密钥。', test: DAO_TEST, proves: 'DAO 测试覆盖列表 stage/offer 聚合但未直接覆盖 loadCandidateTimeline 的权限、排序与旧事件兼容。', related: ['get-studio-resume-review-timeline-http', 'get-studio-resume-http'],
  }),
  defineResumeHttp({
    slug: 'bulk-delete-studio-resumes-http', method: 'POST', path: '/api/w/:slug/studio/resumes/bulk-delete', source: WRITE_SOURCE, line: 552, manifest: 'JSON 200；错误 400/401/404/409', technology: 'Bulk visibility guard + distributed cleanup', module: 'background-jobs', summary: '校验非空 ids，按 organizationId+recruiting visibility 批量加载；任一 queued/processing 则整体 409，否则删除可见行并逐条清理语义、重复和聊天状态。', use: '招聘台多选删除少量候选人。', params: [{ name: 'body.ids', type: 'non-empty string[]', required: true, description: '空数组/清洗后为空返回 400；源码未设置显式最大数量。' }], returns: '200 { success: true; deletedCount: number } | 400 | 401 | 404 | 409', result: '返回实际 deletedCount；不存在/越界 ID 不会被删除，但当前不会逐项报告，部分输入可能静默少删。', code: `const result = await fetch('/api/w/acme/studio/resumes/bulk-delete', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ ids: selectedIds }), credentials: 'include' }).then(r => r.json())`, visible: '多选候选人批量消失并返回实际删除数', error: 'ids 空、visibility=none、任一解析中或实际删除数小于请求数', recovery: '409 等待/取消解析；比较 deletedCount 与去重后 ids 并刷新；大批次改 202 job。', practice: '限制批量数量并去重；在事务内校验+删除避免状态竞态，外部清理用批量 outbox/worker，不在请求内串行 N 次远程调用。', proves: 'manifest 代表 family test；当前 route-behavior 只直测单删清理，未覆盖批量部分命中、数量上限和并发状态变化。', related: ['delete-studio-resume-http', 'list-studio-resumes-http'],
  }),
  defineResumeHttp({
    slug: 'dedup-check-studio-resumes-http', method: 'POST', path: '/api/w/:slug/studio/resumes/dedup-check', source: READ_SOURCE, line: 151, manifest: 'JSON 200；错误 401', technology: 'Zod identity input + hybrid semantic dedup', module: 'semantic-search', summary: '接收可选姓名/邮箱/电话/ResumeProfile，在 activeOrg 内运行规则+语义重复搜索并记录结构化诊断日志。', use: '创建前即时提示潜在重复，而不持久化候选人。', params: [{ name: 'body', type: '{ name?; email?; phone?; resumeProfile? }', required: true, description: '字符串有限长；所有字段可空意味着可能返回空匹配。' }], returns: '200 { matches: ResumeDuplicateMatch[] } | 400 | 401', result: '上传/手工录入流程可在保存前展示重复候选及证据；validator 400 未被 manifest 静态收集。', code: `const { matches } = await fetch('/api/w/acme/studio/resumes/dedup-check', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ name, email, phone, resumeProfile }), credentials: 'include' }).then(r => r.json())`, visible: '保存前出现规则与语义重复预警', error: '字段超限、无有效信号、embedding 服务失败或日志泄露 PII', recovery: '输入错误修正；服务故障降级精确规则并标明“不完整”；日志只留 hash/计数/内部 id。', practice: '对空信号短路、限流和请求取消；当前 console.info 输出 matches 需审查 PII，观测应脱敏并记录算法版本/延迟。', proves: 'dedup 单测覆盖 check/force 业务冲突策略，route-behavior 覆盖持久化快照；本预检 handler 和诊断日志没有直接测试。', related: ['list-studio-resume-duplicate-matches-http', 'find-semantic-resume-duplicates'],
  }),
  defineResumeHttp({
    slug: 'list-studio-resume-skill-suggestions-http', method: 'GET', path: '/api/w/:slug/studio/resumes/skill-suggestions', source: READ_SOURCE, line: 127, manifest: 'JSON 200；错误 401', technology: 'Zod coercion + canonical skill index', summary: '按 activeOrg 查询 canonical skill 统计，支持大小写不敏感 prefix，limit 默认 50 且限制 1..100，按候选人数降序/normalized 升序。', use: '招聘台技能筛选 autocomplete。', params: [{ name: 'prefix', type: 'query string ≤80', required: false, description: 'trim 后大小写不敏感前缀。' }, { name: 'limit', type: 'integer 1..100', required: false, defaultValue: '50', description: 'Zod coerce 并限幅。' }], returns: '200 { records: SkillSuggestion[] } | 400 | 401', result: '输入 React 时展示当前组织常用技能及候选人数，不泄漏兄弟组织数据。', code: `const { records } = await fetch('/api/w/acme/studio/resumes/skill-suggestions?prefix=rea&limit=20', { credentials: 'include' }).then(r => r.json())`, visible: '技能筛选器按组织热度补全输入', error: '非法 limit、prefix 超长、计数触发器/索引漂移', recovery: '400 恢复默认；周期对账 canonical counts 与候选人技能数组。', practice: 'autocomplete debounce、取消前次请求和短私有缓存；唯一规范化键+事务 delta/删除触发器保证并发计数一致。', test: SKILL_TEST, proves: 'DAO 测试直证组织隔离、prefix、排序、limit clamp、规范化计数与 DELETE 触发器；未直接覆盖 HTTP validator/401。', related: ['list-studio-resumes-http', 'embed-resume-semantic-texts'],
  }),
]
