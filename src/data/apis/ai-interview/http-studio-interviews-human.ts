import { defineInterviewHttp as d, err, p, PIPELINE_TEST, OFFER_ROUTE_TEST, LIVEKIT, DRIZZLE_TX } from './http-studio-interviews-shared'
import type { ApiEntry } from '../types'

const candidate = p('id', 'path candidateId', true, '真人轮次与 Offer 子路由中的 :id 是候选人 interviewRecordId。')
const humanRound = p('roundId', 'path humanInterviewRoundId', true, '真人复面轮次 ID；DAO 同时按 organizationId 校验。')
const auth = err('activeOrg/session 或 humanInterview/offer 权限缺失', '返回 401/403，DAO 不执行。', '恢复工作区并申请与动作匹配的 CRUD 权限。')

export const httpStudioInterviewHumanApis: ApiEntry[] = [
  d({
    slug: 'list-studio-human-interview-rounds-http', method: 'GET', path: '/api/w/:slug/studio/interviews/:id/human-interview-rounds', source: 'human-route.ts', line: 55, permission: 'humanInterview.read',
    summary: '按 candidateId+organizationId 返回全部真人复面轮次（含 cancelled），按 sortOrder 升序并组装面试官等子表。', when: '候选人详情展示真人复面时间线。', parameters: [candidate], returnType: '200 HumanInterviewRound[]', returnDescription: '无记录通常为空数组；无 activeOrg 401。', exampleNotes: ['列表保留取消轮次，UI 不应只按存在性判断进行中。', '历史 candidate :id 与 AI round :id 语义不同。'], effectTitle: '展示真人复面时间线', effectDescription: '用户按业务顺序看到计划、完成与取消轮次。', metrics: [{ label: 'order', value: 'sortOrder asc' }], outputs: [{ label: 'candidate rounds', value: 'all statuses', tone: 'good' }, { label: 'none', value: '[]', tone: 'neutral' }], errors: [auth, err('candidateId 不存在/跨组织', 'DAO 返回空集合而非明确 404 的可能性。', 'UI 同时依赖候选人详情；高保证接口可先验证 parent。')], practices: ['明确空集合与不存在的 contract。', '子表批量加载，避免逐轮 N+1。'], test: PIPELINE_TEST, proves: 'core DAO 直测按 sortOrder 返回全部轮次且包含 cancelled；HTTP 401 与不存在语义未直测。', related: ['create-studio-human-interview-round-http', 'get-studio-interview-round-http'] }),
  d({
    slug: 'create-studio-human-interview-round-http', method: 'POST', path: '/api/w/:slug/studio/interviews/:id/human-interview-rounds', source: 'human-route.ts', line: 64, permission: 'humanInterview.create',
    summary: '验证候选人存在且未 closed/offer，强制 format=online 且清空 location/meetingUrl，创建轮次和面试官关联；第一轮会把可推进候选人推到 human_interview 并记录活动。', when: 'AI/筛选后安排第一轮或追加真人在线复面。', parameters: [candidate, p('label/scheduledAt/interviewerIds/...', 'JSON HumanInterviewRoundInput', true, '输入经共享 schema；服务端覆盖 format/location/meetingUrl。')], returnType: '200 HumanInterviewRound', returnDescription: '候选人不存在 404，closed/offer 或 body 非法 400。', exampleNotes: ['format/location/meetingUrl 由服务端定为在线会议。', '创建成功可能同步推进候选人 pipelineStage。'], effectTitle: '安排真人复面并推进阶段', effectDescription: '新轮次、面试官 junction、活动日志和缓存同步更新。', metrics: [{ label: 'format', value: 'online forced' }, { label: 'first round', value: 'auto-advance' }], outputs: [{ label: 'eligible candidate', value: 'round created', tone: 'good' }, { label: 'closed/offer', value: '400', tone: 'warn' }], errors: [auth, err('候选人 closed 或已 offer', '400，不创建轮次。', 'closed 先重新激活；offer 阶段禁止倒序新建。'), err('并发创建第一轮', 'sortOrder/阶段推进可能竞争。', 'DAO 用事务、唯一排序策略或锁 parent。')], practices: ['轮次、面试官关联、阶段推进和活动日志宜由事务/outbox 保证一致。', '时间统一存 UTC，并在 UI 展示工作区时区。'], test: PIPELINE_TEST, proves: 'core DAO 直测轮次+junction、sortOrder 自增及首轮条件推进；route 的 closed/offer 门禁与活动日志未直测。', sources: [DRIZZLE_TX], related: ['list-studio-human-interview-rounds-http', 'create-studio-human-interview-meeting-http'] }),
  d({
    slug: 'patch-studio-human-interview-round-http', method: 'PATCH', path: '/api/w/:slug/studio/interviews/:id/human-interview-rounds/:roundId', source: 'human-route.ts', line: 127, permission: 'humanInterview.update',
    summary: '部分更新真人复面轮次及 validUntil；DAO 同步仍为 scheduled 的关联会议时间，并拒绝调整已结束会议。', when: '改期、调整面试官或有效期且轮次尚可编辑时。', parameters: [candidate, humanRound, p('round fields + validUntil', 'JSON partial', true, 'validUntil 可空 instant；其它字段来自 input schema partial。')], returnType: '200 HumanInterviewRound', returnDescription: 'validator 400；DAO EditRoundError 映射其 4xx 状态。', exampleNotes: ['candidate :id 当前未传入 DAO，真正 scope 由 roundId+organizationId 保证。', '改期会同步 scheduled 会议，不应另发第二个请求。'], effectTitle: '轮次与待开始会议同步改期', effectDescription: '一处修改保持轮次和 scheduled meeting 时间一致。', metrics: [{ label: 'meeting sync', value: 'scheduled only' }], outputs: [{ label: 'editable', value: 'updated round', tone: 'good' }, { label: 'ended meeting', value: 'rejected', tone: 'warn' }], errors: [auth, err('已结束关联会议或轮次状态不可编辑', 'EditRoundError 返回 4xx。', '保留历史，创建新轮次而非改写。'), err('并发改期', '最后写可能覆盖并触发重复通知。', '加入 version/If-Match，并对通知使用 outbox 去重。')], practices: ['parent path id 与 round 归属应显式联合验证。', '时间变更、会议同步和通知用事务/outbox。'], test: PIPELINE_TEST, proves: 'core DAO 直测改期同步 scheduled 会议且拒绝已结束会议；HTTP parent id 一致性与 validator 未直测。', related: ['create-studio-human-interview-round-http', 'complete-studio-human-interview-round-http'] }),
  d({
    slug: 'cancel-studio-human-interview-round-http', method: 'POST', path: '/api/w/:slug/studio/interviews/:id/human-interview-rounds/:roundId/cancel', source: 'human-route.ts', line: 227, permission: 'humanInterview.delete',
    summary: '以最长 500 字原因取消 pending 真人轮次，并在 DAO 中结束/取消关联会议；逐个 best-effort 删除 LiveKit 房间，写候选人活动并失效缓存。', when: '招聘方取消尚未完成的真人复面。', parameters: [candidate, humanRound, p('reason', 'JSON string|null', false, '取消原因，trim 后最多 500 字。')], returnType: '200 HumanInterviewRound', returnDescription: '非 pending 等状态由 EditRoundError 返回 4xx；房间删除失败不改变 200。', exampleNotes: ['成功只保证数据库取消，LiveKit 清理是 best-effort。', 'UI 记录原因但避免写入敏感无关信息。'], effectTitle: '取消轮次并关闭关联会议', effectDescription: '轮次变 cancelled，会议终止；房间尽力清理。', metrics: [{ label: 'round guard', value: 'pending only' }, { label: 'room cleanup', value: 'best effort' }], outputs: [{ label: 'pending', value: 'cancelled', tone: 'good' }, { label: 'cleanup failure', value: '200 + warning', tone: 'warn' }], errors: [auth, err('轮次已完成/取消', 'DAO 拒绝并返回 4xx。', '刷新最新状态，不重复改变历史。'), err('LiveKit 删除失败', 'console warning 或配置错误被吞掉，DB 已取消。', '写 durable cleanup job/告警并周期对账房间。')], practices: ['状态转换幂等并记录 actor/reason。', '外部房间清理进入可重试 outbox，不只 console.warn。'], test: PIPELINE_TEST, proves: 'core DAO 直测仅 pending 可取消、completed 拒绝及会议结束；LiveKit fail-open 与活动日志没有 direct route test。', technology: 'Hono + Drizzle + LiveKit RoomService', sources: [LIVEKIT, DRIZZLE_TX], related: ['complete-studio-human-interview-round-http', 'delete-studio-human-interview-meeting-http'] }),
  d({
    slug: 'complete-studio-human-interview-round-http', method: 'POST', path: '/api/w/:slug/studio/interviews/:id/human-interview-rounds/:roundId/complete', source: 'human-route.ts', line: 171, permission: 'humanInterview.update',
    summary: '校验 outcome、结构化 feedback 与可选 0–100 score，将 pending 轮次标记完成，记录活动，结束该轮所有会议并 best-effort 删除 LiveKit 房间。', when: '真人复面结束后提交正式结论和反馈。', parameters: [candidate, humanRound, p('outcome/feedback/score', 'JSON', true, 'outcome 与 feedback 必填；score 可空整数 0–100。')], returnType: '200 HumanInterviewRound', returnDescription: '非法 body 400；非 pending/不存在由 EditRoundError 映射 4xx。', exampleNotes: ['反馈属于高敏评价，只向授权角色展示。', '完成后关联会议 DB 状态已结束，房间清理可能稍后补偿。'], effectTitle: '固化真人复面结论', effectDescription: '结果、分数、反馈、活动与会议终态同步更新。', metrics: [{ label: 'score', value: '0–100 nullable' }, { label: 'meeting cleanup', value: 'best effort' }], outputs: [{ label: 'pending round', value: 'completed', tone: 'good' }, { label: 'repeated complete', value: '4xx', tone: 'warn' }], errors: [auth, err('feedback/outcome/score 非法', 'validator 400。', '按共享 schema 修正并保留草稿。'), err('轮次非 pending', 'DAO 拒绝。', '刷新状态，不能覆盖既有正式结论。'), err('LiveKit 删除失败', '200 仍返回，房间可能残留。', '进入 durable reconciliation。')], practices: ['正式评价加审计、版本与严格访问控制。', '完成/会议结束/活动日志应避免跨系统部分成功。'], test: PIPELINE_TEST, proves: 'core DAO 直测完成写 outcome/score/feedback 且非 pending 拒绝，并覆盖按轮结束会议；LiveKit 清理未直测。', technology: 'Hono + Drizzle + LiveKit RoomService', sources: [LIVEKIT, DRIZZLE_TX], related: ['cancel-studio-human-interview-round-http', 'create-studio-offer-draft-http'] }),
  d({
    slug: 'list-studio-offer-drafts-http', method: 'GET', path: '/api/w/:slug/studio/interviews/:id/offer-drafts', source: 'routes/offer-drafts/route.ts', line: 28, permission: 'offer.read',
    summary: '按 candidateId+organizationId 返回 Offer 全版本草稿/发送/响应历史。', when: '候选人详情展示 Offer 版本时间线。', parameters: [candidate], returnType: '200 OfferDraft[]', returnDescription: 'candidate path 缺失 404；无草稿为空数组。', exampleNotes: ['不要只展示最新版本而隐藏 superseded/终态审计。', '薪资字段需最小权限与脱敏。'], effectTitle: '展示 Offer 版本历史', effectDescription: 'HR 看见草稿、已发、counter、接受/拒绝/过期演进。', metrics: [{ label: 'scope', value: 'candidate + org' }], outputs: [{ label: 'versions', value: 'offer timeline', tone: 'good' }, { label: 'none', value: '[]', tone: 'neutral' }], errors: [auth, err('候选人无 Offer', '返回空数组。', '在真人面试 readiness 满足后创建。')], practices: ['薪酬字段单独授权和访问审计。', '版本不可变；修改生成可追踪的新 version 或限制 draft 状态。'], test: OFFER_ROUTE_TEST, proves: 'direct route test 请求挂载 candidate path 并验证 list DAO；字段脱敏和跨组织仍需 contract test。', related: ['create-studio-offer-draft-http', 'patch-studio-offer-draft-http'] }),
  d({
    slug: 'create-studio-offer-draft-http', method: 'POST', path: '/api/w/:slug/studio/interviews/:id/offer-drafts', source: 'routes/offer-drafts/route.ts', line: 40, permission: 'offer.create',
    summary: '仅允许 human_interview/offer 且未 closed 的候选人；处于真人阶段还必须通过 readiness，创建递增版本、可选立即发送，并把候选人推进 Offer、写活动和失效缓存。', when: '真人复面结论完整后起草或立即发送 Offer。', parameters: [candidate, p('offer fields + sendImmediately', 'JSON', true, 'Offer schema 字段；sendImmediately 可选 boolean。')], returnType: '200 OfferDraft', returnDescription: '候选人 404；阶段/readiness/body 非法 400。', exampleNotes: ['sendImmediately=true 把创建与外发语义绑定，UI 应二次确认。', '成功可能 supersede 旧 sent 版本并推进 pipelineStage。'], effectTitle: '创建版本化 Offer', effectDescription: '合格候选人获得新 Offer 版本，状态与活动时间线同步。', metrics: [{ label: 'version', value: 'auto increment' }, { label: 'gate', value: 'human readiness' }], outputs: [{ label: 'ready', value: 'offer draft/version', tone: 'good' }, { label: 'not ready', value: '400', tone: 'warn' }], errors: [auth, err('closed、错误阶段或真人轮次未就绪', '400，不创建 Offer。', '重新激活或完成所有必要真人轮次反馈。'), err('并发创建版本', '版本号/旧 sent supersede 可能竞争。', '锁 candidate/版本集合并用唯一约束。')], practices: ['readiness 与创建在同一一致性边界重新检查。', '立即发送使用 transactional outbox，避免 DB 成功但外发失败。'], test: OFFER_ROUTE_TEST, proves: 'direct route test 覆盖 CRUD 权限、readiness 门禁、活动与缓存副作用；core DAO 另测版本递增/supersede 与首版推进。', sources: [DRIZZLE_TX], related: ['list-studio-offer-drafts-http', 'send-studio-offer-draft-http'] }),
  d({
    slug: 'patch-studio-offer-draft-http', method: 'PATCH', path: '/api/w/:slug/studio/interviews/:id/offer-drafts/:draftId', source: 'routes/offer-drafts/route.ts', line: 108, permission: 'offer.update',
    summary: '部分更新当前组织内 Offer；DAO 只允许 draft 状态，更新后记录版本/职位活动并失效缓存。', when: '编辑尚未发送的 Offer 草稿。', parameters: [candidate, p('draftId', 'path offerDraftId', true, 'Offer 版本 ID。'), p('offer fields', 'JSON partial', true, 'offerDraftInputSchema.partial。')], returnType: '200 OfferDraft', returnDescription: '非法 body 400；非 draft/不存在/跨组织由 OfferDraftError 返回 4xx。', exampleNotes: ['candidate :id 未传给 DAO，需依赖 draftId+organizationId scope。', 'sent 后必须走 respond/cancel，而非 PATCH。'], effectTitle: '编辑未发送 Offer', effectDescription: '草稿字段和活动时间线更新，已发送历史不可改写。', metrics: [{ label: 'editable status', value: 'draft only' }], outputs: [{ label: 'draft', value: 'updated', tone: 'good' }, { label: 'sent/terminal', value: 'rejected', tone: 'warn' }], errors: [auth, err('Offer 非 draft', 'OfferDraftError 返回 4xx。', '使用响应/撤回流程或创建新版本。'), err('并发编辑', '最后写覆盖。', '增加 version/If-Match。')], practices: ['同时验证 URL candidateId 与 draft.interviewRecordId，防路径资源混淆。', '金额/币种使用精确类型和跨字段校验。'], test: OFFER_ROUTE_TEST, proves: 'direct route test 证明更新保留活动/缓存副作用；core DAO 直测仅 draft 可编辑、sent 拒绝。', related: ['create-studio-offer-draft-http', 'send-studio-offer-draft-http'] }),
  d({
    slug: 'cancel-studio-offer-draft-http', method: 'POST', path: '/api/w/:slug/studio/interviews/:id/offer-drafts/:draftId/cancel', source: 'routes/offer-drafts/route.ts', line: 205, permission: 'offer.delete',
    summary: '将当前组织内可撤回的已发送 Offer 变为 expired，写 cancelled 活动并失效缓存；终态版本不可再撤回。', when: '招聘方撤回仍有效的 Offer。', parameters: [candidate, p('draftId', 'path offerDraftId', true, '要撤回的 Offer 版本。')], returnType: '200 OfferDraft', returnDescription: '不存在、状态不允许由 OfferDraftError 映射 4xx。', exampleNotes: ['POST 表达状态命令，不物理删除审计历史。', '撤回后通知候选人的外部同步需可靠投递。'], effectTitle: '撤回 Offer 并保留历史', effectDescription: 'sent 版本转 expired，版本记录仍可审计。', metrics: [{ label: 'transition', value: 'sent → expired' }], outputs: [{ label: 'sent', value: 'expired', tone: 'good' }, { label: 'terminal', value: 'rejected', tone: 'warn' }], errors: [auth, err('Offer 已终态/草稿/不存在', 'DAO 拒绝。', '刷新版本状态，不重复撤回。')], practices: ['命令幂等并保留撤回 actor/time/reason。', '候选人通知使用 outbox 与去重键。'], test: PIPELINE_TEST, proves: 'core DAO 直测 sent→expired 且终态不可撤回；route 的权限、活动/缓存由 offer route family 测试约束。', related: ['send-studio-offer-draft-http', 'respond-studio-offer-draft-http'] }),
  d({
    slug: 'respond-studio-offer-draft-http', method: 'POST', path: '/api/w/:slug/studio/interviews/:id/offer-drafts/:draftId/respond', source: 'routes/offer-drafts/route.ts', line: 170, permission: 'offer.update',
    summary: '记录候选人对已发送 Offer 的 accepted/declined/counter；接受/拒绝进入终态，counter 保持 sent 并保存 candidateCounter，随后写活动与失效缓存。', when: 'HR 录入候选人正式回应或还价。', parameters: [candidate, p('draftId', 'path offerDraftId', true, 'Offer 版本。'), p('response/candidateCounter', 'JSON', true, 'response 为共享枚举；counter 时携带还价结构。')], returnType: '200 OfferDraft', returnDescription: '非法响应 400；错误状态/归属由 OfferDraftError 映射 4xx。', exampleNotes: ['counter 不等于接受/拒绝，Offer 仍 sent。', '终态响应应要求确认并保留来源证据。'], effectTitle: '记录候选人 Offer 回应', effectDescription: '版本状态、还价数据和候选人活动保持一致。', metrics: [{ label: 'terminal', value: 'accepted/declined' }, { label: 'counter status', value: 'sent' }], outputs: [{ label: 'accepted/declined', value: 'terminal', tone: 'good' }, { label: 'counter', value: 'sent + counter', tone: 'neutral' }], errors: [auth, err('response 与 candidateCounter 组合非法', 'validator 400。', 'counter 才提交还价字段。'), err('Offer 状态不可回应', 'DAO 返回 4xx。', '刷新当前版本。')], practices: ['终态响应使用幂等命令与审计证据。', '还价金额采用结构化货币字段，不解析自由文本。'], test: PIPELINE_TEST, proves: 'core DAO 直测 accepted/declined 终态化和 counter 保持 sent+记录数据；route family 测试约束活动/缓存。', related: ['send-studio-offer-draft-http', 'cancel-studio-offer-draft-http'] }),
  d({
    slug: 'send-studio-offer-draft-http', method: 'POST', path: '/api/w/:slug/studio/interviews/:id/offer-drafts/:draftId/send', source: 'routes/offer-drafts/route.ts', line: 142, permission: 'offer.update',
    summary: '把可发送草稿推进为 sent，写 offer_draft_sent 候选人活动并失效缓存；当前路由调用 DAO，不直接呈现邮件供应商投递。', when: 'Offer 草稿审核通过并正式标记发送时。', parameters: [candidate, p('draftId', 'path offerDraftId', true, '当前组织内可发送草稿。')], returnType: '200 OfferDraft', returnDescription: '状态/归属错误由 OfferDraftError 映射 4xx。', exampleNotes: ['200 代表业务状态 sent，不应自动等同邮件已送达。', 'UI 将投递状态与 Offer 状态分开显示。'], effectTitle: '把 Offer 标记为已发送', effectDescription: '草稿进入 sent 并出现在候选人活动时间线。', metrics: [{ label: 'state', value: 'sent' }, { label: 'delivery proof', value: 'not in route' }], outputs: [{ label: 'sendable draft', value: 'sent', tone: 'good' }, { label: 'already sent/terminal', value: '4xx', tone: 'warn' }], errors: [auth, err('Offer 状态不可发送或跨组织', 'DAO 拒绝。', '刷新版本并选择最新 draft。'), err('业务 sent 与实际通知分离', '可能 UI 显示 sent 但候选人未收到邮件。', '采用邮件 outbox/provider messageId/webhook 状态。')], practices: ['明确区分 offer status 与 delivery status。', '外发采用 outbox、幂等键、供应商 messageId 和退信 webhook。'], test: OFFER_ROUTE_TEST, proves: 'direct route family 测试约束 CRUD 权限、send 活动和缓存副作用；未证明真实邮件投递，因为 handler 本身不调用邮件供应商。', related: ['respond-studio-offer-draft-http', 'cancel-studio-offer-draft-http', 'create-studio-offer-draft-http'] }),
]
