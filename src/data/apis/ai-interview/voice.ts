import { AI_INTERVIEW_COMMIT } from '../helpers'
import type { ApiEntry } from '../types'

export const voiceApis: ApiEntry[] = [
  {
    slug: 'build-livekit-agent-session', name: '_build_session', signature: '_build_session(*, proc, selected_voice, state) -> AgentSession', kind: 'function',
    project: 'ai-interview', module: 'voice-agent', technology: 'LiveKit Agents 1.6.4', verifiedCommit: AI_INTERVIEW_COMMIT,
    summary: '装配实时语音流水线：ElevenLabs STT、DashScope 兼容 LLM、MiniMax TTS、Silero VAD、音频 turn detector 与自适应打断。', whenToUse: 'LiveKit worker 接到面试 dispatch 后，为每个房间创建独立 AgentSession 时。',
    sourcePath: 'apps/livekit-agent/src/agent.py', sourceLine: 282,
    parameters: [{ name: 'proc', type: 'JobProcess', required: true, description: '持有预热 Silero VAD 的 worker process。' }, { name: 'selected_voice', type: 'str', required: true, description: '后台选中的 MiniMax voice id。' }, { name: 'state', type: 'SessionState', required: true, description: '录制、指标、上下文等会话状态。' }],
    returns: { type: 'AgentSession', description: '关闭 preemptive generation，启用动态端点、adaptive interruption 与用户单轮限制。' },
    example: { language: 'py', code: `session = _build_session(\n    proc=ctx.proc, selected_voice=voice_id, state=state\n)\nawait session.start(agent=InterviewAgent(dispatch), room=ctx.room,\n                    room_options=_build_room_options(allow_text_input=True))`, explanation: ['STT 使用 scribe_v2_realtime，LLM thinking 关闭以降低首 token 延迟。', '端点 1–4 秒动态调整，打断需 0.8 秒且支持误打断恢复。'] },
    effect: { title: '实时面试通话状态', description: '音频从候选人到回答播报形成完整可观测回路。', metrics: [{ label: 'STT', value: 'scribe_v2_realtime' }, { label: 'endpoint', value: '1.0–4.0s' }, { label: 'interruption', value: 'adaptive · 0.8s' }], output: [{ label: 'candidate speech', value: 'final transcript · 860ms', tone: 'good' }, { label: 'agent reply', value: 'first audio · 1.4s', tone: 'good' }, { label: 'false interruption', value: 'resume ≤2s', tone: 'neutral' }] },
    errors: [{ condition: '任一 provider key 缺失', behavior: 'session 初始化或首次调用失败。', recovery: 'worker 启动时做全链路配置自检。' }, { condition: '网络抖动造成误打断', behavior: '可能截断 TTS。', recovery: 'adaptive 模式 + 2 秒 false interruption resume。' }],
    bestPractices: ['VAD 在 prewarm 阶段加载，避免每房间冷启动。', 'STT/LLM/TTS 指标按 speech id 聚合。', 'provider、模型、端点与打断参数都通过回放集做版本化评测。'],
    tests: [{ path: 'apps/livekit-agent/tests/test_agent_session_config.py', proves: 'STT/LLM/TTS、turn handling 与用户单轮限制配置。' }, { path: 'apps/livekit-agent/tests/test_agent_configuration.py', proves: 'worker 与 session 装配边界。' }], officialSources: [{ label: 'LiveKit AgentSession', url: 'https://docs.livekit.io/agents/build/sessions/' }, { label: 'LiveKit turn detection', url: 'https://docs.livekit.io/agents/build/turns/' }], related: ['ready-check-task', 'interview-agent-enter-wrap-up'],
  },
  {
    slug: 'ready-check-task', name: 'ReadyCheckTask', signature: 'ReadyCheckTask(opening_instructions: str) -> AgentTask[bool]', kind: 'agent-task',
    project: 'ai-interview', module: 'voice-agent', technology: 'LiveKit AgentTask', verifiedCommit: AI_INTERVIEW_COMMIT,
    summary: '用受限工具集执行开场与就绪确认，只允许明确确认或拒绝，避免主面试 Agent 在候选人未准备时抢跑。', whenToUse: '会话启动后的第一阶段；结果为 true 才切入正式 InterviewAgent 流程。',
    sourcePath: 'apps/livekit-agent/src/ready_check_task.py', sourceLine: 45,
    parameters: [{ name: 'opening_instructions', type: 'str', required: true, description: '已替换候选人姓名/岗位占位符的管理端开场话术。' }], returns: { type: 'AgentTask[bool]', description: 'confirm_ready 完成为 true；decline_interview 完成为 false。' },
    example: { language: 'py', code: `ready = await ReadyCheckTask(dispatch.prompts.opening)\nif not ready:\n    await WrapUpTask(end_call_tool)\n    return\n# continue interview`, explanation: ['on_enter 只生成配置好的开场回复。', '工具文档就是模型的动作边界。'] },
    effect: { title: '开场状态机', description: '候选人决定继续或退出，系统不会把寒暄误当答案。', metrics: [{ label: '可用工具', value: '2' }, { label: '输出', value: 'boolean' }, { label: '未知状态', value: '继续澄清' }], output: [{ label: '“准备好了”', value: 'confirm_ready → interview', tone: 'good' }, { label: '“今天不方便”', value: 'decline_interview → graceful close', tone: 'warn' }] },
    errors: [{ condition: '模型未调用任一工具', behavior: 'Task 不完成并继续对话。', recovery: '提示词要求只在明确意图时调用；会话级超时兜底。' }],
    bestPractices: ['任务工具保持最小权限。', '开场内容来自快照，不在 worker 硬编码业务文案。', '拒绝路径也要执行礼貌收尾与资源清理。'],
    tests: [{ path: 'apps/livekit-agent/tests/test_ready_check_task.py', proves: '工具暴露、确认/拒绝结果与开场指令。' }], officialSources: [{ label: 'LiveKit Agent tasks', url: 'https://docs.livekit.io/agents/build/workflows/' }], related: ['build-livekit-agent-session', 'interview-agent-enter-wrap-up'],
  },
  {
    slug: 'interview-agent-enter-wrap-up', name: 'InterviewAgent.enter_wrap_up', signature: 'async enter_wrap_up(self) -> str | None', kind: 'method',
    project: 'ai-interview', module: 'voice-agent', technology: 'LiveKit function_tool', verifiedCommit: AI_INTERVIEW_COMMIT,
    summary: '把模型触发的面试收尾变成带时间软门和幂等锁的受控任务，防止过早收尾或重复结束。', whenToUse: '系统时间提示进入收尾阶段后，模型完成当前回答并调用。',
    sourcePath: 'apps/livekit-agent/src/interview_agent.py', sourceLine: 152,
    parameters: [], returns: { type: 'str | None', description: '过早/重复调用返回拒绝说明；合法调用进入 WrapUpTask，正常路径返回 None。' },
    example: { language: 'py', code: `# model-visible tool; application-side timing guard remains authoritative\nresult = await agent.enter_wrap_up()\nif result:\n    logger.info('wrap-up rejected: %s', result)`, explanation: ['16 分钟前 30 秒之外的调用被软门拒绝。', '_wrap_up_started 防止并发重复进入。'] },
    effect: { title: '可预测的面试收口', description: '时间提示、最后一问、礼貌结束与删房间形成确定性阶段。', metrics: [{ label: 'soft gate', value: '16:00 - 30s' }, { label: 'final wrap', value: '19:00' }, { label: 'hard limit', value: '20:00 + grace' }], output: [{ label: '12:10 调用', value: 'rejected · continue interview', tone: 'warn' }, { label: '16:05 调用', value: 'WrapUpTask started', tone: 'good' }, { label: '重复调用', value: 'rejected · idempotent', tone: 'good' }] },
    errors: [{ condition: '模型过早调用', behavior: '返回拒绝文本，不切换任务。', recovery: '继续按系统题目列表提问。' }, { condition: 'WrapUpTask 被取消', behavior: '可能没有正常 end_call。', recovery: '外层 hard timeout 与 session shutdown 兜底。' }],
    bestPractices: ['时间约束必须由程序执行，不能只写提示词。', '结束操作做幂等防重。', '正常、候选人主动结束、断线和硬超时共享同一清理路径。'],
    tests: [{ path: 'apps/livekit-agent/tests/test_interview_agent_init.py', proves: '工具注册与时间属性。' }, { path: 'apps/livekit-agent/tests/test_wrap_up_task.py', proves: '收尾任务与 end_call 行为。' }], officialSources: [{ label: 'LiveKit function tools', url: 'https://docs.livekit.io/agents/build/tools/' }], related: ['ready-check-task', 'post-livekit-token'],
  },
  {
    slug: 'post-livekit-token', name: 'POST /api/interview/:id/:roundId/livekit-token', signature: 'POST path params → { token, serverUrl, roomName, isReconnect }', kind: 'http',
    project: 'ai-interview', module: 'realtime-api', technology: 'LiveKit Server SDK 2.15', verifiedCommit: AI_INTERVIEW_COMMIT,
    summary: '在事务行锁内执行面试轮次状态机，校验必填表单，签发 LiveKit token，并把不可变上下文快照作为 Agent dispatch metadata。', whenToUse: '候选人首次进入或 3 分钟热重连 LiveKit 房间时；前端 prepareConnection 与 start 可重复调用。',
    sourcePath: 'apps/ai-recruitment-copilot-backend/src/server/routes/interview/route.ts', sourceLine: 37,
    parameters: [{ name: 'id', type: 'path string', required: true, description: '面试记录 ID。' }, { name: 'roundId', type: 'path string', required: true, description: '当前轮次 ID。' }],
    returns: { type: 'JSON token response', description: 'participantToken、serverUrl、roomName、participantName 和 isReconnect；dispatch 包含提示、简历、题目、voice、录制开关。' },
    example: { language: 'ts', code: `const res = await fetch(\n  \`/api/interview/\${id}/\${roundId}/livekit-token\`,\n  { method: 'POST' },\n)\nif (res.status === 409) showRequiredForms()\nconst connection = await res.json()`, explanation: ['SELECT … FOR UPDATE 防止双标签页竞争。', '已有 room/identity 时只读复用，保证两次取 token 幂等。'] },
    effect: { title: '热重连状态机', description: '候选人短暂断网后回到原房间，不创建第二场面试。', metrics: [{ label: '重连窗口', value: '180s' }, { label: '并发保护', value: 'row lock' }, { label: '上下文来源', value: 'active snapshot' }], output: [{ label: 'pending', value: 'new room + in_progress', tone: 'good' }, { label: 'interrupted · 82s', value: 'reuse room · reconnect', tone: 'good' }, { label: 'interrupted · 201s', value: '410 grace_expired', tone: 'warn' }] },
    errors: [{ condition: '必填表单未完成', behavior: '409 forms_required。', recovery: '前端引导完成表单后重试。' }, { condition: '重连窗口过期', behavior: '事务内置 completed 并返回 410。', recovery: '展示本轮已结束，联系管理员。' }, { condition: '第二设备使用同 identity', behavior: '当前实现仍签发 token，新连接可能接管旧连接。', recovery: '增加 active lease/tab nonce，或在产品上明确“新连接接管”。' }, { condition: 'LiveKit 配置缺失', behavior: '500 安全错误。', recovery: '部署启动自检 API key/secret/url。' }],
    bestPractices: ['发 token 前在数据库行锁内重验 completed。', 'prepareConnection 路径保持幂等，不提前改状态。', 'metadata 使用快照而不是实时拼接易漂移数据。', '不要把注释中的 occupied 409 当成已实现；当前 TokenResolution 没有该分支。'],
    tests: [{ path: 'apps/ai-recruitment-copilot-backend/src/server/routes/interview/__tests__/dispatch-contract-source.test.ts', proves: 'dispatch 元数据契约。' }, { path: 'apps/ai-recruitment-copilot-backend/src/server/routes/interview/__tests__/candidate-context-snapshot-source.test.ts', proves: '上下文读取自活动快照。' }], officialSources: [{ label: 'LiveKit access tokens', url: 'https://docs.livekit.io/home/get-started/authentication/' }, { label: 'LiveKit explicit dispatch', url: 'https://docs.livekit.io/agents/server/agent-dispatch/' }], related: ['build-livekit-agent-session', 'ready-check-task'],
  },
]
